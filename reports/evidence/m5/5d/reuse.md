# M5/5D — 재활용 출처 (ADR 0009 D-6 오른쪽 자리)

> 왼쪽 자리(모듈 옆 최소 포인터)는 각 모듈 docstring 첫 줄 `Reuse: <원본 경로>@<commit>`.
> 이 표는 그 이식이 **수행한 수정·튜닝 내역**을 담는다(D-6.1 #3). 조인 키는 `module`
> (저장소 루트 기준 POSIX 경로) — `ml-engine/tools/reuse_provenance_check.py`(S-7)가
> 이 표와 docstring 포인터를 대조한다. `results.py`·`policy.py`·`registry/artifact.py`는
> 신규 작성(포인터 없음 — 계약 신설, 이 표에 행이 없다).

| module | original_path | commit | 수행한 수정·튜닝 |
| --- | --- | --- | --- |
| ml-engine/src/ml_engine/inference/reserve_draw.py | bid-vector/app/domain/reserve_draw_distribution.py | ed4b06c | 닫힌식·완전열거 산술(`DrawMeanDistribution`·`exact_draw_mean_distribution`·`draw_mean_moments`)은 그대로. `_validated_values`의 `ValueError` 셋(`draw_count < 1`·비유한/≤0 값·`len(values) < draw_count`)을 `Unmeasurable` 결과 타입으로 치환했다(scope.md ①, 위협 모델 (a)) — 산식 본체는 무편집. |
| ml-engine/src/ml_engine/inference/assessment.py | bid-vector/app/domain/assessment_shrinkage.py | ed4b06c | `resolve_assessment_posterior`(3계층 순차 수축)·`_compose_level_weights`·`_blend_predictive_variance`·`_shrink_optional_level`·`_trusted_variance`·`_sample_count`는 legacy 산식 그대로(`shrink_toward`는 자체 재구현하지 않고 5B `features/shrinkage.py`를 재사용, D-5B-6 재사용 대상). 수정: (1) `global_level.sample_count < 1`의 `ValueError`를 `Unmeasurable(INSUFFICIENT_SAMPLES, TOO_FEW_DRAWS)`로(위협 모델 (a)). (2) `level_weights: dict[str, float]`를 `LevelWeights`(3필드 dataclass)로 승격(설계 검토 (1) `dict[str,Any]` 재유입 방지). (3) **K5 provenance 게이트 신설**(D-5D-5, legacy 부재) — `AssessmentProvenance`·`AssessmentSample`·`CleanAssessmentSample`·`admit_clean`은 legacy에 없던 새 타입/함수다(ML-04 문면 요구 충족을 위한 추가, 「결합 절단이 전부」의 두 번째 예외). (4) κ(`AGENCY_PRIOR_STRENGTH`·`CATEGORY_PRIOR_STRENGTH`)·`MIN_PREDICTIVE_STD`·`MIN_SAMPLES_FOR_VARIANCE` 모듈 상수는 이식하지 않고 `InferencePolicy.assessment_*`(D-5D-8, 값은 legacy-behavior 그대로 policy-values.md에 승인)로 외부화했다. |
| ml-engine/src/ml_engine/inference/maturity.py | bid-vector/app/domain/settlement_maturity.py | ed4b06c | `week_start_utc`·`build_weekly_maturity`(KST 주 경계, 관측 없는 주 제외)는 legacy 그대로. 수정: (1) `MaturityWindow.maturity` 프로퍼티의 `0/0 → 0.0` 접힘(`:83-86`)을 제거하고 `Maturity = Observed \| NoObservation` sealed 타입 + `resolve_maturity` 함수로 대체(data-dictionary.md §6.4). (2) `app.core.time`(`ensure_utc`·`to_kst`) 의존을 이식하지 않고 `zoneinfo.ZoneInfo("Asia/Seoul")`(stdlib)로 대체 — 값은 legacy `KST = timezone(timedelta(hours=9))`와 동일. (3) `MATURITY_WINDOW_LENGTH` 모듈 상수는 이식하지 않고 `InferencePolicy.maturity_window_days`(D-5D-8)로 외부화. K7 strict 승격(legacy는 mypy strict 아일랜드 8개 중 유일 미등재, 5D 가 등재). |
| ml-engine/src/ml_engine/inference/scenario.py | bid-vector/app/ai/predictors/scenario_spec.py | ed4b06c | `scenario_bid_rates`의 산식(`clamp(scale*(center + sign*z*std))`, 괄호 위치 포함)을 `build_scenario_candidates`로 그대로 이식. 수정: (1) `CANDIDATE_SCENARIOS`(z·가중치·라벨 3-튜플)·`SCENARIO_INTERVAL_Z`·`_BID_RATE_DIGITS`·`clamp_bid_rate` 밴드는 모듈 상수 대신 `InferencePolicy.scenario_*`(D-5D-8)로 외부화. (2) `ScenarioCandidate = dict[str, float\|str]`를 `Candidate`(frozen dataclass, `Decimal` 필드)로 승격 — legacy `round(rate, 4)`(float)를 경계 `Decimal` quantize(D-5D-1, scale 보존)로 대체. (3) `_PRICE_DIGITS`·`predicted_price` 산출은 이식하지 않았다(원 단위 가격 산출은 Kotlin 1B 소관, out_of_scope). (4) `resolve_uncertainty`(§6.5 분기, `sample_size < min_samples_for_variance` 이면 `Unmeasurable`)는 legacy에 없던 신규 함수 — 합성 `confidence`(`historical/statistics.py` 등)는 이식하지 않았다(scope.md ④, 3자리 계수 전부 폐기). |
| ml-engine/src/ml_engine/inference/predict.py | bid-vector/app/ai/predictors/award_rate_gbm.py | ed4b06c | `unlearned_category_reason`(판정 단일 지점, 가용성 게이트·예측 경로 공용)을 `segment_availability`로 이식 — `rows <= 0`/`0 < rows < minimum`을 다른 문자열이 아니라 `UnmeasurableReason`×`UnmeasurableDetail` enum 조합으로 가른다(scope.md ②). `minimum = max(1, int(settings...))` 클램프는 `max(1, policy.gbm_min_category_rows)`로 그대로(D-5D-3). `_guarded_center_rate`·`build_award_rate_gbm_prediction`의 오케스트레이션(가드 → 예측 → 시나리오 → 결과 조립)을 `predict_bid_rates`로 이식하되 `booster.predict(matrix)`를 `BoosterLike` Protocol 뒤로 두어 test 는 fake 를 주입한다(D-2b 고정 항목). `_estimate_confidence`(합성 신뢰도 5상수)는 이식하지 않았다(§6.5, scope.md ③). `LoadedAwardRateGbmModel`·`load_award_rate_gbm_model`(파일 경로·캐시)은 이식 대상이 아니다 — artifact 로딩은 `registry/artifact.py`(신규, checksum 게이트 포함)가 대신한다. |

## 이 slice 가 신규 작성한 것(포인터 없음, 이 표 대상 아님)

`results.py`(결과 타입, wire 계약에서 도출) · `policy.py`(`InferencePolicy` — 5A `load_policy`
형태를 감싸는 신규 결과 타입 경계) · `registry/artifact.py`(`ArtifactManifestV1`·
`load_artifact` — legacy 에 checksum 검증이 없었다, digest §2 「checksum 대조 없음」).
