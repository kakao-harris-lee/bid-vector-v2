# M5/5C-2 — 재활용 출처 (ADR 0009 D-6 오른쪽 자리)

> 왼쪽 자리(모듈 옆 최소 포인터)는 각 모듈 docstring 첫 줄 `Reuse: <원본 경로>@<commit>`.
> 이 표는 그 이식이 **수행한 수정 내역**을 담는다(D-6.1 #3). 조인 키는 `module`(저장소
> 루트 기준 POSIX 경로) — `ml-engine/tools/reuse_provenance_check.py`(S-7)가 이 표와
> docstring 포인터를 대조한다.
>
> `evaluation/policy.py`·`evaluation/verdict.py`는 신규 작성(포인터 없음 — legacy 에
> 정책 로더·3값 판정 결과 타입 대응물이 없다, D-5C2-11)이라 이 표에 행이 없다.
>
> **설계 검토 (4) 대비 편차** — 홀드아웃 실행기(scope ⑦)는 설계 검토가 `training/holdout.py`
> 파일 하나로 계획했으나, 구현 중 설계 래칫 `file_loc_soft_limit`(500줄)을 지키기 위해
> `holdout.py`(public 진입점)·`_holdout_fit.py`(창 분할 + GBM 학습)·`_holdout_window.py`
> (베이스라인·진단·안정성·조립) 세 파일로 나눴다 — `pyproject.toml` allowlist 편집은
> 팀장 소관이라 대신 분리를 택했다(checklist.md 「계약과 어긋나 판단이 필요했던 자리」).
> 셋 다 같은 원본(`award_rate_holdout.py`)을 가리킨다 — 오케스트레이션 자체를 나눈 것이지
> 서로 다른 legacy 파일을 이은 것이 아니다.

| module | original_path | commit | 수행한 수정·튜닝 |
| --- | --- | --- | --- |
| ml-engine/src/ml_engine/evaluation/scoring.py | bid-vector/app/services/ml_training/award_rate_scoring.py | ed4b06c | `rmse_bias_std`·`paired_t`·`improvement_ratio`(`:128-202`)를 값 무변경 이식 — numpy 직접 구현 유지(scipy 미도입, ddof 차이로 수치가 달라진다). 시그니처·산식 전부 legacy 그대로, 반환 타입만 그대로(tuple). |
| ml-engine/src/ml_engine/evaluation/baselines.py | bid-vector/app/services/ml_training/award_rate_scoring.py | ed4b06c | `BASELINE_SPECS`(`:60-73`)·`group_mean_predictions`(`:149-174`)를 이었다. 수정: (1) `BaselineSpec.key`가 `AwardRateTrainingRow`(직접 속성)가 아니라 5B `FeatureFacts`를 받는다 — evaluation 층이 `ml_engine.training`을 모른다(layers), 실행기가 `TrainingRow.facts`/`.label.value`를 풀어 넘긴다. (2) 금액대 밴드 키(`_budget_band_key`, legacy 는 `synthetic_experiment` 업무 모듈 private import)를 `amount_band_key(facts, edges)`로 — 경계값은 `EvaluationPolicy.amount_band_edges`에서만(`OPEN-5C-BUDGET-BAND-SOURCE` 종결, D-5C2-3). (3) `category`/`agency` 결측(wire `Missing`)은 legacy 에 없던 상태라 `"unknown"` sentinel 키로 폴백(신규 분기, 방어적). (4) 다섯 baseline 표 생성이 함수(`baseline_specs(policy)`)로 — `min_count`·밴드 경계가 정책에서 오므로 모듈 상수가 아니라 정책 함수. |
| ml-engine/src/ml_engine/evaluation/segments.py | bid-vector/app/services/ml_training/award_rate_scoring.py | ed4b06c | `SEGMENT_SPECS`(`:84-90`)·`segment_scores`/`_one_segment_score`(`:177-234`)를 이었다. 수정: (1) 축을 `category`+`published_floor`(legacy)에서 `category`+`amount_band`(D-5C2-9)로 — `published_floor`는 `FeatureFacts`에 없고 백필 커버리지의 함수라 학습·서빙 의미가 달랐다(F3). `amount_band`는 신규 축(베이스라인 축 재사용, `.baselines.amount_band_key`). (2) 축 목록이 정책(`segment_axes`)에서 온다 — legacy 는 코드 상수 tuple. (3) 채점 함수(`rmse_bias_std`·`paired_t`·`improvement_ratio`) 자체는 값 무변경. |
| ml-engine/src/ml_engine/evaluation/diagnostics.py | bid-vector/app/services/ml_training/award_rate_diagnostics.py | ed4b06c | `minimum_detectable_improvement`(`:154-187`)·`required_row_count`(`:140-151`)·`coverage_splits`(`:198-235`)·`unlearned_cells`(`:238-260`)·`category_counts`(`:276-287`)·`summarize_stability`(`:122-137`)를 값 무변경 이식 — 검정력 50% 기준·`None` 유지 규율·`sign_consistent`/`verdict_consistent` 함께 읽어야 한다는 문면 그대로. 수정: (1) `unlearned_cells`/`category_counts`가 `AwardRateTrainingRow`가 아니라 `FeatureFacts` 시퀀스를 받는다(layers, baselines.py 와 같은 사유). (2) `StabilityTrial`/`StabilitySummary`/`CoverageSplit`/`UnlearnedCell`/`CategoryCount`가 pydantic `StrictModel` → frozen dataclass. |
| ml-engine/src/ml_engine/evaluation/windows.py | bid-vector/app/services/ml_training/award_rate_windows.py | ed4b06c | `WindowPolicy`(`:104-110`)·`plan_evaluation_windows`(`:241-278`)·`_partition_windows`·`_EXCLUSION_RULES`(`:151-163`)·`indices_in_window`(`:164-179`)·`holdout_overlaps`(`:332-367`)의 구조를 이었다(embargo + 표본 하한 + 최근 N, 규칙 표 순서 「먼저 걸리는 사유가 기록된다」). 수정: (1) `MaturityWindow`(5D K7 산출, 계산 포함)를 이식하지 않고 `WeekMaturity(start, end, opened_count, settled_count)`(입력, D-5C2-2)로 — 성숙도 계산은 5D 소관, 이 모듈은 비율만 파생(`maturity_ratio`, K7 `Observed.ratio`와 3줄 중복, 알려진 제한). (2) `WindowPolicy`(legacy, 완화 seam — 조사 02 §2-7 #1)를 없애고 임계는 전부 `EvaluationPolicy`에서만 — 낱개 필드 주입 경로 폐쇄(D-5C2-10 #1). (3) `rows: Sequence[AwardRateTrainingRow]` → `Sequence[StratifiedRow]`(Protocol, `opened_at`·`stratum`만 요구) — evaluation 이 `ml_engine.training`을 모른다(layers). (4) 제외 사유 enum 에 `TRAINING_REJECTED` 1개 추가(legacy 4 + 1, 설계 검토 (1) 표). (5) `plan_evaluation_windows`가 겹치는 `maturities`를 `InvalidMaturityInput`으로 거부(신규 — legacy 는 겹침을 가정하지 않았다, 우회 후보 (13)). (6) 비율 분할(`train_fraction`) API 없음(F4, 정적 test 로 확인). |
| ml-engine/src/ml_engine/evaluation/report.py | bid-vector/app/services/ml_training/award_rate_backtest_report.py | ed4b06c | `BacktestReport`(`:70-160`)·`build_backtest_report`·`_assemble`·`_corpus_profile`·`_category_counts`·`_strata_counts`·`_unaccounted_row_count`의 구조(공시 전용 조립)를 이었다. 수정: (1) `gate_passed_at_latest_window`/`_all_origins` bool 쌍(legacy) → `Promotion = Promotable | NotPromotable | PromotionNotEvaluable`(신규, D-5C2-6) — latest-window 하나에서만 파생, `all_origins`는 창별 결과로만 진단. (2) pydantic `StrictModel` → frozen dataclass 전체, canonical JSON(5B `manifest.py::canonical_json`과 같은 규칙 — 대상 타입이 달라 새 함수, 5B 파일 편집 없음) + `NaN`/`Infinity` 거부(`CanonicalizationRejected`, 우회 후보 (18)). (3) JSON 직렬화 helper 15개가 설계 래칫 `dict[str, Any]` 약한 경계에 걸려, 5C-1 `artifact_writer.py::_JsonValue`와 같은 재귀 named type alias(`_JsonValue`)로 대체(신규 패턴 적용, 로직 변경 없음). (4) `WindowResult`에 창별 `release_id`(신규 필드, 5C-1 결정적 파생 재사용) 추가. |
| ml-engine/src/ml_engine/training/holdout.py | bid-vector/app/services/ml_training/award_rate_holdout.py | ed4b06c | `evaluate_award_rate_holdout`/`_build_report`(legacy)의 최상위 오케스트레이션 뼈대(코퍼스 admission → 창 계획 → 창별 평가 → report 조립)를 이었다. `run_holdout` 하나만 남기고 창 단위 세부는 `_holdout_fit.py`/`_holdout_window.py`로 분리(파일 크기 사유, 위 편차 절 참고). |
| ml-engine/src/ml_engine/training/_holdout_fit.py | bid-vector/app/services/ml_training/award_rate_holdout.py | ed4b06c | `_split_at_window`(`:224-262`, 경계 동시각 규칙)·`_HoldoutSplit`(`:204-221`)·`_gbm_predictions`의 "학습만" 부분을 이었다. 수정: (1) `_gbm_predictions`의 "아티팩트 → predictor 로더 → `predict_rates`"(서빙과 같은 경로)를 이식하지 않았다 — evaluation/training 이 `inference`를 import 할 수 없어(layers) 재현 불가(D-5C2-7). 대신 `TrainedArtifact.feature_manifest`에서 `AwardRateFeatureSpace`를 재구성해 `booster.predict(matrix)`를 직접 부른다(`feature_space_from_manifest`, 신규 — 잃는 것은 `OPEN-5C2-SERVING-PATH-PARITY`). (2) 두 GBM 변형(`gbm_all_strata`/`gbm_gate_stratum_only`) 학습이 5C-1 `train_award_rate_gbm` 호출로(legacy 는 자체 `train_award_rate_gbm`). (3) `build_row`가 거부할 행(`base_amount`/`denominator_source` wire Missing)을 사전에 걸러내는 `is_buildable`/`usable_test_rows`는 신규(legacy 에는 이 결측 상태가 없었다). |
| ml-engine/src/ml_engine/training/_holdout_window.py | bid-vector/app/services/ml_training/award_rate_holdout.py | ed4b06c | `_baseline_scores`(`:270-291`)·`_model_scores`(`:294-319`)·`_build_report`(`:322-343`)·`_assemble`(`:388-427`)의 "채점·조립" 부분과 `award_rate_backtest_report.py` `_one_window_stability`(`:220-259`)의 안정성 sweep 구조를 이었다. 수정: (1) 안정성 sweep 이 `evaluate_award_rate_holdout` 전체(GBM 두 변형)를 재실행하는 legacy 와 달리, 게이트 모델(`gbm_all_strata`) 하나만 seed 마다 재학습한다(보수 변형은 판정에 쓰이지 않으므로 비용 절감, 결과는 동일 정보량). (2) `GateOutcome`(3값, D-5C2-4) 파생을 `evaluation.verdict.gate_outcome` 호출로 위임 — legacy 판정식은 `award_rate_diagnostics.py::build_verdict`(bool). (3) 게이트 변형(`gbm_all_strata`) 학습 실패는 창 전체를 `WindowSkip`(→ `TRAINING_REJECTED`)으로, 보수 변형(`gbm_gate_stratum_only`) 실패는 그 모델 점수만 생략(신규 회복 경로, legacy 에 실패 상태 없음). |
