# M5/5D-2 — 재활용 출처 (ADR 0009 D-6 오른쪽 자리)

> 왼쪽 자리(모듈 옆 최소 포인터)는 각 모듈 docstring 첫 줄 `Reuse: <원본 경로>@<commit>`.
> 이 표는 그 이식이 **수행한 수정·튜닝 내역**을 담는다(D-6.1 #3). 조인 키는 `module`
> (저장소 루트 기준 POSIX 경로) — `ml-engine/tools/reuse_provenance_check.py`(S-7)가
> 이 표와 docstring 포인터를 대조한다.

| module | original_path | commit | 수행한 수정·튜닝 |
| --- | --- | --- | --- |
| ml-engine/src/ml_engine/inference/observations.py | bid-vector/app/ai/predictors/distribution_extraction.py | ed4b06c | `observe_reserve_draw`(관문 흐름)·`realized_assessment_ratio`(1-기반 인덱스 선택·평균·개연 밴드)의 산술은 legacy 그대로. 수정: (1) legacy 는 `ratios = [p/base for p in reserve_prices if p > 0]` 뒤 `len(ratios) != 15`로 「개수 부족」과 「가격이 음수라 빠짐」을 한 사유로 접었다 — 이 모듈은 `PRICE_COUNT_MISMATCH`(원 개수 ≠15)와 예비가 검증(양수성 → verifier r1 F-2 뒤 4성분 규칙으로 확장, 아래 (7))을 분리된 사유로 낸다(조용한 drop 금지, 위협 모델 (b)). (2) legacy 는 예외(`ValueError`)로 실패했다 — 이 모듈은 전부 `SampleRejected(reason)` 결과 타입이다(위협 모델 (a), verifier r1 F-1 로 `Rate` 파싱 경로의 예외 탈출을 마저 막아 이 서술이 사실이 됐다 — 아래 (8)). (3) K6(`draw_mean_moments`)가 자체적으로 내는 `Unmeasurable`(표본 전부 동일값·모분산 0, golden M-3 이후 추가된 경로로 legacy 에는 없었다)을 삼키지 않고 `RESERVE_DRAW_UNMEASURABLE`로 전파한다. (4) `bid_to_assessment_ratio`(환산 계수)는 이식하지 않는다(D-5D2-6 — legacy 자신이 basis 혼재·미검증 근사라고 적음). 대신 `observed_bid_rate` 자체를 legacy 가 그 비에 적용하던 같은 수치 밴드(`policy.bid_ratio_plausible_*`, 0.5~1.5)로 직접 sanity 확인한다(`BID_RATE_OUT_OF_BAND`). (5) `base_amount`(`CompetitionSample.base_amount`, 바로 `Money`) 성분 넷(verifier r2 N-4 — `Money`는 필드 다섯이나 `vat_treatment`는 5B 도 판정하지 않는다) 검증은 5B `features/facts.py::_resolve_base_amount`와 같은 규칙(basis·currency·provenance·amount_won)을 재현했다 — 그 함수는 `BaseAmountFact`(oneof) 를 받고 이 wire 필드는 oneof 가 아니라서 직접 재사용하지 못했다(5B 파일 편집 금지, 함수 자체는 호출하지 않음 — 규칙만 재현, 소규모 중복 6줄). (6) `AssessmentProvenance` 라벨 미러는 legacy 에 대응물이 없다(legacy 는 provenance 축이 없었다, `BASIS_CLEAN` 문자열 하나만 검사) — 5D 의 K5 provenance 게이트(D-5D-5)를 위해 wire `BaseAmountProvenanceLabel` 5값을 그대로 미러하는 신규 매핑이다. (7) **verifier r1 F-2** — `reserve_prices`의 `Money`가 `amount_won > 0`만 보던 구판을 `_validate_money_amount`(위 (5)와 같은 4성분 규칙 — `base_amount`와 함수 하나 공유, 중복 재구현 금지)로 확장·대체했다. 사유 `NON_POSITIVE_PRICE`를 `RESERVE_PRICE_INVALID`로 개명·포섭했다(scope.md 계약 갱신 이력 2026-09-15 두 번째 행 근거). (8) **verifier r1 F-1** — `observed_bid_rate.fraction`을 관문 없이 `Decimal()`로 읽어 미설정(`""`)·비수치 문자열에서 `decimal.InvalidOperation`이 `serve_bid_rates` 밖으로 새던 결함을 `parse_rate`(신규, 빈/비수치/비유한 거부)로 막았다 — legacy 에는 이 파싱 자체가 없다(값을 그대로 썼다). `award_rate`(optional)도 있으면 같은 관문(조립기가 소비하지 않아도 파싱 가능성은 확인). 사유 `BID_RATE_UNPARSEABLE` 신설. |
| ml-engine/src/ml_engine/inference/availability.py | bid-vector/app/ai/predictors/distribution.py | ed4b06c | `ReserveDrawDistributionPredictor.check_availability`의 최소 표본 임계 둘(`observations`·`ratio_samples` 길이 비교)을 이식. 수정: (1) legacy 는 그 임계를 **availability 조회 경로에서만** 검사했다 — `_estimate_distribution`(실제 추정)은 재검사 없이 `if not observations: raise ValueError`만 했다(01_digest.md §1 「predictor 를 직접 호출하면 표본 1건으로도 추정이 나온다」, legacy 결함). 이 모듈은 가용성 조회와 조립기(`distribution.py`)가 **같은 함수 객체**(`distribution_availability`)를 호출하게 해 그 우회를 구조적으로 막는다(D-5D2-7). (2) `PredictorAvailability(bool, str)`(자유 문자열 사유)를 `Available | Unmeasurable`(`UnmeasurableReason`×`UnmeasurableDetail` enum 조합, `predict.py::segment_availability`와 같은 패턴)로 대체 — `Available` 마커는 새로 정의하지 않고 `predict.py`의 것을 재사용한다(값 없는 마커 타입 중복 금지). (3) `PRICE_PREDICTION_DISTRIBUTION_MIN_RESERVE_RECORDS`(8)·`PRICE_PREDICTION_DISTRIBUTION_MIN_BID_RATIO_SAMPLES`(3) 모듈 상수는 이식하지 않고 기존 `InferencePolicy.reserve_min_reserve_records`·`bid_ratio_min_samples`(5D 가 이미 그 값으로 도입 — 이 slice 신설 아님)로 소비한다. |
| ml-engine/src/ml_engine/inference/distribution.py | bid-vector/app/ai/predictors/distribution.py | ed4b06c | `_estimate_distribution`의 조립 흐름(관측 추출 → K6 draw 모멘트 평균 → 3계층 K5 수축 → `predictive_std = sqrt(posterior.std**2 + mean(draw_variance))` → 투찰율 축 환산 → 시나리오)을 그대로 이식. 수정: (1) legacy 는 이력 행의 `agency_name`/`category` 로 3계층(발주기관/공종/전역)을 나눴다 — M2 wire `CompetitionSample`에는 그 필드가 없어(D-2B-3 인용 근거 밖) **global 레벨만** 채운다(알려진 제한, 모듈 docstring·checklist.md). (2) `bid_ratio = float(median(ratio_samples))`는 legacy 그대로(축만 `bid_to_assessment_ratio`→`observed_bid_rate_i/center_i`, D-5D2-6). (3) `historical_sample_size`(observation_count)·`agency_match_sample_size`(문자열 설명에만 실림, 구조화 안 됨)를 `Diagnostics.agency_sample_count`·`agency_sample_below_threshold`(golden `ml-kernel-011`, ML-04 ②)로 구조화 — 이 조립기에서는 agency 축이 항상 없어 실제로는 항상 `0`/`(0 < threshold)`이지만, 함수(`_resolve_diagnostics`)는 K5 가 agency/category 를 채울 미래를 위해 세 갈래(DIRECT/PARENT_CATEGORY/GLOBAL)를 온전히 구현한다. (4) `_estimate_distribution_confidence`(합성 신뢰도 5상수)는 이식하지 않는다(§6.5, scope.md 「만들지 않는 것」). (5) `_build_distribution_explanation`(한국어 설명 문자열)은 이식하지 않는다 — Kotlin 표현 계층 소관. (6) `ValueError`(관측 0건·비율 표본 0건) 둘은 `distribution_availability`(D-5D2-7) 결과 타입으로 대체(위협 모델 (a)). (7) `OPEN-5D2-SAMPLE-SEGMENT`(scope.md 계약 갱신 이력 2026-09-15) 반영 — `DistributionRequest.samples`를 `SegmentedSample`(wire 표본 + `segment: SampleSegment \| SegmentMissing`)로 감쌌다(legacy 에 대응 없음, 신규). `from_proto`는 표본마다 항상 `SegmentMissing()`을 낸다(M2 2F 가 `agency_id`·`category_code`를 `CompetitionSample`에 추가하면 채워질 자리). 팀장 계약 문구의 `Missing`은 5B `features.Missing(reason: int)`과 이름이 겹쳐 `SegmentMissing`으로 명명했다(값 있는 필드가 없는 별개 마커). |

## 이 slice 가 신규 작성한 것(포인터 없음, 이 표 대상 아님)

`engine.py`(진입점 — legacy `orchestration.py`는 predictor 선택·폴백이 전부라 D-5D2-1(b)
분포 단독 결정 아래 재활용 대상이 아니다, 신규 조립) · `results.py`/`policy.py` 확장(기존
5D 파일에 필드 추가 — 이 slice 의 신규 필드는 legacy 대응물이 없다: `Diagnostics.
agency_sample_count`·`agency_sample_below_threshold`, `IntervalSource.
POSTERIOR_PREDICTIVE`, `UnmeasurableDetail.TOO_FEW_OBSERVATIONS`·`TOO_FEW_RATIO_SAMPLES`,
`DistributionRelease`, `InferencePolicy.assessment_agency_sample_threshold`) ·
`DistributionRequest`(신규 내부 타입 — legacy `PricePredictionContext`와 형태가 다르다,
wire `from_proto` 검증 진입점).
