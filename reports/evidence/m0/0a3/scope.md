# Slice 계약 — M0 / 0A3 결정 근거 정정 (X-1) + §10.1 형태 7

```yaml
milestone: m0
slice: 0a3-decision-basis-correction
base_sha: 48151b9   # 0B Codex 4차 approve verdict 등재 직후의 HEAD
head_sha: 77c5b98   # 이 scope 커밋의 직전 커밋. 「`head_sha`와 range」 절 참조
in_scope:
  - reports/evidence/m0/0a2/decisions.md      # X-1 정정 한정 + provenance 등재
  - reports/evidence/m0/0a2/checklist.md      # §10.1 형태 7 등재 한정
  # capability-map.md 는 §13 「0B — 예산 basis 불일치」 인계 행의 아래 두 자리만.
  # ① X-1 정정(`사용자 영향`)  ② F-4 조건부화(`검증 방법`) — ②는 최초 계약 밖이었고
  # 운영자 결정 2026-08-27 로 열렸다. 자세한 것은 A4 와 「갱신 이력 — 수정 라운드 2」.
  - docs/discovery/capability-map.md          # §13 인계 행의 `사용자 영향`(X-1) + `검증 방법`(F-4)
  - reports/evidence/m0/0a3/
out_of_scope:
  - capability-map.md 의 위 두 자리 밖 모든 서술 (X-2~X-6 포함 — 아래 「(A) 결정」 참조)
  - §13 인계 행의 나머지 필드(`관찰` · `V2 예방 제약` · `근거` · `마이그레이션` · `STR-16 주의`)
  - capability-map.md §13 밖의 X-1 프레이밍 (아래 「발견했으나 고치지 않은 것」 O-2)
  - docs/discovery/regression-ledger.md (0B Codex approve로 확정)
  - 다른 M0 산출물: data-dictionary(0C), ADR(0D), fixtures
  - 활성 OPEN 45건 전부 — 해소하지 않는다
  - Kotlin/Spring/Python 애플리케이션 코드
  - .claude/ 하네스
  - bid-vector/ symlink 아래 기존 저장소 수정 (읽기 전용)
acceptance_commands:
  - "N/A — 문서 slice. 아래 A1~A5를 checklist.md로 대조"
rollback: "N/A — 문서 산출물은 git revert로 복구"
```

작성: 2026-08-27, v2-slice-pipeline. 초안은 `_workspace/m0-0a3/00_draft_scope.md`이며
이 파일이 정본이다 — 초안의 `base_sha: TBD`를 **`48151b9`**로 확정하고,
초안에 없던 **A5**(§10.1 형태 7)를 팀 리드 지시로 더했다.

## 왜 이 slice가 필요한가

0C 선행 조사(`_workspace/m0-0c/01_scout_data_dictionary_preflight.md` §0.6 X-1)가
**`decisions.md`의 `OPEN-STR-01` 결정 근거 서술이 legacy와 어긋난다**는 것을 찾았다.

- 서술: `Project.budget_estimate`는 **추정가격(ex-VAT)**이고, 기초금액과의 차이는
  **VAT·사정률만큼 체계적**이다.
- 실측(`git show ed4b06c:`로 직접 열어 확인, 출력은 `commands.md` **C-1**):
  `app/domain/money.py:30`이 `BUDGET_ESTIMATE`를 **"추정가격: 부가세 포함 추정 총액"**으로
  선언하고, `app/schemas/bid_summary.py:69-77`이 **"저장된 추정가격의 부가세 포함 여부가
  이력상 일관되지 않다"**고 legacy 스스로 공시한다.

**이 프레이밍은 팀 리드가 분석 단계에서 만들었고** 산출물과 결정 기록으로 승격됐다
(§10.1 **형태 2**의 사례다).

**결정 자체는 유효하다.** 두 금액이 다른 basis라는 것과 운영자 의도가 기초금액이라는 것은
그대로이고 `legacy-defect` 판정도 그 위에 선다. 무너지는 것은 **"그 차이가 VAT만큼"이라는
정량 서술**뿐이다.

0B ledger의 같은 프레이밍은 commit **`3eb5a6e`**에서 이미 정정됐다. 남은 것은 그때
**out_of_scope였던 두 파일**이다.

두 번째 일은 **§10.1에 형태 7을 등재**하는 것이다. 0B가 그 근거를 세웠으나
**§10.1이 `reports/evidence/m0/0a2/checklist.md`에 있어 0B의 out_of_scope**였고,
0B는 판단만 기록하며 *"표를 고치려면 별도 slice 계약이 필요하다"*고 적었다
(`reports/evidence/m0/0b/commands.md` **C-8.5**). **이 slice가 그 계약이다.**

## acceptance 항목

- **A1.** `decisions.md`의 `OPEN-STR-01` 절과 `capability-map.md` §13에서 **ex-VAT 단정과
  "VAT만큼" 정량 서술이 제거**되고, legacy 실측(`money.py` · `bid_summary.py`)이 근거로
  달린다. **결정 내용(기초금액, `legacy-defect` 판정)은 바뀌지 않는다.**
- **A2. `decisions.md`의 provenance 규약을 지킨다.** 이 파일은 **원본 문장을 지우지 않고**
  취소선 + 인용 블록으로 정정하며, **「원본 절에 대한 slice 변경 — 전수」 목록과
  재현 명령(`git diff -U0 6a4e49b -- reports/evidence/m0/0a2/decisions.md`)에 이 정정을
  등재**한다. 그 목록이 갱신되지 않으면 A2 실패다.
- **A3.** 정량 서술이 사라진 자리에 **그 축을 소유하는 활성 `OPEN`**이 적힌다.
  **새 `OPEN`을 만들지 않는다.**
  > **팀 리드 지시와 어긋난 지점 — 소유자는 `OPEN-QUAL-10`이 아니라 `OPEN-REG-05`다.**
  > 지시 메시지는 *"그 축은 활성 `OPEN-QUAL-10`이 소유한다"*로 왔으나 **0B가 그 귀속을
  > 이미 반증하고 바로잡았다.** `OPEN-QUAL-10`은 **시공능력평가금액 축**(공고 게시 요건
  > `cnstrtnAbltyEvlAmtList` ↔ 운영자 보유액)이고, 기초금액↔추정가격의 과세 처리는
  > **`OPEN-REG-05`**(`docs/discovery/regression-ledger.md` §9, 활성)가 소유한다.
  > 근거는 `reports/evidence/m0/0b/scope.md`의 **verifier F-2** 두 라운드다 —
  > 계열 A 스윕이 `R-BASIS-01`~`OPEN-QUAL-10`을 *"오탐 — 다른 금액 축"*으로 판정했는데
  > 산출물에는 그 귀속이 남아 있었고, `OPEN-REG-05`를 신설해 옮겼다.
  > **지시대로 `OPEN-QUAL-10`을 적으면 0B가 고친 오류를 되살리고 두 문서를 서로
  > 모순되게 만든다**(ledger는 *"`OPEN-QUAL-10`은 … 이 질문의 소유자가 아니다"*라고
  > 적는다). **지시의 취지 — "새 OPEN을 만들지 말고 이미 등록된 소유자를 가리켜라" —
  > 는 `OPEN-REG-05`로 그대로 충족된다.** 두 id를 병기해 어느 쪽이 소유자이고 어느 쪽이
  > 아닌지를 그 자리에 적었다.
  > `OPEN-REG-05`는 ledger의 자체 id 공간이라 **`capability-map.md` §12의 활성 OPEN
  > 집계에 들어가지 않는다** — A4의 불변과 충돌하지 않는다.
- **A4.** 불변: `capability-map.md`의 **변경이 §13 「0B — 예산 basis 불일치」 인계 행의
  두 자리에 한정된다** — **`사용자 영향`(X-1 정정)**과 **`검증 방법`(F-4 조건부화)**.
  **그 행의 나머지 필드와 그 행 밖은 무변경**이다. 활성 OPEN **45**, capability **95**,
  분류 **61/17/6/11**, `git diff --check` 0. **어디가 바뀌었는지는
  `git diff -U0 <base>...<head> -- docs/discovery/capability-map.md`가 내고 셈을 여기
  옮겨 적지 않는다.**
  > **계약이 작업 중에 넓어졌다 — 숨기지 않는다.**
  > **최초 계약의 `in_scope`는 `§13 X-1 서술 한정`이었고 A4는 *"X-1 외 서술 무변경"*이었다.**
  > **F-4(`검증 방법`의 `OPEN-REG-05` 조건부화)는 그 계약에서 밖이었다** — 이 파일의
  > **O-1 행**이 최초에 그것을 *"고치지 않았다"*로 등재한 것이 그 증거다.
  > **운영자 결정(2026-08-27)이 그것을 승인해 범위가 실제로 넓어졌다.**
  > **승인된 것은 두 자리뿐이다** — §13 인계 행의 **`사용자 영향`**과 **`검증 방법`**.
  > **「같은 행 전체」가 아니다**: 그 행의 `관찰`·`V2 예방 제약`·`근거`·`마이그레이션`·
  > `STR-16 주의`는 **여전히 out_of_scope**이고 손대지 않았다.
  > **A4의 표현도 함께 바꿨다** — 대조가 **「hunk 하나」·「한 줄 교체」**라는 **셈**이었고
  > 두 번째 자리가 들어오면서 어긋날 수 있게 됐다. **셈 대신 대상으로** 적어 다음 편집에
  > 낡지 않게 했다(Codex 1차 **F-2**가 걸린 것과 같은 부류).
  > **넓어지지 않은 것도 분명히 한다** — **X-2~X-6**과 **O-2**(§13 밖의 X-1 프레이밍)는
  > **승인 대상이 아니었고 여전히 out_of_scope**다. **0C 데이터 사전이 정본**이 된다.
  > 근거: **운영자 결정 2026-08-27** · **Codex 1차 medium**(`capability-map.md:3171`) ·
  > **Codex 2차 medium**(이 문면의 자기모순을 지목).
- **A5.** §10.1 표에 **형태 7 「미결을 확정으로 쓰기」**가 등재되고 **0B 실물로 근거가
  달린다** — Codex 1차 high(`R-COL-02`) · verifier F-1(`OPEN-DEC-07`) · verifier
  F-2(`R-BASIS-01` 귀속). 인용은 0B evidence를 직접 열어 확인한다.

## (A) 결정 — X-2~X-6은 0C가 정본이 된다 (운영자 승인 2026-08-27)

0C 선행 조사가 `capability-map.md`에서 **부정확 5건**을 더 찾았다. 운영자가
**"0C가 정확한 서술을 자기 문서에 쓰고 capability-map 부정확은 알려진 제한으로 기록"**을
승인했다(2026-08-27).

| # | 내용 | 0C에서의 처리 |
| --- | --- | --- |
| **X-2** | DEC-11 3행이 2행과 같은 밴드다(별도 밴드가 아니라 술어 한 벌) | 0C 밴드 인벤토리가 정본 |
| **X-3** | DEC-11 표에 **없는 밴드가 최소 2종** — `EXCLUDED_ASSESSMENT_BAND`(사정률 1±0.001 표본 제외, **DEC-04 편향 방향의 원인**) · `BID_RATIO_PLAUSIBLE`의 별도 사본 | 0C 밴드 인벤토리가 정본 |
| **X-4** | 투찰율 클램프 출처가 docstring 언급을 가리킨다(실제 리터럴은 다른 파일) | 0C가 실제 위치를 인용 |
| **X-5** | SET-07 S4가 **3개 논리합**인데 2개로 요약됐다(`total_count`가 별도 축) | 0C "정산됨" 정의가 정본 |
| **X-6** | DEC-05가 `bid_target.py`를 **디렉터리 없이** 인용한다 | 0C가 전체 경로로 인용 |

**이 slice는 X-2~X-6을 고치지 않는다.** 0C 계약이 이 표를 입력으로 받아
**"capability-map의 해당 서술은 부정확하며 0C가 정본"**을 명시한다.

**X-1만 예외인 이유**: 그것은 **운영자 결정의 근거 서술**이라 0C 문서로 대체되지 않는다.
나중에 "이 결정이 무엇에 근거했는가"를 되짚을 때 그 자리에 남는다.

## 0A2·0B에서 가져오는 규율 — 처음부터 적용한다

0A2가 열두 라운드를, 0B가 네 번의 Codex 라운드를 태운 지점은 **승인된 문서의 재편집**과
**evidence 자기서술**이었다. 그 재발 지점을 계약에 박는다.

| 규율 | 이 slice에서의 적용 |
| --- | --- |
| **형태 2** — legacy를 열지 않고 인용하기 | X-1 근거 두 자리를 `git show ed4b06c:`로 직접 열었다. **행 범위는 블록 경계까지** — `bid_summary.py`의 `Field(` 호출은 **`:69`에서 시작**한다(0B L-1이 `:70`으로 걸렸다). 출력은 `commands.md` C-1 |
| **형태 5** — 정정을 인용 지점에 전파하지 않기 | X-1 프레이밍을 in_scope 두 파일에서 **패턴으로 전수 훑었다**(C-3). §13 밖에서 나온 것은 **고치지 않고 등재**했다(O-2) |
| **형태 6** — 셈으로 전칭을 주장하기 | **셈을 산문에 옮겨 적지 않는다.** 불변은 「확인할 것 / 명령」 표로 두고 **결과를 여기 적지 않는다.** `decisions.md` 전수 목록의 *"네 자리뿐"* 셈은 값을 고치는 대신 **지웠다**(0B verifier `N-1` 처방) |
| **형태 7** — 미결을 확정으로 쓰기 | A3의 소유 `OPEN` 귀속이 정확히 이 축이다. **소유자를 지목할 때 그 OPEN이 같은 축인지 확인**했다 — 그 확인이 팀 리드 지시와의 불일치를 냈다 |
| **이력 절 규약**(0B) | 이력 절은 **자기가 속한 커밋의 diff를 서술하지 않는다.** 이미 커밋된 SHA만 지목하고 파일 목록은 `git show --stat --format='' <SHA>`에 맡긴다 |
| **줄 번호 자기참조 금지** | legacy 파일 행 범위만 예외. `decisions.md` 전수 표의 훅 번호는 **그 표 자신의 설계**이며, 이 slice가 **HEAD 기준 좌표임을 그 자리에 명시**했다 |
| **절 머리에 표의 마지막 번호 금지** | 형태 7 절 머리는 *"왜 새 형태인가"*이고 표의 마지막 번호를 박지 않는다(0B `N-1`) |

## 커밋 구성

| 커밋 | 내용 |
| --- | --- |
| `b3276cb` | X-1 정정 — `decisions.md` `OPEN-STR-01` + provenance 등재 · `capability-map.md` §13 |
| `03d7240` | §10.1 형태 7 등재 |
| `cb1be88` | evidence 최초 — `scope.md` · `checklist.md` · `commands.md`. **Codex 1차가 리뷰한 head** |
| `6857ae8` | **수정 라운드 1** — F-1(high). 반대 방향 단정을 선언/관측으로 좁힌다 |
| `90dde40` | **수정 라운드 1** — F-3(medium). C-4를 재현 가능한 명령으로 |
| `7f546f1` | **수정 라운드 1** — F-2(medium) + evidence 동기화. Codex verdict JSON 등재 |
| `74132ea` | **수정 라운드 2** — F-4(medium). §13 `검증 방법`을 `OPEN-REG-05` 조건부로 |
| `7f25aa4` | **수정 라운드 2** — evidence 동기화 + **A4 갱신** |
| `4aa4e3c` | **수정 라운드 3** — verifier `G-1`~`G-5`. evidence 자기서술 정정 |
| `491684f` | **수정 라운드 4** — verifier `H-1`~`H-4`. `H-1`은 앞 라운드가 지운 참인 절을 되돌린다 |
| `f6bcf18` | **수정 라운드 5** — verifier `P-1`. X-1 스윕 축을 넓히고 O-2 서술을 실측에 맞춘다 |
| `fbdedb6` | **수정 라운드 6** — 정의되지 않은 이름 하나를 지운다(운영자 승인). **Codex 2차가 리뷰한 head** |
| `2f8b65a` | **수정 라운드 7** — Codex 2차 `M-1`~`M-3`. 계약 범위를 실제 승인에 맞추고 C-3.1의 범위를 좁힌다 |
| `0775a71` | **수정 라운드 8** — verifier `N-1`·`N-2`. 그 라운드가 만든 구조 주장 둘을 사실로 되돌린다 |
| `f49155d` | **수정 라운드 9** — verifier `Q-1`. 두 축의 발견을 합친 자리를 갈라 적는다 |
| `2f74829` | **수정 라운드 10** — Codex 3차 `R-1`. `O-2`를 출처 층으로 다시 판정한다 |
| `a264d6f` | **수정 라운드 11** — Codex 3차 `R-2`. C-3.3의 출력 순서를 `sort`로 고정한다 |
| `5d2ca03` | **수정 라운드 12** — verifier `V-1`~`V-3`. 적은 대로 하고, 귀속과 환경 서술을 실측에 맞춘다 |
| `d2cced5` | **수정 라운드 13** — verifier `W-1`~`W-3`. 개명이 끊은 포인터를 잇고 보장 주장을 지운다 |
| `32f7a63` | **수정 라운드 14** — verifier `Y-1`. 포인터 축을 넓히고 그 능력을 실행으로 잰다 |
| `6b99420` | **수정 라운드 15** — verifier `Z-1`(blocker)·`Z-2`. 편집 잔해를 지운다 |
| `aabde68` | **수정 라운드 16** — Codex 4차 `A`. 남은 두 항목의 층을 `legacy-behavior`로 |
| `d8fcef8` | **수정 라운드 17** — Codex 4차 `B`. 스크립트 인벤토리를 셋으로, 셈을 지운다 |
| `47e1716` | **수정 라운드 18** — Codex 4차 `C`. 라운드 15 자기 기록을 실측에 맞춘다 |
| `7bcf2e7` | **수정 라운드 19** — verifier `F-1`. 동결 이력 본문을 원문으로 되돌리고 표시를 단다 |
| `c69c04c` | **수정 라운드 20** — verifier `F-2`. 출력 블록을 선언 SHA의 트리에서 다시 뜬다 |
| `b940074` | **수정 라운드 21** — Codex 5차 `G-1`. C-9 명령의 축약 경로를 전체 경로로 |
| `fea90f2` | **수정 라운드 22** — Codex 5차 `G-1`. C-9를 새 선언 SHA의 트리에서 다시 뜬다 |
| `f18b990` | **수정 라운드 23** — Codex 6차 `H-1`·`H-2`. 산문을 출력 자리에서 걷어낸다 |
| `9910c96` | **수정 라운드 23b** — C-10 스캐너의 리터럴 백틱을 없앤다(추출이 깨졌다) |
| `4cccf21` | **수정 라운드 24** — 두 블록을 선언 SHA의 트리에서 다시 뜬다 |
| `77c5b98` | **수정 라운드 25** — verifier `GI-1`·`GI-2`. 스캐너가 명령을 논리 단위로 집게 하고, 인벤토리에 C-10 행을 더한다 |
| 이 커밋 | **수정 라운드 26** — C-9·C-10을 그 수정의 트리에서 다시 뜬다 |

**파일 목록도 셈도 여기에 옮기지 않는다** — `git show --stat --format='' <SHA>`가 낸다.

**커밋을 나눈 이유는 각 커밋에서 문서가 자체 정합**하기 때문이다. X-1 정정과 형태 7 등재는
서로 독립이고, 수정 라운드는 **finding별로 갈라** 리뷰가 하나씩 볼 수 있게 했다.

## 발견했으나 고치지 않은 것 — 계약이 금지한 자리

**계약이 「X-1 외의 것이 눈에 띄면 고치지 말고 보고한다」로 정했다.** 아래는 그때
in_scope 선언(**최초 문면은 *"§13 X-1 서술 한정"***)을 벗어났던 것들이다.
**그 선언은 이후 운영자 승인으로 `검증 방법`까지 넓어졌다**(A4의 인용 블록 참조) —
**`O-1`이 닫힌 것이 그 결과**이고, **`O-2`·`O-3`은 그 승인 밖이라 그대로 열려 있다.**
**`O-1`은 이후 운영자 결정으로 계약이 열려 닫혔다**(수정 라운드 2 · **F-4**) —
행을 지우지 않고 **처리 열에 그 사실을 적는다.** **`O-2`·`O-3`은 여전히 열려 있다.**

| # | 무엇 | 왜 문제인가 | 판단 |
| --- | --- | --- | --- |
| **O-1** — **닫혔다** | §13 같은 행의 **`검증 방법`**이 *"과세/비과세 공고 경계 쌍"*을 **무조건**으로 요구했다. **Codex 1차가 같은 진단을 medium으로 냈다**(verdict의 `capability-map.md:3171` finding) | 0B ledger의 대응 항목(`R-BASIS-01`)은 같은 요구를 **`OPEN-REG-05` 조건부**로 돌리고 *"지금 만들면 legacy의 미결 과세 의미로 corpus 경계가 굳는다"*고 적는다. 조건 표시 없이 두면 **형태 7(계열 A) 그 자체**이며, 정정된 `사용자 영향`과 **같은 행 안에서 어긋났다** | **운영자 결정(2026-08-27)으로 닫았다** — **수정 라운드 2 / F-4**. ledger `R-BASIS-01`의 조건부 서술을 따라 **결정 무관 / 조건부(`OPEN-REG-05` 종료 후)**로 갈랐다. **새 문면을 발명하지 않았다.** 최초 판단(*"범위 밖이라 고치지 않는다"*)은 그 시점의 계약을 따른 것이라 지우지 않는다 |
| **O-2** | **`capability-map.md` §13 밖에, 두 금액의 차이를 `legacy-behavior` 층에서 정량화·방향화하면서 `OPEN-REG-05`를 지목하지 않은 서술이 남아 있다** — **§6 NOTI(투찰률)**(*"**과세 공고에서** 하한 여유가 **부풀어 보인다**"*)와 **§7 SET(SET-09 F-4)**(*"**과세 공고에서 약 10%** 어긋난다"*). **자리와 수는 산문에 옮겨 적지 않는다** — **`commands.md` C-3.4**의 스윕 출력과 그 아래 **판정 표**가 정본이다 | 팀 리드 지시는 X-1이 *"두 파일에 남아 있다 — `decisions.md` `OPEN-STR-01` 절과 `capability-map.md` §13"*이라 적었으나 **그 완결 주장이 참이 아니다.** **§7의 `약 10%`는 0B ledger `R-BASIS-06`이 같은 주장에 ※를 달아 `OPEN-REG-05` 소유로 돌린 그 문장인데 여기엔 그 귀속이 없다.** **판정 기준도 0B가 세웠다** — verifier **`F-c`**의 판정 *"**「legacy 서술이니 그대로」로는 부족하다** — 귀속 없이 적으면 이 문서의 확정 서술로 읽힌다"*이며, **유보를 실제로 단 것은 `F-3`(`R-BASIS-06`)과 `F-b`(`R-BASIS-03`)**다. <br> **⚠ 이 행은 수정 라운드 10에서 좁혔다**(Codex 3차 medium). 앞서 **§5 DEC**와 **§6 금액 라벨·acceptance**까지 넣었으나 **그것들은 `authoritative` 층**(조달청 정의 `추정가격` = 부가세 제외를 정확히 인용)**이거나 표시 요구**라 **X-1이 무너뜨린 저장값 층을 말하지 않는다.** **`O-2`가 줄었고 0C 인계 내용이 달라진다** — 그 자리들은 **고칠 대상이 아니라 그대로 쓸 수 있는 인용**이다 | **고치지 않았다.** in_scope가 *"§13 X-1 서술 한정"*이라 §5·§6을 건드리면 **A4의 구조적 불변(§13 밖 무변경)이 깨진다.** 위치는 `commands.md` **C-3.4**의 스윕 출력이 낸다 |
| **O-3** | `decisions.md` 전수 표의 **훅 번호가 편집마다 낡는다** | 이 slice가 훅 하나를 더하자 뒤따르는 값이 전부 밀렸다. **값을 고치는 「인스턴스 고치기」**이며 다음 편집에 다시 낡는다(0B `F-4`·`N-1`과 같은 뿌리) | **셈만 지우고 번호는 유지**했다. 번호를 없애려면 표의 설계(훅↔행 1:1)를 바꿔야 하고 그것은 이 slice의 위임 범위 밖이다. **그 자리에 「정본은 명령의 출력」임을 명시**했다 |

**활성 OPEN을 해소한 것도, 새로 만든 것도 없다.**

## 이 커밋에 대해서는 주장하지 않는다

**이 slice의 evidence 셋은 한 커밋이고 이 절이 그 커밋에 실린다.** 그래서 **SHA를 적지
않고 이 커밋에 대해 아무것도 주장하지 않는다** — 0B가 세운 「이력 절 규약」이 그렇게 정한다.

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다** — 값을 두 곳에 두지 않는다.
  커밋이 자기 SHA를 담을 수 없어 그 필드는 **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**. in_scope 준수는 `git diff --name-only 48151b9...HEAD`로
  확인한다.

## 불변 — 이 절은 수를 옮겨 적지 않는다

**셈을 산문에 옮기지 않는다.** 아래는 **확인할 것과 그것을 내는 명령**이고, **결과를
여기 적지 않는다.** 실행 출력의 자리는 `commands.md`다.

| 확인할 것 | 명령 |
| --- | --- |
| in_scope 준수 | `git diff --name-only 48151b9...HEAD` — **목록을 옮겨 적지 않는다.** 나오는 경로가 전부 위 `in_scope` 아래에 있는가만 본다(Codex 1차 F-2) |
| `capability-map.md` 변경이 §13 한 행인가 | `git diff -U0 48151b9...HEAD -- docs/discovery/capability-map.md \| grep -E '^@@'` · `grep -n '^## 13\. ' docs/discovery/capability-map.md` |
| capability 총수 · 분류 4종 · 활성 OPEN 총수 | `commands.md` **C-4** — 인라인 본문을 marker로 뽑아 stdin으로 실행한다. base와 HEAD 양쪽. **커밋되지 않은 스크립트 파일에 의존하지 않는다**(Codex 1차 F-3) |
| 새 `OPEN` 신설 0 | `git diff 48151b9...HEAD -- docs/discovery/capability-map.md \| grep -cE '^\+.*\\\| OPEN-[A-Z]+-[0-9]+ \\\|'` |
| `decisions.md` 전수 표가 훅과 1:1인가 | `git diff -U0 6a4e49b -- reports/evidence/m0/0a2/decisions.md \| grep -E '^@@'` |
| X-1 프레이밍이 in_scope에 살아 있는가 | `commands.md` **C-3**의 스윕 |
| 공백 오류 | `git diff --check 48151b9...HEAD` — **결과를 옮겨 적지 않는다**(Codex 1차 F-2) |
| legacy 인용의 블록 경계 | `commands.md` **C-1** |
| 형태 7이 인용하는 0B 자리가 실재하는가 | `commands.md` **C-5** |

**이 커밋에 대해서는 아무것도 주장하지 않는다.**

## 규격상 N/A 항목 (evidence-pack §"문서 slice")

| 항목 | 처리 |
| --- | --- |
| `acceptance_commands` | N/A — 문서 slice. A1~A5 대조가 대신한다 |
| `differential.json` | N/A — Python/V2 실행 비교가 없다 |
| `golden-manifest.json` | N/A — fixture를 사용하지 않는다 |
| `rollback.md` | N/A — 문서 산출물은 `git revert`로 복구. base `48151b9`로 되돌릴 수 있다 |
| `codex-review-*.json` | **존재** — 목록은 `git ls-files 'reports/evidence/m0/0a3/codex-review-*.json'`로 재현된다. codex-reviewer 레인이 작성했고 spec-writer는 읽기만 한다(append-only) |

---

## 갱신 이력 — 수정 라운드 1 (Codex 1차 `request_changes`)

verdict 정본: `reports/evidence/m0/0a3/codex-review-20260827T132148Z.json`(등재, append-only).
`reviewed_base` **`48151b9`** → `reviewed_head` **`cb1be88`**. **severity별 건수는 옮겨
적지 않는다** — 그 JSON의 `findings`가 정본이다.

**건별 처리는 `checklist.md` §8**에 있다. 여기에는 이 slice의 계약에 걸리는 것만 적는다.

### F-1이 뒤집은 것 — 등재하는 형태를 그 라운드에 저질렀다

`decisions.md`의 정정 블록이 **`budget_estimate`는 ex-VAT가 「아니다」**라고 **반대 방향으로
새로 단정**했다. 근거가 지지하는 것은 `money.py`의 **선언**뿐이고 바로 다음 근거가
**저장 값의 일관성 부재를 공시**한다. **실제 의미는 활성 `OPEN-REG-05` 소유**이며
`commands.md` C-1이 스스로 그렇게 적어 **자기 evidence와도 어긋났다.**

**이 slice는 §10.1에 형태 7 「미결을 확정으로 쓰기」를 등재하는 그 라운드에 같은 형태를
저질렀다.** 0A2 §10.1이 형태 3에 대해 적은 것과 같은 구조다 — *"규칙을 적는 것과 그
라운드의 자기 evidence에 적용하는 것은 다른 일"*. **지우지 않고 2차 정정 주석에 남겼다.**

이 계약의 「0A2·0B에서 가져오는 규율」 표는 **형태 7의 적용을 A3의 소유 `OPEN` 귀속으로만
좁혀 적고 있었다.** 그 좁힘이 이 실패를 덮지 못했다 — **같은 블록의 legacy 사실 서술에도
같은 축이 걸린다.** 표를 고쳐 적지 않는다(그 시점의 판단이다). **드러난 것을 여기 적는다.**

### F-2 · F-3이 가리키는 공통 뿌리

둘 다 **evidence가 자기 실행 환경을 정확히 말하지 못한 것**이다.
F-2는 **range가 커밋과 함께 움직이는데 그 전에 뜬 목록을 산문에 박았고**,
F-3은 **커밋되지 않은 스크립트에 기댔다.** 처방도 하나다 —
**낡을 수 있는 값을 옮겨 적지 않고, 대상을 가리키거나 정본에서 다시 계산한다.**

- **F-2**: 목록·셈을 지우고 **명령만 남겼다.** 출력을 기록하는 검사는
  **`capability-map.md`로 경로를 한정**해 뒤 커밋에 낡지 않는 형태로 바꿨다.
- **F-3**: 인라인 본문을 **marker로 집어**(줄 번호가 아니다) `assert`로 유일성을 확인한 뒤
  stdin으로 실행한다. **base와 HEAD 양쪽에서 실제로 돌렸다.**

**출력 블록의 머리에 한 HEAD를 박던 것도 함께 걷어냈다** — 수정 라운드마다 낡는다.
**블록이 각자 자기 실행 시점을 선언**한다.

### 넷째 finding은 건드리지 않았다

`docs/discovery/capability-map.md:3171` — 이 계약의 **O-1**이다. **운영자 판단 대기**이며
지금 손대면 **A4의 구조적 불변(§13 한 줄 교체)이 깨진다.** 판단이 오면 별도로 처리한다.

### 이 커밋에 대해서는 주장하지 않는다

**이 라운드의 마지막 커밋이고 이 절이 그 커밋에 실린다.** 그래서 **SHA를 적지 않고 이
커밋에 대해 아무것도 주장하지 않는다** — 0B가 세운 「이력 절 규약」이 그렇게 정한다.
무엇이 들어갔는지는 `git show --stat --format='' HEAD`가 낸다.

- `head_sha` → `90dde40`(직전 커밋). **range `48151b9...HEAD`**.

### 불변 — 이 절은 수를 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| `capability-map.md` 이번 라운드 무변경 | `git diff --stat cb1be88..HEAD -- docs/discovery/capability-map.md` |
| `regression-ledger.md` 무변경 | `git diff --stat 48151b9...HEAD -- docs/discovery/regression-ledger.md` |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| `decisions.md` 전수 표 ↔ 훅 1:1 | `commands.md` **C-3.5** |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` — `.claude/`·`CLAUDE.md`가 나오지 않아야 한다 |

**이 커밋에 대해서는 아무것도 주장하지 않는다.**

---

## 갱신 이력 — 수정 라운드 2 (F-4 · 운영자 결정 2026-08-27로 열림)

**앞 라운드가 「운영자 판단 대기」로 두었던 Codex 1차 넷째 finding을 닫았다.**
위 「수정 라운드 1」 절의 *"넷째 finding은 건드리지 않았다"*는 **그 시점의 계약을 따른
정확한 기록**이라 고쳐 쓰지 않는다 — **계약이 바뀐 것이지 그 판단이 틀렸던 것이 아니다.**

### F-4 — 같은 행 안의 모순을 ledger 정본대로 갈랐다

§13 「0B — 예산 basis 불일치」 인계 행이 **앞쪽에서 과세 처리를 `OPEN-REG-05` 소유라 하고
뒤쪽 `검증 방법`에서 과세/비과세 경계 쌍을 조건 없이 요구**했다. **한 행 안에서 미결을
미결이라 적고 동시에 fixture로 확정한 것**이며 **이 slice가 §10.1에 등재한 형태 7 그
자체**다.

**새 문면을 발명하지 않았다.** authoritative 정본은 0B ledger `R-BASIS-01`의 `검증 방법`
이고 **직접 열어**(`commands.md` **C-6**) 그 두 갈래를 옮겼다 — **결정 무관(무조건)**과
**조건부(`OPEN-REG-05` 결정에 따라 확정)**, 사유(*"지금 만들면 legacy의 미결 과세 의미로
corpus 경계가 굳는다"*)까지 같다.

**나머지 `검증 방법` 항목과 그 행의 다른 필드는 건드리지 않았다.**

### A4를 이 라운드에 바꿨다 — 근거를 남긴다

**계약을 작업 중에 고쳤다는 사실이 문면에 드러나야 한다.** 최초 A4는 불변을
**「hunk 하나」·「한 줄 교체」라는 셈**으로 대조하게 돼 있었고, F-4가 **같은 행의 두 번째
자리**를 고치면서 그 셈이 계약과 어긋날 수 있게 됐다.

- **근거**: **운영자 결정 2026-08-27**(넷째 finding을 닫으라는 승인)과
  **Codex 1차 medium**(`capability-map.md:3171` finding).
- **바꾼 것**: 불변의 **표현**이다. **셈을 지우고 대상으로 적었다** —
  **「`capability-map.md` 변경이 §13의 「0B — 예산 basis 불일치」 인계 행에 한정된다」**.
  **Codex 1차 `F-2`가 걸린 것과 같은 부류**의 처방이다(값을 옮겨 적으면 다음 편집에 낡는다).
- **바꾸지 않은 것**: **지키는 범위**다. **X-2~X-6과 O-2는 여전히 out_of_scope**이고
  **0C 데이터 사전이 정본**이 된다. `commands.md` **C-3.4**가 O-2 자리의 무변경을 낸다.

**A4 본문의 인용 블록에 같은 내용을 적었다** — 계약을 읽는 사람이 이력까지 오지 않아도
바뀐 사실과 근거를 그 자리에서 본다.

### 출력 블록을 전부 다시 떴다

**`capability-map.md`가 이 라운드에 바뀌었으므로** 앞 라운드의 출력 중 그 파일에 걸리는
것이 낡는다. **C-1~C-6 전부를 이 라운드의 최종 상태에서 재실행**했고 **각 블록이 자기
실행 시점 HEAD를 첫 줄에 선언**한다(한 값을 파일 머리에 박지 않는다 — 0B `N-1`).

### 이 커밋에 대해서는 주장하지 않는다

**이 라운드의 마지막 커밋이고 이 절이 그 커밋에 실린다.** **SHA를 적지 않고 이 커밋에
대해 아무것도 주장하지 않는다** — 「이력 절 규약」이 그렇게 정한다. 무엇이 들어갔는지는
`git show --stat --format='' HEAD`가 낸다.

- `head_sha` → `74132ea`(직전 커밋). **range `48151b9...HEAD`**.

### 불변 — 이 절은 수를 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| `capability-map.md` 변경이 그 인계 행에 한정 | `commands.md` **C-2.1 · C-2.2** |
| O-2 자리 무변경 | `commands.md` **C-3.4** |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| `decisions.md` 전수 표 ↔ 훅 1:1 | `commands.md` **C-3.5** |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` — `.claude/`·`CLAUDE.md`가 나오지 않아야 한다 |

**이 커밋에 대해서는 아무것도 주장하지 않는다.**

---

## 갱신 이력 — 수정 라운드 3 (verifier `not-ready` · `G-1`~`G-5`)

**Codex 1차 네 건은 전부 처방대로 닫힌 것으로 확인됐다**(verifier `V-1`~`V-6` pass).
막은 것은 **evidence 자기서술**이고 **공통점은 하나다** — **낡을 수 있는 것을 산문에
옮겨 적었고 뒤 편집이 그것을 무효로 만들었다.**

**값을 고치지 않고 지웠다.** 0B `N-1`이 세운 것이다 — **정확한 값으로 교체하면 다음
편집에 또 낡는다.** **다만 무엇을 지웠는지는 갈래가 다르다** — `G-2`·`G-3`·`G-4`는
**좌표·커밋 서술**을 지우고 **바로 위 출력 블록**을 가리키게 했고(그쪽이 *"출력 블록은
재실행했는데 그 아래 판정 산문만 옛 좌표로 남았다"*에 해당한다), `G-1`은 **`O-1`의 상태
선언**을, `G-5`는 **중복된 값**을 지웠다 — 둘이 가리키는 곳은 출력 블록이 아니다.
건별 처리는 `checklist.md` **§10**에 있다.

### G-4가 무거운 이유 — 내가 세운 규약을 그 자리에서 어겼다

`commands.md` C-2 판정이 *"그 파일을 바꾸는 커밋은 **이 블록과 같은 커밋에 실린다**"*라고
**자기 커밋을 서술**했다. **틀렸고**(두 커밋이다) **이 계약이 세운 「이력 절은 자기가 속한
커밋의 diff를 서술하지 않는다」를 그 자리에서 어긴다.**

**결론 자체는 참이었다** — verifier가 HEAD에서 재실행해 확인했다. **틀린 것은 그 결론을
떠받치는 근거**였고, **근거를 커밋 관계에서 경로 한정으로 바꿨다.** 규약은 이력 절만이
아니라 **evidence의 모든 산문**에 걸린다는 것이 이 건의 교훈이다.

### G-1 — 「현재 상태」 절과 「이력」 절을 갈랐다

`checklist.md` §6·§7은 **산출물의 현재 상태를 선언하는 자리**인데 **수정 라운드 2가 닫은
`O-1`을 열려 있다고 적고 있었다.** 같은 패키지의 다른 자리는 닫혔다고 적어 **evidence가
자기 상태를 두 가지로 말했다.**

- **현재 상태를 말하는 자리**(§6·§7 · `scope.md`의 O-1 행)는 **현재로 맞췄다.**
- **라운드별 이력 절**(§8·§9 · 이 파일의 앞 이력)은 **내용을 고쳐 쓰지 않았다** —
  그 시점의 정확한 기록이고 「이력 절 규약」이 그렇게 정한다. **한 자리만 손댔다**:
  §6에서 `O-1`을 빼면서 §8의 포인터가 **없어진 자리를 가리키게 됐고, 살아 있는 자리
  (`scope.md`의 O-1 행)로 돌렸다.** **판단도 서술도 바꾸지 않았고 가리키는 곳만 바꿨다.**
  `checklist.md` §10 표에 등재했다. **이 파일의 앞 이력은 실제로 무변경**이다.
- **A1~A5 요약 표를 끊고 있던 blockquote를 표 밖으로 뺐다** — 요약만 읽는 사람에게 넷째
  finding이 미처리로 보였다.

### 출력 블록 — 이 라운드는 산출물을 건드리지 않았다

`G-1`~`G-5`는 **전부 evidence 산문**이고 **산출물(`capability-map.md`·`decisions.md`·
`0a2/checklist.md`)을 한 자도 바꾸지 않았다.** 근거는 아래 「불변」 표의 첫 명령이다.
기록된 출력이 여전히 실측과 같은지는 **각 블록의 명령을 다시 돌려 확인한다** — 블록은
자기 실행 시점 HEAD를 첫 줄에 선언한다.

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다** —
**파일 목록도 셈도 여기 옮겨 적지 않는다.** 산출물 무변경 같은 **사실 주장은 아래 표의
재현 명령과 함께** 남긴다.

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 7f25aa4..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| O-2 자리 무변경 | `commands.md` **C-3.4** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` — `.claude/`·`CLAUDE.md`가 나오지 않아야 한다 |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 4 (verifier `H-1`~`H-4`)

`G-1`·`G-3`·`G-4`·`G-5`는 **처방대로 닫힌 것으로 확인됐다.** 건별 처리는
`checklist.md` **§11**에 있다. 여기에는 이 계약에 걸리는 것만 적는다.

### `H-1` — 지우는 처방이 참인 절까지 지웠다

> **⚠ 수정 라운드 10의 출처 층 재판정으로 이 절의 `O-2` 귀속은 무효다.**
> **현재 판정은 `commands.md` C-3.4의 판정 표가 낸다.** 이력 본문은 고쳐 쓰지 않는다.

앞 라운드가 `G-2`를 닫으며 C-3.1에 *"살아 있는 단정은 **없다**"*를 넣었다. **거짓이다** —
위 출력의 매치 하나(**§5 DEC-01**)가 그 분류 넷 어디에도 안 들어가고 **그 자체가 살아
있는 단정**이며, 그것이 바로 이 계약의 **`O-2`**다. **같은 패키지의 네 자리가 「남아
있다」고 적어** evidence가 같은 사실을 두 가지로 말했다.

**`G-2`의 처방(「좌표를 지워라」)은 옳았고, 그 옆에 붙어 있던 참인 절까지 함께 지운 것이
실패다.** **낡는 것(좌표)과 낡지 않는 것(「하나 남아 있다」는 사실)을 갈랐어야 했다.**
지금 문면은 **사실은 남기고 좌표는 `C-3.4`에 맡긴다.**

**`O-2`는 손대지 않았다** — `H-1`은 그 존재를 **정확히 적으라**는 것이지 고치라는 것이
아니다. **`O-2`는 out_of_scope로 살아 있어야 하고**, `commands.md` **C-3.4**가 그 두
자리를 그대로 찍는다.

### `H-3`·`H-4` — 이력 절을 쓰면서 이력 절 규약을 어겼다

**둘 다 앞 라운드의 「갱신 이력 — 수정 라운드 3」 절에서 났다.**

- **`H-3`**: `G-2`·`G-3`·`G-4`에 대해 참인 진단을 **「다섯 다」로 넓혔다.** `G-1`이 지운
  것은 **상태 선언**, `G-5`가 지운 것은 **중복된 값**이고 **둘 다 출력 블록을 가리키지
  않는다.** **전칭을 지우고 갈래를 갈랐다** — 형태 6이 겨냥하는 형태를 그 형태를 처리하는
  절에서 냈다.
- **`H-4`**: 같은 절이 **「산출물을 한 자도 바꾸지 않았다」는 자기 커밋 diff 주장**을
  하면서 동시에 **「이 커밋에 대해 아무것도 주장하지 않는다」**고 적었다. 앞 라운드들은
  커밋이 여럿이라 **「이 라운드」≠「이 커밋」**이었는데 **커밋이 하나가 되자 둘이 같아졌다.**
  **사실 주장은 참이고 재현 명령도 있었다 — 거짓인 것은 선언**이라 **선언을 지웠다.**

**규약의 적용 범위가 다시 좁게 잡혔다.** `G-4`가 *"이력 절만이 아니라 evidence의 모든
산문에 걸린다"*를 교훈으로 적은 **바로 그 라운드의 이력 절**에서 둘이 났다.

### `H-2` — 「고쳐 쓰지 않았다」가 한 자리에서 거짓이었다

앞 라운드가 §6에서 `O-1`을 빼면서 **§8의 포인터가 없어진 자리를 가리키게 됐고, 살아 있는
자리로 돌렸다.** **편집은 옳다** — 끊어진 포인터를 두는 편이 더 나쁘다. **틀린 것은
「이력 절을 고쳐 쓰지 않았다」는 선언**이었고 **§10 표에 등재되지도 않았다.**
선언을 **실측에 맞게 좁히고** §10에 그 편집을 등재했다.

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다** — 파일 목록도
셈도 여기 옮겨 적지 않는다. 산출물 무변경 같은 **사실 주장은 아래 표의 재현 명령과 함께**
남긴다.

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 4aa4e3c..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` — `.claude/`·`CLAUDE.md`가 나오지 않아야 한다 |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 5 (verifier `P-1` · 게이트 안 1건)

`H-2`·`H-3`·`H-4`는 닫혔다. 건별 처리는 `checklist.md` **§12**에 있다.
여기에는 이 계약에 걸리는 것만 적는다.

### 먼저 측정했다 — 문장은 그 다음이다

**팀 리드가 이번엔 대체 문장을 주지 않았다.** `H-1`이 **처방에 실린 예시 문장을 검증 없이
넣은 데서** 났기 때문이고, 같은 형태를 반복하지 않기 위해서다. **제약만 받고 문장은
실측에서 썼다.**

**앞 축(C-3.4)이 좁았다** — 패턴이 어휘 하나(`ex-VAT|부가세 별도`)였고 **줄 단위**였다.
**둘 다 넓혔다**: 어휘를 `ex-VAT` · `부가가치세` · `부가세` · `VAT` · `과세`로, 단위를
**블록**으로. **여러 줄에 걸쳐 숨은 후보가 실제로 있었다** — 0B 가 "창 밖" 미탐으로 두 번
태운 자리와 같은 형태다.

**축은 후보만 좁힌다.** *"이 블록이 이 문서의 단정인가, 귀속된 인용인가"*는 **정본을 계산할
수 없어** 후보 전건을 사람이 읽고 **C-3.4 아래 판정 표**에 근거와 함께 남겼다
(0B C-1 의 두 축 구분). **스윕 본문은 C-3에 인라인**했다(형태 4).

> **⚠ 수정 라운드 10의 출처 층 재판정으로 이 절의 `O-2` 귀속은 무효다.**
> **현재 판정은 `commands.md` C-3.4의 판정 표가 낸다.** 이력 본문은 고쳐 쓰지 않는다.

**결과는 `commands.md` C-3.4가 낸다** — **자리도 수도 산문에 옮겨 적지 않는다.**
요지만 적으면: **살아 있는 X-1 프레이밍이 §5 DEC · §6 NOTI · §7 SET에 걸쳐 있고**,
**부가세 제외 단정**과 **과세로 차이를 설명·정량화하는 서술** 두 갈래다.
**귀속된 인용**(조달청 문서)과 **§13 정정문의 원문 인용**은 제외했다.

### `H-1`과 `P-1`의 뿌리 — 전칭의 범위가 문면에서 갈리지 않았다

`H-1`은 *"살아 있는 단정은 없다"*가 **출력 안의 판정인지 문서 전체의 판정인지** 갈리지
않아 후자로 읽혀 거짓이 됐고, `P-1`은 그것을 고치며 **문서 전체에 대한 수**를 **좁은
출력의 근거로** 적어 다시 거짓이 됐다.

**처방**: **판정하는 문장은 자기가 어느 출력에 걸린 판정인지 문면에 적고, 문서 전체에
대한 주장은 그것을 실제로 훑는 축에 맡긴다.** C-3.1은 이제 **자기 출력에 한정**됨을
스스로 밝히고 **사각지대를 별도 항목으로 세워** C-3.4를 가리킨다.

### 앞 라운드 표의 낡은 수를 지웠다 — 이 편집을 등재한다

앞 라운드들의 「불변 — 확인할 것 / 명령」 표에 *"O-2 **두 자리** 무변경"*이 있었고
**축이 넓어지면서 그 수가 낡았다.** 그 표들은 **그 시점의 기록이지만 「확인할 것」은
검증자가 지금 도는 자리**라 낡은 수를 두면 오도한다. **수만 지우고 가리키는 곳은 그대로
뒀다** — *"O-2 **자리** 무변경 / `commands.md` C-3.4"*. **판단도 명령도 바꾸지 않았다.**
`H-2`가 지적한 그대로 **이 편집을 `checklist.md` §12에 등재**했다.

### `O-2`는 손대지 않았다

`capability-map.md`는 **이번 라운드 무변경**이다. `P-1`은 **`O-2`의 존재를 정확히
적으라**는 것이지 out_of_scope 를 침범하라는 것이 아니다. **0C 데이터 사전이 정본**이 된다.

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다** — 파일 목록도
셈도 여기 옮겨 적지 않는다. 산출물 무변경 같은 **사실 주장은 아래 표의 재현 명령과 함께**
남긴다.

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 491684f..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| C-3.4 스윕이 고정점인가 | 그 절의 명령을 다시 돌려 출력이 같은지 본다 |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` — `.claude/`·`CLAUDE.md`가 나오지 않아야 한다 |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 6 (마지막 1건 · 운영자 승인 2026-08-27)

`P-1`이 닫히고 **게이트 안 0건**으로 `ready-for-review` 판정을 받았다.
**재작업 상한에 도달해 운영자가 이 한 건만 더 고치고 리뷰로 가라고 결정했다.**
건별 처리는 `checklist.md` **§13**에 있다.

### 정의되지 않은 이름을 불렀다

C-3.4 스윕 본문 절이 **`EXTRACT`라는 이름으로 추출 방식을 불렀는데 그 이름이 패키지
어디에도 정의돼 있지 않다.** **실물을 열어 확인했다** — C-3.4는 `BLK4`, C-4.1은 `BLK`를
쓰고 **`EXTRACT`는 그 한 줄에만 나온다.**

**재실행이 막히지는 않는다.** 그러나 **그 절의 제목이 「형태 4 — 이름으로만 부르지
않는다」**이고, **Codex 1차가 이미 같은 계열을 medium으로 냈다**(`inv0a3.py` 미존재).
**규칙을 적은 절이 그 규칙을 어긴 것**이라 고친다.

**이름을 지우고 자리를 가리키게 했다.** 실제 변수명으로 바꾸는 쪽도 가능했으나
**그 절의 논지가 「이름으로만 부르지 않는다」**이고 이름을 적으면 **그것이 또 낡을 수 있는
자리**가 된다. **명령은 바로 위 출력 블록에 그대로 있다.**

**뒤따르는 명제 셋은 참이라 건드리지 않았다** — 줄 번호가 아니라 marker로 집고 ·
`assert`로 유일성 확인 · stdin 으로 흘려 넣어 커밋되지 않은 스크립트 파일에 의존하지
않는다. **두 절의 실물에 대조해 확인했다.**

### 게이트 밖 나머지는 고치지 않았다

**범위를 넓히지 않았다** — 검증자가 *"어느 것도 현재 상태 선언을 거짓으로 만들지 않는다"*고
판정했고 **다음 slice 소관**이다.

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다** — 파일 목록도
셈도 여기 옮겨 적지 않는다. 사실 주장은 아래 표의 재현 명령과 함께 남긴다.

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**. **이 커밋 뒤 Codex 2차로 간다.**

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat f6bcf18..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| C-3.4 스윕이 여전히 고정점인가 | 그 절의 명령을 다시 돌려 출력이 기록과 같은지 본다 |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` — `.claude/`·`CLAUDE.md`가 나오지 않아야 한다 |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 7 (Codex 2차 `request_changes`)

verdict 정본: `reports/evidence/m0/0a3/codex-review-20260827T204954Z.json`(등재, append-only).
`reviewed_base` **`48151b9`** → `reviewed_head` **`fbdedb6`**. **severity별 건수는 옮겨
적지 않는다.** **1차 네 건은 전부 해소 판정을 받았고 산출물 지적은 1차 이후 없다.**
건별 처리는 `checklist.md` **§14**에 있다.

### `M-1` — 계약이 자기모순이었다. 넓어진 사실을 적는다

**A4는 「§13 인계 행 밖 무변경」으로 넓혔는데 YAML `in_scope`는 여전히
`§13 X-1 서술 한정`이었고, A4 인용 블록은 *"범위는 넓어지지 않았다"*고 적었다.**

**범위는 실제로 넓어졌다.** F-4(`검증 방법`의 조건부화)는 **최초 계약에서 X-1 밖**이었고
**이 파일의 `O-1` 행이 최초에 그것을 「고치지 않았다」로 등재한 것**이 그 증거다.
운영자 결정 2026-08-27이 그것을 승인했다.

**승인 범위를 두 항목으로 특정했다** — §13 인계 행의 **`사용자 영향`(X-1 정정)**과
**`검증 방법`(F-4 조건부화)**. **「같은 행 전체」라는 느슨한 경계를 지웠다**: 그 행의
`관찰`·`V2 예방 제약`·`근거`·`마이그레이션`·`STR-16 주의`는 **여전히 out_of_scope**이고
그 사실을 `out_of_scope` 목록에 명시했다. **넓어지지 않은 것**(X-2~X-6 · `O-2`)도 같은
자리에서 갈랐다.

**전수로 맞췄다** — YAML · A4 본문 · A4 인용 블록 · 「발견했으나 고치지 않은 것」 머리말 ·
`checklist.md` §9 · `commands.md` C-3.4 판정 · C-8 행. **한 자리만 고치면 다음 라운드가
또 열린다.**

### `M-2` — C-3.1의 범위를 좁혔다

> **⚠ 이 절은 수정 라운드 8에서 한 번 더 고쳤다** — 절 머리가 *"인스턴스가 아니라 **겹침
> 자체를 없앴다**"*였고 말미가 *"두 축이 이제 서로의 결론을 **반증할 수 없다**"*였다.
> **둘 다 거짓이다**(`N-1`) — **§13 ⊂ `capability-map.md` 전체**라 **두 범위는 여전히
> 포개져 있고 §13을 둘 다 판정한다.** 참인 부분(§5 매치 배제)은 그대로 둔다.
> 「갱신 이력 — 수정 라운드 8」 참조.

**앞 세 라운드가 같은 자리에서 났다.** `H-1`(*"살아 있는 단정은 없다"*) ·
`P-1`(*"하나 남아 있다"*) · 이번 `M-2`(*"매치가 전부 ①~④ 중 하나"*) — **전부 C-3.1의
같은 판정 문장**이고, 뿌리는 **C-3.1과 C-3.4의 범위가 겹쳤다**는 것이다. 겹치면 **같은
매치를 두 축이 다르게 판정**하고, 그때마다 **산문 한쪽만** 고쳐 왔다.

**이번엔 명령의 범위를 좁혔다** — C-3.1이 **`decisions.md` 전체**와
**`capability-map.md` §13**을 본다. §13은 **절 머리로 잘라낸다**(줄 번호가 아니라 내용).
**§5 매치가 더는 그 출력에 섞이지 않는다**(§6·§7은 **이 축의 패턴에 걸리지 않아**
애초에 대상이 아니었다 — **C-3.4가 `P-1` 라운드에 낸 것**이다. 원래 이 자리가 셋을
열거했고 **수정 라운드 9가 좁혔다**). **C-3.4가 문서 전체를 맡는다.**
**두 범위는 그래도 포개져 있다** — §13을 둘 다 본다.

`checklist.md`의 **A1 근거 행**과 **§1**도 같은 범위로 동기화했고, **C-3 절 머리에 두 축의
범위 분할을 선언**했다.

### `M-3` — 스크립트 자기 인벤토리가 거짓이었다

머리글과 C-8이 *"스윕 스크립트를 새로 만들지 않았다 · 유일한 스크립트"*라고 **현재형으로**
적었는데 **`P-1` 대응으로 C-3.4에 스윕이 인라인됐고 실제로 재실행된다.**

**머리글에 「이 패키지가 쓰는 스크립트」 표를 신설**해 **둘**을 각각 **어디 · 무엇을 하나 ·
왜 있나**로 적었다. **X-1 스윕의 도입 근거는 `P-1`**이다 — **결함이 관측된 뒤에 넓혔고**
0B가 세운 순서(결함이 먼저, 도구가 나중)를 지킨 것이다. C-8 행은
**「근거 없이 늘린 축·스크립트」**로 바꿔 **참인 주장만** 남겼다.

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다** — 파일 목록도
셈도 여기 옮겨 적지 않는다.

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat fbdedb6..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| C-3.1 출력에 out_of_scope 매치가 섞이지 않는가 | `commands.md` **C-3.1** — `capability-map.md` 쪽 매치가 **§13 안의 것뿐**이어야 한다 |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` — `.claude/`·`CLAUDE.md`가 나오지 않아야 한다 |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 8 (verifier `N-1`·`N-2`)

**전수 규율은 작동했다** — 앞 라운드의 「고침」이 전건 실제로 고쳐졌고 「손대지 않음」
판단도 옳다는 판정을 받았다. **그런데 그 규율이 거짓 명제를 전수로 퍼뜨리는 데 쓰였다.**
건별 처리는 `checklist.md` **§15**에 있다.

### 앞 라운드가 만든 구조 주장 둘이 거짓이었다

- **`N-1`** — *"두 축의 범위가 겹치지 않는다"* · *"서로의 결론을 반증할 수 없다"*.
  **§13 ⊂ `capability-map.md` 전체**이므로 **두 범위는 포개져 있고 §13을 둘 다 판정한다.**
  **오늘 두 판정이 어긋나지 않는 것은 우연히 같은 결론이기 때문이지 범위를 갈라서가 아니다.**
- **`N-2`** — *"C-3.1의 검사 범위가 A1의 범위와 같다"*. **`decisions.md`를 절로 자르지
  않으므로 상위집합이다.** 자기 출력의 provenance 절 매치가 그것을 보인다.
  **방향은 보수적이라 A1이 요구하는 것을 놓치지 않는다** — 틀린 것은 **등식**이다.

**둘 다 사실로 되돌렸고 새 구조 주장으로 대체하지 않았다.** 범위를 진짜로 가르려면
**C-3.4에서 §13을 빼야** 하는데 그러면 그 축이 「문서 전체」를 못 본다 —
**그 교환은 하지 않았다.**

### 왜 `N-1`이 났나 — 서사를 만들었다

**`M-2` 대응에서 참인 부분(범위 축소 — §5 매치 배제)은 실제로 됐다.** 거기에 **「뿌리를 고쳤다」는 더 큰
주장**을 얹었고 그게 거짓이었다. **이 slice가 반복해 온 형태의 마지막 변종**이다 —
앞서는 **낡을 수 있는 값**을, 이번엔 **검증되지 않은 구조 주장**을 산문에 적었다.
**둘 다 「대상이 스스로 내지 않는 것을 적은 것」**이다.

### 라운드별 이력 절은 고쳐 쓰지 않고 표시했다

**§14와 이 파일의 라운드 7 이력**은 **내용을 고치지 않고 ⚠ 재정정 표시**를 달았다 —
`H-1` 때 `checklist.md` §10 `G-2` 행에 한 것과 같은 관례다. **참인 부분(§5 매치 배제)은
그대로 둔다.**

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다.**

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 2f8b65a..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| C-3.1 출력에 out_of_scope 매치가 섞이지 않는가 | `commands.md` **C-3.1** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` — `.claude/`·`CLAUDE.md`가 나오지 않아야 한다 |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 9 (verifier `Q-1`)

**`N-1`·`N-2`는 둘 다 닫혔다.** `Q-1`은 그 수정에 **딸려 들어온 별개의 사실 오류**다.
건별 처리는 `checklist.md` **§16**에 있다.

### 두 축의 발견을 하나로 합쳤다

앞 라운드가 *"C-3.1이 전체를 grep해 **§5·§6·§7**의 out_of_scope 매치가 섞였고 그것이
Codex 2차 `M-2`였다"*라고 적었다. **거짓이다.**

**C-3.1의 패턴은 `ex-VAT|VAT·사정률만큼` 둘뿐**이고 **§6(`부가세 별도`)·§7(`과세 공고`)은
거기 걸리지 않는다.** **옛 C-3.1이 실제로 낸 out_of_scope 매치는 §5 하나**이고
**Codex 2차 `M-2` 원문도 §5 단수**다.

**§5·§6·§7이라는 셋은 `C-3.4`가 낸 것**이다 — **`P-1` 라운드**에 넓힌 **5어휘 블록 스윕**.
**서로 다른 축이 서로 다른 라운드에 낸 발견인데 회고를 쓰면서 합쳤다.**
`H-3`이 *"셋에 대해 참인 진단을 다섯으로 넓혔다"*로 걸린 것과 **같은 계열**이다.

**§5 단수로 좁히고 §6·§7이 어느 축에서 나왔는지 갈라 적었다.** **합치지 않는다.**

### 새 셈을 만들지 않았다

**「하나」·「셋」을 세는 것이 요점이 아니다** — **어느 축이 무엇을 냈는지**가 요점이라
그렇게 적었다. **불변 표 라벨도 축에 맞췄다** — *"출력에 §5·§6·§7 자리가 없어야 한다"*는
**§6·§7이 애초에 그 패턴에 안 걸려** 검사로 의미가 없었고, **`capability-map.md` 쪽 매치가
§13 안의 것뿐이어야 한다**로 바꿨다.

### 손대지 않은 자리

**C-3.4에 귀속된 열거**(*"살아 있는 X-1 프레이밍은 §5 · §6 · §7에 걸쳐 남아 있다"*)는
**참이라 그대로 뒀다.**

### 명령도 산출물도 바꾸지 않았다

검증자가 *"문면 두 자리 수정으로 닫힌다"*고 판정했고 **문면만 고쳤다.**
`capability-map.md` **무변경**, **`O-2` 생존**, C-3.1·C-3.4의 **명령 불변**.

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다.**

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 0775a71..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **명령이 바뀌지 않았는가** | `git diff 0775a71..HEAD -- reports/evidence/m0/0a3/commands.md` — `$`로 시작하는 줄에 변경이 없어야 한다 |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` — `.claude/`·`CLAUDE.md`가 나오지 않아야 한다 |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 10 (Codex 3차 `R-1` · 출처 층)

verdict 정본: `reports/evidence/m0/0a3/codex-review-20260828T005833Z.json`(등재, append-only).
`reviewed_base` **`48151b9`** → `reviewed_head` **`f49155d`**. **2차 세 건은 해소 판정을
받았다.** 건별 처리는 `checklist.md` **§17**에 있다.

### `O-2`가 출처 층을 섞었다

**X-1이 무너뜨린 것은 저장값 층**이다 — legacy `Project.budget_estimate`의 **선언**과
**저장 관측**이 갈린다는 것. **조달청 문서의 `추정가격`은 실제로 부가세 제외**이고
**`commands.md` C-1의 마지막 주석이 그 구분을 스스로 적고 있었다.**

C-3.4가 **「문서의 자기 목소리인가」만 보고 출처 층을 가르지 않아** `authoritative`
인용과 **표시 요구**까지 `O-2`에 넣었다. **`data-extract.md` §1의 세 층 구분**과
**이 slice의 핵심인 「선언 / 관측」 구분**(`F-1`을 닫은 그 구분)을 다시 섞은 것이다.

**판정 축을 둘로 갈랐다** — ① **어느 층인가** ② **저장값의 과세 처리를 근거 없이
단정하거나 차이를 정량화·방향화하는가.** **`O-2`에 남는 것은 ②가 참인 항목뿐**이다.

### 기준은 0B가 세운 것이다

0B verifier **`F-c`** — *"「legacy 서술이니 그대로」로는 부족하다 — 귀속 없이 적으면 이
문서의 확정 서술로 읽힌다"*. **`observed` 귀속만으로는 부족하고 `OPEN-REG-05`를 지목해야
한다.** **남는 두 항목 다 그 지목이 없다.** **새 기준을 만들지 않았다.**

> **⚠ 수정 라운드 16이 이 기준의 층 라벨을 `legacy-behavior`로 바로잡았다**(Codex 4차 `A`).
> **현재 판정은 `commands.md` C-3.4가 낸다.** 이 절의 본문은 그 시점의 기록이다.

### `O-2`가 줄었다 — 0C 인계가 달라진다

**줄어든 자리는 오탐이 아니라 「층이 다른 참인 서술」**이고 **0C는 그것들을 고칠 대상이
아니라 그대로 쓸 수 있는 인용으로 받는다.** **「전부 오탐이었다」가 아니다** —
**두 자리는 그대로 `O-2`에 남는다.** 어느 자리가 어느 층인지는 **C-3.4 판정 표**가 낸다.

### `capability-map.md`는 out_of_scope로 유지했다

**무변경**이다. 이 라운드가 바꾼 것은 **그 자리들에 대한 판정과 기록**이지 자리 자체가
아니다.

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다.**

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat f49155d..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 11 (Codex 3차 `R-2` · 출력 순서)

건별 처리는 `checklist.md` **§18**에 있다.

### 환경에 따라 순서가 달랐다

**기록이 여러 파일을 인자로 주는 `grep`의 파일 간 출력 순서에 의존했다.**
그 순서는 **구현·환경에 따라 다르다** — **기록을 뜬 환경에서는 재현됐으나 리뷰어
환경에서는 아니었다.** 매치 내용과 A3 결론은 같고, 어긋난 것은 **`commands.md` 머리의
자기 기록**(*"접거나 손으로 압축한 블록은 없다"*)이다.

**두 명령에 `| sort`를 붙이고 출력을 그 상태에서 다시 떴다.** **의존 하나를 지운 것이지
값을 고친 것이 아니다.** **「어디서 돌려도 같다」고는 주장하지 않는다** — 뒤따르는
`cut -c`가 남은 의존이며 **한계로 적었다**(`commands.md` C-3.3).

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다.**

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 2f74829..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| C-3.3이 고정점인가 | 그 절의 두 명령을 다시 돌려 출력이 기록과 같은지 본다 |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 12 (verifier `V-1`·`V-2`·`V-3`)

**이 라운드의 핵심은 통과했다** — 검증자가 **후보 아홉 전건을 독립 판정해 전건 일치**
판정을 냈다. **줄어든 셋도 남은 둘도 옳고 되돌릴 판정이 없다.** 세 건은 전부
**앞 두 커밋이 새로 적은 문장**이다. 건별 처리는 `checklist.md` **§19**에 있다.

### `V-1` — 적었는데 하지 않았다

§17이 *"라운드별 이력 절에 표시를 달았다"*고 적었으나 **그 라운드는 그 행위를 하지
않았다.** 그 range 에서 추가된 표시는 **그 주장 문장 자신**과 **살아 있는 `O-2` 행** 둘뿐이고
**이력 절은 한 글자도 안 바뀌었다.** **라운드 8의 문장을 옮겨 적었을 뿐**이다 —
**`H-2`와 같은 형태**다.

**이번에는 실제로 달았다** — `scope.md`의 **라운드 4·5 이력**과 `checklist.md`의
**§11 `H-1` 행·§12 `P-1` 결과 절**. **이력 본문은 고쳐 쓰지 않았다.**
**그리고 그 사실을 실행으로 확인해 `commands.md` C-7에 남겼다** — `V-1`이 잡힌 그 명령이다.

### `V-2` — 서로 다른 라운드의 행위를 합쳤다

*"0B verifier `F-c`가 `R-BASIS-03`·`R-BASIS-06`에 유보를 달았다"*는 **`F-c`가 한 일이
아니다.** **0B의 그 `F-c` 블록이 직접 적는다** — **`R-BASIS-06`은 `F-3`이,
`R-BASIS-03`은 `F-b`가** 달았다. **`F-c`는 판정을 했다.** **직접 열어 확인하고 갈라
적었다.** **기준 자체는 그대로**이며 재판정 결과도 바뀌지 않는다.
**`Q-1`·`R-1`과 같은 계열**이다.

### `V-3` — 측정하지 않은 것을 단정하고 새 보장을 만들었다

**(a)** *"이 저장소의 `grep`은 …이라 병렬 완료 순으로 낸다"* — **측정하지 않은 단정**이고
Codex 3차의 직접 관측과 어긋난다. **원인을 「인자 순서에 의존하던 것」으로** 고치고
**어느 구현이 어떤 순서를 내는지는 주장하지 않는다** — **측정한 것은 "두 환경에서 순서가
달랐다"까지다.**
**(b)** *"이제 어디서 돌려도 같은 출력이 나온다"* — **새 보장 주장**이고 거짓이다.
뒤따르는 `cut -c`가 남은 의존이다. **보장 주장을 지우고 한계로 적었다** —
**그 교환은 하지 않았다.** **`N-1`과 같은 형태**다.

### 「적은 대로 했는지」를 실행으로 확인했다

`V-1`이 **`git diff … | grep` 한 줄로 잡혔다.** **「한 것과 적은 것이 다름」은 이 slice가
반복해 온 네 형태 중 기계로 막을 수 있는 것**이고 이번에 그 자리를 만들었다
(`commands.md` **C-7**). **마커를 셸 변수로 쪼개** 그 절의 서술이 **자기 검사에 걸리지
않게** 했다 — **그 자기참조 하나를 없앤 것이고, 이 절이 매 라운드 같은 출력을 낸다는 뜻이
아니다**(검사 대상이 라운드마다 바뀐다).

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다.**

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat a264d6f..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **표시가 이력 절에 실제로 들어갔는가** | `commands.md` **C-7** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 13 (verifier `W-1`·`W-2`·`W-3`)

**`V-1`·`V-2`·`V-3`은 셋 다 실제로 닫혔다**는 판정을 받았다. **산출물 판정·수치·범위와
`O-2` 두 자리는 손댈 것이 없다.** 남은 셋은 **닫는 과정에서 새로 적은 문장**이다.
건별 처리는 `checklist.md` **§20**에 있다.

### `W-1` — 개명이 포인터를 끊었다

앞 커밋이 `commands.md`의 「이 slice가 만들지 않은 것」을 **`C-7` → `C-8`로 개명**하고
**그 번호에 새 절**을 넣었다. **그 절을 이름으로 부르던 네 문장은 안 고쳤다** —
앞 커밋에서 참이었고 **HEAD에서 거짓이 됐다.**

**이력이 낡은 것이 아니다** — **이력의 판단은 지금도 참**이고 거짓이 된 것은
**가리키는 이름**이다. **동결 관례는 본문에 적용되지 포인터에는 적용되지 않는다**
(`H-2`가 세운 선례). **네 자리의 이름만 `C-8`로 돌리고 그 편집을 §20에 등재했다.**

**이것은 「전수로 맞췄다」를 적은 라운드가 낸 것이다.** 그 스윕은 **명제**를 훑었고
**절 이름 개명**은 대상이 아니었다. **규칙을 더한다 — 절 이름·번호를 바꾸면 그 이름을
부르는 자리를 훑는다.** 대조 명령과 출력은 `commands.md` **C-7 (b)**에 있다.

> **⚠ 이 절이 원래 적은 예방 주장은 성립하지 않았다**(verifier `Y-1`) — 그 검사는
> **`commands.md` 접두가 붙은 인용만** 봤고 **`W-1`의 자리는 전부 접두가 없다.**
> **수정 라운드 14가 축을 넓히고 능력을 실행으로 쟀다.** 「갱신 이력 — 수정 라운드 14」 참조.

### `W-2` — 새 절에 보장 주장 둘이 붙어 있었다

`C-7`의 기록 블록이 **HEAD 재실행과 달랐고**, 그 위에 *"고정점이다"*와 머리글의
*"기록된 블록은 **전부** 낡지 않는 형태다"*가 서 있었다.
**`C-7`은 검사 대상이 라운드마다 바뀌는 절**이라 **고정점이 아니다** —
**`N-1`·`V-3`(b)와 같은 형태**다.

**보장을 지우고 사실만 적었다** — *"쪼갠 것은 자기참조 하나이고 검사 대상은 라운드마다
바뀐다 … 그때 다시 뜬다"*. **머리글의 전칭도 갈랐다** — 낡지 않는 형태로 짠 블록을
열거하고 **`C-7`은 그 부류가 아님을 명시**했다. **블록은 `W-1`·`W-3` 수정 뒤에 다시 떴다.**

### `W-3` — 절 머리가 자기 본문에 반박당했다

*"네 재발 형태를 표로 막았다"*인데 본문은 **하나만** 기계로 막을 수 있다고 적는다.
**`checklist.md` §19와 같은 문면으로 맞췄다.**

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다.**

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 5d2ca03..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **포인터가 실재하는 절을 가리키는가** | `commands.md` **C-7** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 14 (verifier `Y-1`)

**`W-1`·`W-2`·`W-3`은 셋 다 실제로 닫혔다**는 판정을 받았고 **불변·`O-2`·in_scope 전부
통과**했다. 남은 하나는 **그 라운드가 만든 검사의 능력을 재지 않고 적은 것**이다.
건별 처리는 `checklist.md` **§21**에 있다.

### 검사를 만들면서 그 능력을 재지 않았다

앞 라운드가 `C-7`에 포인터 대조를 넣고 세 자리에 **예방 주장**을 적었다.
**그 검사는 `W-1`이 난 자리를 구조적으로 못 본다** — **``commands.md`` 접두가 붙은 인용만**
보는데 **`W-1`의 자리는 전부 접두가 없다.** **`W-1`이 살아 있던 커밋에서 돌리면 통과한다.**

**형태는 둘의 합이다** — **거짓 보장 주장**(`N-1`·`V-3`(b)·`W-2`)과 **측정하지 않은 것을
단정**(`Q-1`·`R-1`·`V-2`).

### (가)를 택했다 — 넓히고, 그 능력을 실행으로 쟀다

**이 slice의 규약은 「결함이 관측된 뒤에 넓힌다」이고 `W-1`이 그 관측된 결함**이다.
접두를 가리지 않고 **모든 `C-N` 인용**을 보며, **번호가 절을 넘겨준 뒤에 낡은 인용**을
`git blame`으로 지목한다.

**합격 기준이 실행이었고 두 실행을 C-7 (b)에 남겼다** — **`5d2ca03`에서 `W-1`의 자리를
잡고**(다른 지목 없음) **현재 HEAD 에서 지목 0**이다.

**처음 짠 축은 더 넓었고 그래서 좁혔다** — 「제목이 바뀐 번호」를 전부 지목하게 했더니
**HEAD 에서도 지목이 남았다.** **제목 다듬기와 번호 이동을 구분하지 못했기 때문**이고,
**번호 이동**으로 좁히자 두 기준이 동시에 성립했다. **오탐이 는다는 이유로 좁힌 것이
아니라 축이 다른 것을 세고 있었기 때문**이며, 그 근거를 절에 적었다.

### 못 보는 것을 적었다

**「전부 막는다」고 적지 않았다.** C-7 (b)의 표가 못 보는 것을 열거한다 —
제목만 다듬은 번호 · `C-5` 면제 · 줄 이동으로 `blame`이 갱신되는 경우 ·
코드 블록 안의 이름 · 다른 파일의 절 이름.

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다.**

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat d2cced5..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **넓힌 검사가 `W-1`을 잡고 HEAD 에서 0인가** | `commands.md` **C-7 (b)** — 두 실행 |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 15 (verifier `Z-1`(blocker)·`Z-2`) — **삭제 라운드**

**`Y-1`이 요구한 것은 전부 됐다**는 판정을 받았고 **불변도 출력 블록도 하나 안 깨졌다.**
**막은 것은 그 편집이 남긴 잔해**다. 건별 처리는 `checklist.md` **§22**에 있다.

### 구간을 갈아 끼우면서 옛 구간을 안 지웠다

앞 라운드가 `commands.md`의 C-7 구간을 새 것으로 바꾸면서 **옛 C-7 (b) 블록이 그대로
남았다** — 중복 절 머리 하나 · 고아가 된 명령·출력 쌍 · **닫히지 않은 코드펜스**(그 뒤
렌더가 깨진다) · **존재하지 않는 grep을 가리키는 판정문 둘**.
고아가 된 그 명령은 **`Y-1`이 지운 옛 좁은 검사**라 남기면 안 되는 것이었다.

**형태는 「한 것과 적은 것이 다름」의 변종**이다 — 이번엔 **문장이 아니라 파일 구조**가
의도와 달라졌다. **규칙을 더한다 — 구간을 옮기거나 대체하면 옛 구간이 남았는지 확인한다.**
**확인 명령과 출력은 `checklist.md` §22**에 있다(펜스 짝 · 절 머리 중복).

### 지우기 전에 대조했고 손실이 없다

**삭제 구간에만 있던 문면 둘을 찾았고 둘 다 패키지 안 다른 자리에 살아 있다** —
표는 `checklist.md` §22에 있다. **멈추지 않고 지운 판단이며 다르게 보면 되돌릴 수 있다.**

### `commands.md`의 교체 대상 구간에는 새 서술을 넣지 않았다

**살아 있는 판정문이 이미 있어 그 구간에 새 문장이 필요 없었고**, 넓힌 축의
**판정·사각지대 서술은 손대지 않았다**(이미 통과했다). **`Z-2`는 한 셀을 좁힌 것**이다.

**`scope.md`·`checklist.md`에는 이력·판정 서술을 추가했다** — 이 절과
`checklist.md` §22다. **이력 절은 쓸 수밖에 없고 실제로 썼다.**

> **⚠ 이 절의 머리가 원래 *"새 서술을 쓰지 않았다"*였고 **라운드 전체에 대한 주장으로
> 읽혀 실측과 어긋났다**(Codex 4차 `C`). 주장을 **`commands.md`의 교체 대상 구간**으로
> 한정한다. **무엇이 얼마나 바뀌었는지는 `git diff --numstat`가 낸다.**

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다.**

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 32f7a63..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **펜스 짝 · 절 머리 중복 없음** | `checklist.md` §22의 두 명령 |
| **출력 블록이 여전히 전수 재현되는가** | `commands.md` C-3.4 · C-4 · C-7 의 명령 |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` |

**절 머리 수** — **`commands.md`만 하나 줄었다(중복이 빠진 것이고 정상이다).**
**`scope.md`·`checklist.md`는 이 라운드가 이력·판정 절을 더해 늘었다.**
**수는 `grep -c '^#\{2,4\} '`가 낸다 — 여기 옮겨 적지 않는다.**

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 16·17·18 (Codex 4차 `A`·`B`·`C`)

verdict 정본: `reports/evidence/m0/0a3/codex-review-20260828T030325Z.json`(등재, append-only).
`reviewed_base` **`48151b9`** → `reviewed_head` **`6b99420`**.
**3차 두 건은 해소 판정을 받았고 `O-2` 재판정 자체도 타당하다고 인정됐다.**
건별 처리는 `checklist.md` **§23·§24·§25**에 있고 **커밋도 셋으로 나눴다.**

### `A` — 판정은 맞고 층 라벨이 틀렸다

**층은 「그 값이 어디서 왔는가」로 정해진다 — 문구가 아니다.** `data-extract.md` §1이
**`observed`(운영 데이터·로그 관측)**와 **`legacy-behavior`(기존 Python이 낸 결과)**를
가르고, `capability-map.md` §0도 **기존 Python의 출력·수치를 `legacy-behavior`**라 적으며,
**0B `R-BASIS-06`은 그 `~10%`를 「legacy의 서술」로 명시 판정**한다.

C-3.4가 **`(관찰된 회귀)` 문구와 표 제목**을 근거로 두 항목을 `observed`로 올렸다.
**legacy 결과를 운영 관측과 같은 층에 두면 0C 인계의 provenance가 왜곡된다.**
**두 항목을 `legacy-behavior`로 바로잡았고 바뀐 것은 층 라벨과 근거 서술뿐**이다 —
**`O-2`에 남는다는 판정과 `OPEN-REG-05` 유보 필요성은 그대로**다.
**3차 finding의 한 겹 아래**다(3차는 `authoritative` 대 나머지).

### `B` — 인벤토리가 다시 거짓이 됐다

**Codex 2차 `M-3`에서 고친 자리가 후속 변경으로 다시 거짓이 됐다** — `C-7` 포인터 대조가
늘면서 `python` 블록이 셋이 됐는데 인벤토리는 **둘**이라 적고 있었다.
**표에 그 행을 더하고**(도입 근거 `W-1`/`Y-1`) **셈을 지웠다** — 머리글에 **대조 명령**을
두고 **표가 그 출력과 1:1로 대조되는 것**임을 적었다. **표 자체는 남겼다** —
**목적·도입 근거는 명령이 못 낸다.**

### `C` — 지시 문구를 자기 기록으로 옮겼다

라운드 15가 *"새 서술을 쓰지 않았다"*를 **무조건**으로 적었으나 **실측은 두 파일이 늘었다**
(이 파일의 이력 절과 `checklist.md` §22). **`commands.md`에서 지웠다는 대응은 맞고 틀린
것은 라운드 전체의 자기 기록**이다. **주장을 `commands.md`의 교체 대상 구간에 한정**하고
**두 파일에 추가한 사실을 명시**했다. **수는 `git diff --numstat`와 `grep -c`가 낸다.**

**`B`와 `C`가 같은 것을 말한다** — **앞 라운드의 참인 기록이 뒤 라운드에 거짓이 된다.**
`B`는 대상이 늘어서, `C`는 지시 문구를 옮겨 범위가 사라져서.
**그래서 고친 뒤 무엇이 낡았는지 훑고 그 명령·출력을 `commands.md` C-9에 남겼다.**

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다.**

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 세 라운드에 무변경 | `commands.md` **C-9 (d)** · `git diff --stat 6b99420..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **인벤토리 ↔ `python` 블록** · **산문에 살아 있는 셈** | `commands.md` **C-9 (a)·(b)** |
| **`O-2`에 남는 두 항목이 그대로인가** | `commands.md` **C-3.4 판정 표** — 층 라벨만 바뀐다 |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 19·20 (verifier `F-1`·`F-2`)

**층 판정은 옳다** — 검증자가 값의 출처를 직접 따라가 **두 항목 다 `legacy-behavior`로
독립 판정**했고 **일치**했다. **`B`·`C`도 처방까지 이행됐고 불변은 안 깨졌다.**
건별 처리는 `checklist.md` **§26·§27**에 있고 **커밋도 둘로 나눴다**(처방의 순서 때문이다).

### `F-1` — 동결 이력 본문을 고쳐 놓고 「안 고쳤다」고 적었다

라운드 16이 0B `F-c` 기준 문면을 *"세 자리 전수"*로 고쳤는데 **그 중 둘이 동결 이력
본문**이었다 — 이 파일의 **「갱신 이력 — 수정 라운드 10」**과 `checklist.md` **§17**.
**⚠도 안 달았고**, 그 위에 *"동결 이력은 본문을 고치지 않았다"*를 적었다.

**이 패키지 자신의 관례를 어긴다** — `G-5`와 `W-1`이 세운 **본문/포인터 구분**이다.
**`observed` → `legacy 서술`은 이름이 아니라 기준 문장 자체다.**
**두 자리를 원문으로 되돌리고 ⚠ 후속 정정을 달았다.**

**형태가 아프다** — **Codex 4차 `C`(「한 것과 적은 것이 다름」)를 고치는 라운드가 같은
형태를 새로 만들었다.** **전수 스윕이 「기준 문면」으로 돌면서 그 자리가 동결 이력인지
보지 않았다.**

### `F-2` — 커밋 안의 블록은 자기 커밋의 트리에서 뜰 수 없다

C-7·C-9의 선언 HEAD가 **기록된 출력이 나온 트리와 달랐다.** **뿌리는 `head_sha`와 같다.**

**커밋된 뒤에 다시 떴다** — `F-1`을 먼저 커밋하고 **그 커밋을 체크아웃한 worktree에서
두 블록을 실행**해 **그 SHA로 선언**했다. **선언된 SHA의 트리에서 재현되는지 확인**했고
**어긋나는 줄 0**이다.

**「이제 항상 맞는다」고 적지 않았다** — **한계를 적었다**: *"이후 편집이 들어오면 다시
떠야 한다"*. **둘 다 고쳤다** — 한쪽만 고치면 **같은 파일 안에서 계약을 지키는 블록과
어기는 블록이 공존한다.**

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다.**

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 두 라운드에 무변경 | `git diff --stat 47e1716..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **두 블록이 선언 SHA에서 재현되는가** | 그 SHA를 체크아웃한 worktree에서 C-7·C-9의 명령을 돌린다 |
| **층 판정 · `O-2` · 인벤토리 무변경** | `commands.md` **C-3.4** · **C-9 (a)** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |
| 하네스 두 파일이 range 밖인가 | `git diff --name-only 48151b9...HEAD` |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 21·22 (Codex 5차 `G-1`)

verdict 정본: `reports/evidence/m0/0a3/codex-review-20260828T040618Z.json`(등재, append-only).
`reviewed_base` **`48151b9`** → `reviewed_head` **`c69c04c`**.
**4차 세 건은 전부 해소 판정**을 받았고 **residual risk가 `O-2`의 위치·성격·출처 층이
evidence와 실물에서 일치한다고 명시**한다. 건별 처리는 `checklist.md` **§28**에 있다.

### 명령이 축약 경로를 썼다

C-9 (c)의 `for` 명령이 `git show <sha>:…/$f.md`로 적혀 **그대로 돌면 실패**하고
**`0 → 0`**을 낸다. **기록된 값은 그 명령에서 나올 수 없다.** **결론과 수치는 맞고 명령이
안 돈다** — `F-3`·`W-1`과 같은 계열이며 **머리글의 재현 계약과 어긋난다.**

**전체 경로로 바꿨고 직접 확인했다** — 축약 경로는 실패하고 전체 경로는 기록된 값을 낸다.
**C-9 머리에 한계를 더했다**: *"명령에 축약·자리표시자를 쓰지 않는다."*

### 처방의 순서를 지켰다 — `F-2`와 같다

**고쳐 커밋한 뒤**(`b940074`) **그 커밋의 트리에서 C-9를 다시 떠 그 SHA로 선언**했다.
**커밋 안의 블록은 자기 커밋의 트리에서 뜰 수 없다.**

### 전수 훑기 — 측정한 범위만 적는다

**`commands.md`의 `$`로 시작하는 명령**에서 **축약·자리표시자를 훑었고 남은 것은 없다.**
**첫 스윕이 지목한 넷은 git range 문법**(`48151b9...HEAD`)이라 **오탐이었고 넷 다 실제로
도는 것을 확인**했다. **「모든 명령이 재현된다」고는 적지 않는다** — 본 것은 **축약 표기
하나의 축**이다.

### 이 편집이 다른 블록을 깨지 않았다

**두 블록을 각자 선언한 SHA의 worktree에서 다시 돌려 기록과 대조**했다 — **둘 다 어긋남 0.**

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다.**

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 두 라운드에 무변경 | `git diff --stat c69c04c..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **두 블록이 각자 선언 SHA에서 재현되는가** | 그 SHA를 체크아웃한 worktree에서 C-7·C-9의 명령을 돌린다 |
| **축약·자리표시자가 남은 명령** | `checklist.md` §28의 스윕 |
| **층 판정 · `O-2` · 인벤토리 무변경** | `commands.md` **C-3.4** · **C-9 (a)** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 23·23b·24 (Codex 6차 `H-1`·`H-2`)

verdict 정본: `reports/evidence/m0/0a3/codex-review-20260828T043354Z.json`(등재, append-only).
`reviewed_base` **`48151b9`** → `reviewed_head` **`fea90f2`**.
**5차 한 건은 해소 판정**을 받았고 **산출물 지적은 다섯 라운드째 0건**이다.
건별 처리는 `checklist.md` **§29**에 있다.

**운영자가 정지 규칙을 정했다(2026-08-28)** — **이번 두 건을 고치고 Codex 7차를 돌린다.
7차가 산출물 무지적이면 남은 evidence 자기서술 finding을 「알려진 제한」으로 기록하고
slice를 닫는다.** 그래서 **범위를 넓히지 않고 이 둘만 닫았다.**

### 두 건은 한 형태다 — 산문이 출력 자리에 서 있다

- **`H-1`** — C-9 (d)의 `(빈 출력 = 무변경)`이 **명령이 낸 문자열이 아니었다.**
  **같은 파일 `C-6`이 이미 옳은 형태를 갖고 있었고**(명령에 `echo` 포함) **그 패턴을
  적용**했다. **구조적 불변 자체는 실제 빈 출력으로 성립하고 바뀐 것은 기록 방식**이다.
- **`H-2`** — `checklist.md`의 명령 블록 **셋에 선언 SHA가 없었다.**
  **살아 있는 블록**은 **선언 SHA를 달고 명령이 문자열을 직접 내게** 했고,
  **동결 이력 안의 둘**은 **본문을 고치지 않고 ⚠ 후속 표시로 선언 SHA를 밝혔다** —
  **`F-1`이 동결 본문을 고쳐 걸린 전례를 반복하지 않는다.**

### 전수는 한 스캐너로 훑었다 — `commands.md` C-10

**두 축을 한 번에 본다** — ① **산문이 출력 자리에 선 줄**(바로 위 `$` 명령에 `echo`가
없는 경우) ② **`$` 명령을 담고도 선언 SHA가 없는 블록**. **세 파일 전부**가 대상이다.
**수정 전(`fea90f2`)과 수정 후(`9910c96`) 두 실행을 나란히 실었다.**
**수정 전 스캔이 낸 것은 Codex가 지목한 것과 정확히 같다.**

> **⚠ 그 「수정 후」는 `9910c96`의 트리에서 잰 것이고, 그때 아직 비어 있던 블록이
> 다음 커밋(`4cccf21`)에서 채워졌다.** 채워진 명령의 `; echo`가 **`\` 연속줄 다음**에
> 있어 **줄 단위로 위를 훑던 스캐너가 거짓으로 지목**했다 — **그래서 이 절의
> 「사라졌다」류 서술은 잰 트리 밖에서 성립하지 않는다**(verifier `GI-1`).
> **수정 라운드 25가 스캐너를 논리 명령 단위로 고쳤고 라운드 26이 세 트리에서 다시
> 쟀다** — `commands.md` **C-10**. **본문은 고치지 않는다**(`F-1`의 전례).

**「모든 블록이 선언 SHA를 갖는다」고 적지 않았다** — **적은 것은 이 스캔의 범위**이고,
**동결 이력 안의 둘은 계속 지목된다**(본문을 고치지 않으므로).

### 라운드 23b — 스캐너가 자기 펜스를 깼다

인라인한 스캐너 본문에 **리터럴 백틱 세 개**가 있어 **marker 추출 정규식이 그 자리에서
끊겼고** 추출한 본문이 **`SyntaxError`로 죽었다** — **즉 그 블록은 재현되지 않았다.**
**C-4·C-7이 같은 이유로 `chr(96)*3`을 쓰고 있었는데 이 스캐너에만 빠졌다.**
**고치고 그 이유를 주석에 적었다.**

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다.**

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 세 라운드에 무변경 | `git diff --stat fea90f2..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **산문이 출력 자리에 선 줄 · 선언 SHA 없는 블록** | `commands.md` **C-10** |
| **각 블록이 자기 선언 SHA에서 재현되는가** | 그 SHA를 체크아웃한 worktree에서 그 절의 명령을 돌린다 |
| **층 판정 · `O-2` · 인벤토리 무변경** | `commands.md` **C-3.4** · **C-9 (a)** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 갱신 이력 — 수정 라운드 25·26 (verifier `GI-1`·`GI-2`)

검증 보고서: `_workspace/m0-0a3/35_verifier_report_h1h2.md`. 판정 **`not-ready`**.
**`H-1`·`H-2`의 실질은 실행으로 통과**했고, **두 건 다 앞 라운드가 새로 들여온 서술**의
문제다. 건별 처리는 아래에 적는다 — `checklist.md`에는 절을 더하지 않았다.

**운영자 정지 규칙(2026-08-28)이 그대로 적용된다** — **이번이 마지막 수정이고 다음은
Codex 7차**다. 그래서 **범위를 넓히지 않고 이 둘만 닫았다.**

### `GI-1` — 스캐너가 명령을 줄 단위로 봐서 거짓 지목을 냈다

**앞 라운드는 「수정 후」를 `9910c96`에서 쟀는데, 그때 비어 있던 블록이 다음
커밋(`4cccf21`)에서 채워졌다.** 채워진 명령은 `\` 로 줄이 이어지고 **`; echo` 가 그
뒤쪽 줄**에 있다 — **논리적으로 한 명령인데 스캐너는 줄 단위로 위를 훑어** 그 `echo`를
못 보고 **산문이 출력 자리에 섰다고 지목**했다. **그 자리가 하필 이 라운드가
「고쳤다」고 적은 블록**이라, **「사라졌다」류 서술이 HEAD 에서 거짓**이 됐다.

**처방 (가)를 택했다** — 실물이 **거짓 지목**이었기 때문이다. `; echo` 는 실제로 같은
논리 명령 안에 있고 그 문자열은 **명령이 낸 것**이다. **재는 자가 틀린 것이지 재어진
자리가 틀린 것이 아니다.** (나)는 **옳은 자리를 계속 지목하게 두고 판정만 좁히는 것**이라
**뒤 라운드마다 같은 거짓 지목을 다시 읽게 된다.**

**고친 것은 `prev` 하나다** — 논리 명령 단위로 집는 `cmd_before` 로 바꿔 **`\` 연속줄을
앞 명령에 잇는다.** **이 수정이 앞 측정을 바꾸지 않는다는 것을 같은 트리에서 옛
스캐너와 나란히 돌려 확인**했다(`commands.md` **C-10** (2)).

**「이제 전부 잡는다」고 적지 않는다**(verifier `Y-1`이 그 형태였다) — **잰 트리만**
적었다.

### `GI-2` — 스크립트를 늘리고 인벤토리에 행을 더하지 않았다

머리글이 **「스크립트가 늘면 행을 더한다」**로 규정하는데 **C-10 스캐너를 늘리고
행을 더하지 않았다.** **인벤토리가 낡은 것은 이번이 세 번째**다(Codex 2차 `M-3`,
Codex 4차 `B`, 그리고 이번). **앞의 둘은 산문에 박은 셈이 거짓이 된 것**이고
**이번은 행 자체가 빠진 것**이라 **셈을 지운 것만으로는 막히지 않았다.**

**행을 더하고**(위치 `C-10` · 무엇을 하나 · **도입 근거 Codex 6차 `H-1`·`H-2`**),
**대조 명령의 출력과 표가 맞는지 다시 쟀다** — `commands.md` **C-9 (a)**.

### 처방의 순서를 지켰다 — `F-2`·`G-1`과 같다

1. **스캐너와 인벤토리를 고쳐 커밋**(`77c5b98`).
2. **그 커밋을 체크아웃한 worktree에서 C-9·C-10을 다시 실행**하고 **`77c5b98`로 선언**.
3. 갱신을 **이 커밋**에 담았다.

**커밋 안의 블록은 자기 커밋의 트리에서 뜰 수 없다** — `head_sha`와 같은 뿌리다.

**`77c5b98`만 놓고 재면 `commands.md`의 절 머리가 줄어 보인다** — **걷어낸 출력 블록
안에 `##`로 시작하는 구분 줄이 들어 있었기 때문**이고 **라운드 26이 다시 담았다.**
`4cccf21`과 이 커밋을 놓고 재면 늘어난다. **세는 명령은 아래 표에 있다.**

### `head_sha`와 range

**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다.**

- **`head_sha`의 정본은 이 파일 머리의 yaml 하나다.** 커밋이 자기 SHA를 담을 수 없어
  **직전 커밋**을 가리킨다.
- **리뷰 range는 `48151b9...HEAD`**.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 두 라운드에 무변경 | `git diff --stat 4cccf21..HEAD -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **절 머리가 줄지 않았는가** | `for f in scope checklist commands; do git show 4cccf21:reports/evidence/m0/0a3/$f.md \| grep -c '^#\{2,4\} '; grep -c '^#\{2,4\} ' reports/evidence/m0/0a3/$f.md; done` |
| **산문이 출력 자리에 선 줄 · 선언 SHA 없는 블록** | `commands.md` **C-10** |
| **각 블록이 자기 선언 SHA에서 재현되는가** | 그 SHA를 체크아웃한 worktree에서 그 절의 명령을 돌린다 |
| **인벤토리 표 ↔ 실물 `python` 블록** | `commands.md` **C-9 (a)** |
| **층 판정 · `O-2` 무변경** | `commands.md` **C-3.4** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**
