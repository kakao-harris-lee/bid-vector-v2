# M0 / 0A3 — acceptance 대조 checklist

`scope.md`의 **A1~A5**를 대조한다. **수치는 이 파일이 내지 않는다** — 판정의 근거가 되는
출력은 전부 `commands.md`에 있고 여기는 **어느 출력이 무엇을 증명하는가**만 적는다
(§10.1 **형태 6**: 셈을 산문에 옮기지 않는다).

| # | 판정 | 근거 |
| --- | --- | --- |
| **A1** | **pass** | `commands.md` C-1 · C-3.1 · C-3.2. **C-3.1의 검사 범위는 A1의 범위를 포함하는 상위집합이다** — `decisions.md`를 **절로 자르지 않고** 보기 때문이며, **방향이 보수적이라 A1이 요구하는 것을 놓치지 않는다.** §13 밖은 A1이 주장하지 않으며 **C-3.4**가 본다 |
| **A2** | **pass** | `commands.md` C-3.2 · C-3.5 |
| **A3** | **pass, 단 소유 `OPEN` id가 지시와 다르다** | `commands.md` C-2.3 · C-3.3 |
| **A4** | **pass** | `commands.md` C-2 · C-4.2 · C-4.3 |
| **A5** | **pass** | `commands.md` C-5.1 ~ C-5.4 |

> **Codex 1차 `request_changes`는 두 라운드에 걸쳐 전부 닫혔다.**
> **수정 라운드 1**(§8) — F-1(high) · F-2 · F-3(medium).
> **수정 라운드 2**(§9) — 넷째 finding(`capability-map.md`의 `검증 방법`)을
> **운영자 결정 2026-08-27로 열어 닫았다**(**F-4**). 건별 처리는 그 두 절에 있다.

---

## 1. A1 — ex-VAT 단정과 "VAT만큼" 정량 서술 제거, legacy 실측이 근거로 달림

**무엇이 틀렸고 무엇으로 바뀌었나:**

| 자리 | 틀린 서술 | 처리 |
| --- | --- | --- |
| `decisions.md` 「OPEN-STR-01」 | *"`Project.budget_estimate`는 **추정가격(ex-VAT)**이다"* | **취소선**(원본 보존) + 정정 인용 블록 |
| `decisions.md` 「OPEN-STR-01」 | *"기초금액과 추정가격은 **VAT·사정률만큼 체계적**으로 다르므로 … **한 방향으로 일관되게** 누락/포함시킨다"* | **취소선**(원본 보존) + 정정 인용 블록 |
| `capability-map.md` §13 `사용자 영향` | 위와 같은 문장 | **교체** — basis 정의에서 차이가 나온다는 것까지만 남기고 크기·방향을 뺐다. 원문은 정정문 안에 인용으로 남긴다 |

**A1이 주장하는 범위는 그 둘이다** — `decisions.md`의 `OPEN-STR-01` 절과
`capability-map.md` **§13**. **§13 밖은 A1이 주장하지 않고** `commands.md` **C-3.4**가
따로 본다(`scope.md` **O-2**). **C-3.1은 이 범위를 포함하는 상위집합을 본다** —
`decisions.md`를 **절로 자르지 않기 때문**이고, **상위집합이라 A1이 요구하는 것을 놓치지
않는다.**

**legacy 실측이 근거로 달렸다** — `app/domain/money.py:30`(부가세 **포함** 선언)과
`app/schemas/bid_summary.py:69-77`(저장 값의 과세 처리가 **이력상 일관되지 않다**는
legacy 자신의 공시). **행 범위는 블록 경계까지 떴다**(`Field(` 호출이 `:69`에서 시작).
확인 출력은 `commands.md` **C-1**.

**결정 내용은 바뀌지 않았다.** 아래 셋이 전부 그대로다 — 정정 블록이 그 사실을 명시한다.

| 그대로인 것 | 근거 |
| --- | --- |
| 두 금액이 **서로 다른 basis**다 | 운영자 답 2026-08-26 |
| 운영자 의도는 **기초금액**이다 | 운영자 답 2026-08-26 |
| **`legacy-defect`** 판정 | 위 둘에서 유도된다 — 크기·방향에 의존하지 않는다 |

**무너진 것은 정량 서술 하나뿐이다.** 방향(*"한 방향으로 일관되게"*)도 함께 철회했다 —
방향을 말하려면 두 금액의 대소 관계가 정해져야 하는데 그것이 저장 값의 과세 처리에 달렸고
legacy가 그 처리의 비일관을 공시하기 때문이다. **0B ledger `R-BASIS-01`이 같은 판단을
같은 근거로 이미 내렸다**(그 항목의 `사용자 영향` 안 ※ 주석).

---

## 2. A2 — `decisions.md` provenance 규약 준수 + 전수 목록 등재

이 파일은 **authoritative 결정 기록**이고 그 규약은 *"원본 문장은 지우지 않는다.
slice가 덧붙이는 것은 별도 절이거나 인용 블록(`>`)·취소선이며, 그 자리에서 slice
산물임을 밝힌다"*이다.

| 요구 | 처리 | 출력 |
| --- | --- | --- |
| 원본 문장 보존 | 두 문장 다 **취소선으로 남았다.** 지운 것 없음 | C-3.2 |
| slice 산물임을 그 자리에서 밝힘 | 정정 블록 머리가 *"2026-08-27 정정 (slice 0A3 — 0C 선행 조사 X-1)"* | C-3.2 |
| **절 단위 provenance 표**에 등재 | 「OPEN-STR-01」 절 안의 2026-08-27 정정 블록(slice 0A3 · X-1) 행 추가 | C-3.1 — **좌표는 그 출력이 낸다** |
| **「원본 절에 대한 slice 변경 — 전수」 목록**에 등재 | 훅 `144` · `147` 행 추가 | C-3.5 |
| 재현 명령과 **1:1** | `git diff -U0 6a4e49b -- …`의 훅 전부가 표의 행으로 설명된다 | C-3.5 |

**표의 셈을 지웠다.** 그 자리에 *"원본 절 안에 들어간 것은 위 `144`·`235`·`250`/`252`·
`340` **네 자리뿐**이며, 성격은 정정 2 · 철회 1 · 포인터 1이다"*가 있었고 이 slice의
정정이 다섯째로 들어오면서 낡았다. **값을 고치는 대신 셈을 지우고 표를 가리키게 했다** —
0B verifier `N-1`이 같은 형태에 내린 처방이며, 값을 고치면 **다음 편집에 다시 낡는다.**

**훅 번호는 지우지 않았다.** 표의 설계가 훅↔행 대응이라 번호를 없애려면 설계를 바꿔야
하고 그것은 이 slice에 위임되지 않았다. 대신 **그 열이 HEAD 기준 좌표이고 정본은 명령의
출력임을 표 아래에 명시**했다. 이것이 남은 부채라는 것은 `scope.md` **O-3**에 적었다.

---

## 3. A3 — 정량 서술이 사라진 자리에 그 축의 소유 `OPEN`이 적힌다

**두 파일 모두에 적혔다**(C-3.3) — 소유자는 **`OPEN-REG-05`**(활성,
`docs/discovery/regression-ledger.md` §9 「기초금액과 추정가격의 과세 처리 — 두 금액의
차이가 무엇으로 이루어지는가」)다. **새 `OPEN`을 만들지 않았다** — C-2.3이 registry
추가 0을 내고 C-4가 총수 불변을 낸다.

### 팀 리드 지시와 다른 id를 적었다 — 그 이유

지시 메시지는 *"그 축은 활성 **`OPEN-QUAL-10`**이 소유한다"*로 왔다. **그 귀속은 0B가
이미 반증하고 바로잡은 것**이라 그대로 적을 수 없었다.

| 사실 | 근거 |
| --- | --- |
| `OPEN-QUAL-10`은 **시공능력평가금액 축**이다 — 공고 게시 요건(`cnstrtnAbltyEvlAmtList`)과 운영자 보유액의 unit·basis·과세·기준 시점 | `capability-map.md` §12 registry 행 |
| 0B의 계열 A 스윕이 `R-BASIS-01`~`OPEN-QUAL-10`을 **"오탐 — 다른 금액 축"**으로 판정했다 | `reports/evidence/m0/0b/commands.md` C-8.2 판정 표 |
| **그런데 산출물에는 그 귀속이 남아 있었다** — verifier F-2가 그것을 적출했고, 등록된 소유자가 없음을 확인한 뒤 **`OPEN-REG-05`를 신설해 옮겼다** | `reports/evidence/m0/0b/scope.md` 「Codex 1차 대응 재검증」 F-2 · 「재검증 2차」 F-2 |
| ledger가 **명시적으로** *"`OPEN-QUAL-10`은 시공능력평가금액 축이라 이 질문의 소유자가 아니다"*라고 적는다 | `docs/discovery/regression-ledger.md` (C-3.3 출력) |

**지시대로 적었다면** 0B가 고친 오류를 되살리고, ledger와 `capability-map.md`가
**정면으로 어긋나는 상태**를 다시 만든다 — 그것이 정확히 0B verifier F-2가 잡은 형태다
(*"같은 문서가 한 자리에서 소유한다, 다른 자리에서 소유자가 아니다"*).
**그리고 이 slice가 등재하는 형태 7의 실물 사례가 바로 그 F-2다.**

**지시의 취지는 충족된다.** *"새 OPEN을 만들지 마라 — 이미 등록돼 있다"*가 요구하는 것은
**이미 등록된 소유자를 가리키는 것**이고 `OPEN-REG-05`가 그것이다.
**두 id를 병기**해 어느 쪽이 소유자이고 어느 쪽이 왜 아닌지를 그 자리에 적었다 —
**상충하는 근거를 지우지 않는다.**

**A4와 충돌하지 않는다**: `OPEN-REG-05`는 ledger의 자체 id 공간(`OPEN-REG-NN`)이고
`capability-map.md` §12의 활성 registry에 들어 있지 않다. 중앙 registry 통합은 0B가
**별도 slice**로 미룬 항목이다. C-4가 활성 OPEN 총수 불변을 낸다.

---

## 4. A4 — 불변

| 확인할 것 | 어떻게 성립하는가 | 출력 |
| --- | --- | --- |
| `capability-map.md` 변경이 **§13의 「0B — 예산 basis 불일치」 인계 행에 한정** | hunk 위치가 §13 시작 줄보다 뒤이고 §12 시작 줄보다 뒤다. **§10 집계표·§12 registry·그 행 밖의 §13 항목은 diff에 없다.** **셈을 옮겨 적지 않는다** — 어디가 바뀌었는지는 C-2.2의 출력이 낸다 | C-2.1 · C-2.2 |
| 활성 OPEN 총수 | base와 HEAD에서 **같은 본문**을 실제로 실행해 값을 비교했다. 본문은 C-4.1이 marker로 뽑는다 | C-4.2 ↔ C-4.3 |
| capability 총수 | 〃 | C-4.2 ↔ C-4.3 |
| 분류 4종 | 〃 | C-4.2 ↔ C-4.3 |
| `git diff --check` | **출력을 옮겨 적지 않는다** — range 전체를 보는 검사라 evidence 커밋이 자기 파일을 더하면 앞서 뜬 값이 낡는다. **리뷰 head에서 리뷰어가 직접 돌린다** | C-2의 첫 표(명령만) |
| 새 `OPEN` 신설 0 | registry 행 추가를 diff에서 직접 센다 | C-2.3 |

**값을 여기 적지 않는다** — 두 실행의 출력이 나란히 C-4에 있고, **같은지 다른지가 판정**이다.
`scope.md`의 acceptance 문면이 값을 적는 것은 그것이 **계약이 요구한 목표값**이기
때문이며, 이 절이 하는 일은 **그 목표값을 다시 옮겨 적는 것이 아니라 실측 두 벌을
가리키는 것**이다.

---

## 5. A5 — §10.1에 형태 7 「미결을 확정으로 쓰기」 등재

| 요구 | 처리 | 출력 |
| --- | --- | --- |
| 표에 일곱 번째 행 | `7` 행 추가. 기존 `1`~`6`·`3′` 무변경 | C-5.1 |
| **0B 실물로 근거** | 셋을 인용했다(아래 표) | C-5.3 |
| 인용을 직접 열어 확인 | 여섯 자리 전부 `grep -n`으로 실재 확인 | C-5.3 · C-5.4 |
| 절 머리에 표의 마지막 번호 금지 | 절 머리는 *"왜 새 형태인가"* | C-5.2 |

**등재한 실물 셋** — 전부 0B evidence에서 인용했다:

| 실물 | 무엇이 선점됐나 | 소유 `OPEN` |
| --- | --- | --- |
| **Codex 1차 high** | ledger `R-COL-02`가 *"분류 불가(`unknown`)는 재시도 가능이 될 수 없다"*를 `검증 방법`에서 고정 | `OPEN-OPS-01`(활성) |
| **verifier F-1** (high) | `R-PROV-02`의 `검증 방법`과 **§10 fixture-curator 인계**가 마진 값을 조건 표시 없이 고정. **후자는 하류로 나가는 자리**라 legacy 값으로 corpus 경계가 굳을 뻔했다 | `OPEN-DEC-07`(활성 · M1 차단) |
| **verifier F-2** | `R-BASIS-01`이 차이 크기를 **잘못된 소유자**에게 넘겼다 — 같은 라운드의 스윕이 스스로 *"다른 금액 축"*이라 판정하고도 산출물에는 귀속이 남아 있었다 | `OPEN-QUAL-10`이 아니라 `OPEN-REG-05` |

**규칙은 처방까지 적었다** — 결정 무관/조건부 분리(0A2가 `OPS-09`에 쓴 기법), `동반 OPEN`
표기, 그리고 **소유자를 지목할 때 같은 축인지 확인**(F-2가 그것으로 났다).
**적출 축**은 0B가 만든 `openstance`(C-8.2)이며 **두 겹 + 숫자 리터럴 대조**라는 구조와
**한계**(특징어 부족 항목은 축이 찍고 사람이 판정 · `^### [RP]-` 블록 밖 인계 자리는
축이 보지 않음, C-9.9)도 함께 적었다.

**왜 지금 등재하는가**: 0B가 판단은 내렸으나 *"§10.1은 `reports/evidence/m0/0a2/
checklist.md`에 있고 0B의 out_of_scope다. 표를 고치려면 별도 slice 계약이 필요하다"*로
멈췄다(C-5.4의 축어 출력). **이 slice가 그 계약이다.**

---

## 6. 이 slice가 하지 않은 것

**계약이 「X-1 외의 것이 눈에 띄면 고치지 말고 보고한다」로 정했다.** 발견은
`scope.md` 「발견했으나 고치지 않은 것」에 등재했다. 요지만 여기 남긴다 —
**판단의 근거는 그 표에 있고 위치는 `commands.md` C-3.4가 낸다.**

**`O-1`은 여기 없다 — 수정 라운드 2에서 닫혔다**(§9 · `scope.md`의 그 행).
아래는 **지금도 열려 있는 것**이다.

- **O-2** — **`capability-map.md` §13 밖에, 두 금액의 차이를 `legacy-behavior` 층에서
  정량화·방향화하면서 `OPEN-REG-05`를 지목하지 않은 서술이 남아 있다** —
  **§6 NOTI(투찰률)**과 **§7 SET(SET-09 F-4)**. **자리와 수는 `commands.md` C-3.4**의
  스윕 출력과 판정 표가 낸다 — **여기 옮겨 적지 않는다.** 지시가 적은
  *"두 파일에 남아 있다"*는 **완결 주장으로 참이 아니다.**
  **`authoritative` 층의 인용**(조달청 정의 `추정가격` = 부가세 제외)**과 표시 계약은
  `O-2`가 아니다** — 수정 라운드 10이 그 구분을 넣었다(§17).
- **O-3** — `decisions.md` 전수 표의 훅 번호가 **편집마다 낡는 구조**다.

**활성 OPEN을 해소한 것도 새로 만든 것도 없다**(C-2.3 · C-4).

---

## 7. 알려진 제한

1. **§13 행의 내부 모순은 닫혔다**(수정 라운드 2 · **F-4**). `사용자 영향`과
   `검증 방법`이 이제 같은 축을 같게 말한다 — 후자의 과세/비과세 경계 쌍이
   **`OPEN-REG-05` 조건부**로 돌았다. **남은 것은 그 `OPEN` 자체이며 이 slice가 소유하지
   않는다.**
2. **O-2가 살아 있는 오류로 남는다.** **§6 NOTI(투찰률)**과 **§7 SET(SET-09 F-4)**이
   두 금액의 차이를 **방향·크기로 말하면서 `OPEN-REG-05`를 지목하지 않는다** —
   0B ledger는 같은 주장에 그 유보를 달았고 `capability-map.md`는 달지 않았다.
   **§13만 고쳐서는 문서가 한 목소리를 내지 않는다.** 자리는 `commands.md` C-3.4가 낸다.
   **§5와 §6 금액 라벨·acceptance는 여기 들어가지 않는다** — `authoritative` 층의 인용과
   표시 계약이다(수정 라운드 10 · §17).
3. **형태 7의 적출 축(`openstance`)을 이 저장소로 옮기지 않았다.** 0B evidence 안에
   인라인돼 있고 이 slice는 **가리키기만 한다.** 축을 공용 자리로 올릴지는 그것을
   요구하는 slice가 판단한다 — 근거 없이 도구를 옮기지 않는다.
4. **legacy 인용의 「내용 정합」은 사람이 읽은 것이다.** 경로·행 범위·존재는 명령이
   확인하지만 *"그 행이 이 주장을 뒷받침하는가"*는 정본을 계산할 수 없다(0B C-1의 두 축
   구분 그대로). C-1에 전문을 실어 **읽은 것을 그대로 볼 수 있게** 했다.
5. **Codex 리뷰 레인의 사각지대는 그대로다** — 리뷰어 worktree에 `bid-vector`가 없어
   legacy 인용을 원리적으로 재현하지 못한다. **C-1의 검증은 이 레인과 verifier가 진다.**

---

## 8. 수정 라운드 1 — Codex 1차 `request_changes` (high 1 · medium 3)

verdict 정본: `reports/evidence/m0/0a3/codex-review-20260827T132148Z.json`(등재, append-only).
`reviewed_base` **`48151b9`** → `reviewed_head` **`cb1be88`**.
**severity별 건수는 옮겨 적지 않는다** — 그 JSON의 `findings`가 정본이다.

**셋을 고쳤고 하나는 운영자 판단 대기로 두었다.**

| # | 지운 문면 | 왜 거짓·불능인가 | 남긴 문면 |
| --- | --- | --- | --- |
| **F-1** (high) | *"**`budget_estimate`는 ex-VAT가 아니다.** legacy는 그 반대로 선언한다"* | **한 단정을 지우고 그 자리에 반대 단정을 놓았다.** 인용한 근거가 지지하는 것은 `money.py`의 **선언**이지 저장 값의 실제 과세 처리가 아니며, **바로 다음 근거가 그 값의 일관성 부재를 공시**한다. 같은 패키지의 `commands.md` C-1이 *"어느 쪽이 맞다고 이 slice가 정하지 않는다"*고 적어 **자기 evidence와도 어긋났다** | **선언과 관측을 갈랐다** — *"legacy가 그렇게 「선언」한다는 것이 이 인용이 지지하는 전부"*, *"이 문서는 ex-VAT라고도 VAT 포함이라고도 단정하지 않으며 그 판정은 활성 `OPEN-REG-05`가 소유한다"*. 원본에 대해 남는 것은 **「ex-VAT」 단정에 근거가 없다**는 것까지다 |
| **F-2** (medium) | `git diff --name-only` **출력 목록**과 *"in_scope **세 파일만**"* 판정 | **evidence 커밋이 자기 파일을 range에 더하므로 커밋 전에 뜬 목록은 언제나 불완전하다.** 기록은 세 경로였고 리뷰 head에서는 여섯이었다. **인스턴스를 고치면 다음 커밋에 다시 낡는다** | **목록도 셈도 지우고 명령만 남겼다.** 판정 문면은 *"in_scope 밖 경로가 없다"*이고 근거는 그 명령이다. `git diff --check`도 같은 처리를 했다. 출력을 기록하는 검사는 **`capability-map.md`로 경로를 한정**해 뒤 커밋에 낡지 않게 했다 |
| **F-3** (medium) | `(cd "$TMPD" && python3 inv0a3.py)` | **저장소에도 archive에도 그 파일이 없다** — 그대로 돌리면 `exit 2`이고 **뒤에 기록된 수치는 그 명령에서 나올 수 없다**(형태 4). Codex가 인라인 본문을 직접 추출해 돌린 결과 **수치 자체는 base·head 양쪽 다 맞았다** — 틀린 것은 **명령**이다 | **인라인 본문을 파일에서 뽑아 stdin으로 흘려 넣는다.** 블록은 **줄 번호가 아니라 marker로 집고** `assert`로 유일성을 확인한다. 추출기는 `chr(96)*3`으로 리터럴 백틱을 피해 **자기가 들어가는 fenced 블록을 깨지 않는다** |

**넷째 finding(`capability-map.md:3171`)은 건드리지 않았다.** `scope.md`의 **O-1** 행과 같은
것이고 Codex도 같은 진단을 냈다 — *"정정된 같은 행이 과세 처리를 `OPEN-REG-05` 소유라
하면서 뒤의 `검증 방법`은 경계 쌍을 조건 없이 요구한다"*. **운영자 판단 대기**이며 지금
손대면 A4의 구조적 불변(§13 한 줄 교체)이 깨진다. 판단이 오면 별도 계약으로 처리한다.

### F-1이 이 slice에 대해 말하는 것

**§10.1에 형태 7 「미결을 확정으로 쓰기」를 등재하는 그 라운드에 같은 형태를 저질렀다.**
0A2 §10.1이 형태 3에 대해 적은 것과 글자 그대로 같은 구조다 — *"하필 3번이 1·2번의 규칙을
세운 라운드에서 나왔다. 규칙을 적는 것과 그 라운드의 자기 evidence에 적용하는 것은 다른
일이며, 이 표는 그 사실 자체를 기록으로 남긴다."*

**지우지 않고 `decisions.md`의 2차 정정 주석에 남겼다** — 형태 7의 실물이다.
**형태 7의 표 행을 고치지는 않았다**: 그 행이 세는 것은 **0B의 실물**이고 이 건은
**등재 라운드 자신의 재발**이라 성격이 다르다. 재발을 표에 접으려면 그 판단이 별도로
필요하고, **이 라운드는 지시받은 셋만 처리한다.**

### 이 라운드의 불변 — 수를 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| `capability-map.md` 이번 라운드 무변경 | `git diff --stat cb1be88..<head> -- docs/discovery/capability-map.md` |
| `regression-ledger.md` 무변경 | `git diff --stat 48151b9...<head> -- docs/discovery/regression-ledger.md` |
| capability 총수 · 분류 4종 · 활성 OPEN 총수 | `commands.md` C-4 (base ↔ HEAD 두 실행) |
| in_scope 밖 경로 없음 · 공백 오류 없음 | `commands.md` C-2의 첫 표 |
| `decisions.md` 전수 표 ↔ 훅 1:1 | `commands.md` C-3.5 |

**결과를 여기 적지 않는다.**

---

## 9. 수정 라운드 2 — F-4 (Codex 1차 넷째 finding, 운영자 승인으로 열림)

**앞 라운드가 「운영자 판단 대기」로 두었던 것**(§8 말미 · `scope.md` **O-1**)을
**운영자 결정(2026-08-27)으로 닫았다.** §8의 그 판단은 **그 시점의 계약을 따른 정확한
기록**이라 지우지 않는다 — 계약이 바뀐 것이지 판단이 틀렸던 것이 아니다.

| | |
| --- | --- |
| **지운 문면** | `capability-map.md` §13 「0B — 예산 basis 불일치」 인계 행의 `검증 방법`: *"과세/비과세 공고 경계 쌍으로 필터 결과가 basis에 따라 갈리는지 고정하고, **같은 쌍이 감시·검색 양쪽에서 같은 결과**를 내는지도 함께 고정한다 — …"* |
| **왜 모순인가** | **같은 행 앞쪽**이 *"두 금액의 과세 처리 = 차이의 크기와 방향은 활성 `OPEN-REG-05`가 소유한다"*고 적는다. 뒤쪽이 **과세/비과세로 정의된 경계 쌍**을 조건 없이 요구하면 **한 행 안에서 미결을 미결이라 적고 동시에 fixture로 확정한다** — **형태 7 그 자체**다 |
| **남긴 문면** | ledger `R-BASIS-01`의 조건부 서술을 **그대로 따랐다**(새 문면을 발명하지 않았다). **결정 무관(무조건)** = **basis 태그가 다른 값 쌍**을 어느 쪽이 크다는 전제 없이 고정 + 감시·검색 두 경로가 같은 답을 내는지. **조건부(`OPEN-REG-05` 결정에 따라 확정)** = **과세/비과세로 정의된 경계 쌍은 그 `OPEN`이 닫힌 뒤에 만든다** — *"지금 만들면 legacy의 미결 과세 의미로 corpus 경계가 굳는다"* |
| **정본 확인** | `docs/discovery/regression-ledger.md`의 `R-BASIS-01` `검증 방법`을 **직접 열어** 확인했다. 출력은 `commands.md` **C-6** |

**나머지 `검증 방법` 항목은 건드리지 않았다** — 그 행의 `검증 방법`은 이 한 자리다.
**`관찰`·`V2 예방 제약`·`근거`·`마이그레이션`·`STR-16 주의`도 무변경**이며
`commands.md` C-2.2의 hunk 지목이 그 사실을 낸다.

### A4가 이 라운드에 바뀌었다

**계약을 작업 중에 고쳤다는 것을 문면에 남긴다.** 최초 A4는 불변을 **「hunk 하나」·
「한 줄 교체」라는 셈**으로 대조하게 돼 있었고, F-4가 **같은 행의 두 번째 자리**를 고치면서
그 셈이 계약과 어긋날 수 있게 됐다. **셈을 지우고 대상으로 적었다** —
**「`capability-map.md` 변경이 §13의 「0B — 예산 basis 불일치」 인계 행에 한정된다」**.
근거는 **운영자 결정(2026-08-27)**과 **Codex 1차 medium**이며 `scope.md` A4의 인용 블록에
같은 내용이 있다.

**범위는 실제로 넓어졌다 — 두 자리로.** 최초 계약은 `§13 X-1 서술 한정`이었고
**`검증 방법`은 그 밖**이었다(그래서 `O-1`로 등재돼 있었다). **운영자 결정이 그것을
승인했다.** 승인된 것은 §13 인계 행의 **`사용자 영향`과 `검증 방법` 둘뿐**이고,
**같은 행의 나머지 필드는 여전히 out_of_scope**다.
**넓어지지 않은 것**: **X-2~X-6**과 **O-2**(§13 밖의 X-1 프레이밍)는 **승인 대상이
아니었고** 손대지 않았다 — **0C 데이터 사전이 정본**이 되며 `commands.md` C-3.4가
그 자리가 그대로임을 낸다.

### 이 라운드의 불변 — 수를 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| `capability-map.md` 변경이 그 인계 행에 한정 | `commands.md` **C-2.1 · C-2.2** |
| O-2 자리가 무변경 | `commands.md` **C-3.4** |
| `regression-ledger.md` 무변경 | `git diff --stat 48151b9...<head> -- docs/discovery/regression-ledger.md` |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** (base ↔ HEAD 두 실행) |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 10. 수정 라운드 3 — verifier `not-ready` (`G-1`~`G-5`)

**Codex 1차 네 건은 전부 처방대로 닫힌 것으로 확인됐다**(`V-1`~`V-6` pass).
막은 것은 **evidence 자기서술**이고 **공통점은 하나다** — **낡을 수 있는 것을 산문에
옮겨 적었고 뒤 편집이 그것을 무효로 만들었다.**

**값을 고치지 않고 지웠다**(0B `N-1` 처방) — **정확한 값으로 교체하면 다음 편집에 또
낡는다.** **다만 무엇을 지웠는지는 갈래가 다르다**: `G-2`·`G-3`·`G-4`는 **좌표·커밋
서술**을 지우고 **바로 위 출력 블록을 가리키게** 바꿨고, `G-1`은 **`O-1`의 상태 선언**을
현재로 맞추며 §9와 `scope.md` O-1 행을 가리키게 했으며, `G-5`는 **중복된 값**을 지우고
머리 yaml을 가리키게 했다.

| # | 지운 문면 | 왜 거짓인가 | 남긴 문면 |
| --- | --- | --- | --- |
| **G-1** (high) | §6이 `O-1`을 미처리로 열거하고, §7이 *"`O-1`을 닫지 않아 §13 행이 내부적으로 어긋난 채 남는다 … 이 slice는 그 결정을 대신하지 않는다"*라고 적었다 | **`O-1`은 수정 라운드 2에서 닫혔다.** 같은 패키지의 `scope.md` O-1 행과 §9는 닫혔다고 적어 **evidence가 자기 상태를 두 가지로 말했다.** §6·§7은 **이력 절이 아니라 「현재 상태」를 선언하는 자리**다 | §6에서 **`O-1`을 뺐다**(*"`O-1`은 여기 없다 — 수정 라운드 2에서 닫혔다"*). §7 1번을 **닫힌 현재 상태**로 바꿨다 — *"§13 행의 내부 모순은 닫혔다 … 남은 것은 그 `OPEN` 자체이며 이 slice가 소유하지 않는다"*. **`O-2`는 그대로 열려 있다** |
| **G-1** 부수 | A1~A5 요약 표 **한가운데(A4 행과 A5 행 사이)**를 끊던 blockquote | 표를 갈라 놓아 **요약만 읽는 사람에게 넷째 finding이 미처리로 보였다** | **표 밖으로 뺐고** 두 라운드의 대응이 드러나게 다시 썼다 |
| **G-1** 부수 2 <br> *(수정 라운드 4에서 등재 — `H-2`)* | §8 말미의 포인터 *"이 파일 **§6의 `O-1`**과 같은 것이고"* | **§6에서 `O-1`을 빼면서 그 포인터가 없어진 자리를 가리키게 됐다.** §8은 이력 절이라 **내용은 고치지 않지만**, 끊어진 포인터를 그대로 두는 편이 더 나쁘다 | *"**`scope.md`의 `O-1` 행**과 같은 것이고"* — **가리키는 곳만 바꿨다.** 판단도 서술도 그대로다. **이 편집이 이 표에 등재되지 않아 `H-2`가 났다** |
| **G-2** (medium) | C-3.1 판정의 **좌표 열거**(취소선·산문·provenance 표·§13 정정문 각각의 줄 번호) | **`cb1be88` 시점의 좌표다.** 그중 둘은 지금 **매치가 아니고**, 실제 매치 넷이 열거에서 **빠졌다.** 위 출력 블록은 매 라운드 갱신됐는데 **그 아래 산문만 남았다** — Codex `F-2`와 같은 부류 | **좌표를 지웠다.** *"**위 출력이 낸 매치는 in_scope 안에서 전부** ① 취소선 안 ② 정정 블록의 설명 산문 ③ provenance 표 ④ §13 정정문 중 하나다 — **그 넷 중 어느 것도 살아 있는 단정이 아니다.** **§13 밖에 살아 있는 단정이 하나 남아 있고**(§5 DEC-01) … **어느 줄인지는 C-3.4가 찍는다.** **좌표를 여기 옮겨 적지 않는다**"* <br> **⚠ 이 문면은 수정 라운드 4에서 한 번 더 고쳤다** — 최초 처방이 *"살아 있는 단정은 **없다**"*였고 그것이 **거짓**이었다(`H-1`). §11 참조 |
| **G-3** (medium) | C-3.5 판정과 그 아래 주석의 **훅 번호**(묶인 자리 한 쌍, R-07 훅) | **표가 이미 다른 값을 갖는다** — 수정 라운드 1이 `decisions.md`를 바꿔 이하 훅이 전부 밀렸다. **세 줄 위 같은 블록의 awk 출력이 현재 값을 찍는다** | **번호를 지웠다.** *"표에는 인접 훅을 한 행으로 묶는 자리가 있다 — 파일 머리 · `OPEN-DEC-09` · 이 slice가 더한 X-1 정정. **어느 훅인지는 위 awk 출력이 낸다**"*. 주석도 *"X-1 정정 훅이 새로 들어오면서 그 아래 훅이 전부 이동했고"*로 **특정 번호 없이** |
| **G-4** (medium) | C-2 판정의 *"그 파일을 바꾸는 커밋은 **이 블록과 같은 커밋에 실린다**"* | **다른 커밋이다.** 게다가 이 계약이 세운 **「이력 절은 자기가 속한 커밋의 diff를 서술하지 않는다」를 그 자리에서 어긴다.** **결론(*"뒤 커밋에 낡지 않는다"*)은 참**이고 틀린 것은 **그 결론을 떠받치는 커밋 서술**뿐이다 | **커밋 서술을 지우고 근거를 경로 한정으로 바꿨다** — *"이 셋은 다른 파일을 바꾸는 커밋에 낡지 않는다 — 경로가 `-- docs/discovery/capability-map.md`로 **한정돼 있기 때문**이다. **근거는 경로 한정 하나이고 커밋 관계가 아니다** — 이 블록은 자기가 실린 커밋에 대해 주장하지 않는다"* |
| **G-5** (low) | `scope.md` 본문 절(「이 커밋에 대해서는 주장하지 않는다」)의 *"`head_sha` → `90dde40`"* | **계약 파일이 자기 필드에 대해 두 값을 말한다** — 머리 yaml은 다른 값이다. 그 자리는 **이력 절이 아니라 본문**이라 라운드마다 갱신 대상이 된다 | **SHA를 지우고 yaml을 가리킨다** — *"**`head_sha`의 정본은 이 파일 머리의 yaml 하나다** — 값을 두 곳에 두지 않는다"*. 「갱신 이력 — 수정 라운드 1」 안의 같은 표기는 **그 라운드의 동결된 기록이라 건드리지 않았다** |

### `G-2`·`G-3`·`G-4`가 같은 것을 말한다

**출력 블록은 매 라운드 다시 떴는데 그 아래 판정 산문은 대상에서 빠져 있었다.**
0A2 §10.1 **형태 5**가 *"블록만이 아니라 그 블록을 인용하는 산문도 대상"*이라 적고
**0B `H-R3`가 정확히 같은 자리에서 두 번 났는데**, 이 slice는 그 규칙을 **출력 블록의
재실행**으로만 적용하고 **판정 산문에는 적용하지 않았다.**

**`G-1`·`G-5`는 이 진단에 들어가지 않는다** — `G-1`이 지운 것은 좌표가 아니라 **상태
선언**이고 `G-5`가 지운 것은 **중복된 값**이다. 둘 다 가리키는 곳이 **출력 블록이 아니다**
(§9·`scope.md` O-1 행 / 머리 yaml). **셋에 대해 참인 진단을 다섯으로 넓히지 않는다.**

**처방은 공통이다** — **옮겨 적지 않으면 낡을 수 없다**(형태 6 · 0B `N-1`).

### 이 라운드는 산출물을 건드리지 않았다

`G-1`~`G-5`는 **전부 evidence 산문**이다. `capability-map.md`·`decisions.md`·
`0a2/checklist.md`는 **한 자도 바뀌지 않았고**, 그래서 기록된 출력 블록이 **여전히 실측과
같다**(각 블록의 명령을 다시 돌리면 확인된다).

### 이 라운드의 불변 — 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 7f25aa4..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| O-2 자리가 무변경 | `commands.md` **C-3.4** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 11. 수정 라운드 4 — verifier `H-1`~`H-4`

`G-1`·`G-3`·`G-4`·`G-5`는 **처방대로 닫힌 것으로 확인됐다.** 네 건이 남았고
**`H-1`은 앞 라운드가 참인 절을 지운 것**이라 **되돌리는 방향**이다.

| # | 지운 문면 | 왜 거짓인가 | 남긴 문면 |
| --- | --- | --- | --- |
| **H-1** (high) | C-3.1 판정의 *"… 중 하나이며 **살아 있는 단정은 없다**"* | **거짓이다.** 위 출력의 매치 하나(**§5 DEC-01**)가 ①~④ 어디에도 안 들어가고 **그 자체가 살아 있는 단정**이다 — *"투찰율이 곱해지는 base는 추정가격(ex-VAT)이 아니라…"*. `scope.md` **O-2**가 등재한 자리이고(**⚠ 수정 라운드 10의 출처 층 재판정으로 이 귀속은 무효다 — §5는 `O-2`가 아니다. 현재 판정은 C-3.4가 낸다**), **같은 패키지의 네 자리(C-3.1 두 줄 아래 · C-3.4 판정 · `scope.md` O-2 · §6·§7)가 전부 「남아 있다」고 적어** evidence가 같은 사실을 두 가지로 말했다 | 분류 서술은 유지하되 **범위를 명시하고 「없다」를 되돌렸다** — *"위 출력이 낸 매치는 **in_scope 안에서** 전부 ①~④ 중 하나다 — **그 넷 중 어느 것도 살아 있는 단정이 아니다. §13 밖에 살아 있는 단정이 하나 남아 있고**(§5 DEC-01), 그것은 **out_of_scope**라 이 slice가 고치지 않았다 — **어느 줄인지는 C-3.4가 찍는다**"*. **좌표는 다시 박지 않았다** |
| **H-2** (medium) | `scope.md` 라운드 3 절의 *"라운드별 이력 절(§8·§9 · 이 파일의 앞 이력)은 그 시점의 정확한 기록이라 **고쳐 쓰지 않았다**"* | **그 커밋이 §8의 포인터 한 자리를 고쳤다** — `G-1`이 §6에서 `O-1`을 빼면서 §8의 포인터가 허공을 가리키게 돼 살아 있는 자리로 돌린 것이다. **편집 자체는 옳고 틀린 것은 선언**이며, **§10 표의 어느 행도 그 편집을 등재하지 않았다** | 선언을 **실측에 맞게 좁혔다** — *"**내용을 고쳐 쓰지 않았다** … **한 자리만 손댔다**: … 살아 있는 자리로 돌렸다. **판단도 서술도 바꾸지 않았고 가리키는 곳만 바꿨다.** `checklist.md` §10 표에 등재했다. **이 파일의 앞 이력은 실제로 무변경**이다"*. **§10에 「`G-1` 부수 2」 행을 더했다** |
| **H-3** (low) | *"**다섯 다** 좌표를 지우고 바로 위 출력 블록을 가리키게 바꿨다"* · *"**다섯 다 뿌리가 하나다** … 그 아래 판정 산문만 옛 좌표로 남았다"* (`checklist.md` §10 머리·「이 다섯이 같은 것을 말한다」 · `scope.md` 라운드 3 머리) | **`G-2`·`G-3`·`G-4`에 대해서만 참이다.** `G-1`이 지운 것은 좌표가 아니라 **`O-1`의 상태 선언**이고 남긴 문면은 §9와 `scope.md` O-1 행을 가리킨다. `G-5`가 가리키는 것은 **머리 yaml**이다. **둘 다 출력 블록이 아니고 「판정 산문」도 아니다** — **셋에 대해 참인 진단을 다섯으로 넓혔고 그것이 형태 6이 겨냥하는 형태**다 | **전칭을 지우고 갈래를 갈랐다** — *"**공통점은 하나다** — 낡을 수 있는 것을 산문에 옮겨 적었다. **다만 무엇을 지웠는지는 갈래가 다르다**: `G-2`·`G-3`·`G-4`는 좌표·커밋 서술을 지우고 위 출력 블록을, `G-1`은 상태 선언을 지우고 §9와 O-1 행을, `G-5`는 중복된 값을 지우고 머리 yaml을 가리키게 했다"*. 절 머리도 **「`G-2`·`G-3`·`G-4`가 같은 것을 말한다」**로 좁혔다 |
| **H-4** (low) | `scope.md` 라운드 3 절의 *"**이 커밋에 대해 아무것도 주장하지 않는다**"* 선언(절 머리와 말미 두 자리) | **같은 절이 「산출물을 한 자도 바꾸지 않았다」는 자기 커밋 diff 주장을 하면서 동시에 「주장하지 않는다」고 적는다.** 앞 라운드들은 커밋이 여럿이라 「이 라운드」≠「이 커밋」이었는데 **이번엔 커밋 하나라 둘이 같아졌다.** **사실 주장은 참이고 재현 명령도 붙어 있다 — 거짓인 것은 선언** | **선언을 지우고 사실 주장과 재현 명령은 남겼다.** 절 머리를 **「`head_sha`와 range」**로 바꾸고 *"**이 커밋이 무엇을 바꿨는지는 `git show --stat --format='' HEAD`가 낸다** — 파일 목록도 셈도 여기 옮겨 적지 않는다. 산출물 무변경 같은 **사실 주장은 아래 표의 재현 명령과 함께** 남긴다"*로. 말미는 **「결과를 여기 적지 않는다」** |

### `H-1`이 말하는 것 — 지우는 처방에도 경계가 있다

**`G-2`의 처방(「좌표를 지워라」)은 옳았고 그 옆에 붙어 있던 참인 절까지 지운 것이 실패**다.
**낡는 것(좌표)과 낡지 않는 것(「하나 남아 있다」는 사실)을 갈랐어야 했다.**
지금 문면은 그 둘을 나눈다 — **사실은 남기고 좌표는 `C-3.4`에 맡긴다.**

**`O-2`는 손대지 않았다.** `H-1`은 그 존재를 **정확히 적으라**는 것이지 고치라는 것이
아니다 — `commands.md` **C-3.4**가 그 자리를 그대로 찍는다.

### 이 라운드도 산출물을 건드리지 않았다

`H-1`~`H-4`는 **전부 evidence 산문**이다. 근거는 아래 표의 첫 명령이다.

### 이 라운드의 불변 — 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 4aa4e3c..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| O-2 자리가 **살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 12. 수정 라운드 5 — verifier `P-1` (게이트 안 1건)

`H-2`·`H-3`·`H-4`는 닫혔다. **`P-1`은 `H-1`이 되돌려 넣은 문장의 수가 틀린 것**이다.

**먼저 측정하고 문장은 그 다음에 썼다.** 팀 리드가 이번엔 대체 문장을 주지 않았다 —
`H-1`이 **처방에 실린 예시 문장을 검증 없이 넣은 데서** 났기 때문이다.

### 측정 — 어휘도 줄 경계도 가리지 않고 훑었다

**앞 축(C-3.4)이 좁았다.** 패턴이 `추정가격.{0,12}(ex-VAT|부가세 별도)` 하나였고
**줄 단위**였다. 두 가지를 다 넓혔다:

| 넓힌 것 | 왜 |
| --- | --- |
| **어휘** — `ex-VAT` · `부가가치세` · `부가세` · `VAT` · `과세` | `부가세 별도`처럼 **말만 다르고 같은 사실**인 자리를 놓친다 |
| **단위** — 줄 → **블록** | 한 문장이 여러 줄에 걸치면 줄 grep 이 놓친다. **실제로 그렇게 숨은 후보가 있었다** — 0B 가 "창 밖" 미탐으로 두 번 태운 자리와 같은 형태다 |

**후보와 자리는 `commands.md` C-3.4의 출력이 낸다** — 여기 옮겨 적지 않는다.
**판정은 기계가 못 한다**(*"이 블록이 이 문서의 단정인가, 귀속된 인용인가"*)라
**후보 전건을 사람이 읽고 C-3.4 아래 판정 표에 근거와 함께** 남겼다.

**판정 축**: *"`capability-map.md`가 **자기 목소리로** ① `추정가격`을 부가세 제외로
**단정**하거나 ② 두 금액의 차이를 **과세로 설명하거나 크기를 정량화**하는가."*

> **⚠ 수정 라운드 10의 출처 층 재판정으로 이 절의 `O-2` 귀속은 무효다.**
> **현재 판정은 `commands.md` C-3.4의 판정 표가 낸다.** 이력 본문은 고쳐 쓰지 않는다.

**결과** — **살아 있는 X-1 프레이밍은 §5 DEC · §6 NOTI · §7 SET에 걸쳐 있고 두 갈래다**:
**부가세 제외 단정**과 **과세로 차이를 설명·정량화하는 서술**. **귀속된 인용**(조달청 문서를
인용한 §3·§12)과 **§13의 정정문이 원문을 인용한 자리**는 제외했다 — 이 문서의 단정이 아니다.

**§7의 것이 특히 무겁다** — *"과세 공고에서 **약 10%** 어긋난다"*는 **0B ledger
`R-BASIS-06`이 같은 주장에 ※를 달아 `OPEN-REG-05` 소유로 돌린 바로 그 문장**인데
`capability-map.md`에는 **그 귀속이 없다.**

### 건별

| # | 지운 문면 | 왜 거짓인가 | 남긴 문면 |
| --- | --- | --- | --- |
| **P-1** (high) | C-3.1 판정의 *"**§13 밖에 살아 있는 단정이 하나 남아 있고**(§5 DEC-01)"* | **실측은 하나가 아니다.** 그 문장이 독자를 C-3.4와 `scope.md` O-2로 보내는데 **그 둘이 다른 수를 낸다** — **같은 패키지가 같은 사실을 두 가지로 말한다**. `H-1`을 열게 한 바로 그 형태다 | **수를 지우고 갈래를 나눴다.** C-3.1은 **자기 출력에만 한정한 판정**임을 문면이 스스로 밝히고(*"패턴은 … in_scope 두 파일만 본다 — **문서 전체에 대한 주장이 아니다**"*), **사각지대를 별도 항목으로 세웠다** — *"이 패턴은 X-1 프레이밍을 다 잡지 못한다 … **문서 전체의 정본은 C-3.4**이며 거기 **살아 있는 X-1 프레이밍이 남아 있다** … **자리도 수도 여기 옮겨 적지 않는다**"* |
| **P-1** 파생 | C-3.4의 **좁은 패턴**과 그 판정(*"§5 DEC-01과 §6 NOTI … 두 자리"*) | 축이 **어휘 하나·줄 단위**라 **같은 사실인데 말이 다른 자리와 여러 줄에 걸친 자리를 못 봤다.** 그 위에 세운 `scope.md` O-2와 `checklist.md`의 대응 서술도 함께 좁았다 | **축을 넓히고**(어휘 5종 · 블록 단위) **본문을 C-3에 인라인**했다(형태 4 — marker 로 집고 `assert` 로 유일성 확인). **판정 표를 신설**했다. `scope.md` **O-2 행**과 `checklist.md` **§6·§7**을 **수 없이 갈래와 절로** 다시 썼다 |
| **P-1** 파생 2 | 앞 라운드들의 불변 표·서술에 남은 *"O-2 **두 자리**"* | **축이 넓어지면서 그 수가 낡았다.** 표는 **그 시점의 기록이지만 「확인할 것 / 명령」은 검증자가 지금 도는 자리**라 낡은 수를 두면 오도한다 | **수만 지우고 가리키는 곳은 그대로 뒀다** — *"O-2 **자리** 무변경 / `commands.md` C-3.4"*. **판단도 명령도 바꾸지 않았다.** `H-2`의 교훈대로 **이 편집을 여기 등재한다** |

### `H-1`과 `P-1`이 같은 것을 말한다

**둘 다 전칭의 범위가 문면에서 갈리지 않아 났다.** `H-1`은 *"살아 있는 단정은 없다"*가
**출력 안의 판정인지 문서 전체의 판정인지** 갈리지 않아 후자로 읽혀 거짓이 됐고,
`P-1`은 그것을 고치며 **문서 전체에 대한 수**를 **좁은 출력의 근거로** 적어 다시 거짓이 됐다.

**처방은 하나다** — **판정하는 문장은 자기가 어느 출력에 걸린 판정인지 문면에 적고,
문서 전체에 대한 주장은 그것을 실제로 훑는 축에 맡긴다.** C-3.1과 C-3.4가 이제 그렇게
갈린다.

### 이 라운드도 산출물을 건드리지 않았다

`P-1`은 **evidence 산문과 evidence 안의 스윕 축**이다. `capability-map.md`는
**이번 라운드 무변경**이고 **`O-2`는 손대지 않았다** — `P-1`은 그 존재를 **정확히 적으라**는
것이지 out_of_scope 를 침범하라는 것이 아니다.

### 이 라운드의 불변 — 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 491684f..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** — **없어지면 안 된다** |
| C-3.4 스윕이 고정점인가 | 그 절의 명령을 다시 돌려 출력이 같은지 본다 |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 13. 수정 라운드 6 — verifier 마지막 1건 (운영자 승인)

`P-1`은 닫혔고 **게이트 안 0건**으로 `ready-for-review` 판정을 받았다.
**재작업 상한에 도달해 운영자가 이 한 건만 더 고치고 리뷰로 가라고 결정했다**(2026-08-27).

| | |
| --- | --- |
| **지운 문면** | C-3.4 스윕 본문 절 머리의 *"**`EXTRACT`는 C-4.1이 쓰는 것과 같은 방식이다**"* |
| **왜 거짓인가** | **`EXTRACT`라는 이름이 이 패키지 어디에도 정의돼 있지 않다.** 실물을 열어 확인한 결과 **C-3.4는 `BLK4`, C-4.1은 `BLK`**를 쓰고 **`EXTRACT`는 그 한 줄에만 나온다.** 재실행이 막히지는 않지만 **그 절의 제목이 「형태 4 — 이름으로만 부르지 않는다」**이고 **Codex 1차가 이미 같은 계열(`inv0a3.py` 미존재)을 medium으로 냈다** |
| **남긴 문면** | **이름을 부르지 않고 자리를 가리킨다** — *"**이 블록을 뽑아 실행하는 명령은 C-3.4 절의 첫 명령이고, 위 출력 블록에 그대로 있다.** **C-4.1도 같은 방식을 쓴다** — 다른 것은 **marker와 그것을 담는 변수뿐**이다."* **뒤따르는 명제 셋은 참이므로 그대로 뒀다** — 줄 번호가 아니라 marker로 집고 · `assert`로 유일성 확인 · stdin 으로 흘려 넣어 **커밋되지 않은 스크립트 파일에 의존하지 않는다** |

**이름을 실제 변수(`BLK4`·`BLK`)로 바꾸는 쪽도 가능했으나 택하지 않았다** — 그 절의 논지가
**「이름으로만 부르지 않는다」**이고, 이름을 적으면 **그것이 또 낡을 수 있는 자리**가 된다.
**명령은 바로 위 출력 블록에 그대로 있으므로 가리키는 것으로 충분하다.**

**네 명제를 실물에 대조했다** — 두 절의 명령이 `marker` 필터 · `assert len(b) == 1` ·
`printf %s "…" | python3 -`을 쓰고 **줄 번호를 쓰지 않는다.** 두 절의 차이는
**marker 문자열과 그것을 담는 변수뿐**이다.

### 이 라운드의 불변

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat f6bcf18..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| C-3.4 스윕이 여전히 고정점인가 | 그 절의 명령을 다시 돌려 출력이 기록과 같은지 본다 |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 14. 수정 라운드 7 — Codex 2차 `request_changes` (medium 3)

verdict 정본: `reports/evidence/m0/0a3/codex-review-20260827T204954Z.json`(등재, append-only).
`reviewed_base` **`48151b9`** → `reviewed_head` **`fbdedb6`**.
**severity별 건수는 옮겨 적지 않는다** — 그 JSON의 `findings`가 정본이다.

**1차 네 건은 전부 해소 판정을 받았고 산출물 지적은 1차 이후 없다.** 남은 셋은 전부
**evidence 자기서술**이며 **`M-2`·`M-3`은 `P-1` 수정이 직접 만들었다** — 축을 넓히자
**출력 내용과 스크립트 목록이 달라졌는데 그것을 서술하는 산문이 안 따라갔다.**

### 이번엔 고치기 전에 전수로 찾았다

**이 slice에서 걸린 모든 건이 하나의 형태다** — *"evidence가 같은 사실을 여러 자리에서
서술하는데 수정이 한 자리만 건드린다."* 그래서 **건별로 `grep`을 돌려 같은 명제를 말하는
자리를 전부 목록으로 만들고 한 번에 맞췄다.** 목록은 `_workspace/m0-0a3/12_fix_codex2.md`에
있다.

| # | 지운 문면 | 왜 거짓인가 | 남긴 문면 |
| --- | --- | --- | --- |
| **M-1** | YAML `in_scope`의 *"§13 X-1 서술 한정"* · A4 인용 블록의 *"**바꾼 것은 불변의 표현이지 불변 자체가 아니다** … 그 범위는 **넓어지지 않았다**"* · §9의 *"**지키는 범위는 넓어지지 않았다**"* | **범위는 실제로 넓어졌다.** F-4는 **최초 계약에서 X-1 밖**이었고 — 이 파일과 `scope.md`의 **O-1 기록이 스스로 그렇게 적는다** — 운영자가 승인해 넓힌 것이다. **승인 사실은 공개했으나 「넓어지지 않았다」가 그것과 모순**됐다 | **승인 범위를 두 항목으로 특정**했다 — §13 인계 행의 **`사용자 영향`(X-1 정정)**과 **`검증 방법`(F-4 조건부화)**. YAML·A4·§9를 그렇게 맞추고 **「같은 행 전체」라는 느슨한 경계를 지웠다** — 그 행의 `관찰`·`V2 예방 제약`·`근거`·`마이그레이션`·`STR-16 주의`는 **여전히 out_of_scope**임을 `out_of_scope`에 명시했다. **「넓어졌다」를 문면에 적고 근거(운영자 결정 · Codex 1차 medium · Codex 2차 medium)를 달았다.** **넓어지지 않은 것**(X-2~X-6 · O-2)도 그 자리에서 갈랐다 |
| **M-2** | C-3.1 판정의 *"그 매치는 전부 ①~④ 중 하나이며 **살아 있는 단정이 아니다**"*(범위가 `capability-map.md` 전체였다) · *"**`capability-map.md` 전체를 보지도 않는다**"* | **명령이 파일 전체를 grep했다.** 그래서 **§5의 살아 있는 단정이 그 출력에 들어왔고**, 같은 파일 **C-3.4 판정표는 그것을 *"포함 — 부가세 제외 단정"*으로 판정**한다. **두 판정이 어긋났고 `checklist.md` A1 pass가 C-3.1을 근거로 인용**한다 | **(가)를 택했다 — 명령의 범위를 좁혔다.** `decisions.md` + `capability-map.md` **§13**(절 머리로 `awk` 슬라이스 — **줄 번호가 아니라 내용으로 집는다**). **§5 매치가 더는 섞이지 않는다**(같은 행 앞머리와 같은 값이다 — 원래 이 자리가 **셋을 열거**했고 **수정 라운드 9가 좁혔다**). **이 축이 하지 않는 일**(문서 전체를 보지 않음 · 어휘 한정)을 별도 항목으로 뒀다. `checklist.md`의 **A1 근거 행**과 **§1**을 함께 동기화했다. <br> **⚠ 이 행이 적은 판정 문면(*"검사 범위가 A1의 범위와 같다"*)과 절 머리의 범위 분할 선언은 수정 라운드 8에서 다시 고쳤다** — **등식이 거짓**이었다(`N-2`). §15 참조 |
| **M-3** | 머리글의 *"**스윕 스크립트를 새로 만들지 않았다** … **유일한 스크립트**는 …"* · C-8의 *"**새 스윕 축·스크립트** \| 이 slice에서 그 부류의 결함이 관측되지 않았다"* | **거짓이다.** `P-1` 대응으로 **C-3.4에 「X-1 프레이밍 후보 스윕」이 인라인됐고 실제로 재실행된다.** 이 파일 §12도 그렇게 기록한다. **스크립트 자기 인벤토리가 현재 head 에서 거짓**이었다 | **머리글에 「이 패키지가 쓰는 스크립트」 표를 신설**했다 — **둘**이고 각각 **어디 · 무엇을 하나 · 왜 있나(도입 근거)**를 적었다. **불변 계산기**는 0A2 수치 축에서 떼어온 것, **X-1 스윕**은 **`P-1`이 앞 축의 미탐을 반증한 뒤에 넓힌 것**이다. C-8 행은 **「근거 없이 늘린 축·스크립트」**로 바꿔 **결함이 먼저, 도구가 나중이라는 순서를 지켰다는 참인 주장**만 남겼다 |

### `M-2`가 C-3.1의 범위를 좁혔다

앞 세 라운드(`H-1`·`P-1`·이번 `M-2`)가 **같은 자리에서 났다** — C-3.1이 `capability-map.md`
**전체**를 grep해 **§5의 out_of_scope 매치가 그 출력에 섞였고**, 판정 산문이 그것을
따라가지 못했다. **이번에 그 명령의 범위를 좁혀 그 매치를 뺐다.**

**§6·§7은 이 축의 발견이 아니다** — C-3.1의 패턴에 걸리지 않고 **C-3.4의 5어휘 블록
스윕**이 `P-1` 라운드에 냈다. **두 축의 발견을 합치지 않는다.**

> **⚠ 이 절은 수정 라운드 8에서 한 번 더 고쳤다** — 원래 *"**겹침 자체를 없앴다** …
> 두 축이 이제 서로의 결론을 **반증할 수 없다**"*라고 적었고 **그것이 거짓이었다**
> (`N-1`). **§13 ⊂ `capability-map.md` 전체**이므로 **두 범위는 여전히 포개져 있다.**
> §15 참조.

### 이 라운드도 산출물을 건드리지 않았다

**`capability-map.md`는 무변경**이고 **`O-2`는 손대지 않았다.**

### 이 라운드의 불변

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat fbdedb6..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** |
| C-3.1 출력에 out_of_scope 매치가 섞이지 않는가 | `commands.md` **C-3.1** — `capability-map.md` 쪽 매치가 **§13 안의 것뿐**이어야 한다 |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 15. 수정 라운드 8 — verifier `N-1`·`N-2`

**전수 규율 자체는 작동했다** — 앞 라운드의 「고침」이 전건 실제로 고쳐졌고 「손대지 않음」
판단도 옳다는 판정을 받았다. **그런데 그 규율이 이번엔 거짓 명제를 전수로 퍼뜨리는 데
쓰였다.** 앞 라운드가 **새 구조 주장 둘**을 만들었고 **둘 다 거짓**이었다.

| # | 지운 문면 | 왜 거짓인가 | 남긴 문면 |
| --- | --- | --- | --- |
| **N-1** | *"**두 축의 범위가 겹치지 않는다**"* · *"**겹치지 않게 갈랐다**"* · *"**겹침 자체를 없앴다**"* · *"**두 축이 이제 서로의 결론을 반증할 수 없다**"* · 절 머리 *"두 축이 범위를 **나눠 본다**"* | **§13 ⊂ `capability-map.md` 전체다.** 실행이 그대로 보인다 — **두 축이 같은 §13 행을 각자 출력하고 각자 판정한다**(C-3.1 분류 **④** / C-3.4 판정 표의 **§13 행**). **오늘 두 판정이 어긋나지 않는 것은 둘이 우연히 같은 결론이기 때문이지 범위를 갈라서가 아니다** — 「반증할 수 없다」는 **구성으로 보장되지 않는다** | **사실만 적었다** — 절 머리를 **「두 축이 보는 범위」**로 바꾸고 *"**두 범위는 포개져 있다** — **§13 ⊂ `capability-map.md` 전체**이므로 **§13은 두 축이 모두 출력하고 각자 판정한다** … **오늘 두 판정이 어긋나지 않지만 그것이 구성으로 보장되지는 않는다.** **이 라운드가 한 것은 C-3.1의 범위를 A1 쪽으로 좁힌 것**이다"*. C-3.4 판정 말미도 *"**§13은 이 축과 C-3.1이 모두 판정한다**"*로 |
| **N-2** | *"**검사 범위가 A1의 범위와 같다**"* · *"**이 명령이 정확히 그 둘을 본다**"* · *"**이 범위와 같게 맞춰져 있다**"* · 불변 표 라벨 *"C-3.1이 **A1 범위만** 보는가"* | **C-3.1의 첫 명령이 `decisions.md`를 절로 자르지 않고 파일 전체를 grep한다** — `capability-map.md` 쪽만 `awk`로 잘랐다. **자기 출력이 반증한다**: `OPEN-STR-01` 절 밖(provenance 절)의 매치가 있고 **분류 ③이 바로 그 절 밖 매치를 위해 존재한다** | **등식을 포함관계로 좁혔다** — *"**A1의 범위를 포함하는 상위집합이다** … **`decisions.md`는 절로 자르지 않고 파일 전체를 본다.** **자기 출력이 그것을 보인다** … **방향은 보수적이다** — 상위집합이라 **A1이 요구하는 것을 놓치지 않는다**"*. **출력 블록에 절 경계를 찍는 명령을 더해** 그 사실이 실행으로 보이게 했다. **`checklist.md`의 A1 `pass` 근거를 포함관계 위로 옮겼다** |

### 전수로 찾고 한 번에 맞췄다

두 명제를 말하는 자리를 `grep`으로 훑었다(*겹치* · *반증* · *범위와 같* · *정확히 그 둘* ·
*A1 범위만*). **목록과 자리별 처리는 `_workspace/m0-0a3/14_fix_n1n2.md`에 있다.**
**라운드별 이력 절**(§14 · `scope.md` 라운드 7)은 **내용을 고쳐 쓰지 않고 ⚠ 재정정 표시**를
달았다 — `H-1` 때 §10 `G-2` 행에 한 것과 같은 관례다.

### 왜 `N-1`이 났나 — 서사를 만들었다

**`M-2` 대응에서 참인 부분(범위 축소 — §5 매치 배제)은 실제로 됐다.** 거기에
**「뿌리를 고쳤다」는 더 큰 주장**을 얹었고 **그게 거짓이었다.**
**이 slice가 반복해 온 형태의 마지막 변종**이다 — 앞서는 **낡을 수 있는 값**을 적었고,
이번엔 **검증되지 않은 구조 주장**을 적었다. **둘 다 「대상이 스스로 내지 않는 것을
산문에 적은 것」**이다.

**지운 자리에 새 전칭을 넣지 않았는지 다시 훑었다** — 남은 것은 **⚠ 블록이 거짓 원문을
인용한 자리**뿐이고 그것은 기록이다.

### 이 라운드도 산출물을 건드리지 않았다

**`capability-map.md`는 무변경**이고 **`O-2`는 손대지 않았다.**
**게이트 밖 두 자리**(`checklist.md`의 capability-map 변경 위치 행 · `scope.md`의 O-2 행)는
**이 수정으로 거짓이 되지 않는다** — 둘 다 이 두 명제를 말하지 않는다.

### 이 라운드의 불변

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 2f8b65a..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** |
| C-3.1 출력에 out_of_scope 매치가 섞이지 않는가 | `commands.md` **C-3.1** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 16. 수정 라운드 9 — verifier `Q-1`

**`N-1`·`N-2`는 둘 다 닫혔다** — 지운 자리에 새 구조 주장이 들어오지 않았고 대체 문면이
절별 실행 대조로 전부 참이라는 판정을 받았다. **`Q-1`은 그 수정에 딸려 들어온 별개의
사실 오류**다.

| | |
| --- | --- |
| **지운 문면** | *"C-3.1이 `capability-map.md` 전체를 grep해 **§5·§6·§7의 out_of_scope 매치가 그 출력에 섞였고**, 그것이 Codex 2차 `M-2`였다"* 계열 |
| **왜 거짓인가** | **C-3.1의 패턴은 `ex-VAT\|VAT·사정률만큼` 둘뿐**이고 **§6(`부가세 별도`)·§7(`과세 공고`)은 거기 걸리지 않는다.** **옛 C-3.1이 실제로 낸 out_of_scope 매치는 §5 하나**다. **네 방향이 같은 값을 낸다** — 그 패턴을 파일 전체에 돌린 결과 · §6·§7 자리를 그 패턴에 직접 걸어 본 결과(**0건**) · **Codex 2차 `M-2` 원문**(*"`capability-map.md:1373`의 살아 있는 **§5** 단정이 출력된다"* — 단수) · **같은 표 앞머리의 옳은 문면**(*"**§5의** 살아 있는 단정이 그 출력에 들어왔고"*). **`M-2`와의 동일시도 함께 틀렸다** |
| **남긴 문면** | **`§5` 단수로 좁히고 축을 갈랐다** — *"**§5의** out_of_scope 매치가 그 출력에 섞였고 그것이 Codex 2차 `M-2`였다"* + **별도 문단**: *"**§6·§7은 이 축의 발견이 아니다.** C-3.1의 패턴(`ex-VAT\|VAT·사정률만큼`)에 **걸리지 않는다** … **그 두 자리는 C-3.4의 5어휘 블록 스윕이 냈고 그것은 `P-1` 라운드의 발견**이다. **두 축의 발견을 합치지 않는다.**"* |

### 원인 — 두 축의 발견을 하나로 합쳤다

**§5·§6·§7이라는 셋은 `C-3.4`가 낸 것**이고(**`P-1` 라운드**에 넓힌 5어휘 블록 스윕),
**§5 하나는 `C-3.1`이 낸 것**이다(**Codex 2차 `M-2`**). **서로 다른 축이 서로 다른
라운드에 낸 발견인데 회고를 쓰면서 하나로 합쳤다.**

`H-3`이 *"셋에 대해 참인 진단을 다섯으로 넓혔다"*로 걸린 것과 **같은 계열**이다.
**회고를 쓸 때 서로 다른 축·라운드의 사실을 합치지 않는다.**

### 불변 표 라벨도 축에 맞췄다

*"출력에 **§5·§6·§7 자리**가 없어야 한다"*는 **§6·§7이 애초에 이 패턴에 안 걸려**
검사 항목으로 의미가 없었다. **`capability-map.md` 쪽 매치가 §13 안의 것뿐이어야 한다**로
바꿨다 — **그것이 이 축이 실제로 지키는 불변**이다.

### 손대지 않은 자리

**`commands.md`의 C-3.4 판정 요약**(*"살아 있는 X-1 프레이밍은 §5 · §6 · §7에 걸쳐
남아 있다"*)은 **C-3.4에 귀속된 참인 서술**이라 **그대로 뒀다.**

### 명령도 산출물도 바꾸지 않았다

**문면만 고쳤다.** `capability-map.md` 무변경, **`O-2` 생존**, C-3.1·C-3.4의 **명령 불변**.

### 이 라운드의 불변

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 0775a71..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **명령이 바뀌지 않았는가** | `git diff 0775a71..<head> -- reports/evidence/m0/0a3/commands.md` — `$`로 시작하는 줄에 변경이 없어야 한다 |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 17. 수정 라운드 10 — Codex 3차 `R-1` (출처 층)

verdict 정본: `reports/evidence/m0/0a3/codex-review-20260828T005833Z.json`(등재, append-only).
`reviewed_base` **`48151b9`** → `reviewed_head` **`f49155d`**.
**2차 세 건(`M-1`·`M-2`·`M-3`)은 해소 판정을 받았다.**

**이 finding은 Codex 1차 이후 처음으로 evidence 자기서술이 아니라 `O-2`의 인계 내용을
지적한다.**

| | |
| --- | --- |
| **지운 문면** | `O-2`가 **§5 DEC · §6 NOTI · §7 SET**을 모두 「살아 있는 X-1 프레이밍」으로 판정하고, 갈래를 *"① `추정가격`을 **부가세 제외로 단정** ② 두 금액의 차이를 **과세로 설명·정량화**"*로 적은 것 |
| **왜 거짓인가** | **X-1이 무너뜨린 것은 저장값 층**이다 — legacy `Project.budget_estimate`가 `money.py`의 **선언**(부가세 포함)과 **저장 관측**(*"이력상 일관되지 않다"*) 사이에서 갈린다는 것. 그런데 **조달청 문서의 `추정가격`은 실제로 부가세 제외**이고 **`commands.md` C-1의 마지막 주석이 그 구분을 스스로 적고 있었다.** C-3.4가 **「문서의 자기 목소리인가」만 보고 출처 층을 가르지 않아**, `authoritative` 인용(§5 · §6 금액 라벨)과 **표시 요구**(§6 acceptance)까지 오류에 넣었다. 이것은 `data-extract.md` §1의 **`authoritative`/`observed`/`legacy-behavior`** 구분과 **이 slice의 핵심인 「선언 / 관측」 구분**(`F-1`을 닫은 그 구분)을 다시 섞는다 |
| **남긴 문면** | **판정 축을 둘로 갈랐다** — ① **어느 층의 서술인가** ② **저장값의 과세 처리를 근거 없이 단정하거나 두 금액의 차이를 정량화·방향화하는가.** **`O-2`에 남는 것은 ②가 참인 항목뿐**이고 **§6 NOTI(투찰률)**·**§7 SET(SET-09 F-4)** 둘이다. C-3.4 판정 표에 **「출처 층」 열을 신설**해 후보 전건을 다시 판정했다 |

### 판정 기준은 0B가 세운 것이다 — 새로 만들지 않았다

0B verifier **`F-c`**가 *"**「legacy 서술이니 그대로」로는 부족하다** — 귀속 없이 적으면
이 문서의 확정 서술로 읽힌다"*로 **판정**했다. **유보를 실제로 단 것은 다른 라운드다** —
같은 `F-c` 블록이 그렇게 적는다: **`R-BASIS-06`은 `F-3`이, `R-BASIS-03`은 `F-b`가**
각각 **인용 귀속 + `OPEN-REG-05` 유보**를 달았다.
**기준은 legacy 서술로 귀속하는 것만으로는 부족하고 그 `OPEN`을 지목해야 한다**는 것이고,
**남는 두 항목 다 그 지목이 없다.**

### `O-2`가 줄었다 — 0C 인계 내용이 달라진다

**줄어든 자리는 오탐이 아니라 「층이 다른 참인 서술」**이다:

- **§5 DEC** — `추정가격(ex-VAT)`은 **조달청 정의 그대로**이고
  `기초금액/사업금액(과세 공고면 VAT 포함)`은 **legacy가 같게 선언한다**
  (`bid_summary.py:57`). **`Project.budget_estimate`의 저장값을 말하지 않는다.**
- **§6 금액 라벨** — 두 금액을 **공식 정의대로 라벨해 다른 행에 싣는 표시 계약**.
- **§6 acceptance** — *"서로 다른 **행으로 표시된다**"*는 **표시**에 대한 요구다.

**0C는 이 셋을 고칠 대상이 아니라 그대로 쓸 수 있는 인용으로 받는다.**
**「전부 오탐이었다」가 아니다** — **두 자리는 그대로 `O-2`에 남는다.**

### 전수로 맞췄다

`O-2`를 **서술하는**(가리키기만 하는 것이 아니라) 자리를 훑어 **한 번에** 맞췄다 —
`commands.md` C-3.4 **판정 표·요약** · `scope.md` **O-2 행** · `checklist.md` **§6 O-2
bullet · §7 「알려진 제한」**. **목록과 자리별 처리는 `_workspace/m0-0a3/18_fix_codex3.md`**에
있다.

> **⚠ 이 자리에 원래 *"라운드별 이력 절은 내용을 고쳐 쓰지 않고 ⚠ 표시를 달았다"*가
> 적혀 있었고 **그 라운드는 그 행위를 하지 않았다**(verifier `V-1`). 라운드 8의 문장을
> 옮겨 적었을 뿐이다. **수정 라운드 12가 실제로 ⚠를 달았다** — `scope.md`의 라운드 4·5
> 이력과 이 파일 §11 `H-1` 행 · §12 `P-1` 결과 절. **확인 명령은 §19에 있다.**

### `capability-map.md`는 out_of_scope로 유지했다

**무변경**이다. 이 라운드가 바꾼 것은 **그 자리들에 대한 판정과 기록**이지 자리 자체가
아니다.

### 이 라운드의 불변

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat f49155d..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 18. 수정 라운드 11 — Codex 3차 `R-2` (출력 순서)

| | |
| --- | --- |
| **지운 문면** | C-3.3의 기록 출력 두 블록(정렬 없는 `grep` 결과) |
| **왜 재현되지 않나** | **기록이 여러 파일을 인자로 주는 `grep`의 파일 간 출력 순서에 의존했다.** 그 순서는 **구현·환경에 따라 다르다** — **기록을 뜬 환경에서는 재현됐으나 리뷰어 환경에서는 아니었다.** **매치 내용과 A3 결론은 같고**, 어긋난 것은 `commands.md` 머리의 자기 기록(*"접거나 손으로 압축한 블록은 없다"*)이다 |
| **남긴 문면** | **의도한 순서를 만드는 명시적 명령을 기록했다** — 두 명령에 **`\| sort`**를 붙이고 **출력을 그 상태에서 다시 떴다.** 블록 머리에 **원인**(기록이 파일 간 출력 순서에 의존했다는 것)을 적었고, 판정에 **왜 고정했는지**와 **남은 의존(`cut -c`)을 한계로** 남겼다 |

**의존 하나를 지운 것이지 값을 고친 것이 아니다** — 매치 내용도 판정도 바뀌지 않았다.
**「어디서 돌려도 같다」고는 주장하지 않는다** — 뒤따르는 `cut -c`가 **문자/바이트 중
무엇을 세는지 구현에 따라 다를 수 있어** 한글이 섞인 줄의 **잘리는 자리가 달라질 수
있다.** **그 교환은 이 라운드가 하지 않았고 한계로 적었다**(`commands.md` C-3.3).
**판정은 잘린 자리에 기대지 않는다.**

### 이 라운드의 불변

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 2f74829..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| C-3.3이 고정점인가 | 그 절의 두 명령을 다시 돌려 출력이 기록과 같은지 본다 |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 19. 수정 라운드 12 — verifier `V-1`·`V-2`·`V-3`

**이 라운드의 핵심은 통과했다** — 검증자가 **후보 아홉 전건을 독립 판정해 전건 일치**
판정을 냈다. **줄어든 셋도 남은 둘도 옳고 되돌릴 판정이 없다.** 세 건은 전부
**앞 두 커밋이 새로 적은 문장**이다.

| # | 지운 문면 | 왜 거짓인가 | 남긴 문면 |
| --- | --- | --- | --- |
| **V-1** (medium) | §17의 *"**라운드별 이력 절**은 내용을 고쳐 쓰지 않고 **⚠ 표시**를 달았다"* | **그 라운드는 그 행위를 하지 않았다.** 그 range에서 추가된 ⚠는 **이 주장 문장 자신**과 **`scope.md`의 살아 있는 `O-2` 행** 둘뿐이고 **라운드별 이력 절은 한 글자도 안 바뀌었다.** **라운드 8의 문장을 옮겨 적었을 뿐**이다. **`H-2`와 같은 형태** — 한 것과 적은 것이 다르다 | **⚠를 실제로 달았다** — `scope.md`의 **라운드 4 이력(`H-1`)**·**라운드 5 이력(`P-1`)**, `checklist.md`의 **§11 `H-1` 행**·**§12 `P-1` 결과 절**. 문면은 짧게 — *"수정 라운드 10의 출처 층 재판정으로 이 절의 `O-2` 귀속은 무효다. 현재 판정은 C-3.4가 낸다"*. **이력 본문은 고쳐 쓰지 않았다.** §17의 문장은 **한 일에 맞게** 다시 썼다 |
| **V-2** (medium) | *"0B verifier `F-c`가 … `R-BASIS-03`·`R-BASIS-06`에 **`OPEN-REG-05` 유보**를 달았다"* | **`F-c`가 한 일이 아니다.** **0B `scope.md`의 그 `F-c` 블록이 직접 적는다** — **`R-BASIS-06`(`~10% 낮은 투찰가`)은 `F-3`이, `R-BASIS-03`(`rate × 1.1`)은 `F-b`가** 유보를 달았다. **`Q-1`·`R-1`과 같은 계열** — 서로 다른 라운드의 행위를 하나로 합쳤다 | **갈라 적었다** — **`F-c`는 판정**(*"「legacy 서술이니 그대로」로는 부족하다"*)**을 했고, 유보를 실제로 단 것은 `F-3`과 `F-b`**다. **직접 열어 확인**했고 **기준 자체는 그대로**다 |
| **V-3** (low) | *"이 저장소의 `grep`은 `ugrep`이라 병렬 완료 순으로 낸다"*(네 자리) · *"이제 **어디서 돌려도 같은 출력이 나온다**"* | **(a)** `/usr/bin/grep`은 **BSD grep**이고 `ugrep`은 **기록을 뜬 셸에서만** 잡힌다 — **측정하지 않은 것을 단정**했고 Codex 3차의 직접 관측과 어긋난다. **(b)** 보장 주장이 거짓이다 — 블록이 `\| sort \| cut -c1-110`인데 **`cut -c`가 문자/바이트 중 무엇을 세는지 구현에 따라 다를 수 있어** 한글이 섞이면 잘리는 자리가 달라질 수 있다. **`N-1`과 같은 형태** — 새 보장 주장을 만들었다 | **원인을 「인자 순서에 의존하던 것」으로** 적었다 — *"기록이 여러 파일을 인자로 주는 `grep`의 **파일 간 출력 순서에 의존**했다 … **어느 구현이 어떤 순서를 내는지는 이 evidence가 주장하지 않는다** — 측정한 것은 *"두 환경에서 순서가 달랐다"*까지다"*. **보장 주장을 지우고 남은 의존(`cut -c`)을 한계로** 적었다 — **그 교환은 하지 않았다.** **판정은 잘린 자리에 기대지 않는다** |

### 「적은 대로 했는지」를 실행으로 확인했다

`V-1`은 **`git diff … | grep '^+.*⚠'` 한 줄로 잡혔다.** 그래서 이번엔 **적기 전에
그 명령으로 확인**했다 — 명령과 출력은 `commands.md` **C-7**에 있다.
**이 slice가 반복해 온 「한 것과 적은 것이 다름」을 기계로 막는 첫 자리**다.

### 이 라운드의 불변

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat a264d6f..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **표시가 이력 절에 실제로 들어갔는가** | `commands.md` **C-7** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 20. 수정 라운드 13 — verifier `W-1`·`W-2`·`W-3`

**`V-1`·`V-2`·`V-3`은 셋 다 실제로 닫혔다** — 표시는 네 자리에 진짜로 달렸고 내용도 참이며
이력 본문은 안 고쳤다. `F-c` 귀속은 0B 원문과 축어 일치하고, 환경 단정은 전부 사라졌으며
**새 보장 주장 0건**이었다. **산출물 판정·수치·범위와 `O-2` 두 자리는 손댈 것이 없다.**
남은 셋은 **닫는 과정에서 새로 적은 문장**이다.

| # | 지운 문면 | 왜 거짓인가 | 남긴 문면 |
| --- | --- | --- | --- |
| **W-1** (medium) | 네 자리가 **`C-7`**로 부르던 「이 slice가 만들지 않은 것」 절 | **앞 커밋이 그 절을 `C-8`로 개명하고 그 번호에 새 절을 넣었다.** 네 문장은 **`a264d6f`에서 참이었고 HEAD에서 거짓이 됐다.** **이력이 낡은 것이 아니라 가리키는 이름이 거짓이 된 것**이며, **동결 관례는 본문에 적용되지 포인터에는 적용되지 않는다**(`H-2`가 세운 선례) | **네 자리의 이름을 `C-8`로 돌렸다** — `scope.md` 라운드 7 이력 셋(`M-1` 전수 목록·`M-3` 두 자리)과 `checklist.md` §14 `M-3` 행. **이력 본문은 고치지 않았고 이름만이다.** 이 행이 그 등재다 |
| **W-2** (medium) | `C-7` 기록 블록 · *"**고정점이다**"* · 머리글의 *"기록된 블록은 **전부** 뒤 커밋에 낡지 않는 형태다"* | 블록이 **HEAD 재실행과 달랐고**, 그 위에 **보장 주장 둘**이 서 있었다. **`C-7`은 검사 대상이 라운드마다 바뀌는 절**이라 **고정점이 아니다** — `N-1`·`V-3`(b)와 같은 형태의 보장 주장이다. 머리글의 전칭은 **이 블록으로 반증된다** | **보장을 지웠다** — *"**이 블록은 고정점이 아니다.** 쪼갠 것은 **자기참조 하나**이고 **검사 대상은 라운드마다 바뀐다** … **그때 다시 뜬다.** … **어느 상태에서도 같은 출력을 낸다는 주장이 아니다**"*. **머리글의 전칭을 갈랐다** — 낡지 않는 형태로 짠 블록을 열거하고 **`C-7`은 그 부류가 아님을 명시**했다. **블록은 `W-1`·`W-3` 수정 뒤에 다시 떴다** |
| **W-3** (low) | `scope.md` 절 머리 *"이 slice의 네 재발 형태를 **표로 막았다**"* | **자기 본문에 반박당한다** — 본문은 **네 형태 중 하나만** 기계로 막을 수 있다고 적는다 | **`checklist.md` §19와 같은 문면으로** 맞췄다 — **「「적은 대로 했는지」를 실행으로 확인했다」**. 본문도 *"네 형태 중 **기계로 막을 수 있는 것**"*으로 좁히고, **자기참조 하나를 없앤 것이지 매 라운드 같은 출력을 낸다는 뜻이 아니라고** 적었다 |

### `W-1`이 말하는 것 — 전수 스윕을 개명에는 돌리지 않았다

**`W-1`은 「전수로 맞췄다」를 적은 라운드가 낸 것이다.** 그 라운드의 스윕은 **명제**를
훑었고 **절 이름 개명**은 대상이 아니었다. **끊긴 포인터 하나가 들어 있는 문장이 바로
*"전수로 맞췄다 … 한 자리만 고치면 다음 라운드가 또 열린다"***였다.

**규칙을 더한다 — 절 이름·번호를 바꾸면 그 이름을 부르는 자리를 훑는다.**
**대조 명령과 출력은 `commands.md` C-7 (b)에 있다.**

> **⚠ 그 자리에 원래 *"이 검사가 `W-1`을 막는다"*류의 예방 주장이 있었고 **그 검사는
> `W-1`이 난 자리를 구조적으로 못 봤다**(verifier `Y-1`) — **`commands.md` 접두가 붙은
> 인용만** 봤고 `W-1`의 자리는 **전부 접두가 없다.**
> **수정 라운드 14가 축을 넓히고 능력을 실행으로 쟀다** — `W-1`이 살아 있던 커밋에서
> **그 자리를 잡고** 현재 HEAD 에서 **지목 0**이다. 두 실행은 C-7 (b)에 있다.
> **「전부 막는다」고는 적지 않는다** — 못 보는 것을 그 절의 표에 적었다.

### 이 라운드의 불변

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 5d2ca03..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **포인터가 실재하는 절을 가리키는가** | `commands.md` **C-7** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 21. 수정 라운드 14 — verifier `Y-1`

**`W-1`·`W-2`·`W-3`은 셋 다 실제로 닫혔다** — 포인터는 다섯 자리 전수로 이어졌고
`C-7` 블록은 HEAD에서 축어로 재현되며 출력 블록 전수 재실행에서 어긋나는 것이 0건이라는
판정을 받았다. **불변·`O-2`·in_scope 전부 통과.** 남은 하나는 **그 라운드가 새로 만든
검사의 능력을 재지 않고 적은 것**이다.

| | |
| --- | --- |
| **지운 문면** | 세 자리의 예방 주장 — *"**이 검사가 `W-1`을 막는다**"* 계열(`commands.md` C-7 판정 · `checklist.md` §20 · `scope.md` 라운드 13 이력) |
| **왜 거짓인가** | **그 검사는 `W-1`이 난 자리를 구조적으로 못 본다** — **``commands.md`` 접두가 붙은 인용만** 봤고 **`W-1`의 자리는 전부 접두가 없다.** **`W-1`이 살아 있던 커밋에서 돌리면 통과한다.** 형태는 **거짓 보장 주장**(`N-1`·`V-3`(b)·`W-2`)과 **측정하지 않은 것을 단정**(`Q-1`·`R-1`·`V-2`)의 합이다 — **검사를 만들면서 그 검사의 능력을 재지 않았다** |
| **남긴 문면** | **(가)를 택해 축을 넓혔다.** 접두를 가리지 않고 **모든 `C-N` 인용**을 보고, **번호가 절을 넘겨준 뒤에 낡은 인용**을 `git blame`으로 지목한다. **본문을 C-7에 인라인**했다(형태 4). 예방 주장 자리는 *"**이 검사는 `W-1`이 난 자리를 잡는다 — 그것을 실행으로 쟀다**. **「막는다」고는 적지 않는다**"*로 |

### 측정을 먼저 하고 문장을 그 다음에 썼다

**합격 기준이 실행이었다.** 두 실행의 명령과 출력은 `commands.md` **C-7 (b)**에 있다.

- **`5d2ca03`(= `W-1`이 살아 있던 커밋)에서** — 지목이 `scope.md` 세 줄과
  `checklist.md` 한 줄이고 **그 넷이 `W-1`이 지적한 자리 전부**다(`checklist.md` 쪽 한 줄에
  인용이 **둘** 있었다). **다른 지목은 없다.**
- **현재 HEAD 에서** — **지목 0.**

**처음 짠 축은 더 넓었고 그래서 좁혔다.** 「제목이 바뀐 번호」를 전부 지목하게 했더니
**HEAD 에서도 지목이 남았다** — **제목을 다듬은 것과 번호가 절을 넘겨준 것을 구분하지
못했기 때문**이다. **번호 이동**(옛 제목이 지금 **다른 번호**의 것)으로 좁히자 두 기준이
동시에 성립했다. **좁힌 근거를 그 절에 적었다** — **오탐이 는다는 이유로 좁힌 것이 아니다.**

### 못 보는 것을 적었다 — 새 보장 주장을 만들지 않았다

`commands.md` C-7 (b)에 표로 남겼다 — **제목만 다듬은 번호는 지목하지 않는다** ·
**`C-5` 면제**(절 머리가 없고 C-4 제목이 그 사실을 적는다) · **줄이 옮겨지기만 해도
`blame`이 갱신돼 낡음 판정을 빠져나간다** · **코드 블록 안의 이름도 같이 본다**(오탐 가능,
지금은 0건) · **다른 파일의 절 이름은 점이 붙어 걸리지 않는다**.
**「전부 막는다」고 적지 않았다.**

### 이 라운드의 불변

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat d2cced5..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **넓힌 검사가 `W-1`을 잡고 HEAD 에서 0인가** | `commands.md` **C-7 (b)** — 두 실행 |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| `regression-ledger.md` 무변경 | `commands.md` **C-6** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 22. 수정 라운드 15 — verifier `Z-1`(blocker)·`Z-2` — **삭제 라운드**

**`Y-1`이 요구한 것은 전부 됐다**는 판정을 받았다 — 넓힌 축이 `W-1`이 살아 있던 커밋에서
그 자리를 잡고 HEAD에서 0이며, 예방 주장은 세 자리 전수로 좁혀졌고 사각지대 서술도
실측으로 확인됐다. **불변도 출력 블록도 하나 안 깨졌다.**
**막은 것은 그 편집이 남긴 잔해**다. **`commands.md`의 교체 대상 구간에는 새 서술을
넣지 않았다** — 살아 있는 판정문이 이미 있어 필요가 없었다.
**`scope.md`·`checklist.md`에는 이력·판정 서술을 추가했다**(이 절과 `scope.md` 라운드 15
이력). **이력 절은 쓸 수밖에 없고 실제로 썼다.**

| # | 지운 것 | 왜 |
| --- | --- | --- |
| **Z-1** (blocker) | `commands.md`의 **옛 C-7 (b) 블록 잔해 한 구간** — 중복 절 머리 `## C-8.` 하나 · 고아가 된 명령·출력 쌍(그 **옛 좁은 검사**가 `Y-1`의 대상이었다) · **닫히지 않은 코드펜스** · 그 안의 옛 판정문 둘 | **앞 라운드가 C-7 구간을 새 것으로 갈아 끼우면서 옛 구간을 안 지웠다.** 펜스가 열린 채로 남아 **그 뒤 렌더가 깨졌고**, 판정문 둘이 **존재하지 않는 grep**을 가리켰다. **형태는 「한 것과 적은 것이 다름」의 변종** — 이번엔 문장이 아니라 **파일 구조**가 의도와 달라졌다 |
| **Z-2** (low) | §21 「왜 거짓인가」 셀의 *"**두 번째 grep 이** …"* | 그 검사가 이제 **스크립트 하나**라 **「두 번째 grep」이 가리키는 것이 없다.** 셀을 *"**그 검사는 ``commands.md`` 접두가 붙은 인용만 봤고 …**"*로 좁혔다 |

### 지우기 전에 대조했다 — 손실 없음

**삭제 구간의 명제 중 살아 있는 판정 구간(C-7의 판정 (a)/(b))에 없는 것이 둘 있었고,
둘 다 패키지 안 다른 자리에 살아 있다.**

| 삭제 구간에만 있던 문면 | 어디에 살아 있나 |
| --- | --- |
| *"이 검사는 `W-1`이 난 자리를 잡는다 — 그것을 **실행으로 쟀다**"* | **그 실질(두 실행의 결과)은 살아 있는 판정 (b)가 직접 낸다.** 문장 자체는 이 파일 §20·§21과 `scope.md` 라운드 13·14 이력에 있다 |
| *"**절 이름·번호를 바꾸면** 이 대조를 돌린다"* | 이 파일 **§20**과 `scope.md` **라운드 13 이력**에 규칙으로 있다 |

**나머지는 전부 살아 있는 구간에 있다** — (a) 판정 · `C-5` 면제 근거 · *"「전부 막는다」고
적지 않는다"*. **삭제 구간에만 있던 것은 옛 좁은 검사의 명령과 그 출력**이고, 그것은
**`Y-1`이 지운 대상**이라 남기면 안 된다.

**멈추지 않고 지운 판단이다** — 계약이 *"`727`~`752`에 없는 내용이 있으면 보고하고 멈춰라"*
였고, **위 둘은 그 구간에는 없으나 패키지에는 있어 손실이 아니라고 판단**했다.
**다르게 보면 되돌릴 수 있다.**

### 요구된 두 확인 — 명령과 출력

```
$ grep -c '^```' reports/evidence/m0/0a3/commands.md
18

$ grep -n '^## C-' reports/evidence/m0/0a3/commands.md
29:## C-1. legacy 인용 재확인 — 직접 열었다 (§10.1 형태 2)
91:## C-2. 범위 — in_scope 준수와 `capability-map.md` 변경의 위치
149:## C-3. X-1 프레이밍 — 두 축이 보는 범위
432:## C-4. A4 불변 — 정본을 산출물에서 직접 계산한다 (**C-5** 형태 7 확인을 같은 실행에 포함)
562:## C-6. F-4 — ledger 정본을 직접 열어 그 조건부 서술을 따랐다
603:## C-7. 「적은 대로 했는지」를 실행으로 확인한다 (verifier `V-1` · `W-1` · `Y-1`)
756:## C-8. 이 slice가 만들지 않은 것
```

**펜스가 짝이 맞고 열린 채 끝나는 블록이 없다.** **절 머리에 중복이 없다.**

### 절 머리 수

**`commands.md`만 하나 줄었다 — 중복이 빠진 것이고 정상이다.**
**`scope.md`·`checklist.md`는 늘었다** — 이 절과 `scope.md` 라운드 15 이력을 더했기
때문이다. **어느 파일이 얼마나 바뀌었는지는 `git diff --numstat 32f7a63..<head> --
reports/evidence/m0/0a3/`와 각 파일의 `grep -c '^#\{2,4\} '`가 낸다** —
**여기 옮겨 적지 않는다.**

> **⚠ 이 자리에 원래 *"`scope.md`·`checklist.md`는 **그대로다**"*가 적혀 있었고
> **실측과 어긋났다**(Codex 4차 `C`). 두 파일 다 늘었다.

### 이 라운드의 불변

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 32f7a63..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **펜스 짝 · 절 머리 중복 없음** | 위 두 명령 |
| **출력 블록이 여전히 전수 재현되는가** | `commands.md` C-3.4 · C-4 · C-7 의 명령을 다시 돌린다 |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| **`O-2` 자리가 살아 있는가** | `commands.md` **C-3.4** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 23. 수정 라운드 16 — Codex 4차 `A` (층 한 겹 아래)

verdict 정본: `reports/evidence/m0/0a3/codex-review-20260828T030325Z.json`(등재, append-only).
`reviewed_base` **`48151b9`** → `reviewed_head` **`6b99420`**.
**3차 두 건은 해소 판정을 받았고 `O-2` 재판정 자체도 타당하다고 인정됐다.**

| | |
| --- | --- |
| **지운 문면** | 남는 두 항목의 층 라벨 **`observed`**와 그 근거 — *"`(관찰된 회귀)`로 귀속"* · *"표 제목이 「관찰된 실패·회귀」"* |
| **왜 거짓인가** | **`data-extract.md` §1이 `observed`를 「기존 운영 데이터/로그에서 비식별화해 관찰한 실제 형태」, `legacy-behavior`를 「기존 Python이 낸 결과」로 가른다.** `capability-map.md` §0도 **기존 Python의 출력·docstring 수치를 `legacy-behavior`**라 명시한다. **0B `R-BASIS-06`은 그 `~10%`를 「legacy의 서술」로 명시 판정**하고 **그 값이 legacy commit 본문의 인용**임을 적는다. **문구(`(관찰된 회귀)`·표 제목)를 층 판정 근거로 쓴 것**이 오류다 — **층은 「그 값이 어디서 왔는가」로 정해진다.** legacy 결과를 운영 관측과 같은 층으로 기록하면 **0C 인계의 provenance가 왜곡된다** |
| **남긴 문면** | **두 항목을 `legacy-behavior`로 바로잡았다.** §6 투찰률 — *"값의 출처가 **legacy 자신의 서술**이다. `(관찰된 회귀)`는 **legacy 저장소가 자기 회귀를 그렇게 부른 것**이고 운영 데이터·로그 관측이 아니다"*. §7 SET F-4 — *"**0B ledger `R-BASIS-06`이 같은 `~10%`를 「legacy의 서술」로 명시 판정**한다 … 표 제목의 「관찰된」은 **legacy 저장소가 자기 실패를 부른 말**이지 층이 아니다"*. **판정 축 서술에 층 판정 기준을 명시**했다 — *"층은 「그 값이 어디서 왔는가」로 정한다 — 문구가 아니다"* |

**바뀐 것은 층 라벨과 그 근거 서술뿐이다** — **두 항목이 `O-2`에 남는다는 판정과
`OPEN-REG-05` 유보 필요성은 그대로**다. 요약에 **`legacy-behavior`는 정답 지위가 없어
그 값으로 V2 계약을 세우려면 그 `OPEN`의 지목이 더 필요하다**는 것을 덧붙였다.

**0B `F-c` 기준의 문면도 맞췄다** — *"`observed`로 귀속하는 것만으로는"* →
*"**legacy 서술로 귀속하는 것만으로는**"*. 세 자리 전수(`commands.md`·이 파일·`scope.md`).

**이것은 3차 finding의 한 겹 아래다** — 3차는 **`authoritative` 대 나머지**를 갈랐고,
이번은 **`observed` 대 `legacy-behavior`**다.

### 동기화한 자리

`commands.md` **C-3.4 판정 축·판정 표 두 행·요약·`F-c` 기준** · `scope.md` **`O-2` 행과
`F-c` 기준** · `checklist.md` **§6 `O-2` bullet과 `F-c` 기준**.
**동결 이력은 본문을 고치지 않았다** — 남은 `` `observed` `` 인용 셋은 **층의 정의**이거나
**세 층 구분을 가리키는 말**이라 그대로다.

### 이 라운드의 불변

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat 6b99420..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **`O-2`에 남는 두 항목이 그대로인가** | `commands.md` **C-3.4 판정 표** — 층 라벨만 바뀐다 |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 24. 수정 라운드 17 — Codex 4차 `B` (스크립트 인벤토리)

| | |
| --- | --- |
| **지운 문면** | 머리글의 *"이 패키지가 쓰는 스크립트 — **둘**이고"* · *"**둘 다** 본문을 …"* · C-8의 *"이 패키지에는 **스크립트가 둘** 있고 **둘 다** 근거가 있다"* |
| **왜 거짓인가** | **`python` 코드펜스가 셋**이다 — **C-7 포인터 대조**가 `32f7a63`에서 추가됐고 `5d2ca03`과 HEAD에서 실제로 실행된다. **Codex 2차 `M-3`에서 고친 인벤토리가 후속 변경으로 다시 거짓이 됐다** |
| **남긴 문면** | **표에 C-7 (b) 행을 더하고**(위치·목적·**도입 근거 `W-1`/`Y-1`**), **셈을 지웠다.** 머리글에 **대조 명령**을 두었다 — *"수를 산문에 옮겨 적지 않는다. 이 표가 몇 줄인지는 **아래 명령이 낸다** — 표는 **그 출력과 1:1로 대조되는 것**이고 스크립트가 늘면 **행을 더한다**"* + `grep -n '^\`\`\`python' …`. C-8 행도 **셈 없이** *"이 패키지의 스크립트는 **전부** 그 순서를 지켰다"* + 셋의 도입 근거로 |

### 이 부류가 다시 낡지 않게 했다

**목록을 산문에 옮겨 적었기 때문에 스크립트가 늘 때마다 거짓이 됐다** — 같은 자리에서
**두 번**(`M-3`·`B`). **0B가 세운 「수를 지우고 블록을 가리켜라」를 적용**했다.
**표 자체는 남겼다** — **각 스크립트의 목적·도입 근거는 명령이 못 낸다.**

### 인벤토리 대조 — 명령과 출력

```
$ grep -n '^```python' reports/evidence/m0/0a3/commands.md
408:```python
459:```python
640:```python
```

**세 블록**이고 **표도 세 행**이다 — 불변 계산기(C-4) · X-1 프레이밍 후보 스윕(C-3.4) ·
포인터 대조(C-7 (b)).

### 이 라운드의 불변

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `git diff --stat aabde68..<head> -- docs/discovery/capability-map.md reports/evidence/m0/0a2/` |
| **인벤토리 표 ↔ `python` 블록** | 위 명령과 머리글의 표 |
| 출력 블록이 여전히 재현되는가 | `commands.md` C-3.4 · C-4 · C-7 |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**

---

## 25. 수정 라운드 18 — Codex 4차 `C` (라운드 15 자기 기록)

| | |
| --- | --- |
| **지운 문면** | §22의 *"이 라운드는 지우는 라운드다 — **새 서술을 쓰지 않았다**"*(무조건) · *"`scope.md`·`checklist.md`는 **그대로다**"* · `scope.md` 라운드 15 이력의 절 머리 *"**새 서술을 쓰지 않았다**"*와 불변 표의 절 머리 서술 |
| **왜 거짓인가** | **실측이 `checklist.md` +69/-1 · `scope.md` +57/-2**이고 **절 머리도 `scope.md` 94→100 · `checklist.md` 64→69**로 늘었다. **§22와 라운드 15 이력 등 새 판단·표·명령 기록이 대량 추가됐다.** `commands.md`에서 옛 구간을 지웠다는 **대응 자체는 맞고**, 틀린 것은 **라운드 전체의 자기 기록**이다 |
| **남긴 문면** | **주장을 `commands.md`의 교체 대상 구간에 한정**했다 — *"**`commands.md`의 교체 대상 구간에는 새 서술을 넣지 않았다** … **`scope.md`·`checklist.md`에는 이력·판정 서술을 추가했다** … **이력 절은 쓸 수밖에 없고 실제로 썼다**"*. 절 머리 서술도 **두 파일은 늘고 `commands.md`만 줄었다**로 고치고 **수는 `git diff --numstat`와 `grep -c`가 낸다**로 돌렸다. **두 자리에 ⚠ 표시**를 달았다 |

### 이 오류의 출처

**팀 리드 지시문의 *"새 서술을 쓰지 마라 — 이것은 삭제 라운드다"*를 기록에 그대로
옮긴 것**이다. **지시의 뜻은 「`commands.md`의 교체 대상 구간에 새 서술을 넣지 마라」**였고
**이력 절은 쓸 수밖에 없으며 실제로 썼다.** **지시 문구를 자기 기록으로 옮기면 그 범위가
사라진다** — `B`와 같은 계열(앞의 참인 기록이 뒤에 거짓이 됨)이다.

### 고친 뒤 무엇이 낡았는지 훑었다

**요구받은 확인이다.** 명령과 출력은 `commands.md` **C-9**에 있다 —
**인벤토리 ↔ 실물** · **산문에 살아 있는 셈** · **라운드 15 실측** · **이 라운드의 산출물
무변경**. 판정도 그 절에 있다.

**그 절을 짜면서 두 가지를 고쳤다** — ① **`commands.md`를 셈 스윕 대상에서 뺐다**:
그 절의 **출력이 자기 패턴에 걸려** 쓸 때마다 매치가 늘었다(C-7이 마커를 쪼갠 것과 같은
이유). ② **파일 간 출력 순서를 `sort`로 고정**했다 — 두 번 돌려 **순서가 달랐다**
(Codex 3차 `R-2`와 같은 부류). **두 검사 다 두 pass 수렴을 확인했다.**

### 이 라운드의 불변

| 확인할 것 | 명령 |
| --- | --- |
| 산출물 셋이 이 라운드에 무변경 | `commands.md` **C-9 (d)** |
| **인벤토리 ↔ `python` 블록** | `commands.md` **C-9 (a)** |
| **산문에 살아 있는 셈** | `commands.md` **C-9 (b)** |
| 활성 OPEN · capability · 분류 4종 | `commands.md` **C-4** |
| in_scope 밖 경로 · 공백 오류 | `commands.md` **C-2**의 첫 표 |

**결과를 여기 적지 않는다.**
