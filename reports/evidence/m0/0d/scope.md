# Slice 계약 — M0 / 0D ADR

```yaml
milestone: m0
slice: 0d-adr
base_sha: 998dc21754e39efa425c3cfad9d5b7d5540ad1ba   # 0A3 종료 + 하네스 커밋 직후의 HEAD
head_sha: d96d38b   # 산출물이 고정된 커밋 = commands.md 의 선언 SHA. 아래 절 참조
in_scope:
  - docs/adr/                       # 신설. ADR 0001~0009
  - reports/evidence/m0/0d/         # 이 패키지
out_of_scope:
  - docs/discovery/capability-map.md      # 0A/0A2/0A3 Codex approve로 확정 — 읽기 전용
  - docs/discovery/regression-ledger.md   # 0B Codex approve로 확정 — 읽기 전용
  - reports/evidence/m0/0a/ · 0a2/ · 0a3/ · 0b/   # 확정된 evidence — 읽기 전용
  - docs/discovery/data-dictionary.md     # 0C 소관. 이 slice는 만들지 않는다
  - docs/discovery/legacy-reference-map.md # 만들지 않는다 — 운영자 결정 (d)가 이 파일을 쓰지 않는다
  - fixtures/                              # fixture-curator 소유
  - 활성 OPEN 50건 전부 — 해소하지 않는다
      # capability-map.md §12 활성 45건 + regression-ledger.md §9 OPEN-REG 5건.
      # 예외는 OPEN-OPS-07 하나이고, 그것도 「ADR 대안 절 기입」이라는 종료 조건으로만 닫는다.
  - Kotlin/Spring/Python 애플리케이션 코드   # M0은 문서 slice다
  - .claude/ 하네스
  - bid-vector/ symlink 아래 기존 저장소 수정 (읽기 전용)
acceptance_commands:
  - "N/A — 문서 slice. 아래 A1~A8을 checklist.md로 대조"
rollback: "N/A — 문서 산출물은 git revert로 복구. 애플리케이션 코드·설정·스키마 변경이 없다"
```

작성: 2026-08-28, spec-writer (v2-slice-pipeline).

---

## 이 slice가 만드는 것

`milestone-0.md` §"Slice 0D"가 **최소 9건의 결정**을 요구한다. 그 9건을 ADR 파일 9개로
쓴다. `milestone-0.md` 「산출물」 블록이 이름을 지정한 **네 파일은 그 이름과 번호를
유지**한다.

| ADR | 파일 | `milestone-0.md` §"Slice 0D" 결정 |
| --- | --- | --- |
| 0001 | `docs/adr/0001-target-architecture.md` | 1 — Kotlin modular application + Python ML engine, service 재작성 / ML 재활용 경계 |
| 0002 | `docs/adr/0002-money-rate-basis.md` | 3 — 금액/rate/basis 표현 |
| 0003 | `docs/adr/0003-contract-transport.md` | 4 — gRPC/Protobuf 내부 계약 |
| 0004 | `docs/adr/0004-persistence-and-events.md` | 5 — DB 및 migration 도구 |
| 0005 | `docs/adr/0005-domain-events-and-outbox.md` | 6 — domain event / outbox / notification 방식 |
| 0006 | `docs/adr/0006-gradle-modules.md` | 2 — Gradle module과 의존 방향 |
| 0007 | `docs/adr/0007-test-pyramid-and-ratchet.md` | 7 — 테스트 pyramid와 mutation 대상 |
| 0008 | `docs/adr/0008-frontend-disposition.md` | 8 — React UI 재사용/재작성/후속 여부 |
| 0009 | `docs/adr/0009-ml-reuse-provenance.md` | 9 — ML 재활용 출처 기록 위치 (**결정** — 운영자 2026-08-29) |

**ADR 번호가 결정 번호와 어긋난다.** ADR 번호는 부여 순서이고 결정 번호는
`milestone-0.md`의 열거 순서다. 위 표가 둘의 대응이며, 각 ADR 머리에도 같은 대응이 있다.

**`0004-persistence-and-events.md`의 이름과 내용**: 이름은 `milestone-0.md` 「산출물」이
지정한 것이고 "events"를 포함하지만, **같은 문서의 결정 목록은 5(DB·migration)와
6(event/outbox/notification)을 별개 결정으로 센다**. 최소 9건을 만족하려면 둘을 한 파일에
합칠 수 없다. 지정된 이름을 바꾸지 않고 **0004는 결정 5를, 0005는 결정 6을** 담으며
0004가 그 자리에서 0005를 가리킨다.

---

## `head_sha`와 range

`head_sha`의 정본은 이 파일 머리의 yaml 하나다 — 값을 두 곳에 두지 않는다.

이 파일은 **자기 커밋의 diff를 서술하지 않는다.**

**`head_sha`는 산출물이 고정된 커밋이며 `commands.md`의 선언 SHA와 같은 값이다.**
**커밋 관계로 정의하지 않는다** — `520bc6c` 이전에 `직전 커밋`으로 적었다가 두 번 낡았다
(뒤 편집이 들어올 때 한 번, 0C 레인이 사이에 커밋했을 때 한 번). **관계는 낡고 SHA는
낡지 않는다.**

그래서 **이 파일을 다시 쓰는 커밋은 이 값을 옮기지 않는다** — 그 커밋이 바꾸는 것은
이력과 규약이지 산출물이 아니다. **어느 출력 블록이 그 SHA의 트리에서 다시 뜨고 어느
것이 빠지는가는 이 파일이 정하지 않는다** — 그 규약과 예외의 **정본은 `commands.md`의
「선언 SHA와 실행 계약」 절**이고, 무엇이 어느 사유로 빠졌는지는 **C-11** 출력이 쌍마다
낸다. **값이 옮겨지는 것은 래칫 대상 블록을 다시 뜰 때뿐이다.**

**이 브랜치는 다른 slice와 공유된다.** 그래서 `in_scope` 판정은 범위가 아니라
**이 slice의 커밋이 건드린 경로**로 잰다 — 선별자는 커밋 제목의 `m0-0d` 태그이고
`commands.md` **C-2**가 그 선별의 크기를 함께 낸다.

Codex 리뷰의 `reviewed_head`는 **리뷰 요청 시점의 HEAD**이며 이 값과 다르다 —
그것이 리뷰 range의 head다.

---

## acceptance — A1~A8

| # | 요구 | 출처 | 대조 |
| --- | --- | --- | --- |
| **A1** | ADR 9건이 `docs/adr/`에 있고, 각 파일이 `상태`·`맥락`·`결정`·`대안`·`결과` 절을 갖는다. 번호는 유일하고 연속이다 | `milestone-0.md` §"Slice 0D", agent-workflow §7 | `commands.md` C-3 |
| **A2** | **OPS-21 표의 후보 전부**가 어느 ADR의 「대안」 절 표에 **판정 한 칸과 사유**를 갖고 등장한다. 판정에는 `채택`·`불채택`·`조건부`뿐 아니라 **`보류`(미조사·확인 미완)도 있으며 그 사실이 드러나야 한다** | `capability-map.md` §12 G6 `OPEN-OPS-07` 행 | `commands.md` C-7 · C-7n |
| **A3** | 판정 기준이 "최신 버전"이 아니라 **M1에서 고정할 Kotlin 2.x + Spring Boot 3.x 조합과의 호환**이다 | `v2-지침서.md` §5, `decisions.md` `OPEN-OPS-07` | `checklist.md` §2 |
| **A4** | **활성 OPEN을 해소하지 않았다.** `capability-map.md`·`regression-ledger.md`가 무변경이고, ADR이 활성 OPEN이 소유한 쟁점을 확정 서술로 선점하지 않는다 | 팀 리드 지시, `0a2/checklist.md` §10.1 형태 7 | `commands.md` C-4 · `checklist.md` §3 |
| **A5** | 이미 확정된 운영자 결정(`OPEN-ML-01` · `OPEN-OPS-05` · 두 갈래 전략)을 **재확정하지 않고 인용**한다 | `reports/evidence/m0/0a2/decisions.md` | `commands.md` C-6 · `checklist.md` §4 |
| **A6** | ML 재활용 래칫 사전 조사가 수행됐고, 그 수치가 **명령으로 재현**된다. 미달 모듈의 M5 판단 재료가 남는다 | `milestone-0.md` §"추가 조사 항목" | `commands.md` C-5 |
| **A7** | ADR이 인용한 legacy 경로·행 범위가 `ed4b06c`에 실재한다 | `0b`·`0a3`가 세운 인용 규약 | `commands.md` C-6 |
| **A8** | `in_scope` 밖 경로 변경이 없고, 공백 오류·secret이 없다 | agent-workflow §6 | `commands.md` C-2 · C-8 |

---

## `OPEN-OPS-07`과 이 slice — 「기입」은 했고 「전건 판정」은 아니다

`capability-map.md` §12 G6가 이 항목의 성격을 적는다 — *"이 항목은 결정이 아니라
**실행으로 닫힌다**"*, 종료 조건은 **ADR 대안 절 기입**이며 *"ADR 작성은 **0D 소관**"*이다.
조사는 `_workspace/m0-open-decisions/ops07-library-survey.md`로 완료돼 있다.

**조사 노트를 옮겨 적지 않았다.** `_workspace/`는 gitignore 대상이라 evidence가 아니므로,
ADR에는 **그 ADR의 판정을 떠받치는 사실만** 출처와 함께 옮기고 나머지는 남기지 않았다.
노트가 `확인 불가`로 적은 것은 ADR에서도 **확인하지 않았다**고 적는다.

**후보가 어느 ADR로 갈렸는가**:

| OPS-21 「필요」 행 | 후보 | 판정을 적은 ADR |
| --- | --- | --- |
| transactional outbox / 스케줄러 | Spring Modulith 이벤트 외부화 · JobRunr · db-scheduler | **0005** |
| advisory lock 추상화 | Spring Integration JDBC lock registry · ShedLock | **0005** |
| 재시도·backoff·circuit breaker·rate limiter | Resilience4j | **0005** |
| 아키텍처 규칙 강제 | ArchUnit · Konsist · Detekt | **0007** |
| 관측 | Micrometer | **0005** |

C-7이 이 대응을 스캐너로 확인한다 — 후보마다 그 ADR의 「대안」 절 **표의 판정 열**을
읽는다.

### 종료 조건에 대한 이 slice의 판정

**종료 조건의 문면은 「ADR 대안 절 기입」이다**(`capability-map.md` §12 G6). 조사 노트의
결과가 전부 ADR에 기입됐고 **조사 범위 밖이었던 후보는 「미조사」로 기입**됐다.

**그러나 「후보 전건 채택/불채택 판정 완료」는 성립하지 않는다** — C-7이 `DEFERRED`를
낸다(수는 그 블록의 `candidates=` 줄이 낸다). Codex 1차 리뷰(high #2)가
`Spring Integration JDBC lock registry`를 지목하며 **`OPS-01`을 덮는 수단이 정해지지
않았다**고 적었고, 이 slice는 그 지적을 받아 **ShedLock을 판정 보류로 되돌리고**
`OPEN-ADR-12`를 신설했다. **Codex 2차 리뷰(medium #3)로 ShedLock은 다시 판정됐다** —
요구 ② 불충족(공식 README 확인)으로 **OPS-01 축에서 탈락**이며, 남는 `DEFERRED`는
**조사 자체가 없는** `Spring Integration JDBC lock registry`다(ADR 0005 §3.2 · §5).

**따라서 이 slice는 `OPEN-OPS-07`의 종료를 스스로 선언하지 않는다.** 기입은 됐고,
**「전건 판정」이 종료 조건에 포함되는지는 운영자와 Codex의 판정에 남긴다.** `ba2e4f9` 이전에
A2에 적었던 *"채택/불채택 판정"*은 **실제보다 강한 주장**이었고 그 커밋이 고쳤다.

---

## 이 slice가 새로 등록하는 `OPEN`

**근거가 없는 자리를 확정 서술로 채우는 대신 등록한다** — `milestone-0.md`가
*"모호한 항목은 기존 Python 구현을 정답으로 채우지 말고 `OPEN`으로 남겨 사용자 결정을
요청한다"*고 요구하고, `0a2` §12.1이 *"신설은 임의 해소의 반대 방향이며 A2 위반이
아니다"*를 세웠다.

**아래 표가 전수다. 수를 이 자리에 옮겨 적지 않는다** — 표가 세고, 등록된 ID 전수는
`commands.md` **C-9.4**가 낸다.

**등록 위치는 ADR 파일 안이며 `capability-map.md` §12가 아니다** — 그 파일은
`out_of_scope`(Codex approve로 확정)라 이 slice가 쓰지 못한다.
`regression-ledger.md`의 `OPEN-REG-01`~`05`가 같은 형태로 자기 문서에만 있고
*"`capability-map.md` §12와의 통합이 필요하며"*라 적은 선례를 따른다.
**§12 통합은 후속 slice의 일이다.**

| ID | 질문 | ADR | 성격 |
| --- | --- | --- | --- |
| `OPEN-ADR-01` | Spring Boot 세대 — OSS 지원이 끝난 3.5로 고정 / 4.x 이동 / 상용 지원 전제 중 무엇인가 | 0001 | M1 버전 고정이 소유. 조사가 `판정 보류`로 남긴 것 |
| `OPEN-ADR-02` | `v2-지침서.md` §2.1의 ML 앵커에 `app/domain/`을 추가하는가 | 0001 | 8개 커널 전부가 §2.1 앵커 밖이다 |
| `OPEN-ADR-03` | `v2-지침서.md` §3.2의 "업무 판정"이 **ML 승격 게이트 판정**을 포함하는가 | 0001 | 포함이면 `ml_release/gate.py`가 이식 대상에서 빠진다 |
| `OPEN-ADR-04` | `predictors/`의 규칙표 4파일이 **수학 커널**인가 **정책 데이터**인가 | 0001 | 후자면 Kotlin `decision`의 정책 테이블로 간다 |
| `OPEN-ADR-05` | `award_landing_ladder.py`의 업무 술어 호출이 §3.2 위반인가 학습 코퍼스 필터인가 | 0001 | 같은 술어가 두 목적에 쓰인다 |
| `OPEN-ADR-06` | 래칫 축에 **클래스/타입 크기**를 추가하는가 | 0007 | 추가하지 않으면 mixin 분해가 파일 래칫의 우회로로 남는다 |
| `OPEN-ADR-07` | mutation 도구 — Kotlin에서 무엇을 쓰는가 | 0007 | `OPEN-OPS-07` 조사 범위 밖. 미조사 |
| `OPEN-ADR-08` | Detekt 버전 경로 — 안정판 / alpha / 정식 대기 중 무엇인가 | 0007 | M1이 고정할 Kotlin 버전에 종속. 조사가 `판정 보류` |
| `OPEN-ADR-09` | V2 기간 중 운영자는 **어느 화면**으로 시스템을 쓰는가 | 0008 | `capability-map.md`에 UI 축이 없다 |
| ~~`OPEN-ADR-10`~~ | **ML 재활용 출처 기록 위치** — **해소**(운영자 결정 2026-08-29: **(d) 조합**). ADR 0009 §5가 결정 근거를 적는다 | 0009 | 운영자 결정 완료 |
| `OPEN-ADR-12` | **OPS-01의 lease 어댑터** — 홀더 강제 종료 시 즉시 해제를 무엇이 만족하는가 | 0005 | Codex 1차 high #2로 신설. `Spring Integration JDBC lock registry` 조사가 선행 |
| `OPEN-ADR-13` | **db-scheduler 실패 처리 기본값** — 부작용 유형별 `onFailure`/`onDeadExecution` | 0005 | Codex 1차 medium으로 신설. 기본값을 이 slice가 확인하지 않았다 |
| `OPEN-ADR-11` | ML 호출의 **동기 RPC 예산** — deadline·재시도·fail-open 금지의 구체값 | 0003 | M2가 소유. 값의 근거가 아직 없다 |

**`OPEN-ADR-10`은 해소됐고**(운영자 결정 2026-08-29) 나머지가 활성이다 —
셈은 `checklist.md` §6이 낸다.
**`OPEN-ADR-09`가 운영자 결정을 직접 요청하는 유일한 항목**으로 남았고 나머지는 후속
마일스톤이 실행·측정으로 닫는다. **`OPEN-ADR-12`는 M1/M4를, `OPEN-ADR-13`은 M4를**
차단한다.

---

## 알려진 제한

1. **Codex 리뷰 레인은 legacy 인용을 재현하지 못한다** — 리뷰어 worktree에 `bid-vector`가
   없다. C-5·C-6의 검증은 이 레인과 verifier가 진다. 0A3 §7과 같은 상태다.
2. **`OPEN-OPS-07` 조사의 미확인 7건(U-1~U-7)을 이 slice가 해소하지 않았다.** 각 ADR의
   「확인하지 않은 것」 절이 그대로 옮긴다. 특히 `Kotlin 버전 호환 공식 서술 부재`(U-1)는
   **"호환된다"가 아니라 "그 축의 서술이 없다"**로만 쓴다.
3. **`Spring Integration JDBC lock registry`는 조사되지 않았다.** OPS-21 표의 후보인데
   조사 노트의 대상 9종에 없다. ADR 0005가 그 사실을 적고 후보로 남긴다 — **불채택이
   아니라 미조사다.**
4. **래칫 측정은 크기 축만 재고 결합도 축을 재지 않았다.** `v2-지침서.md` §5는 fan-in/out,
   public API 수, 순환 의존, duplicate helper를 함께 재라 요구한다. 이 slice는 파일·함수
   줄 수와 import 결합만 실측했다. 나머지 축은 M1 래칫 구현이 소유한다.
5. **수정 라운드 1의 두 발견은 같은 뿌리를 갖는다** — `F-1`은 **조사 노트의 오지목을
   정본 대조 없이 옮긴 것**이고, `F-4`는 **활성 registry와 대조하지 않고 하류 서술을 옮긴
   것**이다. 둘 다 *"옮긴 곳이 맞는지 정본에서 확인"*이 빠진 자리이며, 그 확인을
   `commands.md` **C-9.6**·**C-10**이 이제 명령으로 낸다. **C-9.6은 인접성을 판정하지
   못한다** — 목록만 내고 읽는 것은 사람이다.
6. **`commands.md`의 출력은 「선언 SHA 트리에서 다시 뜬다」까지만 보장되고, 그 보장은
   래칫 대상 블록에 대한 것이다.** C-11이 그것을 명령/출력 쌍에 대해 실행으로 재고,
   **무엇을 대상에서 뺄지도 스스로 정한다** — 자기 자신(`SELF`)과 **선언 SHA 트리 밖의
   상태를 읽는 블록**(`EXT`). 전체 쌍 수·대조한 수·사유별로 빠진 수는 그 출력의 마지막
   줄이 낸다. **C-11이 재지 않는 것**: `checklist.md`·이 파일의 인라인 블록, 각 블록
   아래 **판정 산문의 참·거짓**, C-11 자신, 그리고 `EXT`로 빠지는 블록(C-12).
   **`diff=0`은 「기록이 옳다」가 아니라 「래칫 대상 기록이 그 트리에서 다시 뜬다」이다.**
   **`EXT`로 빠진 기록은 시점 기록이고 `taken_at`으로 자기 시점을 낸다** — 트리를
   고정해도 답이 고정되지 않는 값을 재현되는 것으로 적으면 다음 라운드에 반드시
   어긋난다. 이 slice가 실제로 그렇게 어긋났다(아래 「갱신 이력」의 독립 검증 `H-1`).
   **`EXT` 판정 자신에 구조적 한계가 있다** — 축(트리를 고정하면 답도 고정되는가)을
   토큰으로 근사하므로 **저장소 밖 상태를 그 토큰 없이 읽는 블록은 대조 대상으로 남는다.**
   **토큰 목록과 그 한계의 정본 문면은 `commands.md` `C-11` 절의 `EXT` 표와 그 아래
   문단이다** — 여기서 다시 적지 않는다. **이 slice에 그런 블록이 없다는 것**은 검증 레인의
   무력화 probe와 쌍 전수 probe가 확인한 사실일 뿐 **구조가 막는 것이 아니다.**
7. **선언 SHA를 올리면 그 트리에서 래칫 대상 블록을 전부 다시 떠야 한다.** 라운드 2의
   두 medium이 그 규칙을 어긴 자리에서 났다 — 일부만 다시 뜨면 **같은 파일 안의 두
   블록이 서로 어긋난다**(C-9.3 ↔ C-9.6). C-11이 이제 그 어긋남을 한 번에 낸다.
   **`EXT`로 빠지는 블록은 선언 SHA를 따라 움직이지 않는다** — 그 값은 트리가 아니라
   시점의 함수다.
   **6·7이 말하는 규약의 정본은 `commands.md`의 「선언 SHA와 실행 계약」 절이다** —
   여기 적는 것은 그 규약이 아니라 **그 규약이 이 slice에 남긴 한계와 그것을 어긴
   자리**다. 문면이 갈라지면 그 절과 C-11 출력이 이긴다.
8. **`legacy-reference-map.md`를 만들지 않는다** — **운영자 결정 (d)가 그 파일을 쓰지
   않는다**(ADR 0009 D-4·D-5). **`milestone-0.md` 「산출물」이 그 경로를 적고 있어 개정이
   필요하며**, 그 문서는 `out_of_scope`라 이 slice가 고치지 않고 **사실만 적었다.**
9. **`OPS-01`을 덮는 lease 어댑터가 정해지지 않았다**(`OPEN-ADR-12`). Codex 1차 high #2가
   적출했고, 그때의 ShedLock 불채택 사유는 **예약 작업의 단일 실행**만 보고
   **use case 경계의 lease**를 보지 않았다. **후보 어느 것도 OPS-01의 요구 셋을
   덮는다고 확인되지 않았다** — `Spring Integration JDBC lock registry`는 조사 자체가
   없고, `db-scheduler`·`PostgreSQL 세션 advisory lock`은 요구 ②(홀더 종료 시 즉시
   해제)가 각각 미확인·미조사이며, **`ShedLock`은 요구 ② 불충족이 공식 README로
   확인돼 이 축에서 탈락했다**(Codex 2차 medium #3, ADR 0005 §3.2).
10. **채택 도구의 실패 처리 기본값을 확인하지 않았다**(`OPEN-ADR-13`). Codex가 지목한
   db-scheduler 기본값을 이 slice가 공식 문서로 열어 보지 않았다. **결정(D-11)은
   기본값과 무관하게 성립하나, 그 기본값이 결정과 충돌하는지는 M4가 확인한다.**
11. **커밋 스테이징이 다른 slice와 공유된다.** `af637fc`를 만들 때 0C 레인이 스테이징해
   둔 파일 셋이 `git add docs/adr` 뒤의 `git commit`에 딸려 들어갔고(폐기된 `a9509c6`),
   `git reset --soft` + **`git commit -- <경로>`**로 갈라 다시 커밋했다.
   **이 slice의 커밋은 전부 경로 pathspec으로 한다** — `git add` 뒤의 무인자
   `git commit`은 **인덱스 전체**를 담으므로 공유 트리에서 안전하지 않다.
   **`git commit --amend`도 쓰지 않는다** — 이 브랜치에서 HEAD는 다른 레인의 커밋일 수
   있다. `G-1`~`G-6` 라운드에서 재취득 커밋의 메시지를 고치려 한 `--amend`가 그 사이에
   들어온 0C 레인 커밋(`10eb819`)을 대상으로 잡아 **그 커밋의 메시지를 0D 것으로 덮었다.**
   「덮었다」는 **ref 교체**다 — git 객체는 불변이라 `10eb819` 자신은 변형되지 않았고,
   **메시지만 다른 새 객체가 생겨 HEAD가 그리로 옮겨간 것**이다.
   트리는 바뀌지 않았고 `git reset --hard 10eb819`로 되돌렸다 — 그 커밋의 SHA·트리·메시지가
   복구 전후로 같다. **커밋 메시지의 잘못은 고쳐 쓰지 않고 아래 이력 표의 다음 행이 정정한다.**
   그 대가로 **커밋 메시지만 읽는 독자에게는 그 잘못이 그대로 남는다** — 정정은 이 파일에만
   있다.
   **복구에 쓴 `git reset --hard`도 이 금지 목록에 든다.** `--amend`·무인자 `git commit`과
   같은 축이다 — 공유 트리에서 HEAD·인덱스·작업 트리를 통째로 다루므로 **다른 레인의
   커밋되지 않은 편집을 조용히 지운다.** **이 slice는 그것을 실제로 썼다**(위 복구).
   그리고 **그때 무엇을 잃었는지를 git은 증명하지 못한다** — 남는 것은 정황뿐이고
   (덮인 커밋과 원래 커밋의 트리가 같았다는 것, 곧이어 0C 레인이 정상 커밋했다는 것),
   「아무것도 잃지 않았다」는 git이 낼 수 있는 명제가 아니다. **그 명제는 0C 레인 소관이다.**
   금지를 세우면서 이 slice가 그것을 쓴 사실을 지우지 않는다.

---

## 갱신 이력

**라운드 이력은 이 파일 하나에 모은다.** `checklist.md`·`commands.md`에는 라운드
이력·자기평가 산문을 넣지 않는다 — 0A3이 스물두 라운드를 태운 뒤 후속에 남긴 권고다
(`0a3/checklist.md` §30).

| 라운드 | 내용 |
| --- | --- |
| 최초 | ADR 0001~0009 신설, `OPEN-ADR-01`~`11` 등록, evidence 패키지 작성 |
| `446aba5` | **secret 스캔(C-8)이 잡은 것을 고쳤다** — ADR 0004가 legacy 기본 `DATABASE_URL` 원문을 인용했고 그 문자열이 자격증명 형태(`user:password@host`)다. `agent-workflow.md` §6이 생성물에 남기지 못하게 하는 부류라 **값을 지우고 경로·행과 확인 명령을 가리키게** 바꿨다. 주장은 그대로다 |
| `c731399` | **ADR의 근거 포인터를 그 수를 실제로 내는 블록으로 맞췄다.** 세 자리가 존재하지 않는 출력을 가리켰다 — 0001 §4.2가 C-5.2에 「분해/allowlist 양쪽 재료」가 있다고 적었으나 그 블록은 함수 전수와 결합 축만 낸다. 그 주장을 지우고 이 ADR이 파일별 판정을 하지 않는다는 것을 적었다. mixin 크기와 `OPEN` 후보 실측은 `commands.md` **C-5.2d**·**C-5.2c**를 신설해 그리로 보냈다 |
| **수정 라운드 1** — verifier `not-ready`(medium 4 · low 4) | |
| `fb79029` | **F-1 · F-4 · F-6.** ADR 0005가 `OPS-13`을 「재시도·서킷브레이커」로 두 자리에서 잘못 지목했다 — 정본(`capability-map.md`)의 `OPS-13`은 **「설계 래칫」**이고 OPS-21 재시도 행의 대응은 `OPS-08 (a)(c)`·`COL-03`뿐이다. **조사 노트가 같은 오지목을 갖고 있었고 그것을 옮긴 것**이며, 정본이 이긴다. ADR 0001 §4.1이 활성 **`OPEN-ML-06`**의 쟁점을 확정 서술로 선점한 것(**계열 A**)은 **해소하지 않고 관계를 적었다.** 같은 부류를 전수로 훑어 인접 넷(`OPEN-SET-06`·`OPEN-OPS-02`·`OPEN-OPS-08`·`OPEN-ML-05`)에도 관계만 더했다. ADR 0007 Konsist 행이 떨어뜨린 조사 노트의 한정(`pushed_at`)을 되돌렸다 |
| `779266f` | **F-2 · F-3 · F-5 · F-7 · F-8.** C-3이 스캐너의 3줄을, C-5.4가 `DATABASE_URL` 6행 중 3행을 뺐다 — 둘 다 복원(마스킹 유지). C-9.4의 후행 공백을 `paste`로 없앴고, C-9.5의 「glob 순서」 주장을 `sort`로 대체했으며, C-1 주석의 라운드 이력을 이 파일 소유로 되돌렸다. **C-9.6·C-10을 신설**해 `F-4`·`F-1`이 요구한 전수 훑기의 자리를 만들었다. **함께 들어간 것**: C-8 출력 블록의 행 추가와 주석 재작성, `checklist.md` §9 신설. **라운드 2의 처리가 서로 다르다** — §9는 **지웠고**(라운드 절), C-8은 **다른 측정으로 교체**됐다(좌표 나열 → `matches` + `credential_literals`). 되돌린 것은 §9뿐이다 |
| **수정 라운드 2** — verifier 레인 A `ready-for-review` / 레인 B `not-ready`(medium 2 · low 3) | |
| `b925b91` | **B2-F3 · B5-F4 · B5-F5.** C-3n·C-6n이 `T`를 재할당해 **문서에서 그대로 복사해 돌리면 실패**했다(`99855e7`부터의 존치 결함) — 음성 fixture를 별도 변수로 옮겼다. C-10 출력 자리의 라벨 3줄을 `echo`가 내게 했다. **`commands.md`의 「명령과 출력만 담는다」가 거짓이 돼 있어** 라운드 서술·검증자 지목을 전부 걷어냈고 `checklist.md` §9(라운드 절)도 지웠다. **함께**: 브랜치가 0C 레인과 공유돼 `base...HEAD` 범위에 그 slice 경로가 섞이므로 C-2·C-4·C-8을 **이 slice의 커밋이 건드린 경로** 측정으로 바꿨고, C-1이 untracked를 세지 않게 해 선언 SHA 트리에서 재현되게 했다 |
| `79700e1` | **B2-F1 · B2-F2.** 앞 라운드가 선언 SHA를 올리며 **일부 블록만 다시 떴다** — C-9.3이 `fb79029`에서 27행인데 22행이 기록돼 같은 파일 C-9.6과 어긋났고, C-8 기록은 **어느 트리에서도 재현되지 않았다.** **26쌍 전부를 `b925b91` 트리에서 다시 떴다**(실제로 바뀐 것은 다섯, 나머지 스물하나는 이미 축어 일치). 머리를 블록별 선언 SHA와 실행 계약으로 다시 쓰고, **C-11**(선언 SHA 트리 대조 하네스)을 신설했다 |
| `122ec21` | **A8 회귀 정정.** `79700e1`이 신설한 C-11의 하네스가 명령 문자열을 56자로 자르면서 **자른 자리가 공백이면 후행 공백이 남았고**, 그 출력이 기록되면서 `git diff --check`가 3건을 잡았다. 하네스의 출력 한 줄에 `.rstrip()`을 넣어 원인을 없애고 C-11 출력을 다시 떴다 — **검사를 늘리면 그 검사 자신이 검사 대상이 된다**는 것을 이 자리가 보인다 |
| `9fb5fa7` | **이 표의 마지막 행 라벨과 아래 footer가 어긋나 있었다** — footer가 *"이미 커밋된 SHA만 지목한다"*고 적는데 마지막 행이 `이 커밋`이었다. footer를 실제 관례에 맞췄고 그 행을 `122ec21`로 확정했다. `head_sha`를 `122ec21`로 옮긴다 |
| **수정 라운드 3** — verifier `not-ready`(medium 이상 0 · low 3) | |
| `f300674` | **R3-F1 · R3-F2.** `commands.md` 머리말의 전칭(*"일부가 아니라 전부다"*)이 여덟 줄 아래 표(`C-11 → HEAD`)와 **어긋났다** — `B2-F1`이 적발한 「같은 파일의 두 자리가 어긋난다」와 같은 형태다. 전칭을 **예외 하나로 한정**했다. 그리고 *"`b925b91`은 이 커밋의 직전 커밋"*이 **거짓이 돼 있었다**(`79700e1` 시점에만 참이었고 `122ec21`이 `commands.md`를 고치면서 낡았다) — **부모 관계 주장을 빼고 SHA를 그대로 지목**하며 관계로 부르지 않는 이유를 그 자리에 적었다. 머리말이 두 줄 늘어 **C-11 기록 출력의 행 좌표가 낡아 다시 떴다**; 26쌍 `diff=0`은 그대로다 |
| `f1cd72b` | **R3-F3.** 위 `779266f` 행이 C-8과 `checklist.md` §9를 *"둘 다 되돌렸다"*로 묶었으나 **두 처리가 다르다** — 갈라 적었다. `head_sha`를 `f300674`로 옮긴다 |
| **Codex 1차 수정 라운드** — `request_changes`(high 2 · medium 2) | |
| `af637fc` | **A · B · C(산출물).** **A(high)** — ADR 0009가 결론 없이 `미결`이었다. `milestone-0.md`의 요구는 *"사용자 확인을 받아 결정한다"*이지 미루라는 것이 아니었다. **운영자 결정 2026-08-29로 (d) 조합**을 확정하고 `OPEN-ADR-10`을 닫았다; 두 자리 정합 검사 요구(D-6)를 적고 `milestone-0.md` 「산출물」의 개정 필요를 **사실로만** 적었다. **B(high)** — ShedLock 불채택 사유가 **OPS-01을 덮지 못했다.** `capability-map.md`의 OPS-01을 직접 열어 요구 셋을 세우고 후보 넷을 대조했다 — **어느 것도 셋을 덮는다고 확인되지 않아** ShedLock을 판정 보류로 되돌리고 legacy가 요구 ②를 얻은 기제를 후보로 등재하고 **`OPEN-ADR-12`**를 신설했다. **C(medium)** — D-11로 부작용 유형별 재시도 소유를 정하고, 채택 도구의 기본값을 **확인하지 않았다고 적어** **`OPEN-ADR-13`**으로 M4에 인계했다. **이 커밋은 한 번 0C 파일 셋을 딸려 담았다가**(`a9509c6`, 공유 인덱스) **`git reset --soft` + 경로 pathspec 커밋으로 갈라 다시 만든 것**이며 0C 파일은 그 레인의 인덱스에 그대로 돌려놓았다 |
| `ba2e4f9` | **D(medium).** C-7이 **후보가 든 행 어디에서든 판정 낱말 하나를 찾으면 통과**시켜 `판정 보류 — 조사되지 않음`인 행을 통과로 셌다 — **스캐너가 자기 출력에서 드러난 미조사를 성공으로 바꿨다.** 표의 **판정 열 한 칸**을 읽어 분류하도록 바꾸고, **판정 열과 사유 열에 서로 반대되는 낱말**을 넣은 음성 fixture를 C-7n에 더했다. A2의 *"채택/불채택 판정"*을 실제에 맞춰 고치고, `OPEN-OPS-07` 종료 조건에 대한 판정(기입은 됐고 전건 판정은 아니다)을 적었다 |
| `8e4a05c` | **출력 블록 전부를 `ba2e4f9` 트리에서 다시 떴다** — 실제로 바뀐 것은 아홉이다. 선언 SHA를 옮기고 Codex verdict JSON을 evidence 패키지에 등재한다. `head_sha`를 `ba2e4f9`로 옮긴다 |
| `520bc6c` | **`head_sha` 규약을 커밋 관계에서 떼어냈다.** *"직전 커밋"*은 뒤 편집과 0C 레인의 개입으로 **두 번 낡았다.** 규약을 **「산출물이 고정된 커밋 = `commands.md`의 선언 SHA」**로 바꿨다 — **관계가 아니라 SHA를 지목하므로 이 파일을 다시 써도 옮겨지지 않는다.** 값은 `ba2e4f9`로 불변이다 |
| **verifier 라운드 5** — `not-ready`(medium 1 · low 3). **M-2 · M-4는 통과** | |
| `d0a882e` | **V-1~V-4 — 「결정을 바꾼 커밋이 그 결정을 인용하는 자리를 안 따라간 것」.** `af637fc`가 ShedLock 불채택 사유를 철회하고 ADR 0009를 결정으로 바꿨는데 넷이 따라오지 않았다. **`checklist.md` §2의 판정 기준 표가 철회된 사유(「db-scheduler와의 기능 중복」)를 현행 판정으로 싣고 있었고**(V-1 medium), `scope.md`의 ADR 대응표는 0009를 아직 `(OPEN)`으로 적어 **같은 파일의 신설 `OPEN` 표와 어긋났다**(V-2). 절 머리의 「11건」이 표 13행과 어긋나 **셈을 지우고 표가 세게 했다**(V-3). ADR 0005 §3.2의 **인용 둘이 축어가 아니었다** — 조사 노트와 `f1cd72b` 원문을 직접 열어 맞췄다; **앞 라운드 ADR의 자기 산문이 노트의 말로 귀속된 형태**였다(V-4) |
| `7f1a725` | **출력 블록을 `d0a882e` 트리에서 다시 떴다** — 실제로 바뀐 것은 둘(C-1 · C-2)이다. 선언 SHA를 옮기고 `head_sha`를 그 값으로 맞춘다 |
| **verifier 라운드 6** — `not-ready`(medium 1 · low 1) | |
| `a7f3835` | **W-1 · W-2 — 낡은 셈과 그 위에 얹은 거짓 전칭.** `checklist.md` §3의 **「신설 11건」이 실측과 어긋났다** — `V-3`이 `scope.md`에서 적발한 것과 **같은 수·같은 형태·같은 파일 안**이다. 13으로 교체하지 않고 **지웠다.** 그리고 `d0a882e`가 그 자리를 지나쳐 놓고 **전칭 둘**을 새로 박았다(*"이 셈은 이 자리 하나에만 있다"* · *"셈이 필요한 자리는 `checklist.md` §6 하나다"*) — **49줄 위에 같은 셈이 낡은 값으로 있어 둘 다 거짓**이었다. **셈이 어디 있는지 주장하지 않고 C-9.4를 가리키게 했다.** §7의 「알려진 제한 5건」도 실측과 어긋나 지웠다(W-2). **원인은 훑기 패턴이 좁았던 것** — 앞 라운드는 **결정 낱말**로만 훑어 그 결정에서 **파생된 셈**을 놓쳤다. 이번엔 수 패턴으로 훑고 지시받은 패턴을 **쌍·곳·행·줄**로 넓혀 살아 있는 「26쌍」·「26개」 넷을 더 찾아 `pairs=` 출력을 가리키게 했다 |
| `584305b` | **출력 블록을 `a7f3835` 트리에서 다시 떴다** — 실제로 바뀐 것은 둘(C-1 · C-2)이다. 선언 SHA와 `head_sha`를 그 값으로 맞춘다 |
| **Codex 2차 수정 라운드** — `request_changes`(blocker 0 · high 0 · medium 3). 1차 finding 4건은 재등장하지 않았다 | |
| `445449c` | **#1 — `D-5`와 `D-6`이 한 검사로 이어지지 않았다.** ADR 0009 `D-5`의 오른쪽 자리가 **튜닝 내역만** 담아, `D-6`이 요구한 *"같은 모듈에 대해 같은 원본 경로·기준 commit인가"*를 **대조할 값이 한쪽에 없었다** — 검사가 구현 불가였고 운영자가 (d)를 택하며 인지한 비용 ①의 완화책이 실행되지 않는 상태였다. **`D-6.1`이 대조 계약**(조인 키·양쪽 필수 항목·술어·대상 집합과 시점)**을 확정**하고, **`D-6.2`가 형태·파일 형식·파싱 구조·CI 배선을 M5로 유지**하며, **`D-6.3`이 그 경계를 적는다** — 앞의 것이 없으면 M5가 「정할」 것이 아니라 **정할 수 없다**. `D-1`에 넷째 항목(모듈 식별자)의 출처가 이 ADR임을 적었다 |
| `b281a3b` | **#2 — `D-10`이 역방향 의존을 규정하고 자기가 어기는 조항을 근거로 인용했다.** *"lease는 `workflow` 모듈의 port이고, 도메인은 그 port만 본다(ADR 0006 D-3)"* — ADR 0006 `D-2`는 `workflow`를 **application**에 두고 `D-3`은 방향을 `domain <- application <- adapters/app`으로 고정한다. **갈래 ②를 택했다** — lease는 **use case 진입 경계에서 `workflow`가 적용**하고 **도메인은 보지 않는다.** 근거는 OPS-01 요구 ①이 배격하는 실물(*"소비자마다 다른 레이어"* · *"in-process 우회"*)이 **한 자리에 설 것**을 요구할 뿐 **`domain` 모듈을 지목하지 않는다**는 것과, ADR 0006 `D-2`가 use case를 `workflow` 책임으로 둔 것이다. **`D-10.1` 신설** — 소유·구현·호출·보지 않는 곳의 표, 방향 점검, 그리고 **요구 ①이 실제로 서려면 함께 서야 하는 제약**(모든 진입 경로가 같은 use case 진입점을 지난다). 그 제약을 **architecture test로 표현할 수 있는지는 재지 않았고** §6에 등재했다 |
| `95a0364` | **#3 — ShedLock 사유가 사실과 달랐다.** 앞 라운드가 **조사 노트의 요약을 확인 없이 판정 사유로 승격**했다(*"경계가 `@Scheduled` 메서드이지 use case 진입점이 아니다"*) — 이 slice가 성문화한 **실패 형태 ①**이다. **공식 README를 직접 열어**(`github.com/lukas-krecan/ShedLock`, 2026-08-29) **요구 ①과 ②를 갈라 적었다** — ①은 `PROXY_METHOD`의 *"the lock is applied even if you call the method directly"*와 프레임워크 없는 `DefaultLockingTaskExecutor.executeWithLock(...)`로 **충족 가능**이고, ②는 *"The lock is always released after `lockAtMostFor`"*로 **불충족**(미확인 → 불충족)이다. **조사 노트의 서술 자체는 참이었고 거짓이었던 것은 거기서 끌어낸 추론**임을 적었다. **판정 결론도 다시 봤다** — 「판정 보류 — 확인 미완」이 성립하지 않아 **OPS-01의 lease 어댑터로 불채택**으로 바꿨고, 그 불채택이 **이 축에 한정**됨을 적었다. **확인하지 못한 것**(LockProvider 구현을 열지 않았다 · db-scheduler 해제 기제는 여전히 미확인)은 §6에 갈라 남겼다 |
| `f207412` | **#4 — 리뷰 레인이 넘긴 메타데이터 기록.** `codex-review-gate`가 요구하는 preflight 결과와 reviewer 메타를 2차 리뷰 레인이 **자기 리뷰 대상이라 쓰지 못했다.** **C-12 신설** — C-12.1이 raw output 머리글과 등재된 verdict JSON의 `reviewer`를 대조하고(`reasoning effort: high` 고정이 실측으로 남는다), C-12.2가 memory 오염 preflight를 낸다. **verdict JSON을 evidence 패키지에 등재(커밋)**했다 — 그때까지 untracked였다. 스킬 문면의 `?mode=ro`가 이 환경에서 열리지 않아 `?immutable=1`로 읽었고 **실패 자체를 출력에 남겼다**; `.claude/`는 고치지 않는다. C-11 worktree에 `_workspace` symlink를 더했다 — C-12.1이 선언 SHA 트리에서 재현되려면 그 경로가 있어야 한다 |
| `e1a6d39` | **출력 블록 전부를 선언 SHA `f207412` 트리에서 다시 떴다** — 실제로 바뀐 것은 다섯(C-1 · C-2 · C-7 · C-8 · C-10)이다. **C-12를 C-11 앞으로 옮겼다** — 뒤에 두면 C-11이 자기 출력 길이를 바꿀 때마다 C-12의 기록 좌표가 낡는다. 선언 SHA와 `head_sha`를 그 값으로 맞추고, C-7의 판정 줄에서 낡은 셈을 걷었다 |
| **독립 검증 — Codex 2차 수정 라운드에 대한 자체 게이트** — `not-ready`(high 1 · medium 3 · low 3). Codex 2차 medium 3건의 **실질은 셋 다 고쳐진 것으로 재실측**됐고, 남은 것은 **같은 사실을 말하는 다른 자리**와 **재현되지 않는 블록**이었다 | |
| `8af7a30` | **`H-1`(high) — 재현 래칫 안에 저장소 밖 가변 상태를 넣었다.** `C-11`이 판정 기준 SHA에서 `diff=1`을 냈고, 어긋난 쌍은 `~/.codex/memories_1.sqlite`를 여는 `C-12.2`였다. **값을 다시 뜨는 것으로 닫지 않았다** — 그 파일은 저장소 밖에 있고 계속 변하므로 **트리를 고정해도 답이 고정되지 않는 값을 트리 재현 래칫에 넣은 것**이 결함이다. **C-11이 대조 대상을 명령 문면에서 스스로 정하게 했다** — `SELF`(하네스 자신) 외에 `EXT`(`$HOME` · `~/` · `_workspace/` 중 하나가 문면에 있는 블록)를 더했고, 빠진 쌍을 `SKIP <코드> <근거 토큰>` 행과 사유별 셈으로 **출력이 스스로 말한다.** 제외는 사람의 주의력이 아니라 문면에서 나오므로 새 블록이 저장소 밖을 읽기 시작하면 그것만으로 빠진다. **기록은 지우지 않았다** — `codex-review-gate`가 `commands.md`에 남기기를 요구한다. 두 블록이 `taken_at`을 내는 **시점 기록**이 됐다. `?mode=ro`는 **「열린다」로 뒤집지 않았다** — 실측된 것은 그때 실패하고 지금 성공한다는 것뿐이며 그것이 환경·시점 의존의 증거다. 스킬 문면과 실행이 갈라졌던 사실을 기록으로 남겼고 **`.claude/`는 고치지 않았다** |
| `1350ab3` | **`M-1`(medium).** `checklist.md` A2 행이 「판정 둘이 `보류`다」로 남아 같은 파일 §9(단수)와 C-7 출력에 어긋났다. **앞 라운드 `95a0364`가 §9에서만 셈을 걷고 49줄 위를 두었다.** 셈을 다른 수로 바꾸지 않고 **지우고 `candidates=` 줄을 가리키게 했다** |
| `bf4532f` | **`M-2`(medium).** ADR 0009 `D-5` 왼쪽 셀이 「원본 경로 + 기준 commit**만**」이라 적어 `D-6.1` #2(세 값)와 어긋났고 **화해 문장이 없었다** — Codex 2차 #1이 오른쪽에서 지목한 결함이 왼쪽에 남은 형태다. 왼쪽 셀을 세 값으로 맞추고, **모듈 식별자는 조인 키라 (d)의 분할 축(출처 포인터 ↔ 튜닝 내역) 위에 있지 않다**는 화해 문장을 표 아래에 뒀다 |
| `f90702d` | **`M-3`(medium).** `OPEN-ADR-10` 해소 기록의 「결정」 줄이 **수정 전 분할**을 요약했다. 요약을 새 분할로 바꿔 적지 않고 **`D-5`의 표를 가리키게 했다** — 같은 분할이 두 벌이면 다음 정정에서 다시 한쪽만 낡는다 |
| `0f2fced` | **`L-1`(low).** 「`D-6.1`의 다섯 값을 담을 형식」이 성립하지 않았다 — 다섯 행은 「값」이 아니라 「정하는 것」이고 **술어(#4)와 대상 집합(#5)은 담는 대상이 아니다.** 담기는 것(#2·#3)과 하는 일(#4·#5)을 갈랐다 |
| `13fa35f` | **`L-2`(low).** `C-12.2` 머리의 「두 값」이 블록이 내는 수치와 맞지 않았다. 수를 바꾸지 않고 걷어 **블록을 가리키게 했다** |
| `eafdc90` | **`L-3`(low).** `D-6.1`이 「대조가 성립하기 위한 조건」을 표방하면서 **한쪽 기록이 없을 때**를 술어에도 `D-6.2` 이월 목록에도 넣지 않았다. 술어를 **(ㄱ) 있음 · (ㄴ) 같음**으로 갈랐다 — (ㄱ)이 없으면 **한쪽에 기록을 아예 남기지 않는 것이 검사를 통과하는 길**이 된다 |
| `0767525` | **`H-1` 전수 훑기의 잔여.** 같은 명제를 `scope.md` 「알려진 제한」 6·7이 세 번째 자리에서 낡은 형태로 들고 있었다 — 6은 예외를 `C-11` 하나로만 셌고 7은 「그 SHA에서 전부 다시 떠야 한다」를 무조건 전칭으로 적었다. 둘을 **래칫 대상 블록**으로 한정하고 제외 두 사유를 적었다 |
| `90119b1` | **래칫 대상 출력 블록을 선언 SHA `0767525` 트리에서 다시 떴다** — 기록이 바뀐 블록은 **C-1 · C-2 · C-11**이고, `EXT`로 빠지는 **C-12 둘은 그 트리에서 뜨지 않는다**(정본: `commands.md` 「선언 SHA와 실행 계약」). 선언 SHA와 `head_sha`를 그 값으로 맞춘다 |
| **독립 검증 — `H-1`~`L-3` 수정 라운드에 대한 자체 게이트** — `not-ready`(medium 2 · low 1). **`H-1`의 실질은 통과**로 재실측됐다(`C-11` 2회 재현·축어 일치, `EXT` 제외에 거짓 제외 없음, 29쌍 전수 probe로 놓친 외부 없음, 강제 ①·②·③ 실증). 남은 셋은 **같은 명제(「출력 블록이 선언 SHA 트리에서 다시 뜬다 / 예외가 무엇인가」)의 네 번째·다섯 번째 자리**와 그 라운드 서술의 시점 지시다 | |
| `5d0c58f` | **`F-1`(medium).** 「`head_sha`와 range」 절이 *"`commands.md`의 출력 블록도 그 SHA의 트리에서 그대로 다시 뜬다(**C-11**)"*로 **예외 없는 전칭**을 들었다 — `C-11` 자신의 출력(`skipped_EXT=2`)이 반박하는 주장을 `C-11`을 근거로 인용한 형태다. **문장을 세 벌로 만들지 않았다** — `commands.md`의 **「선언 SHA와 실행 계약」 절을 정본으로 표시**하고 `scope.md`의 두 자리(이 절 · 「알려진 제한」 6·7)가 그리로 가리키게 했다 |
| `3863dd6` | **`F-2`(medium).** 이력 표 마지막 행이 *"출력 블록 **전부**를 선언 SHA `0767525` 트리에서 다시 떴다"*고 적었으나 `90119b1`이 기록을 바꾼 블록은 셋이고 `C-12`의 `taken_at`은 `8af7a30`이 `0767525`보다 **8분 앞서** 넣은 값이다. **값만 고치지 않고 구조로 막았다** — 0C 레인이 오늘 같은 결함을 닫은 규약(`V-3`, 커밋 `446a39f`)을 채택해 **「이 커밋」 행을 없앴다.** 그 행은 자기 커밋의 diff를 서술할 수밖에 없어 이 slice에서 두 번 걸렸다 |
| `722882f` | **`F-3`(low).** `?mode=ro` 절에서 *"이 수정 라운드"*가 **한 절 안에서 두 라운드를 가리켰다** — `commands.md` 머리가 스스로 금지한 형태다. 절 안의 라운드 지시어 셋을 걷고 **SHA와 자리**로 바꿨으며, 갈라짐 서술을 **관측이 남은 자리 표**로 만들었다 |
| `1e641bb` | **게이트 밖 · 원인 후보 기록.** 검증 레인이 `?mode=ro` 진동의 기제를 **WAL sidecar 유무**로 좁혔다(`O-4`). **확정으로 적지 않았다** — 이 slice는 sidecar를 가르는 실험을 돌리지 않았다. *"원인을 확정하지 못했다"*를 그대로 두고 후보와 출처만 병기했다 |
| **독립 검증 — `F-1`~`F-3` 수정 라운드에 대한 자체 게이트** — `not-ready`(medium 3 · low 3). **실행은 전부 섰다**(`C-11` 2회 `diff=0`이고 기록과 바이트 동일, `EXT` 제외 둘은 무력화 probe로 거짓 제외가 아님, 재취득 범위·불변·0C 무접촉·위생 통과). 막힌 것은 ① **고치지 않기로 판단한 훑기 매치 셋이 딛고 선 주장이 거짓**이라는 것(`G-1`)과 ② 그 라운드가 **신설한 두 문장이 실물에 반증된다**는 것(`G-2` 정본 표지 · `G-3` footer 검사 명령)이다 | |
| `5d81dc8` | **`G-1`·`G-2`·`G-4`.** `checklist.md`의 *"이 표의 근거는 **전부** `commands.md`의 출력 블록"*이 거짓이었다 — 「근거」 열 전수에서 **A3=`아래 §2`·A5=`아래 §4`**가 이 파일의 산문이고 **A6은 절반**(`ADR 0001 §4.2`)이다. 같은 문단이 세 줄 아래에서 그 부류를 배제해 **자기를 반증했다.** 전칭을 걷어 두 부류로 갈라 적고, 정본 지목 없이 들고 있던 규약 사본(`SELF`를 빠뜨려 정본보다 좁았다)을 **`commands.md` 「선언 SHA와 실행 계약」 절 지목으로 바꿨다** |
| `37eb1bb` | **`G-2`를 감싸는 주장.** `5d0c58f`가 신설한 정본 표지의 문면(*"같은 규약을 **다른 파일에 다시 적지 않고** 여기를 가리킨다"*)은 `checklist.md` 사본을 걷은 뒤에도 「알려진 제한」 6·7에 의해 반증된다 — 그 절은 **규약이 남긴 한계**를 적으려면 예외 축을 요약해야 하고, 이미 정본을 지목한다. 표지를 **강제되는 문면**으로 바꿨다: 스스로 정하지 않는다 · 요약하는 자리는 지목한다 · 갈라지면 정본이 이긴다 |
| `92e7b3f` | **`G-3`.** `3863dd6`이 「이 커밋」 행을 없애며 **대체 보장**으로 든 검사 명령이 이 브랜치에서 틀렸다 — `git log --oneline <base>..<head>`는 0C 레인 커밋을 함께 낸다(실측 `90119b1..26f28ff` = **14 중 `m0-0d` 5**). 같은 파일 `:79-81`이 선별자를 못 박고 `C-2`도 그것을 끼는데 **footer만 빠뜨렸다.** footer에 `| grep 'm0-0d'`를 넣었다 |
| `9c0eca3` | **`G-5`.** `F-3`이 이름 붙인 결함(한 절 안의 지시어가 서로 다른 라운드를 가리킨다)이 `checklist.md` §9에 그대로였다. blame·실측으로 지시 대상을 재 SHA로 바꿨다 — §1은 `99855e7`→`ba2e4f9`, §9는 `af637fc`→`95a0364`와 `ba2e4f9` 전/후. **앞 라운드의 미처리 사유(「출력 블록 재현과 다른 명제」)는 축을 잘못 붙인 것이다** — `F-3`은 출력 블록 재현이 아니라 지시어에 관한 발견이었다 |
| `9ea5e17` | **`G-5` 전수 스윕.** 지목된 자리(`9c0eca3`이 닫았다)만 고치고 그치면 같은 결함이 `in_scope`의 다른 자리에 남는다. `9ea5e17`이 마주한 트리 `9c0eca3`을 경로별로 전수로 훑어 **항구 산출물 ADR 0005**와 이 파일의 **상시 주장**을 닫았다 — 라운드 지시어를 라운드 이름과 SHA 앵커로 바꿨다. **닫지 않고 둔 것**: 이 이력 표의 행은 **행 라벨이 기준점**이라 낡지 않고, `commands.md`의 매치는 `C-7n` 음성 fixture heredoc 안의 합성 문자열이다. **자리 수도 목록도 이 행에 적지 않는다** — 경로마다 `git show 9c0eca3:<경로> \| grep -nE '이 (수정 )?라운드\|이번 라운드\|앞 라운드\|앞선 라운드\|지난 라운드\|직전 커밋'`이 둘을 함께 낸다. 셈을 산문에 박으면 다음 편집에 낡는다 — 이 slice가 `M-1`·`L-2`에서 택한 방법(수를 걷고 그것을 내는 자리를 가리킨다)을 이 행에도 쓴다 |
| `5012bde` | **`9ea5e17`이 낸 회귀를 재취득이 잡았다.** ADR에 backtick SHA를 넣자 `C-6`의 `citescan.py`가 그것을 **legacy 인용으로 읽어** `legacy=37 ok=37 missing=0` → `legacy=39 ok=37 missing=2`가 됐다 — **A7이 깨진다.** ADR 자리의 앵커를 **라운드 이름**(같은 셀이 이미 쓰는 어휘)으로 바꾸고, citescan 대상이 아닌 `checklist.md`·`scope.md`는 SHA를 유지했다. 그 비대칭이 「어긋남」으로 읽히지 않도록 **규칙을 `C-6` 판정 산문에 적었다** — 근거는 `C-6n`의 `deadbeef1` 행이 실행으로 낸다 |
| `d5122b9` | **래칫 대상 출력 블록을 선언 SHA `5012bde` 트리에서 다시 떴다** — 기록이 바뀐 블록은 **C-1**(HEAD) · **C-2**(`slice_commits` 44 → 51) · **C-11**(자기 출력)이고, `EXT`로 빠지는 **C-12 둘은 그 트리에서 뜨지 않는다**(정본: `commands.md` 「선언 SHA와 실행 계약」). **C-11 기록의 블록 시작 행 번호가 전부 옮겨졌다** — `commands.md` 순증이 `37eb1bb` **+1줄**(머리 정본 표지) · `5012bde` **+6줄**(C-6 판정 산문)이라 쌍 0~14는 **+1**, 쌍 15~28은 **+7**이다. **그 커밋의 메시지는 이 셈을 「다섯 줄 · +6」으로 적었다 — 넣은 줄은 여섯이고 아래쪽 쌍의 이동은 +7이다. 이 행이 정정한다.** `head_sha`를 선언 SHA에 맞췄다 |
| `1e759be` | **`d5122b9`의 메시지를 고치려 한 `git commit --amend`가 그 사이에 들어온 0C 레인 커밋(`10eb819`)을 대상으로 잡아 그 메시지를 0D 것으로 덮었다.** 덮인 커밋과 원래 커밋의 **트리 diff는 0줄**이었고 `git reset --hard 10eb819`로 되돌려 **그 커밋의 SHA·트리·메시지가 복구 전후로 같다.** **0C 산출물 파일은 어느 0D 커밋에도 들어가지 않았다**(경로 질의 양방향 0). 「알려진 제한」 11에 **`--amend` 금지**를 더했다 — 같은 제한이 이미 무인자 `git commit`을 금지하고 있었고 같은 축이다. 메시지를 고쳐 쓰는 대신 위 `d5122b9` 행이 그 셈을 정정한다 |
| `ad8a235` | **래칫 대상 출력 블록을 선언 SHA `1e759be` 트리에서 다시 떴다** — 기록이 바뀐 블록은 **C-1**(HEAD) · **C-2**(`slice_commits`)이고, **`C-11` 자기 출력은 바뀌지 않았다**(그 라운드가 `commands.md`에 줄을 넣지 않아 쌍의 시작 행이 그대로다). `EXT`로 빠지는 **C-12 둘은 그 트리에서 뜨지 않는다**(정본: `commands.md` 「선언 SHA와 실행 계약」). `head_sha`를 선언 SHA에 맞추고 위 `1e759be` 행을 등재했다 |
| **독립 검증 — `G-1`~`G-6` 수정 라운드에 대한 자체 게이트** — `not-ready`(medium 1 · low 2). **핵심 축 셋이 전부 통과했다** — `--amend` 사고의 복구가 **객체 수준에서 완전**하고(부모·트리 oid 정합, 0C blob 무손상, 0D↔0C 경로 질의 양방향 0), 재취득 두 번의 `--stat`과 정정 셈이 실측과 맞으며, `C-6` 회귀의 처방이 `docs/adr` 전수에서 **잔여 없이** 닫혔다. 막힌 것은 **이력 행 하나가 산문에 박은 셈**(`H-1`)이다 — 이 slice가 세 번 데인 형태의 네 번째다. **두 low는 갈린다** — `L-1`은 `_workspace` 보고의 좌표계 축이고 그 디렉터리는 `.gitignore`라 리뷰 range 밖이지만, **`L-2`는 이 파일 안에 있었다**(`ad8a235`의 이 파일이 라운드를 `G-1`~`G-5`로 적었다). `scope.md`는 `998dc21..8782d01` range에 드는 파일이므로 **그 결함은 Codex에 실려 나갈 상태였다.** 「둘 다 실려 나가지 않는다」는 앞 검증 보고의 요약을 옮긴 것이고 **`L-1`에만 참이었다** | |
| `81e1787` | **`H-1`(medium).** 위 `9ea5e17` 행이 든 수 셋(ADR 0005 · 이 표의 행 · 그 합)이 실측과 어긋났다 — 같은 라운드의 검증 보고는 옳은 수를 적어 **패키지와 보고가 갈라졌고, 틀린 쪽이 Codex에 실려 나갈 상태였다.** **수를 다른 수로 바꾸지 않았다** — 맞춰 적으면 다음 편집에 또 낡는다. 이 slice가 `M-1`·`L-2`에서 택해 성공한 방법을 그 행에도 적용해 **셈과 목록을 걷고, 그것을 내는 명령을 고정 트리(`9c0eca3`)에 앵커해 행이 가리키게 했다** — 행은 「무엇을 했는가」만 말하고 **셈은 명령이 낸다** |
| `9f712bd` | **`O-2`(게이트 밖이나 닫았다) · `L-2`(low) · `O-1` · `O-4`.** 「알려진 제한」 11이 `--amend`와 무인자 `git commit`만 금지하고 **복구에 실제로 쓴 `git reset --hard`를 목록에 두지 않았다** — 같은 축의 공유 트리 위험 명령이고 **다른 레인의 커밋되지 않은 편집을 조용히 지운다.** 목록에 더하면서 **그 한계**(「아무것도 잃지 않았다」를 git이 증명하지 못한다 · 그 명제는 0C 레인 소관이다)와 **이 slice가 그것을 썼다는 사실**을 함께 적었다 — 금지를 세우면서 자기가 그것을 쓴 것을 지우지 않는다. 같은 절의 라운드 이름을 `G-1`~`G-6`으로 맞췄고(`L-2`), 「덮었다」가 **ref 교체**라는 것(`O-1`)과 **커밋 메시지만 읽는 독자에게는 잘못이 남는다**는 대가(`O-4`)를 각각 한 줄로 적었다 |
| `d96d38b` | **`O-6`(게이트 밖).** `EXT` 판정이 명령 문면의 토큰으로만 재므로 **저장소 밖 상태를 그 토큰 없이 읽는 블록은 제외되지 않고 대조 대상으로 남는다** — 오늘 그런 블록이 없다는 것은 probe가 확인한 사실일 뿐 **구조가 막는 것이 아니다.** 그 한계를 「알려진 제한」 6에 적었다. **정본(`commands.md` 「선언 SHA와 실행 계약」)은 고치지 않았다** — 여기 적는 것은 규약이 아니라 그 규약이 이 slice에 남긴 한계이고, 6·7이 이미 그 절을 정본으로 지목한다 |

**이력 표에 「이 커밋」 행을 두지 않는다** — 행 라벨은 **이미 커밋된 SHA**뿐이다.
0C 레인이 같은 결함을 같은 규약으로 닫았고(`V-3`, 커밋 `446a39f`) 0D도 그것을 채택한다.
「이 커밋」 행은 **자기 커밋의 diff를 서술할 수밖에 없고 그래서 반복해서 거짓이 됐다** —
이 slice에서 두 번 걸렸다(`9fb5fa7`은 라벨과 이 footer의 어긋남, 독립 검증 `F-2`는
그 행이 든 거짓 전칭).

그래서 **이 표는 라운드의 커밋 전부를 담는다고 주장하지 않는다.** 이 파일을 담은 마지막
커밋은 자기 SHA를 모르므로 표에 없고 **다음 편집이 그 SHA로 등재한다.** 이 slice의 커밋
전수는 `git log --oneline <base>..<head> | grep 'm0-0d'`가 낸다 — **이 브랜치는 다른
slice와 공유되므로 선별자를 빼면 0C 레인 커밋이 함께 잡힌다.** 선별자는 위 「`head_sha`와
range」가 정한 것과 같고 `commands.md` **C-2**도 같은 것을 쓴다. 각 행이 그 커밋이 실제로
바꾼 것만 적는지는 `git show --stat --format='' <SHA>`가 잰다.
