# acceptance 대조 — M0 / 0D

`reports/evidence/m0/0d/scope.md`의 **A1~A8**을 하나씩 대조한다.
**라운드 이력과 자기평가 산문은 이 파일에 없다** — `scope.md`의 「갱신 이력」이 소유한다.
**수와 좌표를 여기 옮겨 적지 않는다** — 근거는 `commands.md`의 명령이다.

| # | 요구 | 근거 | 판정 |
| --- | --- | --- | --- |
| **A1** | ADR 9건이 `docs/adr/`에 있고 각 파일이 `상태`·`맥락`·`결정`·`대안`·`결과` 절을 가지며 번호가 유일·연속 | `commands.md` **C-3** (+ 음성 대조 **C-3n**) | **충족** |
| **A2** | `OPEN-OPS-07` 종료 조건 — OPS-21 표의 후보 전부가 ADR 「대안」 절에 판정과 함께 기입 | **C-7** (+ 음성 대조 **C-7n**) | **충족** |
| **A3** | 판정 기준이 「최신 버전」이 아니라 Kotlin 2.x + Spring Boot 3.x 조합 호환 | 아래 §2 | **충족** |
| **A4** | 활성 OPEN을 해소하지 않았다 | **C-4**(산출물 무변경) · **C-9.5**(언급한 자리) · **C-9.6**(언급하지 않은 활성 OPEN 전수) | **충족** — 아래 §3 |
| **A5** | 확정된 운영자 결정을 재확정하지 않고 인용 | 아래 §4 | **충족** |
| **A6** | ML 재활용 래칫 사전 조사가 수행됐고 수치가 명령으로 재현되며 M5 판단 근거가 남았다 | **C-5.1**~**C-5.3** · ADR 0001 §4.2 | **충족** — 아래 §5 |
| **A7** | ADR이 인용한 legacy 경로·행 범위가 `ed4b06c`에 실재 | **C-6** (+ 음성 대조 **C-6n**) | **충족** |
| **A8** | `in_scope` 밖 경로 변경 없음, 공백 오류 없음, 자격증명 리터럴 없음 | **C-2**(`out_of_scope_paths` · `whitespace_problems`) · **C-8**(`credential_literals`) | **충족** |

---

## 1. 산출물

| 요구 (`milestone-0.md` §"Slice 0D") | 어느 ADR | 상태 |
| --- | --- | --- |
| 1 · Kotlin modular application + Python ML engine, 재작성/재활용 경계 | 0001 | 결정 |
| 2 · Gradle module과 의존 방향 | 0006 | 결정 |
| 3 · 금액/rate/basis 표현 | 0002 | 결정 |
| 4 · gRPC/Protobuf 내부 계약 | 0003 | 결정 |
| 5 · DB 및 migration 도구 | 0004 | 결정 |
| 6 · domain event / outbox / notification 방식 | 0005 | 결정 |
| 7 · 테스트 pyramid와 mutation 대상 | 0007 | 결정 |
| 8 · React UI 재사용/재작성/후속 여부 | 0008 | 결정(`후속`) + `OPEN-ADR-09` |
| 9 · ML 재활용 출처 기록 위치 | 0009 | **미결 — `OPEN-ADR-10`**(운영자 결정 사항) |

**9번이 미결인 것은 미달이 아니라 지시다.** `milestone-0.md`가 *"사용자 확인을 받아
결정한다(2026-08-22 지시)"*로 지정했고, 팀 리드가 *"결론을 네가 정하지 말고 `OPEN`으로
남기고 선택지와 각각의 비용을 적어라"*고 지시했다. ADR 0009 §3이 선택지 넷과 각각의
이점·비용을 적는다.

**`milestone-0.md` 「산출물」이 이름을 지정한 네 파일**(`0001-target-architecture.md` ·
`0002-money-rate-basis.md` · `0003-contract-transport.md` · `0004-persistence-and-events.md`)은
그 이름과 번호를 유지했다. `0004`의 이름과 내용이 갈리는 이유는 `scope.md` 「이 slice가
만드는 것」이 적는다.

---

## 2. A3 — 판정 기준

`decisions.md`의 `OPEN-OPS-07` 절: *"판정 기준은 '최신 버전'이 아니라 **M1에서 고정할
Kotlin 2.x + Spring Boot 3.x 조합과의 호환**이다(`v2-지침서.md` §5)."*

**판정에 최신 버전을 쓴 자리가 없다는 것은 각 판정의 사유가 낸다**:

| 후보 | 불채택·조건부 사유의 축 | 최신 버전이 근거인가 |
| --- | --- | --- |
| JobRunr | **OSS/Pro 경계**(트랜잭션 연동이 Pro)와 **라이선스** | 아니다 — 버전은 최신이고 Kotlin 2.4를 특정해 지원 선언한 유일한 후보다 |
| Spring Modulith | **프레임워크 결합도**와 **모듈 개념 이중화** | 아니다 — 세 라인 병행 유지로 유지보수가 최상위권이다 |
| ShedLock | **db-scheduler와의 기능 중복** | 아니다 — Boot 3.5·4.x를 함께 테스트한다 |
| Konsist | **유지보수 정체**(릴리스 부재 + 내장 컴파일러 고정) | **버전이 근거이나 「최신이 아님」이 아니라 「고정된 내장 컴파일러가 M1 Kotlin보다 낮을 위험」이다** |
| Detekt | **안정판의 기준 Kotlin이 M1 고정 후보보다 낮다** | 같은 축 — 조합 호환이지 최신성이 아니다 |

**Boot 세대 자체가 미결이라는 사실**(`OPEN-ADR-01`)은 ADR 0001 §5가 기록한다.
조사가 *"'현재 지원되는 Spring Boot 3.x'에 해당하는 버전이 조사 시점에 존재하지 않는다"*를
찾았고, **이 slice는 그것을 판정하지 않고 등록했다.**

---

## 3. A4 — 활성 OPEN을 해소하지 않았다

세 겹으로 확인한다.

1. **산출물이 무변경이다** — `capability-map.md` §12(활성 45건)와 `regression-ledger.md`
   §9(`OPEN-REG` 5건)가 등록부이고 둘 다 이 range에서 바뀌지 않았다(**C-4**).
2. **ADR이 활성 OPEN을 언급한 자리를 전수로 낸다**(**C-9.3**) — 어느 ID가 몇 번 나오는지는
   그 명령이 낸다.
3. **「해소」·「확정」이 활성 OPEN ID와 같은 줄에 있는 자리를 전수로 낸다**(**C-9.5**).
   **그 판정은 기계가 못 한다** — 읽은 결과는 C-9.5의 판정 줄에 있다.
4. **ADR이 언급하지 않은 활성 OPEN을 전수로 낸다**(**C-9.6**). 계열 A는 언급한 자리가
   아니라 **언급 없이 그 쟁점을 확정 서술로 쓴 자리**에서 나므로 3번만으로는 그 부류가
   잡히지 않는다. **인접 여부는 그 목록을 읽고 사람이 판정한다.**

**예외는 `OPEN-OPS-07` 하나**이며, 그것도 결정이 아니라 **실행(ADR 대안 절 기입)**으로
닫힌다 — `capability-map.md` §12 G6가 그 성격을 그렇게 적었다. 근거는 **C-7**이다.

**등록부를 이 slice가 쓰지 못한다**(`out_of_scope`). 그래서 신설 11건은 ADR 파일 안에
있고, `regression-ledger.md`의 `OPEN-REG-01`~`05`가 같은 형태로 자기 문서에만 있는 선례를
따랐다. **§12 통합은 후속 slice의 일**이라고 `scope.md`가 적는다.

---

## 4. A5 — 확정된 운영자 결정의 인용

| 결정 | 인용한 ADR | 재확정하지 않았다는 표시 |
| --- | --- | --- |
| `OPEN-ML-01`(8개 커널 전부 ml-engine, 「수학 커널 = ml-engine / 업무 판정 = Kotlin」) | 0001 §1.3 · D-2 · A-2 · A-5 | *"이 ADR은 그 결정을 인용할 뿐 재확정하지 않는다"*(A-5) |
| `OPEN-OPS-05`(브로커 없음, DB 기반) | 0005 §1.2 · D-1 / 0003 §1.1(**축 분리**) / 0006 D-2 | *"이 ADR은 그 결정을 재확정하지 않고 인용한다"*(0005 D-1) |
| 두 갈래 전략(service 재작성 / ML 재활용) | 0001 §1.1 · D-3 | 지침서 §1 인용 |
| `OPEN-NOTI-02`(at-most-once, 놓침 감수) | 0005 §1.2 · D-3 | 인용 |
| `OPEN-STR-01`(basis 1급 필드, 기존 값은 기초금액) | 0002 D-8 | *"재확정하지 않고 인용하며 타입 결정으로 옮긴다"* |
| `OPEN-DEC-08`(legacy `clean` 라벨 미승계) | 0004 D-4 | 인용 |
| `OPEN-SET-02`·`OPEN-QUAL-06`·`OPEN-ML-04`(측정 불가·부재의 1급 상태) | 0002 D-5 | ledger 예방 제약 경유 인용 |
| pull 상호작용 모델 | 0003 §1.4 · 0005 §1.2 · 0008 §1.4 | 인용 |

**`OPEN-OPS-05`의 축 분리를 두 ADR이 같은 인용으로 지킨다** — 0003 §1.1과 0005 §1.1이
`decisions.md`의 「범위 주의」를 각각 인용하고, 서로를 「다른 축」으로 가리킨다.
ADR 0003 A-2가 브로커 기각의 근거로 **`OPEN-OPS-05`를 쓰지 않는다**고 명시한다.

---

## 5. A6 — 래칫 사전 조사

`milestone-0.md` §"추가 조사 항목"의 요구는 셋이다.

| 요구 | 어디에 |
| --- | --- |
| 세 디렉터리의 **파일·함수 크기** | **C-5.1**(파일 축) · **C-5.2**(함수 축, 초과 전수 포함) |
| 세 디렉터리의 **결합 지점** | **C-5.2**의 결합 축 블록 · **C-5.2c** |
| 래칫 만족 여부와 **M5 판단 근거** | ADR 0001 §4.2 · §4.3, **C-5.2d** |

**측정 정의가 명시돼 있다** — 파일 축의 `wc -l` 오차 조건은 C-5.1이, 함수 축의
데코레이터·docstring·중첩 처리는 C-5.2의 스크립트 docstring이 적는다.

**이 slice는 파일별 「분해 / allowlist」를 판정하지 않았다.** 그것은 M5의 결정이며
ADR 0001 §4.2가 그렇게 적고 판단 **순서**만 권고한다. **결론을 내지 않은 것이 요구
미달이 아니다** — `milestone-0.md`가 요구한 것은 *"판단할 근거를 남긴다"*이다.

**8개 커널이 세 디렉터리 밖이라는 어긋남**은 `OPEN-ADR-02`로 등록했다(ADR 0001 §5).
그 어긋남을 이 slice가 지침서 개정으로 해소하지 않았다 — 개정 권한은 운영자에게 있다.

---

## 6. 신설 `OPEN`

11건. 목록과 성격은 `scope.md` 「이 slice가 새로 등록하는 `OPEN`」이, 등록 자리는
**C-9.4**가 낸다. **운영자 결정을 직접 요청하는 것은 `OPEN-ADR-09`·`OPEN-ADR-10` 둘**이다.

---

## 7. 알려진 제한

`scope.md` 「알려진 제한」이 소유한다(5건). 여기서 되풀이하지 않는다.

각 ADR의 **「확인하지 않은 것」 절**이 그 ADR 고유의 공백을 적는다 — 모든 ADR이 이 절을
갖는다는 것은 **C-3**의 대상이 아니므로 아래 명령이 확인한다.

```
grep -c '^## 6\. 확인하지 않은 것\|^## 5\. 이 ADR이 등록하는 `OPEN`' docs/adr/*.md | sort
```

```
docs/adr/0001-target-architecture.md:2
docs/adr/0002-money-rate-basis.md:1
docs/adr/0003-contract-transport.md:2
docs/adr/0004-persistence-and-events.md:2
docs/adr/0005-domain-events-and-outbox.md:2
docs/adr/0006-gradle-modules.md:2
docs/adr/0007-test-pyramid-and-ratchet.md:2
docs/adr/0008-frontend-disposition.md:2
docs/adr/0009-ml-reuse-provenance.md:2
```

**`0002`가 1인 것은 그 ADR의 §5 제목이 「이 ADR이 선점하지 않는 활성 `OPEN`」이라
위 패턴과 다르기 때문**이다. 그 절은 존재하며, 이 검사는 **제목 문자열을 세지 절의
존재를 세지 않는다.**

---

## 8. 리뷰 요청 조건 (`CLAUDE.md`)

| 조건 | 상태 |
| --- | --- |
| 구현 diff가 커밋되어 base/head 고정 | base `998dc21`. head는 `scope.md` 머리 yaml |
| 관련 test/lint/type/architecture/contract 명령 통과 | **N/A — 문서 slice.** 애플리케이션 코드가 없다. 대신 A1~A8을 `commands.md`가 재현한다 |
| 변경된 fixture와 정책 version의 근거 기록 | **N/A** — fixture를 만들거나 바꾸지 않았다 |
| 알려진 제한 기록 | `scope.md` 「알려진 제한」 + 각 ADR 「확인하지 않은 것」 |
| rollback 또는 비활성화 방법 | `scope.md` 머리 yaml — 문서 산출물이므로 `git revert` |
