# Slice 계약 — M0 / 0C 도메인 명세와 데이터 사전

```yaml
milestone: m0
slice: 0c-data-dictionary
base_sha: 2b05684d0e2ed8bac391d0eea4429e05f582a47b   # 착수 시점의 HEAD
review_base: aff62abfca85ca873932fedcad1e65c29ab35730   # 이 slice 첫 커밋의 부모. 아래 「head_sha와 range」
head_sha: 159f69c   # 이 파일을 쓴 가장 최근 커밋 앞의 가장 최근 **0C** 커밋. 「head_sha와 range」 절 참조
in_scope:
  - docs/discovery/data-dictionary.md   # 신설
  - reports/evidence/m0/0c/             # 이 패키지 (decisions-2026-08-28.md 포함)
out_of_scope:
  - docs/discovery/capability-map.md      # 0A/0A2/0A3 Codex approve로 확정 — 읽기 전용
  - docs/discovery/regression-ledger.md   # 0B Codex approve로 확정 — 읽기 전용
  - docs/adr/                             # 0D 소관. 이 slice와 동시에 작업 중이다
  - reports/evidence/m0/0a/ · 0a2/ · 0a3/ · 0b/ · 0d/   # 다른 slice의 evidence — 읽기 전용
  - docs/discovery/legacy-reference-map.md # 존재하지 않는다. 생성 여부는 ADR 0009의 OPEN
  - fixtures/                             # fixture-curator 소유
  - 활성 OPEN — 해소하지 않는다
      # capability-map.md §12 활성분 + regression-ledger.md §9 OPEN-REG.
      # 예외는 운영자 결정 2026-08-28이 이미 닫은 네 건이며, 그것도 「새로 결정」이
      # 아니라 「결정을 근거와 함께 등재」로만 닫는다 — 아래 「이 slice가 닫는 것」.
  - Kotlin/Spring/Python 애플리케이션 코드   # M0은 문서 slice다
  - .claude/ 하네스
  - bid-vector/ symlink 아래 기존 저장소 수정 (읽기 전용)
acceptance_commands:
  - "N/A — 문서 slice. 아래 A1~A8을 checklist.md로 대조"
rollback: "N/A — 문서 산출물은 git revert로 복구. 애플리케이션 코드·설정·스키마 변경이 없다"
```

작성: 2026-08-28, spec-writer (v2-slice-pipeline).

---

## 이 slice가 만드는 것

`milestone-0.md` §"Slice 0C"가 요구하는 **6축**을 덮는 데이터 사전 하나
(`docs/discovery/data-dictionary.md`)와 그 evidence 패키지.

| 축 | `milestone-0.md`의 문구 | 사전의 절 |
| --- | --- | --- |
| 1 | 용어, 단위, basis, nullable 의미 | §1 |
| 2 | aggregate와 상태 전이 | §2 |
| 3 | rule의 입력/출력/reason code | §3 |
| 4 | 정책 version과 effective date | §4 |
| 5 | canonical KONEPS fact와 derived fact 구분 | §5 |
| 6 | ML feature와 업무 판단의 경계 | §6 |

**같은 문서의 금지 사항도 계약이다** — *"모호한 항목은 기존 Python 구현을 정답으로 채우지
말고 `OPEN`으로 남겨 사용자 결정을 요청한다"*. 이 slice의 `OPEN`은 사전 §9에
`OPEN-DIC-NN`으로 등록한다.

### 입력

| 입력 | 위치 | 성격 |
| --- | --- | --- |
| 선행 조사 | `_workspace/m0-0c/01_scout_data_dictionary_preflight.md` | 축별 47항목. **정의를 확정하지 않는 조사 노트** |
| 계약 초안 | `_workspace/m0-0c/00_draft_scope.md` | 이 파일이 대체한다 |
| 운영자 결정 (2026-08-26) | `reports/evidence/m0/0a2/decisions.md` | 커밋됨 |
| 운영자 결정 (2026-08-28, U-1~U-10) | `reports/evidence/m0/0c/decisions-2026-08-28.md` | **이 slice가 커밋한 사본** — 아래 참조 |
| 확정된 상류 산출물 | `docs/discovery/capability-map.md` · `regression-ledger.md` | 읽기 전용 |

**2026-08-28 결정을 evidence로 커밋한 이유**: 원본이 `_workspace/`에 있고 그 디렉터리는
gitignore 대상이라(`.gitignore` 2행) **Codex 리뷰어의 clean worktree에서 재현되지 않는다.**
A3("결정을 재조사하지 않고 인용한다")이 확인 불가능해지므로, **0A2가 2026-08-26 묶음에
대해 한 것과 같은 처리**를 한다 — 원본 절을 문장 단위로 그대로 옮긴 사본을
`reports/evidence/m0/0c/decisions-2026-08-28.md`로 커밋한다. 원본과의 일치는
`commands.md` **C-1**이 대조한다.

---

## `head_sha`와 range

`head_sha`의 정본은 이 파일 머리의 yaml 하나다 — 값을 두 곳에 두지 않는다.

- 이 파일은 **자기 커밋의 diff를 서술하지 않는다.** 커밋이 자기 SHA를 담을 수 없으므로
  yaml의 `head_sha`는 **이 파일을 쓴 가장 최근 커밋 앞의 가장 최근 0C 커밋**을 가리킨다.
  **「직전 커밋」이라 적지 않는다** — 0D가 같은 브랜치에 올리므로 부모가 0D 커밋일 수
  있고, 그러면 그 값이 이 slice의 head가 아니게 된다(수정 라운드 6에서 실제로 그랬다).
  **어느 커밋이 이 slice의 것인지는 `commands.md` C-2.1이 낸다** — 여기 세지 않는다.
  **같은 이유로 라운드 이력 표에 「이 커밋」 행을 두지 않는다** — 이력 표의 행은
  **이미 커밋된 SHA만** 지목하고, 이력 커밋 자신의 행은 **다음 커밋이 세운다.**
  「이 커밋」 행을 두면 그 행이 서술하는 diff를 확인할 수 없고, 실제로 한 번
  **한 커밋이 하지 않은 일을 적는 거짓 서술**이 됐다(Codex 수정 라운드 1).
  **그 뒤에 오는 커밋은 이 값을 갱신하지 않는다** — 뒤 커밋의 경로가 전부 `in_scope`
  안이라 "`in_scope` 밖 경로가 없다"는 판정이 낡지 않기 때문이다.
- **0D가 이 slice와 같은 시각에 작업했고 실제로 섞였다.** `base_sha`는 착수 시점
  HEAD이지만 착수 후 이 slice가 첫 커밋을 올리기 전에 **0D가 세 커밋을 올렸다.**
  그래서 `base_sha..HEAD`에는 0D의 커밋이 들어 있다.
  - **`review_base`는 이 slice 첫 커밋의 부모**이며 **리뷰 range는 `review_base...HEAD`**다.
  - **그 range에도 0D의 커밋이 들어 있다.** 두 레인이 **같은 브랜치를 공유**하므로 이
    slice가 커밋한 뒤에도 0D가 계속 올린다. **`review_base` 이후로 0C 커밋만 남는다는
    뜻이 아니다** — `review_base`가 하는 일은 **이 slice의 첫 커밋 앞을 잘라내는 것**뿐이다.
  - **그래서 `in_scope` 확인은 range가 아니라 경로 필터로 한다:**
    `git diff --name-only <base>...<0C head> -- docs/discovery/data-dictionary.md reports/evidence/m0/0c/`
    또는 `commands.md` **C-2.1**(이 slice의 커밋을 고른 뒤 그 커밋이 건드린 경로 전부를 본다).
  - `base_sha`는 **계약이 고정된 시점**의 기록으로 남긴다 — 두 값이 다르다는 사실 자체가
    병행 작업의 증적이다.
  - 이 slice의 커밋 목록은
    `git log --format='%h %s' <base_sha>..HEAD -- docs/discovery/data-dictionary.md reports/evidence/m0/0c/`
    가 낸다.
- `in_scope` 밖 경로를 이 slice가 건드리지 않았음은 `commands.md` **C-2**가 낸다.
  그 검사는 **이 slice의 커밋 집합을 먼저 고르고 그 커밋이 건드린 경로 전부**를 보므로
  0D의 커밋을 판정하지 않으면서도 혼합 커밋을 놓치지 않는다.
- Codex 리뷰의 `reviewed_head`는 리뷰 요청 시점의 HEAD이며 이 값과 다를 수 있다.

---

## acceptance — A1~A8

| # | 요구 | 출처 | 대조 |
| --- | --- | --- | --- |
| **A1** | `milestone-0.md` §"Slice 0C"의 **6축 전부**를 덮는다 | `milestone-0.md` | `checklist.md` A1 · `commands.md` **C-3** |
| **A2** | **모든 도메인 숫자의 unit/basis/provenance가 정의되거나 `OPEN`**이다. 정의 없는 숫자를 남기지 않는다 | `milestone-0.md` 「완료 조건」 | `checklist.md` A2 · `commands.md` **C-4** |
| **A3** | 확정된 운영자 결정을 **재조사·재확정하지 않고 인용**한다 | 팀 리드 지시, `decisions-2026-08-28.md` | `checklist.md` A3 · `commands.md` **C-1** · **C-5** |
| **A4** | **모호한 것을 legacy로 채우지 않는다** | `milestone-0.md` §"Slice 0C" | `checklist.md` A4 |
| **A5** | **계열 A 금지**(형태 7) — 활성 `OPEN`이 소유한 쟁점을 확정 서술·정의로 선점하지 않는다 | `0a2/checklist.md` §10.1, 0B가 세운 형태 7 | `checklist.md` A5 · `commands.md` **C-6** |
| **A6** | 모든 legacy 인용이 **`ed4b06c`**에서 확인된다. 행 범위는 **블록 경계까지**. **파일명만 쓰지 않는다** | 0B ledger §0.3, 0A3 형태 2 | `commands.md` **C-7** |
| **A7** | 0C가 발견한 미결은 사전 §9에 **`OPEN-DIC-NN`**으로 등록한다. `capability-map.md` §12와 ledger `OPEN-REG`는 **out_of_scope** | 팀 리드 지시 | `checklist.md` A7 · `commands.md` **C-6** |
| **A8** | 불변: 인용 형식 위반 0 · 중복 id 0 · secret 스캔 통과 · `git diff --check` 0 | agent-workflow §6, evidence-pack 스킬 | `commands.md` **C-2** · **C-7** · **C-8** |

---

## capability-map 부정확 — 0C가 정본이다 (운영자 승인 2026-08-27)

**운영자가 승인한 정본 교체는 `X-2`~`X-6` 다섯 건이다.** 0A3이 그 목록을 **다섯 행 표**로
고정했다(`reports/evidence/m0/0a3/scope.md`의 「(A) 결정 — X-2~X-6은 0C가 정본이 된다」 절).

**`O-2`는 승인 목록이 아니다.** 0A3의 in_scope 밖이라 고치지 않고 남긴 **out_of_scope
부채**이며, 같은 문서가 *"**X-2~X-6과 O-2는 여전히 out_of_scope**이고 **0C 데이터 사전이
정본**이 된다"*고 인계했다. 사전 §10 표가 두 성격을 칸으로 갈라 적는다.

**`O-1`은 이 목록에 없다 — 0A3에서 닫혔다.** 운영자 결정(2026-08-27, 0A3 수정 라운드 2 /
F-4)으로 `capability-map.md` §13의 해당 행이 **`결정 무관(무조건)`과 `조건부 —
`OPEN-REG-05` 결정에 따라 확정`으로 이미 갈라졌다.** 사전이 fixture 요구를 조건부로
서술하는 것은 정본 교체가 아니라 **정정된 문면·ledger §10.1과 일치하는 서술**이다.

**이 slice는 `capability-map.md`를 고치지 않는다.** 사전 §10이 각 항목에 대해
**"capability-map의 해당 서술과 다르며 0C가 정본이다"**를 근거와 함께 명시한다.
그 목록의 재현은 `commands.md` **C-9**다.

| # | 성격 | capability-map의 서술 | 사전에서의 정본 |
| --- | --- | --- | --- |
| **X-2** | 승인 | DEC-11 3행이 별도 밴드다 | 밴드 인벤토리 (§1.4) |
| **X-3** | 승인 | DEC-11이 밴드를 여섯 행으로 열거하는데 **그 여섯에 없는 밴드가 최소 두 종** | 밴드 인벤토리 (§1.4) |
| **X-4** | 승인 | 투찰율 클램프 출처가 docstring 언급을 가리킨다 | 밴드 인벤토리 (§1.4) |
| **X-5** | 승인 | SET-07 S4가 세 논리합인데 둘로 요약됐다 | "정산됨" 정의 (§2.3) |
| **X-6** | 승인 | DEC-05가 `bid_target.py`를 디렉터리 없이 인용한다 | 인용 규약 (§0.3) · 투찰가 정의 (§1.2) |
| **O-2** | out_of_scope 부채 | §6 NOTI-08 · §7 SET-09 F-4의 **X-1 프레이밍** 두 자리 | 두 금액의 과세 처리 (§1.2) |

**승인된 목록 밖에서 한 건을 더 찾았다.** `capability-map.md` COL-07이 legacy 필드 집합의
크기를 **어림수**로 적고 같은 문장이 파일을 **디렉터리 없이** 인용한다(X-6와 같은 부류).
**고치지 않고** 사전 **§10.1 X-7**에 승인된 다섯 건과 **구별해** 적었으며, 실측은
`commands.md` **C-7.5**가 낸다. 이것은 팀 리드 지시("틀렸음이 드러나면 고치지 말고
보고한다")에 따른 보고 대상이다.

**`O-2`의 처리**: 0A3이 두 자리로 좁혔다 — §6 **NOTI-08**의 투찰률 항목과 §7 **SET-09
F-4**. 층은 **`legacy-behavior`**이고 `OPEN-REG-05` 귀속이 필요했다. **U-1·U-1b가 그
관찰을 설명한다**(VAT 포함/제외 혼용). **차이의 나머지 성분은 이 결정이 정하지 않으므로
사전은 잔차를 정량화하지 않는다.**

---

## 이 slice가 새로 하는 것 — 운영자 결정 2026-08-28이 만든 작업 셋

| # | 작업 | 사전의 자리 |
| --- | --- | --- |
| 1 | **U-2b의 "직전"** — 기준일이 공고일인가 투찰일인가. **사용자 결정이 아니라 규칙 도출**이므로 0C가 규칙을 쓰고 근거를 남긴다 | §1.2 「시공능력평가금액」 |
| 2 | **U-9** — **"무엇을 오염으로 볼 것인가"의 정의**를 새로 쓴다. **"66%"는 옮기지 않는다** | §7 |
| 3 | **U-6 전수 확인** — `DEC-03`·`DEC-04`·`DEC-08`·`SET-06` acceptance가 전부 「수치가 바뀌는 판정」에 들어가는지 확인한다. **안 들어가는 것이 있으면 고치지 말고 보고한다** | §4.2 · `checklist.md` A3 |

---

## 선행 조사가 스스로 밝힌 약한 축 — 이 slice가 채우는 방법

- **축 4(정책 version)** — 5항목 중 실물은 `effective_from` 하나뿐이고 **legacy 조사로
  채워지지 않는다.** U-6·U-6b가 그 자리를 채운다. 사전 §4는 legacy에서 옮긴 것과
  결정에서 온 것을 항목마다 구분해 표시한다.
- **축 2의 상태 전이** — 상태 *어휘*는 풍부하나 **전이 규칙·전이표가 legacy에 없다.**
  전이는 각 service의 대입문으로 흩어져 있다. **전이표는 0C가 새로 쓴다**(사전 §2.2).
  새로 쓴 전이표는 legacy 인용이 아니라 **도메인 명세**이며 그 사실을 절 머리에 적는다.

---

## 이 slice가 닫는 것

운영자 결정 2026-08-28이 이미 답을 정했고, **0C가 근거와 함께 사전에 등재해야 실제로
닫힌다**(결정 로그 자신이 그렇게 적는다). 이 slice는 **새로 결정하지 않는다.**

| OPEN | 어디서 닫히는가 | 결정 |
| --- | --- | --- |
| `OPEN-REG-05` (ledger §9) | 사전 §1.2 | U-1 · U-1b |
| `OPEN-QUAL-10` (capability-map §12) | 사전 §1.2 | U-2 · U-2b |
| `OPEN-QUAL-06` (capability-map §12) | 사전 §3.2 | U-5 |
| `OPEN-QUAL-11` (capability-map §12) | 사전 §3.2 | U-8 |

**등재는 사전 안에서만 한다.** `capability-map.md` §12와 `regression-ledger.md` §9의
**행 자체는 고치지 않는다**(out_of_scope) — 중앙 registry 통합은 별도 slice의 몫이고
ledger §10.3이 그렇게 인계했다.

## 이 slice가 닫지 않는 것

- **활성 `OPEN` 전부.** 특히 사전이 문면에서 마주치는 것들 — `OPEN-QUAL-05`(면허 유효기간,
  U-7이 **알려진 제한**으로 돌렸다) · `OPEN-QUAL-09`(시공능력 미달 처리) ·
  `OPEN-ML-05`(정책 값 경계) · `OPEN-SET-04`(재관측 노출) · `OPEN-SET-05`(재공고 대사 대상
  선택 규칙) · `OPEN-SET-06`(embargo 임계) · `OPEN-SET-10`(시간축 대체 출처) ·
  `OPEN-DEC-07`(마진 재유도) · `OPEN-DEC-10`(예규 구간별 차등) · `OPEN-NUM-01`/`OPEN-REG-04`
  (오염 수치). 사전은 각 자리에서 **어느 OPEN이 그 쟁점을 소유하는지** 밝히고 정의를
  붙이지 않는다.
- **fixture 실물.** 사전은 fixture가 만족해야 할 조건만 적고 `fixtures/manifest.yaml`을
  만들지 않는다(fixture-curator 소유).
- **`capability-map.md`·`regression-ledger.md`의 수정.** 부정확이 드러난 자리도
  고치지 않고 §10에 정본을 적는다.

---

## 알려진 제한

1. **`ed4b06c`는 이 저장소 밖(`bid-vector` symlink)이라 Codex 리뷰어 worktree에서
   재현되지 않는다.** 0B가 세운 두 축(기계=경로·행 범위 존재, 사람=내용 일치)을 그대로
   적용하고 `commands.md` C-7에 방법과 출력을 남긴다. 이 slice가 유일한 확인 지점이다.
2. **0D가 병행 중이다.** `base_sha..HEAD`에 0D 커밋이 섞이므로 이 slice의 판정은 전부
   **경로 한정**으로 낸다. 0D가 `docs/adr/`에 무엇을 쓰든 이 slice는 그것을 인용하지
   않는다 — 사전이 ADR을 가리켜야 하는 자리는 **번호가 아니라 결정 이름**으로 가리킨다.
3. **면허 그룹 의미론이 운영에서 검증된 적이 없다.** U-8이 확정한 의미론의 legacy 모듈은
   **소비자가 없다**고 스스로 적는다. 사전은 이 사실을 한계로 적는다.
4. **`OPEN-QUAL-05`(면허 유효기간)를 U-7이 「다루지 않음」으로 닫았고 그것은 위험을
   남긴다** — 만료된 면허로 자격을 오판정할 수 있다. 사전은 자격 판정 결과에
   "유효기간 미검증"이 드러나도록 요구한다.
5. **정책 version의 날짜 식별(U-6b)은 같은 날 두 번 바뀌는 정책을 구별하지 못한다.**
   사전이 그 한계를 적고, 넓히는 것은 **관측 뒤**로 미룬다.

---

## 라운드 이력

**이 절만이 이 slice의 라운드 이력이다.** `checklist.md`·`commands.md`에는 이력·자기평가
산문을 적지 않는다(0A3의 교훈).

| 커밋 | 무엇을 했는가 |
| --- | --- |
| `8b938d6` | 계약 확정, 2026-08-28 결정 사본, 사전 6축 작성 |
| `6af4179` | 검증 명령·출력(`commands.md`)과 acceptance 대조(`checklist.md`) |
| `f852716` | `head_sha` 기입과 이 이력 절 |
| `c3020e8` | **A8이 잡은 것을 고쳤다** — `C-6.2`의 기록된 출력에 후행 공백이 있어 `git diff --check`가 지적했다. **산문으로 예외를 두지 않고 명령 자체를 바꿔** 옳은 문자열을 내게 했다 |
| `b833fee` | 위 두 줄의 등재와 `head_sha` 갱신 |

### 수정 라운드 1 — 독립 검증 finding (blocker 1 · high 2 · medium 2 · low 3)

| 커밋 | 무엇을 고쳤는가 |
| --- | --- |
| `cd1fbce` | **문면·셈·귀속** — `F-2`(정본 목록의 범위: 승인은 `X-2`~`X-6` 다섯, `O-2`는 out_of_scope 부채, **`O-1`은 0A3에서 닫혔으므로 표에서 뺐다**) · `F-5`(`X-3`의 귀속을 DEC-11 문면에 맞춤) · `F-4`(§4.3의 셈을 지우고 표를 정본으로) · `F-6`(인용 귀속) · `F-7`(§12 provenance 두 자리) · `F-8`(`commands.md`에서 셈 제거). 게이트 밖 셋도 함께 |
| `1e9e670` | **`F-3`의 뿌리와 `F-1`의 파급** — C-5의 `awk substr`가 **바이트 절단**이라 한글을 깨뜨려 stdout을 그대로 옮길 수 없었다. 문자 단위로 자르는 python으로 바꿨다. 사전의 두 전칭을 **명령이 실제로 내는 범위**에 맞췄다 |
| `c81c8a1` | **`F-1`·`F-3` 본체(1차)** — 출력 블록 **전부**를 `1e9e670` 트리(worktree + `bid-vector`·`_workspace` symlink)에서 `bash -c`로 다시 돌려 **stdout만** 받아 실었다 |
| `6eba815` | **C-10 신설** — 축어 재현을 재는 검사 |
| `5e2c67e` | **A8이 다시 잡았다** — C-5의 자른 끝이 공백이면 기록된 출력에 후행 공백이 남는다. `C-6.2` 때와 같이 **명령을 고쳐** 옳은 문자열을 내게 했다 |
| `989e2c1` | **C-2.2도 같은 형태였다** — `git diff --check`의 지적 줄을 그대로 실으면 이 파일이 다시 후행 공백을 갖는다. **표지로 바꿔 싣도록 명령을 고치고** 출력 블록을 `5e2c67e` 트리에서 **다시 떴다.** 그러면서 **C-10 절을 통째로 지웠고 다음 커밋이 다시 넣었다** — C-10이 *"대상 파일에 이 절이 없다"*고 적을 수 있는 것은 **그 삭제의 결과**다 |
| `5a9a5f5` | C-10을 새 두 SHA로 다시 재고 이력을 기입 |

### 수정 라운드 2 — 독립 검증 finding (high 1 · low 4)

| 커밋 | 무엇을 고쳤는가 |
| --- | --- |
| `ba23629` | **문면과 귀속** — `F-10`(「그 파일은 지금 무조건을 요구하지 않는다」의 주어가 좁혀지지 않았다. `capability-map.md` §13의 그 행이 **무조건으로 요구하는 것은 basis 태그가 다른 값 쌍**이고 `O-1`이 걸었던 경계 쌍만 조건부로 갔다) · `F-11`(`commands.md` 머리의 자기 선언을 실제 형태로) · `F-12`(이 절의 셈을 걷어냄) · `F-13`(A8의 귀속·시점을 고치고 **C-2.3 신설**). **`F-9` 준비** — 머리의 「C-10은 그래서 …」 해법 주장을 걷고 C-10 절을 들어냈다 |
| `9d8d206` | 문면 수정이 트리를 바꿨으므로 **출력 블록을 `ba23629` 트리에서 다시 떴다** |
| `b701d1b` | **`F-9` 본체 — C-10을 두 환경에서 실행으로 잰다.** 「어느 트리에서 돌려도 같은 답이 나온다」는 거짓이었다. `bid-vector`·`_workspace`가 **git 미추적이라 SHA에서 복원되지 않고 실행 CWD에서 온다.** 그 사실을 주장으로 적지 않고 **C-10.1(두 경로 있음) · C-10.2(없는 clean worktree)** 두 블록으로 **재서** 싣는다. 이 이력 절과 `head_sha`도 이 커밋이다. **이 커밋에 대해서는 아무것도 주장하지 않는다** |

**`F-9`가 드러낸 것은 셈이 아니라 적용 범위였다** — C-10의 판정 자체는 정직했고
거짓은 그 판정이 **어디까지 통하는가**였다. 그래서 판정 어휘를 셋으로 갈랐다 —
`PASS` · `INCOMPLETE`(재지 못한 블록이 있다) · `FAIL`. **잴 수 없는 것을 불일치로 세지
않는다.**

### 수정 라운드 3 — 독립 검증 finding (medium 1 · low 4)

| 커밋 | 무엇을 고쳤는가 |
| --- | --- |
| `19c7e57` | **시점·전칭·집합·셈** — `F-14`(**C-2.3이 선언한 SHA가 조상이라 지적이 생기기 전 시점**이었고 「사라진 시점의 출력」이 아니었다) · `F-16`(그 블록만 다른 SHA를 선언해 머리의 전칭을 깼다) — **두 블록을 `C-2.2` 하나로 합쳐 선언 SHA를 공통으로 되돌리고 `base_sha`·`review_base` 두 기준을 함께 내게 했다.** `F-15`(머리에서 걷어낸 선언이 `C-6.3` 각주에 남아 있었다 — 전수로 훑어 고쳤다) · `F-17`(`C-10.2`가 재지 못한 블록을 한 갈래로 합쳐 적었다 — **셋으로 갈랐다**) · `F-18`(둘째 스캔 출력의 셈을 지웠다) |
| `9578308` | **출력 블록을 `19c7e57` 트리에서 다시 뜨고 `C-10`을 자기 참조 없이 다시 짰다** — 대상 파일을 **이 파일 자신**으로 두고 **자기 절은 표지로 걸러 뺀다.** 두 환경 측정은 그대로다 |
| `642e2ab` | **`F-15` 전수 훑기의 나머지** — 머리의 「셈을 산문으로 옮겨 적지 않는다」와 어긋나는 자리가 `commands.md` 산문에 남아 있었다. 그 셈을 걷고 머리의 규칙을 **실제 의도**(「명령이 내는 셈을 산문에 옮겨 적지 않는다」)로 좁혔다 |
| `66b986d` | 이력 등재와 `head_sha` |
| `993bd75` | **`C-10.2`의 블록이 「명령 자리에 선 산문」이었다** — `cp <위 블록 그대로>`는 그대로 실행되지 않는다. `C-10.1`의 본문을 이 파일에서 뽑아 clean worktree에서 돌리는 **실제 명령**으로 바꾸고 두 출력을 다시 떴다. **이 검사를 어느 체크아웃에서 돌려야 하는지**도 절 머리에 적었다 |
| `64aecc7` | 위 두 줄의 등재와 `head_sha` |

### 수정 라운드 4 — 독립 검증 finding (medium 2 · low 2)

**넷이 전부 한 뿌리였다 — `C-10`을 고치면서 그 검사를 설명하는 산문이 안 따라갔다.**
`F-15`(라운드 3)와 같은 형태이고 **이 slice에서 네 번째**다.

| 커밋 | 무엇을 고쳤는가 |
| --- | --- |
| `37c9905` | **`C-10`을 설명하는 자리를 전수로 찾아 한 번에 맞췄다.** `F-19`(머리의 「대상 파일과 실행 트리 두 SHA를 모두 명시한다」는 `9578308`이 걷어낸 구조다) · `F-20`(`scope.md`의 낡은 재서술 — **동결 이력이 아니라 현재 선언이었다.** ⚠ 후속 정정을 달아 라운드 2 시점의 서술임을 밝히고 현재 선언을 가리키게 했다) · `F-21`(자기제외 근거가 틀렸다 — `19c7e57` 트리에 `## C-10` 절은 **있고** 없는 것은 **표지**다) · `F-22`(EOF 빈 줄 지적은 **명령 수정이 아니라 절 재삽입**으로 닫혔다 — 「그때마다 명령을 고쳤다」를 걷고 커밋 표와 한 목소리로) |
| `57dc5ae` | 산문 수정이 트리를 바꿨으므로 **출력 블록 전부를 `37c9905` 트리에서 다시 떴다.** **`C-10` 자신도 다시 쟀다** — `C-10.1`은 두 경로가 있는 저장소 루트에서, `C-10.2`는 없는 clean worktree에서 |
| `9112236` | 이력 등재와 `head_sha` |
| `d879b29` | **`F-21` 자리가 선언 SHA 교체에 다시 낡았다** — 「그 트리에는 표지가 없다」는 `19c7e57`에는 참이었으나 `37c9905`에는 거짓이다(그 커밋에는 표지가 있다). **특정 트리의 상태를 근거로 삼으면 선언 SHA를 옮길 때마다 낡으므로** 구성상 참인 서술로 바꿨다 — 이 검사는 **대상 파일을 CWD에서 읽고 실행 트리만 SHA로 고정**하므로 두 쪽의 기록이 어긋나면 다른 답이 나온다 |
| `227ba0c` | 위 두 줄의 등재와 `head_sha` |

### Codex 수정 라운드 1 — `request_changes` (high 2 · medium 3)

| 커밋 | 무엇을 고쳤는가 |
| --- | --- |
| `c0fe44f` | **`A`(high)** — A2의 숫자 인덱스가 **리터럴만 검사**해 numeric field를 못 봤다. **§12.2「타입이 나르는 필드」를 신설**해 타입 선언과 필드 표가 낸 필드를 등재하고(수인 것에는 단위·basis·provenance, 수가 아닌 것에는 그 사실) **C-4를 C-4.1·C-4.2로 갈랐다.** 그 과정에서 **§6.5의 셋째 성분이 축을 잘못 가리킨 것**을 찾아 고쳤다 — legacy의 `margin`은 하한까지의 거리가 아니라 **신뢰구간 반폭**이다. **`B`(high)** — `v2-지침서.md` §4.3이 **승인한 라벨 다섯**을 바꿨다. **승인 집합으로 되돌리고** 겹치지 않는 두 자리를 **`OPEN-DIC-05`**로 등록했다. **`C`** — 이 파일의 range 자기서술이 거짓이었다(아래 「`head_sha`와 range」). **`D`** — `C-10`이 worktree를 남겼다. **trap**으로 실패 시에도 정리되게 하고 잔존분을 제거했다. **`E`** — 결정 사본의 U-3 소유 `OPEN` 오기를 **취소선과 ⚠ 정정**으로 남겼다(결정 기록이므로 지우지 않는다) |
| `c6db97e` · `87ad2d9` · `aa05167` | 산출물 변경이 트리를 바꿔 **출력 블록을 다시 떴고**, 그 과정에서 **기록된 출력을 손으로 고친 것을 되돌렸다** — `C-1`의 diff가 낸 빈 줄이 후행 공백을 나르므로 **명령을 고쳐** 표지로 싣는다 |
| `295a6cf` | **출력 블록 전부를 선언 SHA `aa05167` 트리에서 다시 떴다** |
| `03072ce` | 이 라운드의 이력 등재와 `head_sha`. **`scope.md`만 건드렸다** |

**`E`는 팀 리드가 결정표에 적은 오기였다** — 이 slice가 그대로 옮겨 담았다.
**전수로 훑은 결과 틀린 자리는 결정 사본의 U-3 요약 행 하나뿐**이고, 같은 파일의 U-3
본문과 `scope.md`·`checklist.md`·사전은 모두 옳은 id를 가리킨다.

**`B`에서 택한 길**: **승인 명세를 따른다.** `v2-지침서.md`는 승인 문서이고 out_of_scope이며,
`OPEN-DEC-08`은 라벨 집합 변경을 승인하지 않았다. **legacy 실측이 겹치지 않는다는 사실은
근거와 함께 적되 집합은 바꾸지 않고 `OPEN-DIC-05`로 넘긴다.**

**같은 형태가 네 번 난 뒤에 구조로 막았다** — **검사의 형태·전제·자기제외 기제를 선언하는
자리를 `commands.md`의 `C-10` 절 머리 하나로 두고, 다른 파일은 가리키기만 한다.**
서술을 여러 곳에 복제하면 고칠 때마다 형제가 남는다는 것이 이 slice가 네 라운드에 걸쳐
배운 것이다.

> **⚠ 후속 정정** — 위 「같은 형태가 네 번 난 뒤에 구조로 막았다」는 **`C-10`을 설명하는
> 자리에만 통했다.** 아래 라운드의 **`V-6`이 다섯 번째**이며 그 대상은 `C-10`이 아니라
> **사전 §0.4의 문장**이었다. **한 자리로 모으는 것은 그 자리를 가리키는 문서에만 통한다** —
> 그래서 아래 라운드는 **정정할 때 그 명제를 적은 자리를 전수로 훑는 것**을 함께 했다.

**`F-14`가 새 규칙 하나를 세웠다** — **SHA를 시점 주장에 쓸 때는 선후를 재고 나서 적는다.**
`git merge-base --is-ancestor`로 확인한다. 이번엔 아예 **시점 주장을 없앴다** — `C-2.2`는
**선언 SHA 하나만** 재고, 지적이 있었던 이력은 **이 표**가 갖는다.

**`C-10`의 두 SHA 구조도 함께 걷었다** — 대상 파일을 SHA로 고정하면 **그 SHA를 만든
커밋이 무엇을 지웠는지**까지 따라 적어야 했다(라운드 2의 「우연이 아니라 삭제의 결과」).
**자기 절을 표지로 빼는 쪽이 그 부채를 만들지 않는다.**

**`F-15`는 「한 자리만 고치고 형제를 남긴」 형태였다** — 그래서 이번엔 **자기 선언을
전수로 훑고** 고쳤다. 훑은 자리와 결과는 `_workspace/m0-0c/08_fix_r3.md`에 있고,
재현 명령은 그 문서가 싣는다.

### 병행 레인과 인덱스 경합 (이 라운드에 실제로 일어났다)

**0D 레인의 커밋 하나가 이 slice의 스테이징된 파일 셋을 함께 담았다.** git 인덱스가
두 레인에 공유되기 때문이다. **0D가 그 커밋을 다시 써서 되돌렸고**(현재 그 커밋은
`docs/adr/`만 담는다) 이 slice는 자기 파일을 **별도 커밋**으로 올렸다.
`commands.md` **C-2.1**이 이 slice의 커밋만 골라 그 커밋이 건드린 경로 전부를 내므로
**혼합이 남았다면 그 블록이 드러낸다.**

**같은 형태가 되풀이됐다** — 기록된 출력이 후행 공백을 나르면 그 파일이 다시
`git diff --check`에 걸린다. 걸린 자리(`C-6.2` · `C-5` · `C-2.2`)마다 **산문으로 예외를
두지 않고 명령을 고쳤다.**

**어디까지 쟀는지만 적는다** — C-10은 **그 절이 선언한 대상·실행 트리와 그 절이 싣는 두
환경**에 대해 잰 것이고, 다른 트리·다른 환경에서 재현된다고 주장하지 않는다.

> **⚠ 후속 정정** — 이 문단은 라운드 2 시점에 **대상 파일 `9d8d206` · 실행 트리
> `ba23629`**를 적고 자기제외를 *"대상 파일에 그 절이 없다"*로 설명했다. **라운드 3의
> `9578308`이 그 두 SHA 구조를 걷어냈고**(대상 파일 = 이 파일 자신, 자기제외 = `SELF-EXCLUDE`
> 표지) 그래서 그 서술은 더는 성립하지 않는다. **검사의 형태를 선언하는 자리는
> `commands.md`의 C-10 절 머리 하나**이며, 이 문단은 그것을 가리킨다.

**커밋을 나눈 이유는 각 커밋에서 산출물이 자체 정합하기 때문이다** — 첫 커밋의 사전은
자기 안에서 정합하고, 둘째 커밋의 evidence는 첫 커밋의 트리를 선언해 그 트리에서
재현된다.

**파일 목록도 셈도 이 절에 옮기지 않는다** — `git show --stat --format='' <SHA>`와
`git log --format='%h %s' <review_base>..HEAD`가 낸다.

### 이 slice에서 실행이 한 번 끊겼다

플랫폼 오류로 `8b938d6` 직후에 실행이 중단됐고 작업 트리에 미완(`scope.md` 수정분,
untracked `commands.md`)이 남았다. **재개 시 `git status`·`git log`로 실측을 떠서 확인한
뒤 이어 썼다.** 산출물의 내용에 그 중단이 남긴 흔적은 없다 — `commands.md`의 모든 블록을
재개 후 다시 돌려 출력을 맞췄다.

### 불변 — 이 절은 수도 좌표도 옮겨 적지 않는다

| 확인할 것 | 명령 |
| --- | --- |
| 이 slice의 커밋 목록 | `git log --format='%h %s' aff62ab..HEAD -- docs/discovery/data-dictionary.md reports/evidence/m0/0c/` |
| **기록된 출력이 실제 stdout인가** | `commands.md` **C-10** (두 환경) |
| `in_scope` 밖 경로 · 공백 오류 | `commands.md` **C-2** |
| 상류 산출물·0D 산출물 무변경 | `commands.md` **C-6.1** |
| 6축 커버 · 도메인 숫자 전수 | `commands.md` **C-3** · **C-4** |
| legacy 인용 | `commands.md` **C-7** |
| 각 블록이 자기 선언 SHA에서 재현되는가 | 그 SHA를 체크아웃한 worktree에서 그 절의 명령을 돌린다 |

**결과를 여기 적지 않는다.**

### 수정 라운드 5 — Codex 수정 라운드 1의 독립 검증 finding (high 1 · medium 3 · low 3)

**Codex finding 다섯(A~E)은 전부 닫힌 것으로 재실측됐다.** 아래는 **닫으면서 새로 생기거나
남은 거짓 서술**을 고친 것이다.

| 커밋 | 무엇을 고쳤는가 |
| --- | --- |
| `33f05f5` | **`V-4`** — 이 slice의 핵심 정정(`estimateMargin`)에 붙은 유일한 legacy 좌표 `app/ai/predictors/historical/statistics.py:49-55`에 **그 산식이 없었다**(그 자리는 `estimate_historical_confidence`이고 `margin`은 **입력 인자**다). 산식이 있는 `app/ai/predictors/historical/__init__.py:294-308`과 세 값이 함께 들어가는 호출부 `:371-375`를 붙이고, `statistics.py:49-55`가 무엇인지 명시했다. **정정의 축 판정은 유지된다** — 틀린 것은 근거 좌표였다. A6의 「직접 연 파일」 목록에 그 파일을 더했다 |
| `494b5b7` | **`V-7`** — `margin` = `t값 × (표준편차 ÷ √표본수)`는 legacy의 **`sample_size > 1` 분기뿐**이다. 표본이 하나면 `margin`은 `std_rate` 자체이고 그 `std_rate`도 같은 자리에서 다시 잡힌다. 분기를 적고 **V2 승계 여부는 정하지 않았다**(그 분기의 계수가 §12.1에 없다) |
| `685fe08` | **`V-2`** — `C-4.2`가 **자기 사각지대를 산문으로 적었고 그 목록이 실제보다 좁았다.** 두 갈래가 더 있고 **이 문서 안에서 실증된다**: **중첩 괄호**(`EffectiveDatedPolicy<T>(entries: …)`에서 **아무 이름도 안 나온다**)와 **블록인용 줄바꿈**(`KonepsFieldContract`에서 **한 이름만 조용히 빠진다**). 추출기를 **균형 괄호 + 줄바꿈 정규화**로 고치고 **본문의 단독 이름**을 세 번째 자리로 더했다. **사각지대 목록과 셈을 산문에서 걷고 그 블록이 같은 실행에서 이름째 내게 했다** — 「덮개 수가 뽑은 이름 수보다 큰 것은 손 등재 때문」이라는 셈 설명은 거짓이었고 지목한 `review_required`는 **덮개에도 없었다.** 같은 명제의 세 사본을 한 번에 고쳤다 |
| `d75f406` | **`V-1`(high)** — §12.2의 전칭 *"이 문서가 선언한 **모든** 타입의 **모든** 필드"*가 거짓이었다(`entries`·`isAuthoritative` 미등재). **원인은 셈이 아니라 순서다** — 고쳐지기 전 `C-4.2`가 그 둘을 **보지 못한 채 `PASS`를 냈고 문장이 그 `PASS` 위에 전칭을 얹었다.** Codex finding **A**(evidence의 거짓 완전성 주장)와 **같은 형태가 한 층 위에서 재발한 것**이다. 전칭을 지우고 **범위를 `C-4.2`에 넘겼으며**(세 자리가 낸 이름은 이 표가 덮거나 그 블록이 「필드가 아님」으로 **이름째** 분류한다), 빠진 둘을 등재했다. 같은 전칭의 사본 둘(`checklist.md` A2 · 이 파일의 `c0fe44f` 행)도 함께 고쳤다 |
| `3e9feb2` | **`V-5`** — `C-4.1`의 자기서술(「사전 §12.1의 값 칸을 정본으로」)이 구현(`## 12.`~`## 13.` 전 구간의 표 첫 칸)과 달랐다. 판정은 바뀌지 않지만 **적은 능력과 실제 능력이 달랐다.** 자기서술·스크립트 주석·출력 라벨과 `checklist.md` 사본까지 맞췄다 |
| `384c780` | **`V-6`** — 사전 §0.4의 정정이 `checklist.md`의 같은 문장에 전파되지 않았다. **「한 자리만 고치고 형제를 남긴 형태」가 이 slice에서 다섯 번째**다. 게이트 밖이던 「`§12.2`가 두 뜻」도 수식어로 닫았다 |
| `446a39f` | **`V-3`** — 라운드 이력의 「이 커밋」 행이 **그 커밋이 하지 않은 일**(출력 블록 재취득)을 적었다. `295a6cf`·`03072ce` 두 SHA 행으로 가르고, **이력 표에 「이 커밋」 행을 두지 않는다**는 규약을 「`head_sha`와 range」에 적었다 |
| `b0d8b42` | **출력 블록 전부를 선언 SHA `446a39f` 트리에서 다시 떴다.** 두 번 돌려 같은 결과를 확인했다 |

**`V-1`과 `V-2`는 한 뿌리다 — 덮개가 못 보는 것을 재기 전에 전칭을 썼다.** 그래서 빠진 둘을
등재하는 것으로 닫지 않고 **범위를 명령에 넘기고, 그 명령이 자기가 못 본 자리를 같은
실행에서 이름째 내게** 했다. *"명령이 내는 셈을 산문에 옮겨 적지 않는다"*는 이 slice의
규율을 **사각지대 서술에까지** 밀어붙인 것이 이번 수정의 형태다.

### 수정 라운드 6 — 수정 라운드 5의 독립 검증 finding (high 1 · medium 3 · low 2)

**`V-1`~`V-7` 일곱은 전부 닫힌 것으로 재실측됐다.** 아래는 **닫으면서 새로 생기거나 남은
거짓 서술**을 고친 것이다.

| 커밋 | 무엇을 고쳤는가 |
| --- | --- |
| `b469512` | **`F-1`(high)** — `C-4.2`의 `decls()`와 `unread` 카운터가 **같은 `\b[A-Z]` 앵커를 공유**했다. **못 읽는 자리와 못 읽었다고 세는 자리가 같으면 구조적으로 자기를 못 본다** — 사전의 `_FloorSchedule(effective_from, brackets)`(legacy frozen dataclass 축어 인용)가 네 사각지대 줄 어디에도 나오지 않았고 `이름 규칙 밖 인자`는 실제의 일부만 셌다. 사각지대 앵커를 **대문자를 전제하지 않는 형태**로 따로 세우고 선언 앵커를 밑줄 선두까지 넓혔다. legacy 축어 인용의 인자를 전용 갈래로 갈라 이름째 내고, 함수·술어 호출은 `NOT_A_DECL` 목록으로 가른다. **`선언 앵커 ⊆ 사각지대 앵커`도 산문이 아니라 블록이 실행으로 재고 깨지면 `FAIL`이다.** 같은 명제의 사본 넷을 한 번에 고쳤다 |
| `2e70f86` | **`F-3`** — *"`이름 규칙 밖 인자`가 이름째 낸다"*고 적었으나 그 줄은 `stray`가 비면 인쇄가 통째로 꺼져 **이름을 하나도 내지 않았다.** 「이름째 낸다」가 설계 의도이므로 **산문이 아니라 명령을 고쳤다** — 조건 없이 전수 인쇄한다. 그래야 새 이름이 셈에만 얹혀 조용히 빠지지 않는다 |
| `5f0870e` | **`F-2`** — `이름 없이 타입만 적힌 인자: 13 — [여덟]`에서 **셈은 인자 수, 괄호 안은 그 인자를 담은 선언 이름**이라 한 줄이 두 가지를 셌다. 자리마다 `선언(타입)`으로 전수 열거해 **셈과 열거가 같은 것을 세게** 했다. 실질 결함은 아니다 — 열셋 전부 라벨 없는 payload다 |
| `20a72d1` | **`F-4`** — `V-1`이 전칭을 대체한 명제 *"덮개 ∨ 「필드가 아님」, 둘 다 아니면 `FAIL`"*이 **commit 해시 규칙으로 빠지는 이름들에 거짓**이었다. 그 규칙은 `commands.md`가 적었으나 사전의 사본이 빠뜨렸다 — **전칭이 서 있던 그 자리에서 같은 형태가 한 칸 좁아져 재발한 것**이다. 명제를 사전에 두 벌로 두지 않고 **명령이 갈래를 세어 내게** 했다 |
| `98997a3` | **`F-5`** — *"필드가 늘면 그 블록이 낸다"*가 **밑줄 이름에 성립하지 않았다.** 실질 누락은 없고(그 이름들은 전부 legacy) **보장 주장이 거짓**이었다. ③의 이름 규칙 밖을 **더 넓은 규칙으로 재어 블록이 내게** 하고, 사전과 `commands.md`의 보장 문장을 걷었다 |
| `be4d1a0` | **`F-6`** — §12.2의 `estimateMargin` 행이 축을 **무조건** 「신뢰구간 반폭」으로 적어 *"표본이 하나면 반폭이 아니고 V2 승계 여부를 정하지 않는다"*는 §6.5와 서로 밀어냈다. 같은 표의 `confidence` 행 관례대로 **분기 단서를 행 안에** 실었다 |
| `d7851ad` | **`F-4`의 다섯 번째 사본** — 같은 이분법이 `checklist.md` A2 요구 칸에도 서 있었다. 요구를 갈래 열거가 아니라 **잔여**로 적어(어느 갈래에도 들지 않은 이름이 남으면 `FAIL`) 갈래가 늘어도 낡지 않게 했다. **한 커밋 늦게 전수 훑기를 지켰다** |
| `159f69c` | **출력 블록 전부를 선언 SHA `d7851ad` 트리에서 다시 떴다.** 두 번 돌려 같은 결과를 확인했다 |

**`F-1`·`F-4`·`F-5`는 한 뿌리다 — 검사가 무엇을 덮는지를 산문이 따로 적었고 그 산문이
명령과 어긋났다.** 계보는 Codex `A`(덮개가 필드를 못 봄) → `V-1`(덮개의 전칭이 거짓) →
`F-1`·`F-4`(사각지대 계측기가 자기 사각지대를 못 보고 대체 명제가 다시 거짓)이며,
**매 라운드 한 층 위에서 같은 것이 났다.** 그래서 이번에는 한 층 더 쌓지 않고 **규칙을
바꿨다**: **덮개 범위·사각지대·「~하면 ~가 낸다」류 보장을 산문에서 걷고, 그 자리에는
명령을 가리키는 한 줄만 둔다.** 명령은 자기 앵커·규칙·빼는 갈래·규칙 밖을 **매 실행마다
스스로 출력한다.** **두 앵커의 관계를 무엇으로 어떻게 재는지의 정본은 `commands.md`
C-4.2 한 자리다** — 이 파일은 옮겨 적지 않는다.
산문에 남긴 것은 명령이 낼 수 없는 것뿐이다 — 왜 그 규칙인가, 무엇을 정하지 않았는가.

**`head_sha` 규약도 이 라운드에서 정밀해졌다.** 0D가 이 slice의 커밋 사이에 실제로 끼어
「직전 커밋」이 0D 커밋이 됐다. 값이 이 slice의 head를 가리키도록 **「직전 0C 커밋」**으로
적고, 어느 커밋이 이 slice의 것인지는 `commands.md` **C-2.1**이 내게 두었다.
