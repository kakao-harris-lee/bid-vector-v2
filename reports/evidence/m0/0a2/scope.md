# Slice 계약 — M0 / 0A2 결정 통합 패스

```yaml
milestone: m0
slice: 0a2-decision-integration
base_sha: 6af7019996ec26903c7b531ed7ede12c22cf176e
head_sha: 3ec50fbe14abd9233aa64f979582dbd5f6bae82e  # 0A2 수정 라운드 7 완결 산출물 커밋. 아래 갱신 이력 참조
in_scope:
  - docs/discovery/capability-map.md
  - reports/evidence/m0/0a2/
  - reports/evidence/m0/0a/commands.md   # H-2 정정 주석 한정. 재작성 금지, append 방식
out_of_scope:
  - Kotlin/Spring/Python 애플리케이션 코드 (M0 금지 사항)
  - 다른 M0 산출물: regression-ledger(0B), data-dictionary(0C), ADR(0D), fixtures/manifest.yaml
  - reports/evidence/m0/0a/ 의 commands.md 외 파일 (0A는 동결. 읽기 전용 참조)
  - .claude/ 하네스
  - 운영 DB query, 실제 외부 API 호출
  - bid-vector/ symlink 아래 기존 저장소 수정 (읽기 전용)
acceptance_commands:
  - "N/A — 문서 slice. 아래 A1~A7을 checklist.md로 대조"
rollback: "N/A — 문서 산출물은 git revert로 복구. 0A head 6af7019로 되돌릴 수 있다"
```

작성: 2026-08-26, v2-slice-pipeline Phase 1

## 배경

0A(capability map)는 `6af7019`에서 동결됐다. Codex 독립 리뷰 3라운드와 verifier 재검증
3라운드를 거치는 동안 **"미해결 OPEN을 확정 서술이 선점한다"는 계열(계열 A)이 네 번
재발**했고, 매 라운드 새 표면을 열 때마다 신규 잔존이 나왔다. 근본 원인은 문장이 아니라
**활성 OPEN 66건 자체**였다 — 미결정 쟁점이 살아 있는 한 어떤 서술이 그중 하나를 무심코
확정할 표면이 계속 남는다.

따라서 인스턴스 추적을 중단하고 결정을 받았다. 운영자가 **29건을 확정**했고, 그 과정에서
**capability 누락 1건**과 **상호작용 모델(pull) 확정**이 나왔다. 이 slice는 그 결정들을
산출물에 한 번에 반영한다.

## acceptance 항목

- **A1.** 확정된 OPEN이 **결정 근거(누가·언제·무엇으로)와 함께** 해소되고, §12 집계와
  본문 인용이 일치한다. 해소된 id는 결번 사유와 함께 §12.1에 기록된다.
- **A2. 미결 OPEN은 하나도 임의 해소되지 않는다.** 결정 기록에 없는 항목을 이 패스가
  닫으면 slice 실패다. 해소 전후 id 집합을 대조해 증명한다.
- **A3.** 신규 capability(운영자가 입찰 목록을 검색하고 투찰가를 요청하는 경로)가
  `V2 필수` 분류·사용자 가치·acceptance scenario·분류 근거를 갖는다.
- **A4.** capability 재분류 5건이 근거와 함께 반영되고 §10 축별 표·집계가 일치한다.
- **A5.** verifier 라운드 6 발견 **M-4·M-5**가 해소되고, **H-2**가 정정된다.
- **A6.** 불변: 분류 줄 형식 위반 0, 중복 capability id 0, `V2 필수` 전건에 사용자 가치와
  acceptance scenario 존재, secret 스캔 통과.
- **A7.** 결정 기록이 `reports/evidence/m0/0a2/decisions.md`로 **커밋된다.**
  `_workspace/`는 gitignore 대상이라 감사 추적이 저장소에 남지 않는다. 결정은 산출물의
  근거이므로 커밋되어야 한다.

## 반영 대상 (운영자 결정, 2026-08-26)

원본: `_workspace/m0-open-decisions/decisions-log.md` (결정 29건, 근거·파급 포함)

1. **M0 차단 8건** — ML-04(정산 관측 시각 canonical fact) · NUM-04 · QUAL-06 · DEC-08 ·
   OPS-07 · ML-01(ML 커널 8개 경계) · OPS-05(브로커 없음) · STR-01(예산 basis = 기초금액)
2. **M1 차단 17건** — QUAL-01/02/08 · COL-02 · DEC-01/02/03/04/05/06/07/09 · SET-02 ·
   STR-03/05/06/07 (+ QUAL-03이 QUAL-01에 따라 닫힘, STR-11이 STR-07에 따라 닫힘)
3. **OPEN-NOTI-02** — at-most-once 확정. `OPEN-STR-08`도 함께 정리
4. **상호작용 모델 pull 확정** — 주기 감시는 제안까지, 투찰가는 on-demand
5. **capability 재분류 5건** — NOTI-06/NOTI-02/NOTI-05 강등 후보, NOTI-10 유지·강화,
   DEC-02 산출 시점 변경
6. **신규 capability** — 목록 검색 + 투찰가 요청 경로

## 주의

- `OPEN-STR-01`은 결정 결과 **`legacy-defect` 판정**이 됐다(운영자 의도는 기초금액인데
  legacy는 추정가격과 비교). 0B regression ledger 인계 항목으로 §13에 기록한다.
- `OPEN-DEC-09`는 **"실행되는 미적용 상태"** 단서가 결정의 일부다. 단서 없이 반영하면
  legacy 결함(선언과 실행의 불일치)을 이식한다.
- ~~`OPEN-DEC-07`·`OPEN-DEC-03`·`OPEN-OPS-07`은 **결정은 확정, 실행 대기**다. 해소로
  기록하되 실행 대기 상태를 명시한다.~~
  > **⚠ 계약 정정 (0A2 라운드 7, Codex 5차 high #3)** — **위 지시는 철회한다.**
  > 세 항목 전부 **종료 조건이 충족되지 않았고**, 그 상태로 해소 계상하면 완료 게이트가
  > 쓰는 OPEN 수를 과소 계상한다. 셋을 분리해 다시 적는다.
  > - **`OPEN-OPS-07`**: 종료 조건은 **ADR 대안 절 기입**이고 그 산출물이 없다.
  >   **ADR 대안 절이 커밋될 때까지 활성·미해소**다(라운드 5에 복원). ADR 작성은
  >   **0D 소관이며 이 slice 범위 밖**이다.
  > - **`OPEN-DEC-03`**: **잠정**이며 예규 원문 확인 후 **재결정 대상**이다. 최종 결정이
  >   없으므로 **활성·미해소**다(라운드 7에 복원). M1 차단 집계에도 포함된다.
  > - **`OPEN-DEC-07`**: 결정은 확정이나 **마진 값이 미산출**이다. 종료 조건(값 산출)이
  >   미충족이므로 **활성·미해소**다(라운드 7에 복원).
  >
  > **이 `주의` 절은 팀 리드가 쓴 계약 지시였고, 라운드 5·7의 복원 결정과 어긋난 채
  > 남아 있었다.** 계약과 산출물이 반대를 말하는 상태였으므로 계약 쪽을 정정한다.
- `OPEN-OPS-03`은 **입력만 부분 확정**(1인 운영)이며 임계는 미결이다. 해소하지 마라.
- capability `OPS-06`(queue topology 배포 게이트)은 브로커 존재를 전제한다. 브로커 없음
  결정으로 **재정의 또는 폐기 대상**이다.
  > **라운드 7 갱신**: 라운드 1은 "그 사실을 기록한다"까지만 했으나, `V2 필수` 분류와
  > broker 전용 acceptance가 남아 활성 `OPEN-OPS-10`에 종속된 것을 확정으로 계상했다
  > (Codex 5차 high #1). 라운드 7이 **분류를 `근거 부족`으로 내리고 acceptance를
  > `OPEN-OPS-10` 조건부로** 돌렸다.

## 갱신 이력

### 2026-08-26 — head 기입 (spec-writer)

| 커밋 | 대응 |
| --- | --- |
| `e06e62a` | 결정 29건 반영 — OPEN 66 → 37 해소(§12.2 신설, §12.1 결번 29행), 조건부 7 → 3 전환, pull 모델 §0.7 신설, capability 재분류 5건, 신규 **STR-16**, §10 재계산, §13 인계(STR-01 `legacy-defect` · DEC-09 선언·실행 불일치 · OPS-06 재정의) |
| `66850e0` | verifier 라운드 6 발견 3건 — **M-4**(DEC-12 A9 한정어 복원) · **M-5**(SET-07 `V2 제약` 조건화) · **H-2**(0A `commands.md` G4 기준 리비전 정정, append) |
| `6a4e49b` | evidence — `decisions.md`(A7, 커밋된 결정 기록) · `checklist.md`(A1~A7) · `commands.md`(검증 명령·출력) |

- `head_sha`를 `6a4e49b`로 기입했다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
  0A와 같은 이유로(커밋이 자기 SHA를 담을 수 없다) `head_sha`는 그 직전 커밋을 가리키며,
  두 커밋의 차이는 이 파일의 `head_sha` 한 줄과 이 절뿐이다.
- **range의 in_scope 밖 변경은 없다.** 이 slice의 커밋 4개는 전부
  `docs/discovery/capability-map.md` · `reports/evidence/m0/0a2/` ·
  `reports/evidence/m0/0a/commands.md`(H-2 append 한정) 안에 있다.
  0A의 base(`3dc7d26`)부터 세던 하네스 무관 커밋 3건은 이 slice의 base(`6af7019`)
  **이전**이므로 이번 range에 들어오지 않는다.
- **산출물 변화**: capability **94 → 95**(STR-16 신규), 분류 **63/14/6/11 → 62/17/6/10**,
  활성 `OPEN` **66 → 37**(소멸 29 · 신규 0 · 임의 해소 0), `capability-map.md`
  2,625 → **2,872줄**.
- **A2 증명**: id 집합을 양방향 대조했다 — 결정 기록에 없는데 소멸한 항목 **0건**,
  결정 기록에 있는데 잔존한 항목 **0건**. `OPEN-OPS-03`은 입력만 부분 확정이므로
  **해소하지 않았다.**
- **미처리(알려진 제한)**: `OPS-06` 재정의(DB 큐 설계 부재에 종속) · STR-11 분류
  (결정 기록에 판단 없음). 상세는 `checklist.md` §9.

### 2026-08-26 — 수정 라운드 2 (verifier `not-ready` 대응, spec-writer)

verifier 판정 `not-ready`, 발견 9건(high 2 / medium 5 / low 2).
**A2는 통과**(소멸 29 / 신규 0 / 임의 해소 0, 독립 재현)이고 A1·A4·A5·A6·A7도 충족이었다.
9건 전부를 처리했다.

| 커밋 | 대응 |
| --- | --- |
| `5192213` | **H-1 · H-2 · M-1** — legacy 실측으로 사실 정정. STR-16 부재 주장 반증(`projects.py`의 라이브 검색 경로), 21,321건 귀속 정정(유사공고 임베딩 백필), legacy on-demand 경로 존재(`predictions.py:20`). §13 0B 인계를 **세 경로**로 재작성 |
| `4c29cce` | **M-2 · M-3 · M-4 · L-1 · L-2** + evidence — OPEN 2건 신설, `잠정` 묶음 분리, QUAL-11 인용 이관, 절 순서, `OPEN-OPS-03` 행 갱신 |
| `0ce2e4c` | **M-5** — 0A `commands.md` 틀린 원문 지점에 정정 절 포인터 append |

- `head_sha`를 `6a4e49b` → `0ce2e4c`로 갱신했다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
  라운드 1과 같은 이유로 `head_sha`는 그 직전 커밋을 가리킨다.
- **range의 in_scope 밖 변경은 없다.**
- **산출물 변화(라운드 2)**: capability **95 불변**, 분류 **62/17/6/10 불변** —
  라운드 2는 분류를 하나도 바꾸지 않았다. 활성 `OPEN` **37 → 39**(`OPEN-QUAL-09` ·
  `OPEN-STR-12` 신설). `capability-map.md` 2,872 → **2,951줄**.
- **A2 재확인**: base 대비 소멸 **29건 불변**, 임의 해소 **0건**. 신규 2건은 **정당한
  신설**이며 A2가 금지하는 임의 해소의 **반대 방향**이다 — 결정이 상위 축만 정하고 하위
  질문을 남겼는데 그 질문을 받던 OPEN이 같은 라운드에 닫혀 **수신처가 사라진** 미결이다.
  등록하지 않으면 `milestone-0.md` 게이트가 쓰는 OPEN 수가 실제 미결을 과소 계상한다.
- **H-2·M-1의 경위를 기록했다**: 두 건 다 팀 리드 분석 단계 서술이 `decisions-log.md`를
  거쳐 0A2에서 **정본으로 승격**된 것이다. `decisions.md`에 정정 절을 신설하고 원본
  문장은 취소선으로 보존했다. 승격 전 legacy 대조를 하지 않은 것이 원인이며, 그 교훈을
  `checklist.md` §10.1에 0B 이후 적용 규칙으로 적었다.
- **미처리(라운드 1에서 이월, 변동 없음)**: `OPS-06` 재정의(DB 큐 설계 부재에 종속).
  STR-11 분류는 **`OPEN-STR-12` 신설로 짝 구조가 복원**됐다.

### 2026-08-26 — 수정 라운드 3 (verifier `not-ready` 대응, spec-writer)

라운드 2 재검증 판정 `not-ready`, 발견 7건(high 1 / medium 2 / low 4).
**라운드 1 발견 9건의 실질은 전부 처리됐고 정정은 실측에 근거하며 정직하다**고 판정됐다 —
H-1·H-2·M-1의 legacy 근거를 verifier가 전건 재현했고 어긋난 것이 없으며,
**계열 A 신규 발생 0건**(추가 79줄 전수 판독), 신규 OPEN 2건도 실재하는 미결로 확인됐다.
남은 것은 한 줄 편집 4건 + 재실행 1건이었다.

| 커밋 | 대응 |
| --- | --- |
| `9903138` | **H-2r · L-4r** — 완화 철회를 문장 전체로 확대, basis 정합 commit 식별자를 저장소 안에서 검증 가능하게 |
| `cc15131` | **M-1r · M-2r · L-1r · L-2r · L-3r** + evidence — OPEN 수 불일치, §9.1 짝 복원, 묶음 호칭, 집계 문장, **명령 출력 재실행** |

- `head_sha`를 `0ce2e4c` → `cc15131`로 갱신했다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
  이전 라운드와 같은 이유로 `head_sha`는 그 직전 커밋을 가리킨다.
- **range의 in_scope 밖 변경은 없다.**
- **산출물 변화(라운드 3)**: capability **95 불변**, 분류 **62/17/6/10 불변**,
  활성 `OPEN` **39 불변** — 라운드 3은 집계를 하나도 바꾸지 않았다. 정정과 표기만
  고쳤다. `capability-map.md` 2,951 → **2,956줄**(+5).
- **H-2r가 이 라운드의 핵심이다.** 라운드 2가 앞문장만 취소선에 넣고 뒷문장
  ("`OPEN-OPS-03`의 모수가 근본적으로 달라진다")을 **철회 표시 밖에 남겨**, 정정문이
  오히려 완화의 결론을 재확인하는 형태가 됐다. 같은 파일의 정정 절과 `capability-map.md`
  §0.7이 정반대를 적고 있었다. 철회 범위를 문장 전체로 넓혔다.
- **M-2r — 라운드 2·3 절의 명령 출력을 재실행해 교체했다.** 라운드 2 기록의 절 번호
  grep이 `63/98/111`이었는데 range 내 어느 커밋에서도 나오지 않는 값이었다.
  **재실행 범위는 라운드 2·3 절(18건)이며 라운드 1 절(12건)은 재실행하지 않았다** —
  그 절은 **라운드 1 시점의 기록**이고, 재실행으로 덮는 것보다 **시점을 밝히는 것이
  정확하다**(`commands.md` 라운드 1 절 제목에 기준 시점을 붙였다). 재실행한 것은 커밋 후
  기재값이 커밋 상태와 일치하는지 재확인했다(절 번호 63/107/120, 조건부·잠정 6행,
  활성 OPEN 39, 2,956줄 — 전부 일치).
  > **라운드 4 정정 (verifier M-4r)**: 원래 "**모든 명령을** HEAD에서 실제로 실행하고"로
  > 적혀 있었다. 참인 것은 라운드 2·3 절뿐이고 라운드 1 절 12건 중 10건은 HEAD에서
  > 재현되지 않는다(그 시점 기준이므로 당연하다). **실행 범위를 확인하지 않고 전칭으로
  > 적은 것**이며, §10.1 3번 규칙의 반대 형태다 — 출력을 손으로 쓴 것이 아니라
  > **재실행 범위를 손으로 넓혔다.**
- **`checklist.md` §10.1에 같은 계열의 세 형태를 표로 정리했다** — ① 확인하지 않고
  **기재**(0A H-1) ② 확인하지 않고 **승격**(0A2 H-2·M-1·L-4r) ③ 실행하지 않고
  **붙여넣기**(라운드 2 M-2r). **셋의 뿌리는 같지만 결과는 같지 않다** — 1·3번은 결론이
  옳았고 근거만 없어 **재실행 후 교체**로 닫혔으나, **2번은 결론 자체가 반증돼 철회**됐다
  (H-2 "큐 폭주의 뿌리가 사라진다", M-1 "legacy는 배치 전용", L-4r의 식별자 전제 둘).
- **미처리(이월, 변동 없음)**: `OPS-06` 재정의(DB 큐 설계 부재에 종속).

### 2026-08-26 — 수정 라운드 4 (verifier `not-ready` 대응, spec-writer)

라운드 3 재검증 판정 `not-ready`, 발견 5건(medium 2 / low 3). **지시받은 7건은 전부 실질
처리됐고 초점이었던 M-2r는 통과**였다 — `$` 명령 17건 전수 재현에서 어긋난 것 0건,
H-2r 철회 완결, L-4r 실측 확인, **계열 A 신규 0건**, 불변 8종 재현.
**남은 5건은 전부 라운드 3이 새로 쓴 문장에서 나왔다.**

| 커밋 | 대응 |
| --- | --- |
| `8c17d08` | **M-3r · M-4r · L-5r · L-6r · L-7r** — 자기진단 절의 완화와 전칭 서술 정정, 수치·범위 3건 + 라운드 4 evidence |

- `head_sha`를 `cc15131` → `8c17d08`로 갱신했다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
- **range의 in_scope 밖 변경은 없다.**
- **산출물 변화(라운드 4)**: capability **95 불변**, 분류 **62/17/6/10 불변**, 활성 `OPEN`
  **39 불변** — 라운드 4는 집계를 하나도 바꾸지 않았다. `capability-map.md`
  2,956 → **2,958줄**(L-6r +2).
- **M-3r가 이 라운드의 핵심이다.** §10.1은 이 slice의 **자기진단 절**인데, 그 안에
  "**세 번 다** 결론 자체는 옳았고 틀린 것은 근거의 지위였다"는 **완화 서술**이 남아
  있었다. **2번 형태(H-2·M-1·L-4r)는 결론 자체가 반증돼 철회됐으므로 거짓이며**, 같은
  패키지의 `decisions.md`("철회. 원본의 사실 오류다")·`capability-map.md` §0.7("사실이
  아니다")과 정면 모순이었다. 표를 `결론 자체 / 근거 / 처리` 3열로 나눠
  **1·3번(결론 옳음 → 재실행 후 교체)과 2번(결론 틀림 → 철회)**을 분리했다.
- **M-4r는 §10.1 3번의 반대 형태다** — 3번이 "출력을 손으로 쓴 것"이라면 이것은
  **"재실행 범위를 손으로 넓힌 것"**이다. §10.1 표에 **3′**로 추가했다.
- **이번 라운드는 새 주장을 만들지 않았다.** 전부 기존 서술의 범위를 좁히거나 수치를 맞춘
  작업이며, 좁힌 자리마다 정정 경위를 남겼다.
- **재실행 범위(전칭으로 적지 않는다)**: `commands.md` R4-1의 명령 4건은 라운드 4 HEAD
  실행분이다. **라운드 1 절 5개는 재실행하지 않았고** 각 제목에 기준 시점을 선언했다.
- **미처리(이월, 변동 없음)**: `OPS-06` 재정의(DB 큐 설계 부재에 종속).

### 2026-08-26 — 수정 라운드 5 (Codex 4차 `request_changes` 대응, spec-writer)

Codex 4차 판정 `request_changes`(blocker 0 / **high 4** / medium 2).
verdict 원문: `codex-review-20260826T102642Z.json`. 사용자가 재작업 상한을 넘겨 이 라운드를
명시 승인했다. **6건 중 4건이 "결정의 파급이 문서 곳곳에 덜 반영된" 유형**이며 0A의
나선과 성격이 다르다.

| 커밋 | 대응 |
| --- | --- |
| `1b7e286` | **high #1~#4** — QUAL-11 조건부 분리, 금액 의미 OPEN, 브로커 전제 제거, QUAL-02 결정의 본문 반영 |
| `3444a69` | **medium #6 + residual** — A3 부재 주장 교체, `inv2.py` 본문 인라인, 라운드 5 evidence |

- `head_sha`를 `8c17d08` → `3444a69`로 갱신했다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
- **range의 in_scope 밖 변경은 없다.** ADR 작성(0D)은 이 slice 범위가 아니다.
- **산출물 변화**: capability **95 불변**, 분류 **62/17/6/10 불변** — 라운드 5는
  capability를 추가·재분류하지 않았다. **활성 `OPEN` 39 → 43** — 신설 3
  (`OPEN-QUAL-10` 금액 의미 · `OPEN-QUAL-11` 허용업종 결합 규칙 · `OPEN-OPS-10` DB 큐 계약)
  + 복원 1(`OPEN-OPS-07`, ADR 기입 미완). **근거 없는 자리를 확정 서술로 채우는 대신
  등록한 결과**다. `capability-map.md` 2,958 → **3,064줄**.
- **조달청 문서를 실제로 다시 열었다**(#2·#4). 결과는 **둘 다 "문서에 없다"**이므로
  확정하지 않고 OPEN으로 등록했다. `cnstrtnAbltyEvlAmtList`에는 단위·과세 주석이 없고
  (같은 문서가 `추정가격`에는 "부가가치세 및 조달 수수료를 제외한 금액(원화,원)"을
  명시하므로 **규약의 누락**이다), `permsnIndstrytyList`에는 결합 규칙이 없다. 상세는
  `checklist.md` §13.1.
- **high #1이 계열 A의 새 형태다** — 우리가 라운드 2에 만든 `OPEN-QUAL-09`를, 라운드 1에
  쓴 QUAL-11의 확정 서술이 선점하고 있었다. **OPEN을 세우면서 그 쟁점을 다루는 본문을
  다시 읽지 않은 것**이 원인이며 `checklist.md` §13.2에 기록했다.
- **§10.1에 형태 2건을 추가했다** — **4** 재실행 불가능한 참조(Codex residual risk),
  **5** 정정을 인용 지점에 전파하지 않기(medium #6). 표가 형태 7종이 됐다.
- **재실행 범위(전칭으로 적지 않는다)**: `commands.md` R5-1의 명령 5건은 라운드 5 HEAD
  실행분이다. 라운드 1 절 5개는 재실행하지 않았고 각 제목에 기준 시점을 선언했다.
- **미처리(이월)**: `OPS-06` 재정의 — 이제 `OPEN-OPS-10`(DB 큐 계약)에 종속되며 §13에
  브로커 파급 4건으로 함께 기록했다.

### 2026-08-26 — 수정 라운드 6 (verifier `not-ready` F-1 blocker 대응, spec-writer)

라운드 5 재검증 판정 `not-ready`, **F-1이 blocker**. 발견 10건이 **전부 수치·인용 전파**
이며 리포트 자신이 "새 결정·조사·운영자 입력이 필요한 항목은 **없다**"고 적었다.
**이번 라운드는 새 서술·주장·절을 만들지 않았다.**

| 커밋 | 대응 |
| --- | --- |
| `840ec57` | **F-1(blocker) · F-2 · F-7 · F-8 · F-9** + §12.1 복원 표시 · §12.2 전문 — 산출물 전파 |
| `36dd0de` | **F-3 · F-5 · F-6 · F-10** + A2 절 기준 시점 — evidence 전파 |

- `head_sha`를 `3444a69` → `36dd0de`로 갱신했다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
- **range의 in_scope 밖 변경은 없다.**
- **집계는 하나도 바뀌지 않았다** — capability **95**, 분류 **62/17/6/10**, 활성 `OPEN`
  **43**, 형식 위반 **0**, 중복 id **0**, `V2 필수` 결측 **0**, secret **3**.
  `capability-map.md` 3,064 → **3,078줄**(전파 +14 — 아래 완결 커밋 포함).
- **F-1이 blocker였던 이유**: §12 머리말이 활성 **43**을 선언하는데 같은 절 본문이
  **39**와 `G3 13 · G6 3`을 적어 **자기 게이트 지표를 두 값으로 동시에 선언**했다.
  `milestone-0.md` 완료 조건이 이 수를 쓴다.
- **F-2는 Codex medium #5의 실질 미해소였다** — registry는 라운드 5에 복원됐는데
  OPS-21 본문이 "`OPEN-OPS-07` 해소"를 유지해 Codex가 인용한 바로 그 지점이 안 고쳐졌다.
- **세 건의 공통 원인이 하나다** — 라운드 5가 자기가 바꾼 수치를 인용 지점에 전파하지
  않았다. 이것은 라운드 5가 `checklist.md` §10.1에 **형태 5**로 등록한 그 형태이며,
  라운드 3의 M-4r과 같은 구조로 **세 번째 반복**이다. 새 형태를 추가하지 않고
  **형태 5 행에 재발 2회를 기록**하고 규칙에 "수치를 바꿨으면 그 수치를 인용하는 지점을
  grep으로 전수 확인한다"를 덧붙였다.
- **커밋 후 전파 누락 자체 확인을 수행했다**(팀 리드 지시) — `39건` · `G3 13` · `G6 3` ·
  `OPS-07`+`해소` · `묶음 8개` · `41%` · `3,064`를 grep으로 훑었다. 산출물 잔존 0건,
  evidence는 라운드 3·4·5 **처리 기록**(그 시점 값이 정확한 것) 외에 잔존 0건.
  **자체 확인에서 실제 누락 1건을 찾아 고쳤다** — `checklist.md` 상단 산출물 메타데이터가
  "3,064줄 — 라운드 5 기준"으로 남아 있었다.
- **미처리(이월, 변동 없음)**: `OPS-06` 재정의 — `OPEN-OPS-10`(DB 큐 계약)에 종속.

#### 라운드 6 완결 — `M0 차단` 서술 정합 (verifier 지시)

라운드 6 재검증에서 **F-1 blocker 해소 · 지시 10건 전부 실질 처리 · 집계 8종 불변 ·
새 절·형태·규칙 0**이 확인됐고, 남은 것이 **한 문장**이라 새 라운드가 아니라 완결로
처리했다.

| 커밋 | 대응 |
| --- | --- |
| `d428977` | `capability-map.md:2862`의 "M0 차단 항목은 전부 해소됐고" → **M0 차단 8건 중 7건 · `OPEN-OPS-07` 1건 활성**. `checklist.md` §10.1 형태 5에 한 줄 추가. 줄 수 전파 3곳 |

- `head_sha`를 `36dd0de` → `d428977`로 갱신했다.
- **집계는 하나도 바뀌지 않았다** — capability **95**, 분류 **62/17/6/10**, 활성 `OPEN`
  **43**, 형식 위반 **0**, 중복 id **0**, `V2 필수` 결측 **0**, secret **3**.
- **왜 모순이었나**: `OPEN-OPS-07`은 **M0 차단 8건 중 하나**이고(위 「반영 대상」 1번)
  라운드 5에 활성으로 복원됐다. 같은 문서 `:2943`·`:2999`가 그 잘못된 해소를 두고
  **"M0 차단 수를 과소 계상했다"**고 적으므로 산출물 안에서 모순이 닫혔다.
  Codex 4차 medium #5가 지목한 축이며, **라운드 6이 편집한 `:2861`·`:2863` 사이 한 줄**이다.
- `checklist.md` §9 제한 3("M0·M1 차단은 `OPEN-OPS-07`을 제외하고 해소됐다")과 정합한다.
- **`M0 차단` 전수 훑기 결과**(팀 리드 지시): 잔재 **0건**. `capability-map.md:2943`·`:2999`는
  과소 계상 사실을 적는 정정 기록이고, `scope.md:58`은 결정 패스의 반영 대상 목록,
  `decisions.md:310`은 **결정 확정** 수(실행 대기 행이 OPS-07을 따로 센다)라 전부 정확하다.
- **부수 전파**: 이 편집으로 줄 수가 3,076 → **3,078**이 되어 `commands.md` R5-1의 `wc`
  출력·줄 수 서술과 `checklist.md` 상단 메타데이터에 반영했다. **형태 5가 지시하는 자체
  grep으로 찾았다.**

#### Codex 5차 송부 전 정리 — 잔여 4건 (verifier 최종 `ready-for-review`)

최종 재검증 판정은 **`ready-for-review`**(필수 수정 해소 · `M0 차단` 전수 훑기 잘못된
주장 0건 · 집계 8종 불변 · 새 주장·절·규칙 0 · 계열 A 신규 0)이며, 송부 전에 잔여 4건만
정리했다. **전부 한 줄이고 evidence에 한정된다** — `capability-map.md`는 건드리지 않았다.

| 커밋 | 대응 |
| --- | --- |
| `a2ff86b` | **F6-2 · F6-3 · F6-4 · F6-5** — self-closing 수치 삭제, 절 제목 실행 시점, 비율 3곳, grep 행 수 |

- `head_sha`를 `d428977` → `a2ff86b`로 갱신했다.
- **집계는 하나도 바뀌지 않았다** — capability **95**, 분류 **62/17/6/10**, 활성 `OPEN`
  **43**, 형식 위반 **0**, 중복 id **0**, `V2 필수` 결측 **0**, secret **3**.
  `capability-map.md` **3,078줄 불변**(이번 커밋이 산출물을 건드리지 않았다).
- **F6-2 실측**: self-closing `<w:p/>`는 **0개**다. 841은 태그 이름 경계를 강제하지 않은
  `<w:p[^>]*/>`가 `<w:pStyle w:val="…"/>` 같은 형제 태그를 잡은 값이었고, 산술로도
  12,892 − 841 = 12,051 ≠ 12,891로 어긋났다. 괄호를 삭제했고 나머지 수치
  (12,892 · 12,891 · 12,641줄 · `:9663`)는 재현되므로 유지했다.
- **F6-4 판단**: 세 지점(`commands.md` 라운드 1 절 주석 · R4-3 표 · `checklist.md` §12 표)
  모두 **"현재 값"이라 적으면서 §9를 가리키는 포인터**였고 §9는 `66 → 43 = 35%`이므로
  셋 다 35%로 고쳤다. 각 지점이 기록하는 대상(L-5r이 44%로 되돌린 사실)은 그대로다.
- **F6-5 실측**: 조건부·잠정 grep의 실제 행 수는 **9**다. `checklist.md`를 9로 통일했다.
- `decisions.md:26`은 지시대로 **손대지 않았다**.

### 2026-08-27 — 수정 라운드 7 (Codex 5차 `request_changes` 대응, spec-writer)

Codex 5차 판정 `request_changes`(blocker 0 / **high 3** / medium 2).
**4차 finding 6건은 residual에 해소로 명시**됐다. high #1·#2가 **같은 부류**여서
인스턴스가 아니라 **부류를 전수 재판정**했다.

| 커밋 | 대응 |
| --- | --- |
| `c46ddb4` | **high #1 · #2 + 추가 발견 · medium #4** — OPS-06 조건부화, DEC-03·DEC-07 활성 복원, NOTI-02 재분류, 집계 정합 |
| `f77f85d` | **high #3 · medium #5 + residual** — 계약 `주의` 절 정정, `decisions.md` provenance 표, evidence 정합, whitespace |

- `head_sha`를 `a2ff86b` → `f77f85d`로 갱신했다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
- **range의 in_scope 밖 변경은 없다.** ADR 작성(0D)은 이 slice 범위가 아니다.

#### 전수 재판정 — 이 라운드의 방식

Codex #1(`OPS-06`)과 #2(`OPEN-DEC-03`)는 **활성 OPEN에 종속된 것을 확정으로 계상한다**는
같은 부류다. **인벤토리 규모**: 활성 OPEN 43건, anchorless 5건, 활성 OPEN을 인용하는 블록
37개(capability 30 + 설계 입력 절 7). 기계적 스윕과 읽기를 병행했고 판정은 읽기로 했다.

**하나의 기준** — *종료 조건이 충족되지 않았으면 활성 registry에 있어야 한다.*

| 항목 | 재판정 |
| --- | --- |
| `OPEN-OPS-07` | 미충족 → 라운드 5에 이미 복원 |
| `OPEN-DEC-03` | 미충족 → **복원**(Codex #2) |
| `OPEN-DEC-07` | 미충족 → **복원**(전수 재판정의 **추가 발견**. `OPS-07`과 같은 `실행 대기` 라벨인데 복원되지 않아 기준이 일관되지 않았다) |
| `OPEN-DEC-08`·`OPEN-ML-01`·`OPEN-STR-07`·`OPEN-QUAL-06`의 "잔여" | **유지** — 종료 조건이 아니라 **하류 작업 지시**이며 결정 자체는 완결이다 |
| `OPS-06` | 분류·acceptance가 확정돼 있었다 → **조건부화**(Codex #1) |

어휘 스윕 8건 중 실제 위반은 **`OPS-06` 1건**이었다.

- **산출물 변화**: capability **95 불변**, 분류 **62/17/6/10 → 61/16/6/12**
  (OPS-06 `V2 필수`→`근거 부족`, NOTI-02 `후속`→`근거 부족` — 둘 다 활성 OPEN 종속).
  활성 `OPEN` **43 → 46**(복원 `OPEN-DEC-03`·`OPEN-DEC-07`, 신설 `OPEN-NOTI-09`).
  `capability-map.md` 3,078 → **3,133줄**.
- **차단 집계**: M0 차단 8건 중 **7건 해소**(`OPEN-OPS-07` 활성), M1 차단 17건 중
  **15건 해소**(`OPEN-DEC-03`·`OPEN-DEC-07` 활성).
- **`주의` 절을 정정했다**(high #3) — 이 절은 팀 리드가 쓴 계약 지시였고 라운드 5·7의
  복원 결정과 어긋난 채 남아 **계약과 산출물이 반대를 말하는 상태**였다. 지시를 철회하고
  세 항목을 분리해 각각의 종료 조건과 활성·미해소 사유를 적었다.
- **미처리(이월)**: `OPS-06` 재정의·폐기 판정 — 이제 **`OPEN-OPS-10`의 종료 조건에
  포함**되므로 별도 이월이 아니라 등록된 OPEN이다.

---

### 갱신 이력 — 라운드 7 완결 (2026-08-27)

verifier 재검증 판정 `not-ready`. 차단 사유는 **라운드 7이 복원한 항목의 인용 지점을
안 고친 것**(F7-1·F7-2)이며, 라운드 6 F-2와 글자 그대로 같은 형태다 —
`checklist.md` §10.1 **형태 5의 세 번째 재발**이다.

- `head_sha`를 `f77f85d` → `304de90`로 갱신했다.
- 이전 라운드와 같은 이유로(커밋이 자기 SHA를 담을 수 없다) `head_sha`는 그 직전
  커밋을 가리키며, 두 커밋의 차이는 이 파일의 `head_sha` 한 줄과 이 절뿐이다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
- **range의 in_scope 밖 변경은 없다.**

#### 이 완결이 한 일

| 커밋 | 내용 |
| --- | --- |
| `db793db` | F7-1~F7-4 — 산출물 상태 정합. §12.1 결번 표의 `DEC-07`·`DEC-03` 행을 같은 표 `OPS-07` 행 형식으로, DEC-02 본문의 "해소" 서술을 활성 복원 상태로, §12 머리 8건 → **9건**(신설 6 + 복원 3), `NOTI-02` acceptance를 `OPEN-NOTI-09` 조건부로 |
| `3198d3c` | F7-5~F7-9 + **활성 OPEN 46건 전수 인용 스윕 기록** + §10.1 형태 5 절차화 |
| `3d3f982` | 전수 스윕 수치를 재현 커밋에 고정(378/51 → **379/52**, 불일치 0 불변) + `scope.md` EOF 공백 |
| `304de90` | scope 완결 이력과 `head_sha` |
| 이 커밋 | 스윕 재현성 서술 정정 — "뒤 커밋은 매치를 더 늘린다"는 **미측정 서술이었고 실측하니 379/52로 같았다**. 두 커밋 양쪽에서 재실행해 확인 |

**형태 5를 절차로 만들었다.** 규칙을 문장("grep으로 전수 확인한다")으로 둔 것이 세 번을
못 막았으므로, `commands.md` R7C-1에 스크립트 본문을 두고 §10.1 형태 5 본문에
**OPEN을 복원·신설·해소한 라운드는 그 라운드 안에서 이 스윕을 돌려 결과(매치 수 ·
불일치 건수 · 불일치 목록)를 기록한다**를 규칙으로 넣었다. 수정 전 상태에 같은 규칙을
돌리면 F7-1·F7-2가 정확히 적출되는 것으로 유효성을 실증했다(R7C-3).

- **전수 스윕 결과**: 활성 OPEN 46건 · 전체 매치 **379건** · 판정 대상 **52건** ·
  **불일치 0건**. `db793db`가 고친 F7-1·F7-2 **외 추가 불일치는 없었다.**
  (이 bullet은 `3d3f982`가 `commands.md`만 고치고 남겨 378/51로 있었다 — **형태 5**.
  verifier V-4로 적출돼 같은 블록 `3d3f982` 행의 379/52와 맞췄다.)
- **산출물 변화**: capability **95 불변**, 분류 **61/16/6/12 불변**, 활성 `OPEN`
  **46 불변**. 조건부 항목을 가진 capability는 **6 → 7**(F7-4의 `NOTI-02`).
  `capability-map.md` 3,133 → **3,144줄**.
- **차단 집계 불변**: M0 차단 8건 중 7건 해소, M1 차단 17건 중 15건 해소.
- **미처리(이월)**: 없다.

---

### 갱신 이력 — 2026-08-27 운영자 결정 반영 (`OPEN-NOTI-09` 확정)

라운드 7 완결 도중 **운영자 결정이 추가**됐다 — NOTI-02(알림 피로도 게이트)를
**`후속`으로 확정**한다. **`축소`가 아니다.** 이 slice가 라운드 7에 신설한
`OPEN-NOTI-09`가 **같은 slice 안에서 해소**됐다.

- `head_sha`를 `304de90` → `3ed76e9`로 갱신했다.
- 이전 라운드와 같은 이유로 `head_sha`는 그 직전 커밋을 가리키며, 두 커밋의 차이는
  이 파일의 `head_sha` 한 줄과 이 절뿐이다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
- **range의 in_scope 밖 변경은 없다.**

#### 처리

| 대상 | 처리 |
| --- | --- |
| `OPEN-NOTI-09` | **해소.** §12.1 결번 표에 사유, §12.2에 결정·근거·상태(`확정`), 라운드 7 신설 표 행에 `→ 2026-08-27 해소`, G3 활성 표에서 행 삭제 |
| NOTI-02 | 분류 `근거 부족` → **`후속` 확정**(귀속: 운영자 결정 2026-08-27). F7-4로 조건부화했던 acceptance 5항목이 **무조건으로 복귀** |
| 라운드 1의 근거 | **보존.** slice 판단이었다는 이력을 지우지 않고, 운영자 결정이 같은 결론을 확정했다는 사실을 덧붙였다 |
| `decisions.md` | 「2026-08-27」 **원본 절** 신설 + provenance 표에 행 추가. 원본 「진행 상황」 표(2026-08-26 스냅샷)는 고치지 않고 델타를 적었다 |

- **산출물 변화**: capability **95 불변**, 분류 **61/16/6/12 → 61/17/6/11**,
  활성 `OPEN` **46 → 45**(G3 16 → 15). 조건부 항목을 가진 capability **7 → 6**,
  조건부 묶음 **9 → 8** + 잠정 1(불변).
- **차단 집계 불변**: M0 차단 8건 중 7건 해소, M1 차단 17건 중 15건 해소.
  `OPEN-NOTI-09`는 G3이며 차단 목록에 없었다.
- **전수 스윕**: 대상이 46 → **45**로 바뀐 상태에서 재실행, **불일치 0건**(`commands.md`
  R8-1). 더해 **역방향 패스**를 돌렸다 — 스크립트는 활성 registry에서 id를 뽑으므로
  **해소한 id는 스캔 대상에서 빠져** 낡은 종속 인용을 잡지 못한다. 이 구멍을 `commands.md`
  R8-2에 기록하고 `checklist.md` §10.1 형태 5 절차의 **보완**으로 넣었다(새 형태 아님).
- **F7-4는 조건부화가 아니라 확정으로 닫혔다** — 라운드 7이 조건화, 같은 slice 안에서
  운영자 결정이 확정. 조건부화가 잘못이었던 것이 아니라 **근거가 없던 자리에 근거가
  생긴 것**이며, 등록된 OPEN이 있었기에 결정이 그 자리를 채웠다.
- **미처리(이월)**: 없다.

---

### 갱신 이력 — 라운드 7 완결 2차 (verifier V-1~V-10, 수치 축 복원)

재검증 판정 `not-ready`, 차단 1건(V-1). **F7-1~F7-9는 9건 전부 해소**됐고 전수 스윕·역방향
패스·결정 반영은 독립 재현으로 확인됐다. 차단 사유는 **2026-08-27 결정을 반영하면서 §10
축별 표 자체를 안 고친 것**이다.

- `head_sha`를 `3ed76e9` → `c58d8f8`로 갱신했다.
- 이전 라운드와 같은 이유로 `head_sha`는 그 직전 커밋을 가리키며, 두 커밋의 차이는
  이 파일의 `head_sha` 한 줄과 이 절뿐이다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
- **range의 in_scope 밖 변경은 없다.**

#### 진단 — 절차화하면서 규칙의 절반을 잃었다

§10.1 형태 5의 원래 정의는 "**정정을 인용 지점에 전파하지 않기**"이고 라운드 6 F-1이
바로 "활성 OPEN 43 **수치** 미전파"였다. 라운드 7 완결이 그 규칙을 절차로 만들면서
**OPEN id grep으로 좁혔고**, 그 결과 **id 축 불일치 0인 상태에서 수치 축 5건**이 남았다.
**형태 5의 네 번째 재발**이며, 이번엔 절차 자체가 원인이다.

**수치 축 스윕을 만들어 규칙을 원래 정의로 되돌렸다**(`commands.md` R9-1에 스크립트 본문,
§10.1 형태 5 본문에 규칙). id 축과 설계가 다르다 — **정본을 산출물에서 직접 계산한 뒤
인용 지점을 패턴으로 추출해 자동 대조**하므로 사람 판정 없이 불일치를 지목한다.
그래서 §10 축별 표를 "읽어 비교"하는 게 아니라 capability 블록을 세어 **표를 검증**하고,
V-1이 그 대조로 잡혔다.

| 커밋 | 내용 |
| --- | --- |
| `098bf03` | **V-1** — §10 NOTI 행 `5\|4\|0\|2` → `5\|5\|0\|1`, 계 행 `61\|16\|6\|12` → `61\|17\|6\|11` |
| 이 커밋의 직전 | 절차 복원(§10.1 수치 축 + `commands.md` R9) + V-2·V-3·V-5·V-7·V-9 + V-4·V-6·V-8·V-10 + 추가 발견 2건 |
| 이 커밋 | scope 이력과 `head_sha` |

- **수치 축 스윕 결과**: 살아 있는 현재 값 주장의 불일치 **9건 전부 수정**(V-1 2지점 ·
  V-2 · V-3 · V-5 · V-7 · V-9 · **추가 발견 2건**), 오탐 3건은 한계로 기록.
  **추가 발견 2건은 verifier 목록에 없던 것**이며, 그중 하나는 문장이 참(라운드 1 시점
  값)인데 **2026-08-27 블록 뒤로 밀려 현재 값 주장이 된** 경우다 — id 축으로는 안 보인다.
- **id 축 스윕 재실행**: 불일치 0(이번 라운드는 OPEN을 신설·복원·해소하지 않았다).
- **산출물 변화**: capability **95 불변**, 분류 **61/17/6/11 불변**, 활성 `OPEN`
  **45 불변**. V-1은 표를 실측에 맞춘 것이지 분류를 바꾼 것이 아니다.
  `capability-map.md` **3,171줄 불변**(V-1은 셀 안 교체다).
- **차단 집계 불변**: M0 차단 8건 중 7건 해소, M1 차단 17건 중 15건 해소.

#### 커밋 구성 근거 (이월분 — 라운드 7 완결 1차에서 넘어온 것)

`3ed76e9`가 **결정 반영과 스윕 기록을 한 커밋에 묶은 것은 의도**다. 스윕 기록이 evidence
파일에 들어가면 스윕 자신의 매치 수가 바뀌므로, 반영과 기록을 나누면 **기록한 수치가 그
커밋에서 이미 틀린 값**이 된다. 같은 커밋에 두어야 재현 지점이 고정된다(`commands.md`
R8-1·R9-2의 "재현 지점은 이 절을 담은 커밋"). 팀 리드가 이 구성을 확인했다.

- **미처리(이월)**: 없다.

---

### 갱신 이력 — 라운드 7 완결 3차 (verifier F2-1~F2-3, 수치 축 커버리지 확장)

재검증 판정 `not-ready`, medium 1 · low 2. **수치 축은 작동한다** — verifier가 R9-1
스크립트를 기계 추출해 직접 실행했고 기록 수치(173/103/9)와 유효성까지 재현했으며,
산출물 `capability-map.md`는 독립 재계산한 **모든 집계 축에서 정합**이다. 남은 것은
evidence 자기 설명이었다.

- `head_sha`를 `c58d8f8` → `0d33111`로 갱신했다.
- 이전 라운드와 같은 이유로 `head_sha`는 그 직전 커밋을 가리키며, 두 커밋의 차이는
  이 파일의 `head_sha` 한 줄과 이 절뿐이다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
- **range의 in_scope 밖 변경은 없다.**
- **이번 라운드는 산출물을 바꾸지 않았다** — `capability-map.md`는 `098bf03` 이후
  그대로(**3,171줄 불변**)이고 수정은 전부 evidence다.

#### 인스턴스가 아니라 축이 문제였다

F2-1(`checklist.md` A1 「결정 근거」 행이 §12.2를 "29행"으로 적음)을 **복원된 수치 축이
못 잡은 이유는 §12.2/§12.1 행 수가 대조 목록에 없었기 때문**이다. 산출물이 자기 절에서
"30행 중 27건"으로 세 번 선언하는 집계인데 커버리지 밖이었다.

| 대상 | 처리 |
| --- | --- |
| 축 확장 | `DEC_ROWS`(§12.2 행 수 = **30**) · `VOID_ROWS`(§12.1 결번 표 행 수 = **35**)를 정본 산출과 `CHK`에 추가. `commands.md` R9-1 본문 갱신, R10이 실행 결과 |
| 커버리지 기준 | §10.1 형태 5에 한 줄 — **"산출물이 스스로 선언하는 집계는 전부 대조 대상"**. 열거는 목록이 아니라 확인된 사례이며 **열거에 없다는 것이 대조 대상이 아니라는 뜻이 아니다** |
| **F2-1** | `checklist.md:20` 29 → **30행**(구성: 29 + 1), `:21` **35행**(0A 5 + 29 + 1), `:173` A7 대조를 **30건 · 두 날짜**로 — **V-6 수정이 만든 비대칭**을 함께 닫았다 |
| **F2-2** | R9-3 자기참조 경고의 시제 — "다음 실행부터는" → **"이미 위 건수 안에 있다"** |
| **F2-3** | `inv2.py` 출력이 5줄만 실려 있었다(스크립트는 6줄). R8-3·R9-5 두 곳에 6번째 줄 복원. 각 블록의 커밋(`3ed76e9`·`c58d8f8`)에서 실행해 **둘 다 30**임을 확인하고 넣었다 |

- **넓힌 축의 재실행**: 추출 **197건** · 불일치 **124건**(판정 대상). **살아 있는 현재 값
  주장의 불일치는 F2-1의 3지점이 전부**였고 전부 고쳤다 — **추가 발견 없음**.
  R9-2의 173/103은 **확장 전 축의 값**이며 그 시점 기록으로 남는다.
- **id 축 재실행**: 매치 367 · 판정 대상 52 · **불일치 0**.
- **집계 불변**: capability **95** · 분류 **61/17/6/11** · 활성 OPEN **45**.
- **미처리(이월)**: 없다.

---

### 갱신 이력 — Codex 송부 전 legacy 인용 정정 (0B 선행 조사 C-1~C-4, verifier F3-1)

재검증 판정 `ready-for-review`(low 1건). **송부 직전에 0B 선행 조사가 legacy를
`ed4b06c`에서 다시 열어 선행 산출물의 인용 오류 4건을 찾았고, 그중 셋이 in_scope에
있었다.** `milestone-0.md`의 Codex 판정 기준 첫 줄이 "근거가 실제 파일/commit과 맞는가"라
틀린 것을 아는 채로 보낼 수 없다.

**이건 저장소 안 정합 검증이 구조적으로 못 잡는 부류다** — Codex worktree에 `bid-vector`가
없어 매 라운드 residual risk에 "저장소 밖 파일·commit 주장은 재현하지 않았다"가 적혔다.
여덟 라운드가 문서 내부 정합을 샅샅이 훑는 동안 **legacy 인용의 정확성은 검증 사각지대**였다.

- `head_sha`를 `0d33111` → `520f928`로 갱신했다.
- 이전 라운드와 같은 이유로 `head_sha`는 그 직전 커밋을 가리키며, 두 커밋의 차이는
  이 파일의 `head_sha` 한 줄과 이 절뿐이다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
- **range의 in_scope 밖 변경은 없다.**

#### 정정 4건 — 전부 `git show ed4b06c:`로 직접 열어 재확인했다

상류 조사의 실측을 그대로 믿지 않았다. 실행 출력은 `commands.md` R11-1~R11-4.

| # | 정정 | 재확인 |
| --- | --- | --- |
| **C-1** | "얕은 하회 0.9~2.7%p가 legacy 자신의 **백테스트 문서**에 한계로 공시" → **리포트 생성 스크립트의 caveat 문자열**(`scripts/backtest_latest_award_holdouts.py:1203-1204`) | 문서 전문 grep 결과 `0.9~2.7`은 **없다**. 문서 `:277`의 같은 한계 서술에는 숫자가 없고, `:240`의 `0.9~1.7%p`는 **다른 축**(보고 낙찰률 basis 독립성)이라 섞지 않는다고 명시했다. 3곳 |
| **C-2** | `floor_applicability.py` 경로 → **`app/ai/`**, 근거 줄 `:125-126` → **`:126-127`** | `git ls-tree`로 경로 확정, `:126-127`이 "조직 종류 어미…기본값 ``applicable`` 로 남는다" 주석임을 확인. 4곳. **인접 인용 2건**(`tests/test_floor_applicability.py:70`, `legal_floor_spec.py:30-31`)은 재확인 결과 **맞아 그대로** 뒀다 |
| **C-3** | `filters.py` 예산 비교 블록 `:62-67` → **`:62-70`** | `:65-68`이 두 guard, `:69-70`이 사유 append. 라운드 2의 실행(`sed -n '62,67p'`)은 6줄만 떴다 — **그 출력은 형태 3에 따라 그대로 두고** 범위 정정 주석을 그 자리에 달았다. 3곳 |
| **C-4** | `app/services/collection_jobs.py` → 실제 `app/tasks/` | **in_scope 산출물·evidence에 이 인용이 없다**(grep 매치 0). 0A 노트는 `_workspace/`이고 범위 밖이라 **손대지 않았다** |

- **F3-1**(verifier low): R10-3 표 제목이 축의 적출 범위를 넓게 말했다 → 제목을 좁히고
  `:173`이 **축이 아니라 사람 판정으로 고쳐진 것**임을 표 위에 적었다.

#### §10.1 형태 판단 — 새 형태를 만들지 않고 형태 2를 넓혔다

근거 셋은 `checklist.md` §10.1 「형태 2의 판단」에 있다 — ① 이 표는 *어디서 났나*가 아니라
**왜 났나**로 나누는데 C-1~C-3의 원인은 하나("legacy를 열지 않고 legacy를 인용했다")이고
형태 2의 규칙 문장이 이미 그것을 요구했다. 빠진 것은 규칙이 아니라 **적용 범위**다.
② 한 원인을 두 형태로 쪼개면 각 형태가 자기 절반만 방어한다 — **형태 5가 절차화에서
수치 축을 잃고 네 번째 재발을 낸 전례**가 있다. ③ Codex 레인의 사각지대는 **레인의
속성**이지 실패 형태가 아니다. **절차화는 하지 않았다** — 근거 없이 스크립트를 만들면
형태 5가 밟은 길을 되풀이한다. **0B가 절차의 필요를 판단할 자리다.**

- **산출물 변화**: capability **95 불변**, 분류 **61/17/6/11 불변**, 활성 `OPEN` **45 불변**.
  **바뀐 수치는 줄 수 하나** — `capability-map.md` 3,171 → **3,176줄**(C-1의 출처 설명 +5).
  **그 파급을 넓힌 수치 축이 스스로 적출**해 `checklist.md:7`·`:582`를 함께 고쳤다.
- **스윕 재실행**: 수치 축 추출 200 · 불일치 128(살아 있는 현재 값 주장의 신규 불일치
  **0건**), id 축 매치 367 · 판정 대상 52 · **불일치 0**.
- **미처리(이월)**: 없다.

---

### 갱신 이력 — Codex 6차 대응 (medium 2건, high 0)

Codex 3차 리뷰 판정 `request_changes` — **blocker 0 / high 0 / medium 2 / low 0**.
직전 high 3건(`OPS-06` · `OPEN-DEC-03` · 이 파일의 「주의」 절)과 medium #4(`NOTI-02`)는
**전부 해소** 판정됐고, Codex가 독립 재집계로 capability **95** · 분류 **61/17/6/11** ·
활성 OPEN **45**를 재현했다. verdict는
`reports/evidence/m0/0a2/codex-review-20260827T015140Z.json`(이번 커밋에 등재, append-only).

- `head_sha`를 `520f928` → `7fe93cc`로 갱신했다.
- 이전 라운드와 같은 이유로 `head_sha`는 그 직전 커밋을 가리키며, 두 커밋의 차이는
  이 파일의 `head_sha` 한 줄과 이 절뿐이다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
- **range의 in_scope 밖 변경은 없다.**

#### medium #1 — provenance 경계 복원

`1fc5968`이 **원본 절 `OPEN-DEC-09`의 legacy 서술을 in-place로 교체**해 slice 정정이
운영자 원본처럼 보였다. 원본 문장을 되살리고 **틀린 조각에만 취소선 + 인용 블록 정정**으로
바꿨다.

**관례 판단** — 이 파일의 두 선례 중 **`R-07` 쪽**을 골랐다. 21,321건은 **주장 자체를
철회**한 건이라 별도 절 + 전면 취소선이 맞았고, C-1·C-2는 **실질 주장(legacy가 이 한계를
스스로 적어 둔다)이 그대로 참이고 식별자·출처 종류만 틀린** 건이라 `R-07`(식별자 정정,
원본 보존, 인용 블록)과 같은 부류다. 파일이 선언한 원칙("원본 문장은 지우지 않는다.
slice가 덧붙이는 것은 별도 절이거나 인용 블록·취소선이며 그 자리에서 slice 산물임을
밝힌다")에도 그대로 맞는다.

함께 고친 전칭 주장 3곳 — `decisions.md` 파일 머리("통합 패스 주석 외 원본 그대로"),
provenance 표의 원본 행("취소선 1건 제외" → **3건**: 21,321 · `R-07` · C-1/C-2),
`checklist.md` A7 slice 추가 목록(**5종 → 6종**, C-1/C-2 행 신설).

#### medium #2 — 수치 축 결과의 기준 SHA

R11-6의 `200 / 128`이 기준 없이 남아 현재 head(203)와 어긋났다. **팀 리드 권고대로
최신 값으로 갱신**하되 이 절도 같은 성질이라 **재현 지점을 함께 못박았다** — `R11-7`을
신설해 커밋별 값을 표로 적었다(`1fc5968` 200 · 뒤 세 커밋 203 · 이 라운드 **204**).
R11-6에는 "`1fc5968`에서만 성립"을 명시했다.

#### residual — 고쳤다

`checklist.md:27`이 **`잠정`인 `OPEN-DEC-03`을 "실행 대기 2건"에 넣어** 바로 위의
"상태 4종을 구분해 기록했다" 선언과 어긋났다. **고치는 쪽을 택했다** — 자기 선언과
모순되는 어휘를 남기면 다음 리뷰에서 다시 나오고, **이 slice가 계열 A·형태 5로 반복해
태운 부류**다. 비용은 한 줄이고 새 주장이 없다. 상태·차단 집계는 원래 정확했고 건드리지
않았다.

- **산출물 변화 없음** — `capability-map.md`는 `1fc5968` 이후 무변경(**3,176줄**).
  capability **95** · 분류 **61/17/6/11** · 활성 `OPEN` **45** 전부 불변.
- **스윕 재실행 (기준 `7fe93cc`)**: 수치 축 추출 **204** · 불일치 **128**(살아 있는 현재 값
  주장의 신규 불일치 **0건**, 새 오탐 1종은 한계로 기록), id 축 매치 **369** · 판정 대상
  **52** · **불일치 0**. **이 절이 인용을 3건 더하므로 이 커밋(`head_sha`의 다음)에서
  재실행하면 수치 축은 207**이다 — medium #2가 지적한 형태를 이 절에서 되풀이하지
  않으려고 기준 커밋과 파급을 함께 적는다.
- **미처리(이월)**: 없다.

---

### 갱신 이력 — Codex 7차 대응 (medium 1건, high 0)

Codex 4차 리뷰 판정 `request_changes` — **blocker 0 / high 0 / medium 1 / low 0**.
직전 2건은 둘 다 해소 판정됐고(수치 축은 Codex가 커밋별로 인라인 numsweep을 재실행해
`ab69012` 203 · `7fe93cc` 204 · head 207을 재현), 독립 집계도 capability **95** ·
분류 **61/17/6/11** · 활성 OPEN **45** · G 분포 **3/7/15/8/7/5**를 재현했다.
verdict는 `codex-review-20260827T021632Z.json`(등재, append-only).

- `head_sha`를 `7fe93cc` → `3ec50fb`로 갱신했다.
- 이전 라운드와 같은 이유로 `head_sha`는 그 직전 커밋을 가리키며, 두 커밋의 차이는
  이 파일의 `head_sha` 한 줄과 이 절뿐이다.
- **리뷰 range는 `6af7019...<현재 HEAD>`이며**, 현재 HEAD는 이 scope 갱신 커밋이다.
- **range의 in_scope 밖 변경은 없다.**
- **산출물 `capability-map.md`는 건드리지 않았다** — `1fc5968` 이후 무변경(**3,176줄**),
  `git diff --stat 1fc5968..HEAD -- docs/discovery/capability-map.md` 출력 0줄.

#### 뿌리는 인스턴스가 아니라 주장의 형태였다

provenance가 "원본 절 안의 slice 정정은 **3건뿐**"이라 **셈으로 전칭을 주장**했고, Codex가
최초 커밋 blob과 head를 통째로 diff해 **4번째**(`OPEN-QUAL-08` 비고)를 찾았다. 편집이 하나
늘 때마다 그 수가 틀리므로 **셈을 버리고 재현되는 목록으로 바꿨다.**

- `decisions.md`에 **「원본 절에 대한 slice 변경 — 전수」**를 신설했다. 재현 명령
  `git diff -U0 6a4e49b -- reports/evidence/m0/0a2/decisions.md`가 주장의 일부이며,
  훅 **10개**를 절에 1:1로 대응시켰다.
- **원본 절 안에 들어간 것은 네 자리뿐**이다 — `144`(「OPEN-STR-01」 `R-07` 정정) ·
  `235`(「M1 차단 묶음」 `OPEN-QUAL-08` 포인터) · `250`/`252`(「OPEN-DEC-09」 C-1·C-2 정정) ·
  `340`(「상호작용 모델」 21,321건 철회). 성격은 **정정 2 · 철회 1 · 포인터 1**.
  **Codex가 찾은 것 외 미선언 변경은 없다.**
- 파일 머리와 provenance 원본 행의 셈 기반 문장도 같은 형태로 교체했고,
  `checklist.md` A7의 판정 근거를 **`decisions.md`의 훅 목록**으로 옮겼다.

#### `OPEN-QUAL-08` 성격 판정 — 의도적. 되돌리지 않고 드러냈다

`git log -S`로 확인하니 라운드 2 `4c29cce`("…인용 이관…")에서 **축어 인용 절과 같은
커밋에** 들어갔다 — 편집 중 섞인 것이 아니라 **원본 표 독자를 옮겨진 인용으로 보내는
포인터**다. 결정 내용·근거를 바꾸지 않으므로 **되돌리지 않았다** — 되돌리면 원본 표에서
축어 인용을 따라갈 수 없어져 **A7(리뷰어가 인용을 따라갈 수 있게 한다)이 도로 깨진다.**
그 자리에 `*(slice 추가 — 라운드 2 4c29cce)*`를 달고 전수 표·A7에 등재했다. 성격이 다른
셋과 구분해 **「포인터 추가」**로 적었다 — 지울 원본 문장이 없어 취소선은 쓰지 않았다.

#### §10.1 형태 판단 — **형태 6을 신설했다**

지금까지 두 번은 새 형태를 만들지 않는 쪽이 옳았고 둘 다 형태 2로 흡수했다. **이번은
다르다고 판단했다.**

- **형태 1**(확인하지 않고 기재)의 규칙 "실행한 것만 적는다"는 **지켜도 막지 못한다** —
  `git diff`를 돌려 맞는 수를 적었어도 **다음 편집에서 다시 틀린다.** 규칙과 실패 모드가
  맞지 않는다.
- **형태 5**(정정을 인용 지점에 전파하지 않기)는 수치 축 커버리지 기준이 "예외 N건"을
  덮으므로 **사후 적출은 된다.** 그러나 형태 5는 *정정한 뒤* 전파하는 규칙이고 이 실패는
  **애초에 전파가 필요한 형태로 쓴 것**이다 — 두 형태는 **예방(6)과 적출(5)로 짝**을 이룬다.
- 남길 규칙이 기존 다섯 어디에도 없다: **"전칭 주장은 셈이 아니라 재현 명령으로 쓴다."**

- **스윕 재실행 (기준 `3ec50fb`)**: 수치 축 추출 **209** · 불일치 **128**(살아 있는 현재
  값 주장의 신규 불일치 **0건**), id 축 매치 **369** · 판정 대상 **52** · **불일치 0**.
  **이 절 자신이 인용을 더하므로 이 커밋에서 재실행하면 더 큰 값이 나온다** — 그래서
  수치를 기준 커밋에 못박고 여기서는 그 값을 쓰지 않는다(Codex 6차 medium #2가 지적한
  형태를 되풀이하지 않는 방법이며, **셈을 재현 지점에 묶는 형태 6의 규칙과 같은 취지**다).
  이 절이 더하는 불일치는 **전부 오탐**이다 — 위 문단의 `G 분포`가 6-튜플이라 `분류4종`
  arm이 앞 네 값만 떼어 읽는다(`checklist.md:249` 계열의 한계). 실제 불일치를 가리지 않는다.
- **집계 불변**: capability **95** · 분류 **61/17/6/11** · 활성 `OPEN` **45**.
- **미처리(이월)**: 없다.
