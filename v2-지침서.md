# 나라장터 입찰 예측 시스템 V2 — 아키텍처 및 AI 개발 지침

- 상태: greenfield rewrite 실행 기준
- 기준일: 2026-08-21
- 기존 구현 참조: [`bid-vector`](./bid-vector) `ed4b06c`
- 구현자: Claude
- 독립 리뷰어: Codex

## 1. 프로젝트 정의

V2의 목적은 Python 8만여 줄을 Kotlin으로 번역하는 것이 아니다. 기존 구현에 축적된
비즈니스 지식과 실패 사례를 조사한 뒤, 필요한 기능만 새 도메인 모델과 계약으로 다시
구현하는 것이다.

기존 `bid-vector`에서 가져오는 것:

- 사용자에게 필요한 기능과 업무 용어
- KONEPS 필드의 실제 형태와 수집 실패 사례
- 금액 basis, 비율 scale, 법정 하한, 자격 판정의 회귀 사례
- 검증 가치가 확인된 데이터 fixture와 반례
- LightGBM/KDE/통계 계산의 수학적 요구와 평가 방법
- 운영상 필요한 멱등성, rate limit, 감사, dry-run 요구

가져오지 않는 것:

- Python 파일·클래스·함수 구조
- FastAPI/SQLAlchemy/Celery의 내부 결합
- 기존 DB schema와 public API의 자동 호환성
- 기존 출력값 자체를 정답으로 보는 characterization
- 버그나 임시 fallback을 보존하는 golden

Python과 V2 결과가 다르면 “Python과 같게 고친다”가 아니라, 승인된 명세·공식 자료·수작업
판정으로 어느 쪽이 맞는지 결정한다.

## 2. 기존 프로젝트에서 확인한 문제

2026-08-21 조사 기준 기존 저장소는 다음 상태다.

| 관찰 | 설계에 주는 교훈 |
| --- | --- |
| `app/**/*.py` 88,588줄 | 파일 이동이나 언어 직역으로는 복잡도가 줄지 않음 |
| 테스트 288개 파일, 약 100,201줄 | 유효한 반례는 재사용하되 테스트 구조는 새 경계에 맞게 다시 작성 |
| 500줄 초과 서비스 다수, 최대 971줄 | 새 프로젝트는 처음부터 책임·의존성 예산을 CI로 강제 |
| 금액 basis/비율 scale/법정 하한 관련 연속 수정 | 원시 `Double` 대신 값 객체와 명시적 단위가 필요 |
| `0.875`와 `87.5`, 기초금액/예정가/추정가격 혼재 | 값 크기로 단위·basis를 추측하는 fallback 금지 |
| 수집·DB·ML·알림이 한 workflow에 결합 | 순수 도메인, application, adapter, ML 경계를 분리 |
| Ruff/mypy/pytest/design ratchet가 있어도 회귀 발생 | green suite 외에 property, mutation, contract, E2E 증거 필요 |

이 수치는 migration 범위를 산정하기 위한 것이 아니라, 기존 구조를 복제하지 않아야 할
근거다.

### 2.1 기존 구현 조사 앵커

M0에서 처음 확인할 구현은 다음과 같다. 이 목록은 이식 대상이 아니라 도메인 지식과 실패
사례를 찾기 위한 탐색 시작점이다.

| 주제 | 기존 구현/검증 앵커 |
| --- | --- |
| 면허 자격 | `app/services/license_eligibility.py`, `tests/test_license_eligibility.py` |
| 기초금액 출처 | `app/services/base_amount_basis.py`, `tests/test_base_amount_basis.py` |
| 하한 미달 | `app/domain/floor_shortfall.py`, `tests/test_floor_shortfall.py` |
| 금액 basis | `app/domain/money.py`, `app/domain/basis_conversion.py`, basis 관련 테스트 |
| 수집 | `app/services/koneps/`, `tests/test_koneps_collection_characterization.py` |
| ML | `app/ai/predictors/`, `app/services/ml_training/`, `app/services/ml_release/` |
| 비대화 방지 | `scripts/design_ratchet.py`, `tests/design_ratchet_baseline.json` |

특히 기존 이력의 #350~#359 계열은 basis, rate, 법정 하한, provenance가 여러 소비 경로에서
조용히 어긋난 사례이고, #361은 predictor null 회귀, #368은 queue 폭주 회귀다. V2는 이
구현을 복사하지 않고 각각을 값 객체·계약·멱등 workflow·실패 주입 test로 예방한다.

## 3. 목표 구조

V2는 과도한 MSA가 아닌 두 개의 명확한 runtime으로 시작한다.

```text
React Web (후속 또는 재사용 결정)
          |
          v
Kotlin modular application
  - public API / auth
  - KONEPS collection adapters
  - qualification / strategy
  - bidding / decision / settlement
  - workflow / outbox / notification policy
          |
          | versioned inference contract
          v
Python ML engine
  - training / evaluation
  - feature transform
  - LightGBM / KDE inference
  - artifact manifest / release
  - serving runtime has no DB and no business decision
```

### 3.1 Kotlin application

Kotlin은 하나의 Gradle 멀티모듈 애플리케이션으로 시작한다. 업무별 배포 서비스로 미리
쪼개지 않는다.

권장 모듈:

- `shared-kernel`: Money, Rate, Basis, 식별자, 시간 타입만
- `procurement`: KONEPS 공고·개찰 canonical facts와 수집 port
- `qualification`: 면허·지역·실적 자격 정책
- `strategy`: 운영자 감시 조건과 전략
- `bidding`: 투찰 계획과 상태
- `decision`: 법정 하한, 추천 후보 평가, reason code
- `settlement`: 개찰 결과 대사와 정산 상태
- `workflow`: use case, transaction, event/outbox 조합
- `adapters`: HTTP, DB, broker, KONEPS, notification, ML client
- `app`: Spring Boot wiring과 public API

의존 방향은 `domain <- application <- adapters/app`이다. domain은 Spring, JPA, JSON,
HTTP, broker를 import하지 않는다. 다른 업무 모듈의 repository/entity를 직접 참조하지
않는다.

### 3.2 Python ML engine

Python은 ML 수학과 모델 생명주기만 소유한다.

```text
ml-engine/
  src/ml_engine/
    contracts/      # generated 또는 Pydantic DTO, ORM 없음
    features/       # training/serving 공용 순수 변환
    training/       # dataset을 입력받아 artifact 생성
    evaluation/     # holdout, calibration, backtest
    inference/      # LightGBM/KDE 순수 use case
    registry/       # manifest, checksum, version
    serving/        # gRPC adapter
    adapters/       # training 전용 dataset/object storage adapter
```

- serving process에는 SQLAlchemy, DB driver, `requests` 기반 외부 수집, 업무 entity가 없다.
- training과 serving은 같은 feature schema와 변환 코드를 사용한다.
- Kotlin이 이미 판정한 자격·법정 하한을 Python이 다시 판정하지 않는다.
- Python 응답은 후보·점수·불확실성·모델 근거만 반환한다. 최종 `bid/review/skip`은 Kotlin이
  결정한다.

### 3.3 통신

- Kotlin↔Python의 기본안은 Protobuf/gRPC다.
- M2에서 deadline, retry, error mapping, compatibility를 포함한 ADR을 승인한다.
- 성능 측정이나 운영 제약이 gRPC의 이점을 입증하지 못하더라도, 이미 채택한 계약을 몰래
  HTTP로 이중 구현하지 않는다. 변경은 ADR로 한다.
- 장시간 training은 동기 RPC로 붙잡아 두지 않는다. job command/status 계약을 별도로 둔다.
- Kotlin이 Python 내부 task 이름이나 Celery wire format을 알지 못한다.

## 4. 핵심 도메인 규칙

### 4.1 금액과 비율

- 확정 원화 금액은 `Long` 원 단위 또는 명시적 `BigDecimal`로 표현한다.
- `Money`는 `amount`, `currency`, `basis`, `vatTreatment`, `provenance`를 가진다.
- `BaseAmount`, `EstimatedAmount`, `YegaAmount`, `BidAmount`를 원시 숫자로 상호 대입하지
  못하게 한다.
- `Rate`는 내부 fraction 표현 하나만 사용한다. percent 입력은 adapter에서 명시적으로
  변환하며 원문 unit을 기록한다.
- 반올림은 `RoundingPolicy`와 version으로 중앙화한다.
- unit/basis/provenance가 없거나 모르는 값은 추측하지 않고 거부 또는 `Unmeasurable`로
  반환한다.

### 4.2 면허 자격

- 단일 요구조건, 그룹 내 AND, 그룹 간 OR를 명시적으로 모델링한다.
- 결과는 boolean 하나가 아니라 `Eligible`, `Ineligible(reasons)`, `Uncertain(reasons)`로
  구분한다.
- 별칭, 포괄 코드, 유효기간, 지역·협회 조건은 versioned policy data로 관리한다.

### 4.3 기초금액 provenance

- `Clean`, `DerivedYega`, `DerivedVat`, `SuspectRatio`, `Unknown`을 명시한다.
- first-match rule의 순서는 도메인 의미가 있으므로 테스트와 정책 version으로 고정한다.
- 원본 값을 조용히 교정하지 않는다. 원본, 판정, 근거, 정책 version을 함께 보존한다.
- 기존 구현이 주장한 “66% 오염”은 M0에서 데이터 출처와 측정 방법을 재검증하기 전에는
  V2 사실로 사용하지 않는다.

### 4.4 낙찰하한 미달

- `추천 투찰률 / 낙찰하한율`이 임계 사정률이라는 basis 관계를 타입과 설명으로 보존한다.
- 표본 부족은 0%가 아니라 `Unmeasurable`이다.
- 빈도는 실제 확률이라고 표시하지 않는다.
- 표본 필터 범위, 최소 표본 수, 경계의 포함/제외는 versioned 정책이다.

### 4.5 workflow와 side effect

- 상태 전이는 명시적 state/event table로 표현한다.
- 단순 validation과 guard clause를 위해 `if`를 쓰는 것은 허용한다. “Zero If-Else”를
  목표로 하지 않는다.
- 3단계 이상 라우팅, 계속 증가하는 특수케이스, 중복 정책은 polymorphism, lookup,
  rule table, state machine 중 가장 단순한 방식으로 분리한다.
- 외부 호출, DB write, event publish, notification은 port 뒤에 둔다.
- 재시도는 멱등성 key와 함께 설계하고, 알림 중복과 out-of-order event를 테스트한다.

## 5. 기술 표준

### Kotlin

- Kotlin 2.x와 현재 지원되는 Spring Boot 3.x 조합을 M1에서 버전 고정한다.
- Gradle Kotlin DSL과 version catalog를 사용한다.
- JUnit 5 + Kotest/AssertJ, property test, Testcontainers를 사용한다.
- domain test에서는 mock framework를 사용하지 않고 값과 fake port를 사용한다.
- adapter test에서만 MockK 또는 test server/container를 사용한다.
- 생성자 주입, 불변 객체, 명시적 result type을 기본으로 한다.
- business control flow에 exception을 쓰지 않는다. programmer error와 infra failure는
  별도 계층에서 처리한다.

### Python ML

- `pyproject.toml` 기반 패키지와 lock된 dependency를 사용한다.
- Ruff, strict typecheck 범위, pytest, import boundary, size ratchet를 CI에 둔다.
- serving dependency와 training dependency를 분리한다.
- 모델 artifact에는 schema version, code version, dataset id, checksum, metric을 포함한다.

### 크기와 결합도

줄 수만 맞추기 위한 파일 분할을 금지한다. 다음을 함께 측정한다.

- 함수/메서드 크기와 복잡도
- 파일 크기
- 모듈 fan-in/fan-out
- public API 수
- 순환 의존
- duplicate mechanical helper

신규 코드 기준 권장 한도는 Kotlin/Python 함수 50줄, 파일 500줄이다. 초과는 자동 실패 또는
명시적 allowlist 사유를 요구하며, baseline을 느슨하게 갱신해서 우회하지 않는다.

## 6. 테스트 우선순위

1. 승인된 예제 기반 unit test
2. 경계·결측·단위 혼입을 생성하는 property test
3. 계약 consumer/provider test
4. mutation test로 중요 rule이 실제로 보호되는지 확인
5. DB/broker/gRPC adapter integration test
6. mock KONEPS와 fake notification을 사용한 E2E
7. 승인된 환경에서만 live read probe

기존 Python과의 differential test는 보조 도구다.

- 일치: 요구 누락 여부를 점검할 수 있으나 합격을 자동 보장하지 않음
- 불일치: `Python defect`, `V2 defect`, `intentional redesign`, `insufficient evidence` 중 하나로
  판정하고 근거를 남김
- 승인되지 않은 Python 출력으로 V2 golden을 자동 생성하지 않음

## 7. AI 작업 규율

Claude는 구현 전에 다음을 제출한다.

1. 기존 구현에서 조사한 파일과 발견한 도메인 지식
2. 그대로 채택하지 않을 구조와 이유
3. V2 설계, 대안, 선택 근거
4. 이번 slice의 in/out scope와 acceptance command
5. 외부 side effect와 rollback 여부

라이브러리를 무조건 도입하지 않는다. 상태 전이가 작으면 sealed type과 전이표로 충분하며,
측정된 필요 없이 Spring Statemachine, WebFlux, Kafka 같은 무거운 도구를 추가하지 않는다.

구현과 독립 리뷰는 [`agent-workflow.md`](./agent-workflow.md)를 따른다. Claude가 구현하고
Codex가 read-only diff review를 승인해야 다음 slice로 진행할 수 있다.

## 8. 안전 규칙

사용자 승인 없이는 다음을 실행하지 않는다.

- 기존/신규 DB write와 backfill
- 실제 KONEPS, LLM, Telegram, email 호출
- 운영 credential 조회 또는 로그 출력
- 원격 push, merge, 배포
- 기존 프로젝트 파일 수정 또는 삭제

테스트는 `ENVIRONMENT=test`, fake clock, fake notification, mock server를 기본으로 한다.

## 9. V2 완료 정의

완료는 “Python 8만 줄을 모두 대체함”이 아니다. 아래의 승인된 V2 제품 범위가 독립적으로
동작하면 완료다.

- Kotlin domain/application이 승인된 자격·금액·하한·workflow 규칙을 단일 소유
- Python serving은 DB와 업무 규칙 없이 모델 추론만 수행
- 계약 version과 모델 artifact로 모든 추천을 재현 가능
- KONEPS 수집 장애와 ML 장애가 업무 상태를 오염시키지 않음
- 중복 event, 재시도, 시간초과, 결측, 단위 혼입이 fail-safe하게 처리됨
- 주요 rule mutation이 테스트에서 생존하지 않음
- 신규 코드가 크기·의존성 래칫을 통과
- Claude 구현 diff에 대한 Codex 독립 승인과 사용자 승인 존재
- 기존 `bid-vector` 없이 clean environment에서 V2 E2E가 통과

기존 프로젝트의 모든 기능을 옮기는 것은 완료 조건이 아니다. M0에서 채택한 capability만
V2 범위이며, 나머지는 명시적으로 폐기하거나 후속 backlog로 둔다.
