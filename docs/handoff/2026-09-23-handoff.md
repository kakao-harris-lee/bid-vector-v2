# 핸드오프 — 2026-09-23, 장비 이동

이 문서는 **다른 장비에서 작업을 이어받기 위한 진입점**이다. 원래 장비(macOS, iTerm2, Orca)에서의 작업은
이 문서를 커밋한 시점에 종료했다. 저장소가 이미 담고 있는 것(지침서·milestone·evidence)은 반복하지 않고,
**저장소 밖에 있던 것**과 **지금 어디서 멈췄는가**만 적는다.

## 1. 먼저 읽을 순서

1. 이 문서.
2. `docs/handoff/2026-09-23-6f4w-lane-instructions.md` — 진행 중 slice(6F-4-w)의 인계와 검토 레인
   지시문 전문. 원본은 gitignore 된 `_workspace/m6-6f4w/00_handoff.md` 였다.
3. `CLAUDE.md` → `README.md` → `v2-지침서.md` → `agent-workflow.md` → `milestone-6.md`
   (특히 「M6 잔여 해소와 배선 — 실측 지도와 순서」 절).
4. `reports/evidence/m6/6f4w/scope.md`(D-6F4W-1~12) · `checklist.md` · `commands.md` · `rollback.md`.

## 2. 저장소 상태 (원래 장비, 2026-09-23 종료 시점)

| 항목 | 값 |
| --- | --- |
| `main` | `02164e44` — PR #42(M6/6F-7) 머지 커밋. origin/main 과 같다. |
| 진행 중 브랜치 | `m6-6f4w/2026-09-23` @ **`1f34858d`**, main 에서 17 커밋 앞, **판정 대상 SHA 로 동결** |
| 그 브랜치의 worktree | `../bid-vector-v2-m6f4w` (원래 장비 경로) — 작업 트리 clean |
| 이 문서의 브랜치 | `handoff/2026-09-23` (main 에서 분기, 문서 두 개만) |
| 잔류 로컬 브랜치 | `_peek_main` = main 과 동일, 용도 끝남 → 지워도 된다 |
| stash | 없음 |
| 미머지 PR | 없음 (PR #41 6A-1, PR #42 6F-7 모두 머지 완료) |
| legacy 참고 저장소 | `bid-vector` symlink → `../bid-vector` @ `ed4b06c` (read-only, 별도 clone 필요) |

**푸시 여부**: 원래 장비는 VPN 이 끊겨 사내 프록시가 닿지 않아 **`m6-6f4w/2026-09-23` 와
`handoff/2026-09-23` 를 푸시하지 못했다.** 두 브랜치는 로컬 커밋 상태다. VPN 을 연결한 뒤 아래를 원래
장비에서 실행해야 다른 장비에서 받을 수 있다.

```bash
cd /Users/harris/Development/private/bid-vector-v2
git push -u origin handoff/2026-09-23 m6-6f4w/2026-09-23
```

푸시가 확인되면 이 절의 「푸시 여부」를 갱신하지 않아도 된다 — origin 의 두 브랜치 존재가 사실이다.

## 3. 새 장비에서 복원

```bash
git clone https://github.com/kakao-harris-lee/bid-vector-v2.git
git clone <legacy bid-vector 저장소> bid-vector && git -C bid-vector checkout ed4b06c
cd bid-vector-v2
git checkout handoff/2026-09-23            # 이 문서
git worktree add ../bid-vector-v2-m6f4w m6-6f4w/2026-09-23
mkdir -p ../bid-vector-v2-m6f4w/_workspace/m6-6f4w   # 레인 리포트 자리 (gitignore)
```

`bid-vector` symlink 는 저장소에 추적된 링크(`../bid-vector`)이므로 형제 디렉터리에 legacy clone 만 두면
된다. 도구 버전은 원래 장비 기준 JDK 21.0.4 · Gradle wrapper 9.6.1 · Python 3.14 · buf 1.72 · Node 22.
acceptance 는 CI job 명령 그대로다(`.github/workflows/ci.yml` — `check` / `ml-engine` / `container`).

저장소 밖에 있어 **같이 옮기지 않으면 새 장비에 없는 것**:

- Claude 메모리 디렉터리 `~/.claude/projects/-Users-harris-Development-private-bid-vector-v2/memory/`
  (M6 레인 지도, 반복 실측 교훈 20여 건). 같은 경로로 복사하면 그대로 살아난다. 핵심은 §5 에 요약했다.
- `.omc/`(OMC 세션 상태) · `_workspace/`(레인 리포트) — 둘 다 gitignore. 6F-4-w 의 `_workspace` 에는
  `00_handoff.md` 하나뿐이었고 그 전문이 옆 문서다. 리포트(10·11·12)는 아직 없다.
- 전역 하네스 `~/.claude/CLAUDE.md` · `~/.claude/fable/active.md` · `~/.claude/review-lane.md` ·
  `~/.claude/agents/*` · `~/.claude/hooks/*`. 프로젝트 하네스(`.claude/`)는 저장소에 있다.

## 4. 멈춘 지점과 다음 행동

**6F-4-w(`WatchSubjectPort` 첫 production 구현 + 감시 텍스트 두 타입 생성 경계 폐쇄)는 구현이 끝나
`1f34858d` 에 동결됐고, 검토 레인 셋을 띄우는 단계에서 멈췄다.** 원래 장비에서는 `Agent` 호출이 Orca
런타임 타임아웃으로 다섯 번 연속 실패했다(장비 문제, slice 문제 아님).

다음 행동은 하나다 — **`1f34858d` 에서 verifier · code-reviewer(`model: sonnet` 명시) · privacy-gate 를
병렬로 띄운다.** 지시문 전문은 옆 문서. 그 뒤 경로: 판정 → (필요시 수정 라운드) → 장부층 일괄 소거 →
`milestone-6.md` 종결 문단 → 푸시·PR 승인 요청 → 판정 코멘트 넷 → CI → 머지 승인 요청.
**사용자 승인 없이 push/merge 하지 않는다.** Codex 는 이 slice 대상이 아니다.

검증 표적 1번(옆 문서 verifier ①): 구현 레인이 「`of` 가 `internal` 이라 구조적으로 더 못 닫는다」고
적었다 — 6F-7 에서 같은 형태의 「못 한다」가 HIGH 로 뒤집혔다. 같은 파일이면 `private` 이 되는지 실제로
컴파일해 본다.

## 5. M6 잔여 지도 (정본은 `milestone-6.md` 해당 절)

배선 4단계: ① 6F-7 알림 요청 → outbox **완료** · ② **6F-4-w 검증 대기(여기)** · ③ 6A-3 + 6F-3 합침
(평가 endpoint + `CapacityPort`, `MlAnalysisPort` 처분을 운영자에게 올림) · ④ `OPEN-6F-ASSEMBLY`
(포트 아홉을 app 에 꽂기, ①~③ 전제).

독립으로 열림: 6A-2(세션 endpoint + app 이미지) · 6B-2(백업·복구 리허설).
운영자 결정 대기: 6B-3(보존 기간) · 6B-4(큐 정책) · 6F-5-b(실 LLM 승인).
배선 뒤: 6D · 6E — `OPEN-6A1-CONNECTION-POOL` 이 닫혀야 운영 진입.

레인 규율 요약(메모리에서): 판정 대상 SHA 를 고정하고 구현 레인을 종료한 뒤 검토를 띄운다 ·
`code-reviewer` 는 호출 시 `model: sonnet` 명시 · 레인 리포트는 worktree 의 `_workspace/` 에 쌓이므로
worktree 를 지우기 전에 루트로 옮긴다 · rollback 기준은 고정 SHA 가 아니라 「분기해 나온 현재 main」 ·
게이트가 자기 입력을 재계산하면 항진명제 · 있는지만 보는 단언은 없어야 할 것을 못 막는다.

## 6. 환경 메모 — 원래 장비에서 「실행이 너무 느리다」의 원인

MCP·플러그인 타임아웃 값의 문제가 아니었다. **GlobalProtect VPN 이 끊겨 사내 프록시
(`ai-proxy.onkakao.net:3128`)에 TCP 연결이 안 됐고**, 외부로 나가는 MCP 넷(jetbrains · playwright 플러그인 ·
github 플러그인 · claude.ai Claude Docs)이 각각 30초 연결 타임아웃까지 기다렸다. 훅은 전부 실측
0.1초 안팎(statusline 0.77초)으로 문제가 아니다. 새 장비에서 같은 증상이면 먼저 VPN 을 확인한다.

시동 비용을 줄이려면(선택): `playwright`·`github` 플러그인은 claude-in-chrome 과 `gh` CLI 와 겹치므로
꺼도 된다 · `jetbrains` MCP 는 `npx -y @jetbrains/mcp-proxy`(버전 미고정)라 매 시동마다 registry 를
조회한다 — IntelliJ 를 안 쓰는 장비에서는 비활성화.
