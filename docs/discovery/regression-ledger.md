# V2 regression ledger — legacy 회귀 사례와 예방 제약

slice **0B**. base `ec115a7`. 계약은 `reports/evidence/m0/0b/scope.md`.

`milestone-0.md`가 요구하는 **8계열**의 legacy 회귀를 사례 단위로 적고, 각 사례가 V2에서
**구조적으로 재발할 수 없게 하는 제약**을 붙인다. 이 문서는 legacy를 복제하기 위한 것이
아니라 **같은 실패를 두 번 하지 않기 위한 것**이다.

## 0. 이 문서의 규약

### 0.1 사례는 5필드를 갖는다

| 필드 | 무엇을 적는가 |
| --- | --- |
| **관찰** | legacy에서 실제로 무슨 일이 있었는가. 코드 동작·commit 기록에서 확인되는 사실만 |
| **사용자 영향** | 운영자가 무엇을 잘못 보거나 잃었는가. **관측되지 않았으면 `판정 불가`라고 적는다** |
| **V2 예방 제약** | 재발을 **구조적으로** 막는 것 — 타입 · 계약 · 테스트 중 최소 하나 |
| **검증 방법** | 그 제약이 실제로 걸려 있음을 무엇으로 고정하는가 |
| **근거** | `ed4b06c` 기준 파일 경로·행 범위, commit 해시와 날짜 |

### 0.2 상태 어휘 셋

- **`legacy에서 수정됨`** — legacy가 그 결함을 고쳤다. **"V2에서 안 해도 된다"는 뜻이
  아니다.** 수정 이력의 존재는 **그 결함 부류가 이 도메인에서 반복된다는 증거**로 읽는다.
  R-BASIS-03이 정확히 그 반례다 — 같은 결함이 세 경로에 있었고 하나만 고쳐졌다.
- **`legacy에 잔존`** — `ed4b06c` 시점에 그대로 있다.
- **`판정 불가`** — 결함의 존재나 영향을 legacy 기록만으로 판정할 수 없다. **억지로 채우지
  않는다.** 판정에 필요한 것이 무엇인지 적고, 필요하면 §10에 `OPEN-REG-NN`으로 등록한다.

### 0.3 인용 규약

- 모든 legacy 인용은 **`ed4b06c` 기준**이며 **저장소 루트부터의 전체 경로**를 쓴다.
  **파일명만 쓰지 않는다** — 기계로 확인할 수 없고 실제로 모호하다(`summary.py`는
  `ed4b06c`에 **두 개** 있다: `app/ai/predictors/historical/` · `app/services/paper_bidding_backtest/`).
- 행 범위는 **블록 경계까지** 뜬다. 서술이 걸리는 문장만 잘라 뜨지 않는다.
- 수치를 인용할 때 **legacy가 기록한 측정일**을 함께 적는다. 측정일이 없는 수치는
  사실로 승격하지 않는다.
- 이 문서 안의 자기참조에 **줄 번호를 쓰지 않는다.** 항목은 id(`R-BASIS-01`)로 가리킨다.
  legacy 파일의 행 범위는 예외다 — 그 파일은 이 저장소가 편집하지 않는다.

### 0.4 예방 제약은 legacy 동작의 복제가 아니다

legacy가 그렇게 한다는 사실은 **관찰**이다. 제약은 승인된 도메인 명세·운영자 결정·
`v2-지침서.md` 규율에서 나온다. **운영자 결정으로 이미 확정된 것은
`reports/evidence/m0/0a2/decisions.md`를 인용한다** — 이 문서가 새로 결정하지 않는다.

"주의한다" · "일관되게 쓴다" 같은 규율 서술은 **예방책으로 계상하지 않는다.** 제약은
타입(컴파일 단계에서 막힌다) · 계약(경계에서 거부된다) · 테스트(회귀가 빨간불이 된다)
중 하나의 형태를 가져야 한다.

### 0.5 근거 확인 방법 — 무엇을 기계로, 무엇을 사람이

`ed4b06c`는 이 저장소 밖(`bid-vector` symlink)이라 **Codex 리뷰어 worktree에서 재현되지
않는다.** 0A2 매 라운드 residual risk에 그 문장이 적혔고 실제로 인용 오류 4건이 0B 선행
조사에서야 발견됐다. 그래서 이 slice는 확인을 **두 축으로 나눈다.**

| 축 | 대상 | 방법 |
| --- | --- | --- |
| **기계** | 경로 존재 · 행 범위가 파일 길이 안 · commit 존재와 subject·날짜 | 스크립트로 전수(`reports/evidence/m0/0b/commands.md`에 본문 인라인) |
| **사람** | 그 행 범위의 **내용**이 서술과 맞는가 | **정본을 계산할 수 없다.** 항목마다 직접 열어 읽고 확인 사실을 evidence에 기록 |

---

## 1. 금액 basis — 기초금액 / 예정가 / 추정가격 / 낙찰가

**계열 요약**: `#350`~`#359` 아홉 커밋이 basis 하나로 반복 수정됐다. 기록된 실제 피해는
**실투찰 실격 1건 + 패찰 2건**이다(`881ce27` 2번째 커밋 본문). 그 문장 자체가 선행 서술의
정정문이다 — 원래 "실격 3건이 전부 하한 밀착"이라 적혀 있었고 리뷰가 기록과 대조해 뒤집었다.

### R-BASIS-01 · 운영자 예산 필터가 운영자가 지정하지 않은 basis로 거른다

- **관찰**: `StrategyFilter`가 운영자 전략의 `min|max_budget_estimate`를
  `Project.budget_estimate`와 비교한다 — `project_budget = float(project.budget_estimate or 0.0)`
  뒤 `< min` / `> max`로 탈락시킨다. `Project.budget_estimate`는 추정가격(ex-VAT)이고,
  운영자 의도가 **기초금액**임은 확정됐다.
- **사용자 영향**: 기초금액과 추정가격은 VAT·사정률만큼 **체계적으로** 다르므로 임의 오차가
  아니다. 경계 근처 공고가 **한 방향으로 일관되게** 누락되거나 포함된다. 운영자에게는
  "그 공고가 존재하지 않는 것"과 구별되지 않는다.
- **V2 예방 제약** — **타입**: basis는 값에 붙는다. 운영자 전략의 금액 필드를 `(값, basis)`
  쌍으로 저장하고 **서로 다른 basis의 금액이 하나의 비교 연산에 들어가는 것을 컴파일
  단계에서 막는다.** 근거는 운영자 결정 `OPEN-STR-01`(basis를 1급 필드로 승격, 기존 저장
  값은 기초금액으로 해석) — `0a2/decisions.md`.
- **검증 방법**: 과세/비과세 경계 쌍을 fixture로 고정하고 같은 운영자 값에 대해 필터 결과가
  basis에 따라 갈리는지 고정한다. **같은 쌍이 감시 경로와 검색 경로(R-BASIS-02)에서 같은
  답을 내는지도 함께 고정한다** — 두 경로가 다른 basis를 쓰면 "제안에는 뜨는데 검색에는
  안 나온다"가 된다.
- **근거**: `app/services/opportunity_monitoring/filters.py:62-70`(`:65-68` 두 guard,
  `:69-70` 사유 append) · `app/models/models.py:70`(`budget_estimate = Column(Float)`),
  `:222-223`(`min|max_budget_estimate`).
- **상태**: **`legacy에 잔존`**

### R-BASIS-02 · 검색 API의 예산 필터도 같은 결함이다 (라이브 경로)

- **관찰**: `GET /projects`의 `budget_min`/`budget_max`가 `Project.budget_estimate >= / <=`로
  필터한다. 파라미터 description이 **`"budget_estimate >= 값으로 필터"`**라고 내부 컬럼명을
  그대로 노출할 뿐 **어느 basis인지 말하지 않는다.** `app/api/routes.py:38`에 wiring된
  라이브 경로다.
- **사용자 영향**: R-BASIS-01과 같되 이쪽은 운영자가 **직접 입력한 숫자**라 오차가 더
  직접 체감된다. 두 경로가 같은 basis 결함을 공유하므로 **오차가 서로를 상쇄해 드러나지
  않는다.**
- **V2 예방 제약** — **계약**: 외부 계약(API 파라미터)이 basis를 명시하고 **basis 미표기
  금액 입력을 거부한다.** capability `STR-16`의 acceptance가 이 basis 일치를 이미 고정한다
  (0A2 확정 — "금액 범위로 검색할 때 그 값이 어느 basis인지가 선언돼 있고, 비교 대상
  금액과 basis가 같다").
- **검증 방법**: 계약 테스트 — `budget_min`의 basis가 스키마에 선언돼 있고 basis 미표기
  요청이 거부되는지 고정.
- **근거**: `app/api/projects.py:71-80`(파라미터 선언), `:112-116`(필터),
  `app/api/routes.py:38`(wiring).
- **상태**: **`legacy에 잔존`**

### R-BASIS-03 · capture 점수 분모가 경로마다 달라 같은 공고가 경로별로 다른 점수를 받았다

- **관찰**: `budget_capture_score = recommended_amount / budget_estimate`에서 분자는 모든
  경로가 기초금액 basis로 산출하는데 **분모는 경로마다 달랐다.** 추정가격을 넣던 **세
  경로**(`allocation.save_decision` 폴백 · `opportunity_monitoring` 스캔→영속 결정 ·
  `paper_bidding_backtest`)에서 과세 공고의 capture가 `rate × 1.1 → 1.0` clamp로 부풀었다.
- **사용자 영향**: 같은 공고가 **어느 경로로 평가됐는지에 따라** opportunity/priority 점수와
  근거 문구가 갈렸다.
- **V2 예방 제약** — **타입 + 테스트**: 판정 커널을 하나만 두고 경로는 입력 조립만 한다.
  커널 경계 시그니처가 basis 타입을 요구한다(legacy도 같은 결론에 도달해
  `DecisionSignals.budget_estimate`를 `money.BaseAmount`로 좁혔다 — 그러나 그것은 **관찰**이고,
  V2의 제약은 `v2-지침서.md` §4.1 Money 계약에서 나온다).
- **검증 방법**: property test — 같은 입력 fact 집합에 대해 세 경로의 판정 산출이 **바이트
  동일**한지 고정.
- **근거**: commit `0755695`(#354, 2026-08-06) 1번째 커밋 본문. 뉴타입을 `bid_base` 경계
  시그니처에 강제한 것은 별개 commit `c4ec93b`(#262, 2026-07-25), `app/services/bid_base.py`를
  만들어 투찰가를 기초금액 기준으로 옮긴 것은 `4645ce4`(#162, 2026-07-15).
- **상태**: **`legacy에서 수정됨`** — 단 **감시·검색 경로는 R-BASIS-01·02로 남았다.**
  같은 결함 부류가 세 경로에 있었고 **하나만 고쳐졌다.**

### R-BASIS-04 · predictor만 basis를 옮기고 예산 상한을 남겨 추천가가 실격선 아래로 내려갔다

- **관찰**: `_resolve_recommended_amount`의 `budget_cap`(추정가격, ex-VAT)이 predictor의
  기초금액-relative `price_range`를 자르면서 **하한까지 깎았다**(`price_lower = min(price_lower, budget_cap)`).
  낙찰하한 × 기초금액이 추정가격을 넘는 과세 공고에서 추천가가 **법정 하한 아래로** 내려간다.
  commit이 원인을 특정한다 — 상한 도입 시점(`6b7f185`, 2026-05-10)에는 predictor도 추정가격
  기준이라 basis가 일치했고, `4645ce4`(#162)가 predictor만 기초금액으로 옮기며 상한을 남겨
  갈렸다. **"설계가 아니라 부수 피해"**라고 commit이 직접 적는다.
- **사용자 영향**: **제출하면 즉시 실격인 추천가.** 운영 DB 홀드아웃 400건 중 **78건**
  (19.5%) — 측정 시점 **2026-08-07**(`2610826` 작성 시점).
- **V2 예방 제약** — **타입**: 서로 다른 basis의 금액이 하나의 `min`/`max` 연산에 들어갈 수
  없다. 그리고 최종 추천가가 **어느 제약에 binding됐는지**가 결과에 실린다 — 값만 있고
  근거가 없는 산출을 만들 수 없게 한다.
- **검증 방법**: 과세 공고(기초금액 > 추정가격)에서 추천가가 법정 하한 이상임을 property로
  고정. 두 basis 값을 같은 `min` 호출에 넣는 코드가 **타입 오류가 되는지** 컴파일 테스트로
  고정.
- **근거**: commit `2610826`(#356, 2026-08-07) 1번째 커밋 본문 · `tests/test_budget_cap_basis.py`.
- **상태**: **`legacy에서 수정됨`**

### R-BASIS-05 · 그 수정의 초판이 "시나리오 범위 하단"을 법정 하한으로 취급했다

- **관찰**: R-BASIS-04 초판이 `price_range_min` **전체**를 상한 위로 풀었다. `price_range_min`은
  후보 예측가의 최솟값 = **시나리오 범위 하단**일 뿐 법정 하한이 아니다. 그 결과 legacy가
  이미 합법이던 공고 **34건**의 값이 올라갔고 **그중 28건이 낙찰가에서 멀어졌다**(과추천).
  상한 초과 권한을 guardrail이 낸 `floor_price` 보고 건으로 한정하니 34 → **12**로 줄었고,
  최종(V3)에서는 두 조건 AND(게시 법정하한 실보고 AND 기초금액÷추정가격 ≤ 1.15)로 좁혔다.
- **사용자 영향**: **하한 미달을 고치는 수정이 반대 방향의 과추천을 만들었다.** 두 방향 다
  basis 계열이다.
- **V2 예방 제약** — **타입**: **시나리오 범위 하단과 법정 하한을 같은 타입으로 두지 않는다.**
  전자는 예측 산출물이고 후자는 규정 해석 결과다. 교차 대입이 불가능해야 한다.
- **검증 방법**: 두 값의 교차 대입이 컴파일 오류임을 고정. 반사실 코호트(이미 합법이던
  공고)에서 **값 변경 0건**임을 fixture로 고정.
- **근거**: commit `2610826` 2번째 커밋 본문(`[80]` 항목) · 3번째 커밋 본문(V3 반사실 실측
  741건).
- **상태**: **`legacy에서 수정됨`**

### R-BASIS-06 · 침묵 폴백 — 검증 안 된 클라이언트 입력이 신호 없이 투찰 base가 됐다

- **관찰**: `predict_project_price`가 predictor budget을 `resolved_bid_base or request.budget_estimate`로
  해석했다. 좌변이 0/falsy면 **basis 무표기 클라이언트 입력**이 아무 신호 없이 투찰 base가
  되고, 과세 공고에서는 그대로 ~10% 낮은 투찰가가 나온다. commit이 "이 저장소 실투찰 실격
  이력과 같은 계열의 실패"라고 적는다.
- **사용자 영향**: 운영자가 보는 추천 투찰가가 **어느 금액에 곱해진 것인지 알 수 없었다.**
- **V2 예방 제약** — **타입**: 폴백 자체를 금지할 필요는 없다. **침묵 폴백을 금지한다** —
  금액과 provenance가 한 값으로 묶여 표시 경계까지 함께 이동하고, **값만 있고 출처가 없는
  응답을 만들 수 없다.** 근거는 `v2-지침서.md` §4.1(Money 계약)이며, legacy가 같은 결론에
  도달한 것(`bid_base_source` 노출)은 **관찰**이다.
- **검증 방법**: 클라이언트 금액이 base로 쓰인 응답에 그 사실이 반드시 실리는지 계약
  테스트로 고정.
- **근거**: commit `0755695` 2번째 커밋 본문.
- **상태**: **`legacy에서 수정됨`**(폴백 유지 + 출처 노출)

### R-BASIS-07 · 영속된 감사 문구가 분모와 다른 금액을 말했다

- **관찰**: 분모를 기초금액으로 고친 뒤에도 영속 감사 문구(`BidDecisionRecord.reasoning`)가
  "예산 대비"라고 말하고 있어 **문구까지 함께 고쳐야 했다.** legacy 코드가 이 위험을 주석에
  남겼다 — "영속 감사 문구가 실제 분모와 다른 금액을 말하면 그 자체로 basis 오해를
  재생산한다".
- **사용자 영향**: 저장된 판단 근거를 나중에 읽는 운영자가 **잘못된 분모를 믿는다.**
  다만 **이미 저장된 과거 행이 소급 정정됐는지는 commit에 기재가 없다** — golden 6건의 문구
  치환만 기록돼 있다. **소급 범위는 `판정 불가`**이며 §10 `OPEN-REG-02`로 등록했다.
- **V2 예방 제약** — **계약 + 아키텍처 테스트**: 도메인이 locale 문자열을 생성·영속하지
  않는다. 영속 감사 기록은 `(reason code, 구조화 payload)`이고 **문구는 표시 시점에
  렌더링**한다. 분모나 임계를 바꾸면 문구가 자동으로 따라온다.
- **검증 방법**: architecture test — 도메인 모듈에서 한국어 문자열 리터럴이 산출물로 나가는
  경로가 없음을 고정. 저장된 감사 payload를 다시 렌더링해 현재 계산과 일치하는지 고정.
- **근거**: `app/services/allocation_core.py:133-179`(문장 리스트 조립, `:141-143`이 그 주석) ·
  `app/models/models.py:344`·`:477`(`reasoning = Column(Text, default="")`) ·
  commit `0755695` 4번째 커밋 `U10-d`.
- **상태**: **`판정 불가`** — 코드는 수정됐다. **과거 영속 행의 상태**는 legacy 기록으로
  판정할 수 없다.

---

## 2. rate scale — fraction / percent

**이 계열은 사용자 영향 축의 근거가 얇다.** 5건 중 **사용자 피해가 관측된 것은 R-RATE-04
하나**뿐이고, 나머지는 **코드가 스스로 공시한 구조 결함**이라 "누가 무엇을 잘못 봤는가"가
비어 있다. 그 넷의 사용자 영향은 **`판정 불가`로 적는다** — §0.1의 규약이고, 억지 서술을
넣지 않는다. 결함의 존재 자체는 코드로 확정된다.

### R-RATE-01 · 같은 규칙이 7곳에 독립 구현됐고 한 곳만 임계가 어긋나 회귀 엔진이 됐다

- **관찰**: "값이 임계치보다 크면 백분율로 보고 `/100`" 규칙과 그 임계치가 **7곳에 독립
  구현**돼 있었고, **정산 백테스트(`paper_bidding_backtest`)만 임계치가 `2.0`**이라
  **`(1.5, 2.0]` 구간의 율을 다르게 해석하는 회귀 엔진**이었다. 통합 모듈의 docstring이
  이 사실을 **자기 존재 이유로** 적는다.
- **사용자 영향**: **`판정 불가`.** `(1.5, 2.0]` 구간의 오해석이 정산 백테스트에서 몇 건을
  틀리게 했는지는 legacy 어디에도 없다. §10 `OPEN-REG-03`.
- **V2 예방 제약** — **타입**: 율은 **생성 지점에서 한 번** 정규화되고 그 뒤로는 단위를
  추측하지 않는다. `Rate` 타입이 fraction임을 보장하고 percent 입력은 **경계에서만**
  변환된다 — 도메인 안에 "값 크기로 단위를 추측하는" 코드 경로가 존재할 수 없다.
- **검증 방법**: architecture test — 도메인 모듈에 크기 기반 단위 추측 분기가 없음을 고정.
  경계값(0.5 · 1.5 · 2.0 전후) fixture로 변환이 한 곳에서만 일어남을 고정.
- **근거**: `app/domain/rate_normalization.py:3-7`(docstring이 7곳·`2.0` 어긋남을 적는다),
  `:70-73`(`PERCENT_SCALE_THRESHOLD: Final[float] = 1.5`), `:105-113`(`to_bid_rate_fraction`).
- **상태**: **`legacy에서 수정됨`**(단일 출처로 통합) — **그러나 수렴은 끝나지 않았다**(R-RATE-02).

### R-RATE-02 · 콜사이트 수렴이 끝나지 않았음을 코드가 스스로 공시한다

- **관찰**: 통합 모듈이 **"콜사이트 수렴은 아직 끝나지 않았다"고 스스로 적고 남은 넷을
  이름으로 열거한다** — `prediction_dataset._resolve_bid_rate`의 tier 게이트 셋 ·
  `prediction_dataset._normalize_bid_rate_value` · `bid_target_signals`의 SQL WHERE ·
  `scripts/measure_stored_bidrate_basis.py::_gate`. 앞 둘은 학습 라벨 해석 경로를 **의도적으로
  동결**했기 때문이고, **SQL은 파이썬 술어를 부를 수 없어 구조적으로 남는다.**
- **사용자 영향**: **`판정 불가`** — 남은 콜사이트가 실제로 잘못된 답을 냈다는 관측이 없다.
  **구조 결함의 존재는 확정이고 피해는 미관측**이다.
- **V2 예방 제약** — **타입**: R-RATE-01과 같은 제약이 이 항목도 닫는다. 콜사이트가
  "옮겨졌는가"를 사람이 추적할 필요가 없어야 한다 — **타입이 맞지 않으면 컴파일되지
  않는다.**
- **검증 방법**: R-RATE-01과 동일. 별도 테스트를 만들지 않는다.
- **근거**: `app/domain/rate_normalization.py:56-61`(그 공시와 넷의 열거).
- **상태**: **`legacy에 잔존`**

### R-RATE-03 · 값 크기로 단위를 추측하는 규칙 자체

- **관찰**: `capacity_score`가 0~1과 0~100 양쪽을 받아 **`>1이면 /100`**으로 정규화한다.
  값 크기가 단위의 근거가 된다.
- **사용자 영향**: **`판정 불가`.** 이 경로로 잘못된 점수가 운영자에게 갔다는 관측이 없다.
- **V2 예방 제약** — **타입**: 이 형태는 **`폐기` 판정을 이미 받았다**(capability `QUAL-11`의
  동반 `폐기` 판정, 운영자 결정 `OPEN-QUAL-08` 분할 확정 — `0a2/decisions.md`). V2는 스케일을
  타입으로 선언하고 **크기 기반 추측 경로를 두지 않는다.**
- **검증 방법**: `QUAL-11` acceptance가 이미 고정한다 — "0~1 스케일과 0~100 스케일을 값
  크기로 구분하는 경로가 존재하지 않는다".
- **근거**: `app/services/classification/text.py:108-117`.
- **상태**: **`legacy에 잔존`** (V2 `폐기` 확정)

### R-RATE-04 · 밴드 밖 율이 `None`이 되어 float 비교에서 크래시했다

- **관찰**: 예비가 패턴 블록에서 밴드 밖 율이 `None`으로 떨어졌고, 그 값이 float 비교에
  들어가 **크래시**했다. `#360`의 회귀이며 `a1ca0b6`(#361, 2026-08-11)이 핫픽스했다.
- **사용자 영향**: **이 계열에서 유일하게 관측된 피해** — 해당 경로가 예외로 죽었다.
  운영자에게는 답이 오지 않는 것으로 나타난다.
- **V2 예방 제약** — **타입**: "값 없음"을 `None`으로 흘려보내지 않는다. **측정 불가는
  1급 상태**이며(`Unmeasurable` / 사유 있는 `Uncertain`) 비교 연산에 들어갈 수 없다.
  근거는 `v2-지침서.md` §4.2와 운영자 결정 `OPEN-SET-02`(동점은 가격만으로 판정 불가인
  **제3 상태**) · `OPEN-QUAL-06`(`Uncertain`을 sealed subtype으로 분해).
- **검증 방법**: 밴드 밖 입력에 대해 산출이 **사유 있는 측정 불가**이고 예외가 아님을
  고정. 측정 불가 값이 비교 연산에 들어가면 컴파일 오류임을 고정.
- **근거**: commit `a1ca0b6`(#361, 2026-08-11) ·
  `app/ai/predictors/historical/summary.py`(그 커밋이 고친 파일).
- **상태**: **`legacy에서 수정됨`**

### R-RATE-05 · 유효 창과 판별 임계가 독립 조정 불가인데 그 사실이 주석에만 있다

- **관찰**: 유효 창 상단 `BID_RATE_PLAUSIBLE_MAX = 1.5`가 판별 임계
  `PERCENT_SCALE_THRESHOLD = 1.5`와 같은 것은 **우연이 아니라 유도 결과**다 — 정당한
  fraction이 나눠지지 않고 살아남으려면 `threshold >= MAX`여야 한다. 즉 **`MAX`를 올리면
  `threshold`도 같이 올려야 하는데 그 유도 관계가 주석으로만 존재한다.**
- **사용자 영향**: **`판정 불가`** — 실제로 한쪽만 바뀌어 사고가 났다는 기록이 없다.
- **V2 예방 제약** — **계약(정책 데이터)**: 두 값이 함께 움직여야 한다면 **하나의 정책
  객체로 묶고 version을 붙인다.** 주석이 아니라 **구조가** 결합을 표현한다. `v2-지침서.md`
  §5(매직넘버 금지 — 정책 데이터/설정 외부화)에서 나온다.
- **검증 방법**: 정책 version이 판정 결과에 실리는지 고정. 두 값을 따로 바꾸는 구성이
  **로드 시점에 거부**되는지 고정.
- **근거**: `app/domain/rate_normalization.py:78-96`(유도 관계 주석), `:101-102`
  (`BID_RATE_PLAUSIBLE_MIN/MAX`).
- **상태**: **`legacy에 잔존`**

---

## 3. VAT / provenance와 파생값 오염

**계열 요약**: 이 계열의 결함은 전부 **"값을 보고 출처를 추측한다"**는 한 뿌리에서 나온다.
legacy도 같은 결론에 도달해 `#359`에서 **값이 아니라 출처로 판정**하도록 바꿨다. 운영자
결정 `OPEN-DEC-08`이 그 원칙을 V2에 확정했다 — **write provenance를 갖고 새로 수집·판정한
행만 `clean`으로 인정하고, legacy 유래 행은 provenance 미상으로 격리한다**(`0a2/decisions.md`).
이 결정 하나가 R-PROV-02 · 03 · 04 · 06 · 07을 함께 닫는다.

### R-PROV-01 · "66% 오염"이 5곳에 사실처럼 복제됐으나 측정 근거가 없다

- **관찰**: `base_amount`의 66%가 실제 기초금액이 아니라는 문자열이 **5곳에 복제**돼 있다.
  이번 조사가 출처 포인터까지 추적했다 — commit `2f9bad7`(#199, 2026-07-17) subject의
  "66.2% 예정가-역산 오염 실측". **그러나 측정 방법·모수·측정일은 여전히 어디에도 없다.**
- **사용자 영향**: 이 수치가 캘리브레이션·백테스트 스코프 결정의 근거로 **다섯 곳에서
  인용**된다. 틀렸을 경우 그 결정들의 근거가 함께 무너진다.
- **V2 예방 제약** — **계약**: **측정일·모수·방법이 없는 수치는 정책 데이터로 승격할 수
  없다.** 정책 값은 version과 근거 문자열을 함께 갖고, 근거가 "실측"이면 **측정 시점이
  필수 필드**다. `v2-지침서.md` §5(매직넘버 금지 — 정책 데이터 외부화)와 `milestone-0.md`의
  "모든 도메인 숫자는 unit/basis/provenance가 정의되거나 `OPEN`"에서 나온다.
- **검증 방법**: 정책 데이터 로드 시점에 측정 시점 필드가 없는 실측 근거를 **거부**하는지
  고정. 판정 결과에 policy version이 실리는지 고정.
- **근거**: `app/services/base_amount_basis.py:3` · `app/services/floor_shortfall.py:16` ·
  `app/models/models.py:287` · `app/core/config.py:589` · `app/ai/holdout_reporting.py:210`
  (다섯 복제 지점) · `docs/operations/latest-award-holdout-backtest.md:295` ·
  commit `2f9bad7`(#199, 2026-07-17) subject.
- **상태**: **`판정 불가`** — `OPEN-NUM-01` 유지. **V2 문서는 이 수치를 인용하지 않는다.**
  출처 포인터를 찾은 것은 진전이지만 **사실로 승격하지 않는다.**

### R-PROV-02 · 분류기가 추정가격을 아예 보지 않아 10배 base도 정수면 `clean`을 통과했다

- **관찰**: `classify_base_basis`가 `Project.budget_estimate`를 인자로 받지 않던 시절
  **"추정가격의 10배 base도 정수이기만 하면 `clean`을 통과했다"**고 commit이 적는다.
  `#358`이 `suspect-ratio` 규칙(`base ÷ 추정가격 > 1.15` = 부가세 1.10 + 측정 마진 0.05)을
  **first-match 테이블 맨 앞**에 넣었다.
- **사용자 영향**: `clean` 버킷은 캘리브레이션·백테스트·집계가 **ground truth로 신뢰하는
  유일한 버킷**이다. 다른 금액 필드가 혼입된 행이 그 버킷에 있으면 밴드 캘리브레이션
  **전체의 전제**가 무너진다.
- **V2 예방 제약** — **타입 + 계약**: `clean` 판정은 **값의 형태가 아니라 write 경로**가
  결정한다(운영자 결정 `OPEN-DEC-08`). 수집 시점에 provenance가 기록되지 않은 금액은
  ground truth 버킷에 들어갈 수 없고, 그 배제가 **타입으로** 강제된다.
- **검증 방법**: provenance 없는 행이 캘리브레이션 corpus에 포함되면 **컴파일 또는 로드가
  실패**하는지 고정. `base ÷ 추정가격` 경계값(1.15 전후) fixture로 판정 순서를 고정.
- **근거**: commit `4a8e064`(#358, 2026-08-08) 2번째 커밋 본문 ·
  `app/services/base_amount_basis.py:200-220`(first-match 규칙 테이블 — `BASIS_SUSPECT_RATIO`가
  `BASIS_CLEAN`보다 앞이다).
- **상태**: **`legacy에서 수정됨`**

### R-PROV-03 · 같은 임계를 게이트는 곱셈형, 분류기는 나눗셈형으로 각자 구현했다

- **관찰**: 라이브 예산 상한 게이트가 곱셈형(`base > cap × 임계`), 저장 행 분류기가
  나눗셈형이라 **경계 근처에서 판정이 갈릴 수 있었다.** 통합하면서 나눗셈형으로 통일하고
  임계 상수를 `app/core/constants.BID_BASE_TRUST_RATIO_MAX`로 승격했다.
- **사용자 영향**: 경계 근처 공고에서 **라이브 게이트와 저장 라벨이 서로 다른 답**을 낼 수
  있었다.
- **V2 예방 제약** — **타입**: 같은 판정을 두 곳에서 구현할 수 없다 — 순수 술어를 하나 두고
  라이브와 배치가 **같은 함수를 부른다.** 부동소수 경계에서 형태 차이가 답을 가르지 않도록
  비교 형태까지 그 술어가 소유한다. `v2-지침서.md` §5(중복 최소화)와 §3의 순수 도메인 커널
  경계에서 나온다.
- **검증 방법**: 경계값 fixture(임계 ±1 ULP)에서 라이브 경로와 저장 경로의 판정이 **동일**
  함을 property로 고정.
- **근거**: `app/services/base_amount_basis.py:126-149`(`exceeds_base_trust_ratio` — 통일
  근거를 docstring이 직접 적는다) · commit `4a8e064` 2번째 커밋(상수 승격).
- **상태**: **`legacy에서 수정됨`**

### R-PROV-04 · 수집 write가 매 주기 백필의 재태깅을 되돌렸다

- **관찰**: `update_project_from_item`이 `budget_estimate or 이전값`으로 **양수면 무조건
  덮었다.** 그 탓에 6h scsbid 개찰 패스(**예정가**)와 추정가격 미공급 재수집(**기초금액
  사본**)이 provenance 분류의 분모와 예산 상한 게이트를 **조용히 바꿨다.** 수정은 값이
  아니라 **출처**로 판정하도록 바꾼 것이다.
- **사용자 영향**: 백필로 재태깅한 provenance가 **다음 수집 주기에 되돌아갔다.** 운영자에게는
  같은 작업을 반복해도 상태가 안 바뀌는 것으로 보인다.
- **V2 예방 제약** — **타입**: 금액은 `(값, 출처)`로만 존재하고 **출처가 다른 값이 서로를
  덮어쓸 수 없다.** 덮어쓰기는 값의 크기가 아니라 **출처 우선순위**로 결정되며 그 우선순위는
  정책 데이터다. 운영자 결정 `OPEN-DEC-08`(write provenance 기준)에서 나온다.
- **검증 방법**: 낮은 신뢰 출처가 높은 신뢰 출처를 덮는 write가 **타입 오류**임을 고정.
  같은 행에 두 주기를 연속 적용해 provenance가 **수렴**하는지(진동하지 않는지) 고정.
- **근거**: commit `ddee938`(#359, 2026-08-08) 2번째 커밋 본문 ·
  `app/services/koneps/budget_fields.py`(그 커밋이 신설) · `app/schemas/koneps_items.py`.
- **상태**: **`legacy에서 수정됨`**

### R-PROV-05 · 어휘 문자열이 두 축에서 충돌해 접두 리네임으로 수습했다

- **관찰**: `base-fallback`이 **서로 다른 두 축에서 같은 값**으로 쓰여 `estimate-base-fallback`
  으로 접두 리네임해야 했다. `#359`가 `EstimatedAmountSource` 어휘를 선언하면서 **이웃
  어휘와 병합 금지 사유를 명시**했다.
- **사용자 영향**: **관측된 직접 영향 없음.** 두 축의 provenance가 같은 문자열로 저장돼
  있었다면 사후 구분이 불가능했을 자리이며, 그 일이 실제로 일어났다는 기록은 없다.
- **V2 예방 제약** — **타입**: provenance 어휘는 **문자열이 아니라 sealed enum**이다. 서로
  다른 축의 어휘는 **다른 타입**이라 같은 필드에 들어갈 수 없고, 병합은 컴파일 오류가 된다.
  `v2-지침서.md` §4.2(sealed 계층)에서 나온다.
- **검증 방법**: 두 축의 enum이 서로 대입 불가임을 컴파일 테스트로 고정. 저장 표현이
  바뀌어도 판정이 안 바뀌는지 round-trip 테스트로 고정.
- **근거**: commit `ddee938` 2번째 커밋 본문(`app/core/constants.py`의 `EstimatedAmountSource`
  어휘 선언 + 이웃 어휘 병합 금지 사유).
- **상태**: **`legacy에서 수정됨`**

### R-PROV-06 · `est == base` 사각지대 — 규칙이 구조적으로 볼 수 없는 코호트가 `clean`의 30%다

- **관찰**: 수집이 추정가격을 못 얻으면 `matching.resolve_budget_estimate`가 `base_amount`를
  **그대로 추정가격으로 쓴다.** 두 금액이 **같은 값의 두 사본**이 되어 비율이 항상 1.0이라
  `suspect-ratio` 규칙에 **구조적으로 걸리지 않는다.** 실측 `clean` 33,601행 중 **10,110행
  (30%)** — 그 수치를 소스 주석이 직접 적는다.
- **사용자 영향**: 캘리브레이션 ground truth 버킷의 **30%가 검증 커버리지 밖**이다.
  운영자에게는 `clean`이라는 라벨만 보이고 **그 라벨이 검증된 것인지 검증 자체가 불가능한
  것인지 구분되지 않는다.**
- **V2 예방 제약** — **타입**: **"검증 통과"와 "검증 불가"를 같은 라벨로 두지 않는다.**
  두 금액이 독립 관측이 아닐 때는 `clean`이 아니라 **사유 있는 `Uncertain`**이 산출된다
  (운영자 결정 `OPEN-QUAL-06` — `Uncertain`을 sealed subtype으로 분해). `OPEN-DEC-08`의
  provenance 기준이 이 코호트를 자동으로 격리한다.
- **검증 방법**: `est == base`인 입력에서 판정이 `clean`이 **아님**을 고정. 커버리지 밖
  코호트의 크기가 리포트에 **항상 함께 실리는지** 고정.
- **근거**: `app/services/base_amount_basis.py:171-175`(사각지대와 30% 실측을 직접 적는다) ·
  `docs/operations/base-amount-basis-backfill.md:136-142`.
- **상태**: **`legacy에 잔존`** — 런북이 **"수집 단계에서 추정가격을 확보하는 것이 여전히
  유일한 해법"**이라고 적는다. 관측만 하고 해소되지 않았다.

### R-PROV-07 · 개찰 파생 분모로 `clean`이 굳은 3,982행은 구조적으로 회복 불가다

- **관찰**: R-PROV-04의 가드 **이전에** 이미 세탁된 분모 위에서 `clean`으로 굳은 행은
  재분류로 **구조적으로 회복되지 않는다** — 백필도 같은 분모를 읽고 `clean` 규칙이
  `derived-yega` 판정보다 앞서기 때문이다. 실측 잔여 **3,982건**(awarded 3,949 ·
  re_notice 32 · cancelled 1)이고 **awarded 3,949는 공고 피드 재수집도 닿지 않아 영구**다.
- **사용자 영향**: 재캘리브레이션·백테스트가 `clean` 버킷을 corpus로 쓸 때 **이 오염이 함께
  들어간다.** 운영자가 보는 정확도 리포트가 그만큼 낙관적이다.
- **V2 예방 제약** — **계약**: 운영자 결정 `OPEN-DEC-08`이 이 항목을 직접 닫는다 —
  **legacy `clean` 라벨을 V2 corpus에 승계하지 않는다.** legacy 유래 행은 provenance
  미상으로 격리되므로 이 3,982행이 corpus에 들어올 경로 자체가 없다.
- **검증 방법**: legacy 유래 행이 ground truth corpus에 포함되면 **manifest 검증이 실패**
  하는지 고정(`fixtures/manifest.yaml`의 출처 분류 — 0C·fixture-curator 소관).
- **근거**: `docs/operations/base-amount-basis-backfill.md:122-132`, `:143-145`
  (런북이 해소 불가를 공시한다).
- **상태**: **`legacy에 잔존`**(해소 불가로 공시됨)

### R-PROV-08 · `0.0`을 "미상"으로 쓰는 관례 — 금액과 정산 여부 양쪽에 있다

- **관찰**: `HistoricalData.base_amount = Column(Float, default=0.0)`이고 각 write마다 `> 0`
  가드를 **손으로 반복**한다. 같은 관례가 정산에도 있다 — 미정산
  `TenderResult.winning_amount`가 `NULL`이 아니라 **`0.0`**이고, 로더가
  `settled = (record.winning_amount or 0.0) > 0.0`으로 판정한다.
- **사용자 영향**: **"값이 0원"과 "값을 모른다"가 같은 표현**이다. 정산 성숙도로 판정하는
  ML 평가 창 embargo가 무력화될 수 있었다.
- **V2 예방 제약** — **타입**: 운영자 결정 `OPEN-ML-04`가 확정했다 — **"미정산"을 `0`으로
  적재하지 않는다.** 부재는 `null`이 아니라 **1급 상태**이고, 금액 타입에 "0원"과 "미상"이
  같은 표현을 가질 수 없다. 정산 관측 시각이 canonical fact로 들어간다.
- **검증 방법**: `0.0`을 미상으로 해석하는 코드 경로가 없음을 architecture test로 고정.
  미정산 레코드가 정산 성숙도 분모에 들어가면 **타입 오류**임을 고정.
- **근거**: `app/models/models.py:273` · `app/services/settlement_maturity.py:6-12`
  (함정을 적는 docstring), `:105`(`settled=(record.winning_amount or 0.0) > 0.0`) ·
  `tests/test_settlement_maturity_loader.py`.
- **상태**: **`legacy에 잔존`** — **규율로만 방어**한다(로더 docstring + 테스트). 타입이
  아니므로 새 write 경로가 생기면 다시 뚫린다.
  ※ 계열 8(성숙도 100% 오판)과 걸치지만 **이 계열이 소유**한다.

---

## 4. 법정 하한과 `Unmeasurable`

**계열 요약**: 이 계열은 **"측정 불가를 어떻게 표현하는가"**가 반복 쟁점이다. legacy는
`None` + 문서 금지로 방어했고(R-FLOOR-05), 그 형태는 호출부가 `?: 0.0`으로 접는 순간
**침묵이 안전으로 읽히는** 구조다. V2는 `v2-지침서.md` §4.2와 운영자 결정
`OPEN-SET-02`(제3 상태) · `OPEN-QUAL-06`(sealed subtype 분해)로 이 축을 타입으로 닫는다.

### R-FLOOR-01 · 지방계약 "명시 제외"가 선언에만 있고 런타임에는 국가계약 tier가 적용된다

- **관찰**: "지방계약(지자체)은 별도 율 체계로 이번 스코프에서 제외한다"가 **docstring 한
  줄뿐이고 실행되는 분기가 없다.** `app/ai/floor_applicability.py`의 선언 테이블은 조직 종류
  어미(공사/공단/청/시/군)를 **의도적으로 넣지 않아** 지자체가 기본값 `applicable`에 남고,
  테스트가 `("울산광역시 울주군", FLOOR_APPLICABLE)`로 그 동작을 **고정**한다.
- **사용자 영향**: 지자체 공고가 **국가계약 하한으로 조용히 판정된다.** legacy 자신이 남은
  한계로 적어 둔다 — "shallow undercuts (0.9~2.7%p, 지방계약/공공기관/수의견적 등 다른 하한
  체계 가능성) outside those gates are NOT resolved and still surface as `below_legal_floor`".
  **그 문자열의 출처는 백테스트 문서가 아니라 리포트 생성 스크립트의 caveat 문자열**이다
  (`scripts/backtest_latest_award_holdouts.py:1203-1204`). 백테스트 문서의 같은 한계 서술에는
  **숫자가 없고**, 그 문서의 `0.9~1.7%p`는 **다른 축**(보고 낙찰률 basis 독립성)의 수치라
  섞지 않는다.
- **V2 예방 제약** — **타입**: 운영자 결정 `OPEN-DEC-09`가 확정했다 — 지방계약을 V2 범위에
  두지 않되 **"실행되는 미적용 상태"로 구현한다.** 지자체 공고를 국가계약 하한으로 조용히
  판정하지 않고 **하한 체계 미지원을 관측 가능한 상태로 산출한다.** 선언과 실행이 일치해야
  한다.
- **검증 방법**: 지자체 발주기관 공고에서 하한 판정이 **생략되고 사유가 남는지** 고정.
  범위 밖 tier가 기본값으로 흘러드는 경로가 **타입상 존재하지 않음**을 고정.
- **근거**: `app/ai/predictors/legal_floor_spec.py:30-31`(제외 선언), `:125-150`(era-tier 표) ·
  `app/ai/floor_applicability.py:55-59`, `:126-127`(기본값 `applicable` 잔류 주석), `:128-163` ·
  `tests/test_floor_applicability.py:70` · `scripts/backtest_latest_award_holdouts.py:1203-1204`.
- **상태**: **`legacy에 잔존`**

### R-FLOOR-02 · 비국가기관 발주에 국가계약 tier를 일괄 적용해 오탐이 났다

- **관찰**: 기관 축 게이트가 없던 시절 비국가기관(산학협력단·협동조합·국유림관리소 등)
  발주에도 국가계약 tier를 적용해 **실제 위법이 아닌 낙찰을 위법으로 표시**했다.
  `below_legal_floor` 플래그를 기관 축 홀드아웃 **2,798건**에 처음 돌렸을 때 **49건**이
  잡혔고 **상당수가 적용 범위 밖**이었다(측정 시점: 라이브 기준선 **2026-07-26**).
- **사용자 영향**: **하한 미달 신호 전체의 신뢰가 훼손된다.** 오탐이 섞인 경보는 운영자가
  통째로 무시하게 만든다.
- **V2 예방 제약** — **타입**: 적용 가능성(`applicable` / `not_applicable` / `uncertain` /
  `separate_regime`)이 **판정 이전 단계의 1급 값**이고, 그것이 결정되지 않으면 하한 판정
  함수를 **호출할 수 없다**(타입이 요구한다). R-FLOOR-01의 `OPEN-DEC-09` 제약과 같은 축이다.
- **검증 방법**: 기관 종류별 fixture에서 적용 가능성 판정이 하한 판정보다 **먼저** 결정되고,
  `uncertain`이 `applicable`로 접히지 않음을 고정.
- **근거**: `app/ai/floor_applicability.py:1-18`(배경 docstring) ·
  `docs/operations/latest-award-holdout-backtest.md:168-189`.
- **상태**: **`legacy에서 수정됨`**(기관 축 게이트 도입) — 단 **지자체는 R-FLOOR-01로
  남았다.** 같은 부류에서 일부만 닫힌 형태다.

### R-FLOOR-03 · 성립 불가한 게시 하한율 `1.00000`이 47건 실재한다

- **관찰**: 게시 하한율 `1.00000`은 "예정가 전액 이상 투찰"을 뜻해 **낙찰하한의 의미가
  성립하지 않는데** 라이브에 **47건** 실재한다. `#357`이 개연 밴드
  (`0.30 ≤ rate ≤ 0.995`)를 수집·가격·검증 **3층**에서 공유하도록 차단했다.
- **사용자 영향**: `#356`이 게시 하한의 **존재**를 근거로 추천가에 예산 상한 초과 권한을
  주면서, 성립 불가한 `1.0`이 통과하면 **기초금액 전액이 추천가**가 될 수 있었다 —
  라이브 400건 중 **1건 실측**, 최대 **+10% 과추천**.
- **V2 예방 제약** — **타입 + 계약**: 외부에서 들어온 율은 **개연 밴드를 통과해야만 도메인
  타입이 된다**(smart constructor). 밴드 밖 값은 `0.0`이나 폴백이 아니라 **사유 있는 거부**로
  산출되고, 수집·가격·검증이 **같은 밴드 정의 하나**를 공유한다.
- **검증 방법**: 밴드 경계(0.30 · 0.995 전후) fixture로 세 층의 판정이 동일함을 고정.
  밴드를 통과하지 않은 값으로 도메인 타입을 만들 수 없음을 컴파일 테스트로 고정.
- **근거**: commit `3447952`(#357, 2026-08-07) · `app/domain/published_floor_rate.py:36-52`
  (밴드 상수와 그 근거) · `tests/test_published_floor_rate.py:31-56`.
- **상태**: **`legacy에서 수정됨`**

### R-FLOOR-04 · 안전 근거가 "소비자가 X만 한다"는 주석이라 소비자가 늘자 조용히 무효화됐다

- **관찰**: 밴드가 "분석 전용이어도 안전하다"고 든 근거는 **"라이브는 게시 하한을 `max()`로만
  접어 올리므로 안전 방향으로만 작동한다"**였다. `#356`이 그 근거를 **무효화했다** — V3
  게이트가 게시 하한의 존재를 근거로 예산 상한 초과 권한을 주기 때문이다. **안전 근거가
  소비자 목록에 의존했고, 소비자가 늘자 아무도 모르게 깨졌다.**
- **사용자 영향**: 무효화된 안전 근거 위에서 `1.00000`이 **가격 경로에 도달 가능**했다
  (R-FLOOR-03의 실측 1건이 그 결과다).
- **V2 예방 제약** — **테스트**: **"현재 소비자가 X만 한다"는 안전 근거를 주석으로 두지
  않는다.** 그런 가정은 **테스트로 표현**되어야 하고, 새 소비자가 그 가정을 깨면 **빨간불이
  된다.** 표현할 수 없는 가정이라면 그 값은 안전하지 않은 것으로 취급한다.
- **검증 방법**: 값의 소비 경로가 늘어날 때 기존 안전 가정 테스트가 깨지는지 고정
  (architecture test — 도메인 값의 소비자 집합이 선언과 일치하는지).
- **근거**: `app/ai/floor_applicability.py:61-66`(무효화 경위를 직접 적는다).
- **상태**: **`legacy에서 수정됨`**(밴드를 단일 출처로 이동 + 3층 공유) — 단 **"근거를
  테스트로 표현"까지는 가지 않았다.** V2가 가져갈 것은 그 미완의 절반이다.

### R-FLOOR-05 · 측정 불가를 `None`으로 표현하고 금지는 문서로만 한다

- **관찰**: 빈도 산출이 측정 불가일 때 `None`을 반환하고, **"`None`은 '위험 없음'이 아니라
  '판정 불가'다. 호출부는 이 둘을 절대 같은 표시로 합치면 안 된다"**를 **docstring으로**
  적는다. 서비스 층은 사유 4종을 나누고 각 사유에 "위험이 없다는 뜻이 아닙니다"를 명시한다.
- **사용자 영향**: 호출부가 `?: 0.0`으로 접으면 **"측정 불가"가 "위험 0%"로 표시된다.**
  침묵이 안전으로 읽힌다.
- **V2 예방 제약** — **타입**: 측정 불가는 `None`이 아니라 **사유를 가진 1급 상태**다
  (운영자 결정 `OPEN-SET-02` 제3 상태 · `OPEN-QUAL-06` sealed subtype 분해). 그 상태를
  숫자 표시로 접는 코드는 **컴파일되지 않는다.** **`OPEN-QUAL-06`이 사유 enum을 legacy
  evidence 문자열 이식이 아니라 도메인 명세가 먼저 정한다고 확정했다** — legacy의 사유 4종은
  후보이지 확정 목록이 아니다.
- **검증 방법**: 측정 불가 상태가 수치 필드에 대입되면 타입 오류임을 고정. 표시 계층에서
  "판정 불가"와 "0%"가 서로 다른 렌더링임을 계약 테스트로 고정.
- **근거**: `app/domain/floor_shortfall.py:144-153`(그 docstring) ·
  `app/services/notice_floor_shortfall.py:51-75`(사유 4종 + 문구).
- **상태**: **`legacy에 잔존`**(형태로는 `None`) — **계승할 것은 사유 분리와 문구의 규율이고,
  형태는 계승하지 않는다.** 이 항목이 측정 불가 어휘의 **소유자**이며 계열 6·7·8은 동형
  적용으로 참조한다.

### R-FLOOR-06 · 운영자 하한 override에 상한 검증이 없고 legacy가 그 위험을 스스로 공시했다

- **관찰**: override는 **의도적으로 게이트되지 않는다** — "운영자 지시이지 스크랩 데이터가
  아니며 조용히 버리면 명시적 결정을 지우는 것"이라는 이유다. 그러나 같은 주석이 **정직한
  단서**를 함께 적는다 — 요청 스키마가 `legal_floor_bid_rate`를 `ge=0.0`으로만 선언하고
  **상한이 없어 `1.0` override가 guardrail floor에 도달한다.** 스키마 조임은 OpenAPI 계약을
  바꾸므로 후속으로 미뤘다고 적는다.
- **사용자 영향**: 운영자의 잘못된 입력이 **검증 없이 가격 경로에 도달**할 수 있다.
  프런트엔드에 노출되지 않아 표면은 API 직접 호출로 한정된다.
- **V2 예방 제약** — **계약**: 운영자 결정 `OPEN-DEC-04`가 확정했다 — 개연 밴드 밖 override는
  **버리지도 통과시키지도 않고 사유와 함께 거부**한다. legacy의 원칙("명시적 지시를 조용히
  버리지 않는다")은 유지하되 **거부를 관측 가능한 결과로** 만든다.
- **검증 방법**: 밴드 밖 override 요청이 **사유 있는 거부**로 응답되는지 계약 테스트로 고정.
  조용히 수용되거나 조용히 무시되는 경로가 없음을 고정.
- **근거**: `app/services/bid_base.py:257-265`(게이트 미적용 선언과 정직한 단서 —
  **0B가 직접 재확인했다**, `commands.md` §9.1 재확인 절).
- **상태**: **`legacy에 잔존`**

### R-FLOOR-07 · 하한 미달 빈도의 최소 표본 수 150의 근거가 도메인이 아니라 통계적 편의다

- **관찰**: `MIN_ASSESSMENT_SAMPLES = 150`의 근거를 소스가 직접 적는다 — 이항 비율 추정의
  표준오차가 최악(p=0.5)에서도 `sqrt(0.25/150) ≈ 4.1%p`라 "37% vs 5%" 수준의 구분은
  견딘다는 것. **도메인 규정이 아니라 통계적 편의**다.
- **사용자 영향**: 표본이 **149건이면 판정 불가, 150건이면 판정된다.** 그 경계에 도메인
  근거가 없다.
- **V2 예방 제약** — **계약(정책 데이터)**: 운영자 결정 `OPEN-DEC-01`이 확정했다 — 150을
  유지하되 **도메인 요구가 아님이 확정**됐으므로 **정책 데이터로 외부화하고 근거 문자열을
  "통계적 편의(운영자 승인 2026-08-26)"로 기록**한다. **코드에 상수로 묻지 않는다.**
- **검증 방법**: 판정 결과에 policy version과 근거 문자열이 실리는지 고정. 임계 변경이
  코드 변경 없이 가능한지 고정.
- **근거**: `app/domain/floor_shortfall.py:42-46`(상수와 그 유도).
- **상태**: **`legacy에 잔존`**(값은 유지, **근거의 성격이 확정됐다**)

### R-FLOOR-08 · 표본 필터가 사정률 1 근방 밴드를 버려 편향 방향이 보장되지 않는다

- **관찰**: 사정률의 물리적 개연 범위 밖 값을 표본에서 제외하는데, 그 필터가 **사정률 1
  근방의 값도 함께 버린다.** 소스가 이웃 밴드(`ASSESSMENT_RATE_PLAUSIBLE_*`, 0.8~1.2)와
  **이름이 비슷하나 다른 밴드·다른 목적**임을 경고하며 **근거 통합 금지**를 적는다.
- **사용자 영향**: 임계 사정률이 1 근방일 때 — **실투찰이 가장 위험한 구간** — 편향 방향의
  보장이 없다. **조용히 안심시키는 방향으로 틀릴 수 있다.**
- **V2 예방 제약** — **계약**: 운영자 결정 `OPEN-DEC-06`이 확정했다 — 측정 가능한 편향을
  숨기지 않고 **구조화된 `biasDirection` 필드로 노출**한다. 표본 필터가 버린 구간이
  **결과에 함께 실린다.**
- **검증 방법**: 표본 제외가 일어난 판정에서 `biasDirection`과 제외 규모가 산출에 실리는지
  고정. 두 밴드가 **서로 다른 정책 값**임을 타입으로 고정(이름 유사에 의한 통합 방지).
- **근거**: `app/domain/floor_shortfall.py:48-57`(밴드와 통합 금지 경고) ·
  `app/services/floor_shortfall.py`(서비스 층 필터).
- **상태**: **`legacy에 잔존`**(공시만 하고 노출은 안 됨)

---

## 5. 면허 group AND/OR, 별칭, 불확실 판정

**계열 요약**: 이 계열은 **같은 판정을 두 곳에 구현한 것**(R-QUAL-01 · 02 · 07)과
**어휘를 데이터가 아니라 코드에 묻은 것**(R-QUAL-03)이 두 뿌리다. 그룹 fold의 의미론은
운영자 결정 `OPEN-QUAL-01`이 확정했다 — **그룹 간 OR / 그룹 내 AND**(조달청 OpenAPI 참고자료
근거, `0a2/decisions.md`). legacy도 `#257`에서 같은 커널로 수렴했고 `#258`이 세 번째
소비자를 이관했다.

### R-QUAL-01 · 같은 fold를 두 곳에 구현해 두 축이 서로 모순했다 (과차단)

- **관찰**: `assess_tech_field`가 모든 `license_limits` 행을 `lmtGrpNo`를 무시하고 **평면
  합산 후 전체 AND**로 대조했는데, 면허 게이트는 **그룹-OR**로 판정했다. 결과:
  게이트가 `eligible`로 통과시킨 operator를 **tech_field 축이 단독으로 과차단**했다.
- **사용자 영향**: **자격 있는 공고가 추천에서 사라졌다.** 운영자에게는 "그 공고가 존재하지
  않는 것"과 구별되지 않는다.
- **V2 예방 제약** — **타입**: 그룹 fold는 **커널 하나**가 소유하고 모든 축(면허·전문분야·
  협회)이 **같은 함수를 부른다.** 자격 판정 입력은 그룹 구조를 가진 타입이라 **평면
  리스트로 접는 경로가 존재하지 않는다.** 의미론은 `OPEN-QUAL-01` 결정이 고정한다.
- **검증 방법**: 다중 `lmtGrpNo` fixture에서 세 축의 판정이 **동일**함을 property로 고정.
  그룹 구조를 잃은 입력으로 판정 함수를 호출할 수 없음을 컴파일 테스트로 고정.
- **근거**: commit `c704fff`(#254, 2026-07-25) 본문 · 후속 `187cea9`(#257, 커널 추출) ·
  `cc4bc9f`(#258) · `tests/test_tech_field_eligibility.py`.
- **상태**: **`legacy에서 수정됨`**

### R-QUAL-02 · 같은 결함의 동형이 협회 축에 잠재해 있었다

- **관찰**: 협회 축도 평면 AND였다. 라이브에서 드러나지 않은 이유를 commit이 명시한다 —
  **"협회 요건이 단일 용어라 아직 여러 그룹으로 나뉜 사례가 없었을 뿐"**. `#254`와 동형인
  결함을 **선제 이관**했다.
- **사용자 영향**: **관측된 피해 없음(잠재).** 데이터가 아직 그 형태를 만들지 않았을 뿐이며
  결함의 존재는 확정이다.
- **V2 예방 제약** — **타입**: R-QUAL-01과 **같은 제약이 이 항목도 닫는다.** 커널이 하나면
  "이 축은 아직 안 옮겼다"가 성립할 수 없다. 별도 제약을 만들지 않는다.
- **검증 방법**: R-QUAL-01과 동일. 축을 추가할 때 커널을 우회하면 컴파일되지 않음을 고정.
- **근거**: commit `cc4bc9f`(#258, 2026-07-25) · `app/services/classification/association.py`.
- **상태**: **`legacy에서 수정됨`**(선제)

### R-QUAL-03 · 포괄 별칭이 전문분야를 collapse시켜 자격 없는 공고를 "참가 가능"으로 냈다

- **관찰**: `LICENSE_ALIASES["ENG001"]`이 `("ENG001", "엔지니어링사업", "엔지니어링",
  "기술사", "감리")`로 **코드 자체를 빼면 전부 포괄 substring**이다. 그래서
  `엔지니어링사업(해양)` · `엔지니어링사업(전기설비)` · `정보시스템 감리법인`이 모두
  ENG001 하나로 뭉쳤다. 라이브 실측 **2026-07-20: eligible 3건 중 1건이 명백한 오탐**
  (AI 플랫폼 감리 용역). 수정은 그 코드를 **대조 키에서 배제**한 것이고, 그 이유를 소스가
  직접 적는다.
- **사용자 영향**: **자격 없는 공고가 "참가 가능"으로 추천됐다.** 자격 판정의 신뢰를 직접
  훼손한다.
- **V2 예방 제약** — **계약(정책 데이터)**: 면허 별칭은 **코드 상수가 아니라 version 있는
  정책 데이터**이고, **전문분야를 구분하지 못하는 포괄어는 대조 키가 될 수 없다**는 것이
  데이터 스키마의 제약이다. 판정 결과에 **어느 별칭 version으로 매칭됐는지**가 실린다.
  `v2-지침서.md` §5(매직넘버 금지 — 정책 데이터 외부화)에서 나온다.
- **검증 방법**: 포괄어만으로 구성된 별칭 집합이 로드 시점에 **거부**되는지 고정.
  `엔지니어링사업(해양)` 대 `엔지니어링사업(전기설비)` fixture로 두 공고가 **구분**되는지
  고정.
- **근거**: commit `aa772f0`(#216, 2026-07-21) ·
  `app/services/classification/taxonomy.py:48-51`(별칭 테이블) ·
  `app/services/license_eligibility.py:144-159`(배제 이유와 실측을 직접 적는다).
- **상태**: **`legacy에 잔존`**(부분 수정) — 게이트 오탐은 막혔으나 **taxonomy의 포괄 별칭과
  어휘 공유는 그대로다.** 배제 목록으로 증상만 눌렀다.

### R-QUAL-04 · 자격 상세를 잘못된 원천에서 읽어 수집이 조용히 0건이었다

- **관찰**: 목록/표적조회 응답에는 자격 상세(`lcnsLmtNm` 등)가 **없고 플래그만** 온다.
  자격 상세는 별도 서브 오퍼레이션 `getBidPblancListInfoLicenseLimit`만이 돌려준다 —
  소스가 **실측 2026-07-19**로 그 사실과 필수 파라미터를 적는다.
- **사용자 영향**: **자격 요건이 있는 공고가 "요건 없음"으로 보여** 게이트가 아무것도 거르지
  못했다. 조용한 0건이라 실패로 보이지도 않았다.
- **V2 예방 제약** — **계약**: 외부 응답은 **선언된 스키마로 파싱**되고, **기대한 필드가
  없으면 "값 없음"이 아니라 계약 위반으로 거부**된다. 어느 오퍼레이션이 어느 필드를
  제공하는지가 **수집 어댑터의 타입에 박힌다** — 플래그만 있는 응답에서 자격 상세를 읽으려
  하면 컴파일되지 않는다.
- **검증 방법**: 플래그만 담긴 응답 fixture에서 자격 상세 추출이 **거부**되는지 고정
  (조용히 빈 결과가 되지 않는지).
- **근거**: commit `f3a3027`(#209, 2026-07-19) ·
  `app/services/koneps/openapi.py:270-289`(서브 오퍼레이션 실측 주석), `:340-365`.
- **상태**: **`legacy에서 수정됨`**

### R-QUAL-05 · 공고 차수의 제로패딩을 `int`로 변환해 1차 공고 전부의 자격을 잃었다

- **관찰**: `getBidPblancListInfoLicenseLimit`은 `bidNtceOrd=0`에 `totalCount=0`을,
  `bidNtceOrd="000"`에 실데이터를 반환한다(라이브 실측 **2026-07-19**). 기존 `int` 변환이
  **1차 공고(차수 `000`) 전부**의 면허제한 상세를 조용히 누락시켰다 — 검증 배치 50건에서
  서브콜 40회 전부 빈 결과였다.
- **사용자 영향**: R-QUAL-04와 **같은 증상**(요건 없음으로 보임)인데 **원인이 다르다.**
  두 결함이 같은 시기에 겹쳐 있어 하나를 고쳐도 증상이 남았다.
- **V2 예방 제약** — **타입**: 외부 식별자는 **문자열 도메인 타입**이고 숫자 변환이 허용되지
  않는다. 제로패딩·선행 0이 의미를 갖는 필드는 `int`로 좁혀질 수 없다.
- **검증 방법**: 차수 `"000"` fixture가 정수 변환 없이 그대로 전달되는지 고정. 식별자
  타입에 산술 연산이 정의되지 않음을 컴파일 테스트로 고정.
- **근거**: commit `d85711d`(#210, 2026-07-19) 본문 ·
  `app/services/koneps/openapi.py:284-289`(필수 파라미터와 실측).
- **상태**: **`legacy에서 수정됨`**

### R-QUAL-06 · 제외 사유가 운영자가 이미 보유한 면허를 포함해 오도했다

- **관찰**: 게이트 영향 리포트가 `ineligible` 공고의 `required_any`(**요구 면허 전체**)를
  카운트해 운영자가 **실제로 보유한** 면허까지 "무엇 때문에 제외되나" 목록에 올렸다. 실제
  제외 원인은 같은 AND 그룹의 **다른 미보유 면허**였다. 수정은 `missing_by_group`(실제 누락)
  기반으로 교체하고 다중 그룹에서는 **누락이 가장 적은 그룹**을 대표로 쓴 것이다.
- **사용자 영향**: **"이 면허가 없어서 제외됐다"는 잘못된 근거를 봤다.** 운영자가 **이미
  가진 면허를 다시 취득하려 할 수 있다.**
- **V2 예방 제약** — **계약**: 운영자 결정 `OPEN-DEC-05`가 확정했다 — 보류·제외 사유는
  **reason 문장이 아니라 필수 reason code**로 구조화된다. 사유는 **판정에 실제로 기여한
  입력만** 참조할 수 있고, 그 참조가 타입으로 강제된다.
- **검증 방법**: 제외 사유에 **운영자가 보유한 면허가 등장하지 않음**을 property로 고정.
  다중 그룹 fixture에서 대표 그룹 선택이 결정적임을 고정.
- **근거**: commit `7553873`(#219, 2026-07-21) 2차 커밋 본문.
- **상태**: **`legacy에서 수정됨`**

### R-QUAL-07 · 같은 면허를 판정하는 경로가 둘이고 같은 공고에 다른 답을 낼 수 있다

- **관찰**: 면허 판정이 **두 곳에 독립 존재**한다 —
  `classification/eligibility.py::assess_license`(입력: 공고 **자유 텍스트** 토큰)와
  `license_eligibility.py::assess_license_eligibility`(입력: `eligibility_raw`의 **구조화
  필드**). 입력 소스 · 별칭 처리 · 그룹 처리 · 결과 어휘가 모두 다르다. **0B가 두 함수를
  직접 열어 확인했다**(`commands.md` 재확인 절).
- **사용자 영향**: **같은 공고에 두 답이 나올 수 있다.** R-QUAL-01이 정확히 그 형태로 터졌다
  — 잠재가 아니라 **이미 한 번 실현된** 구조다.
- **V2 예방 제약** — **타입**: 자격 판정 커널은 **하나**이고 입력 소스(텍스트/구조화)는
  **어댑터가 같은 입력 타입으로 정규화**한다. 두 소스가 다른 답을 낼 수 있다면 그것은
  **입력 품질 차이로 산출에 실려야 하고**(사유 있는 `Uncertain`), 서로 다른 판정 경로가
  되어서는 안 된다. `OPEN-QUAL-06`이 그 `Uncertain` 분해를 확정했다.
- **검증 방법**: 같은 공고의 두 입력 표현에 대해 판정이 **동일하거나 사유 있는 측정 불가**
  임을 property로 고정. 판정 함수가 두 개 존재할 수 없음을 architecture test로 고정.
- **근거**: `app/services/classification/eligibility.py:53-97`(`assess_license`) ·
  `app/services/license_eligibility.py:447-511`(`assess_license_eligibility`).
- **상태**: **`legacy에 잔존`**

---

## 6. KONEPS 필드 누락 · fallback · rate limit

**계열 요약**: 외부 경계에서 **실패가 실패로 보이지 않는** 형태가 반복된다 — 쿼터 소진이
"데이터 없음"으로(R-COL-01), 수집 실패가 합성 데이터로(R-COL-06), 없는 값이 다른 값으로
(R-COL-05). 운영자 결정 `OPEN-COL-02`가 **17개 `resultCode`를 versioned policy data로
등재**하도록 확정했다(`0a2/decisions.md`).

### R-COL-01 · HTTP 200 + 에러 `resultCode`가 "데이터 없음"으로 오집계될 수 있었다

- **관찰**: KONEPS / data.go.kr은 **quota-exceeded와 key-throttle을 HTTP 200 + 에러
  `resultCode`로 신호한다(4xx가 아니다).** 이게 통과하면 payload가 0건으로 파싱되어
  "데이터 없음"으로 오집계된다. legacy는 네 곳에 복사돼 있던 envelope 검증을 한 곳으로
  통합했다.
- **사용자 영향**: **쿼터 소진이 "오늘 공고 0건"으로 보인다.** 운영자가 취할 행동이
  정반대다 — 기다리면 되는 상황에서 조건을 바꾸려 든다.
- **V2 예방 제약** — **계약**: 운영자 결정 `OPEN-COL-02`가 확정했다 — **17개 `resultCode`를
  versioned policy data로 등재**하고 각 코드의 범주를 문서의 조치방안 문구로 배정한다.
  **응답은 envelope 검증을 통과해야만 도메인 값이 되고**, 코드 부재는 "정상"이 아니라
  **분류 불가**로 산출된다.
- **검증 방법**: 17개 코드 각각에 대해 범주 배정이 policy version과 함께 산출되는지 고정.
  코드가 비었거나 목록 밖인 응답이 **성공으로 계수되지 않음**을 고정.
- **근거**: `app/services/koneps/http_client.py:119-157`(통합 경위와 네 복사 지점을 직접
  적는다).
- **상태**: **`legacy에서 수정됨`**(통합·명시 거부)

### R-COL-02 · 라이브 크롤 실패 분류에 rate-limit 버킷이 없고 `unknown`이 재시도 가능이다

- **관찰**: `classify_live_failure`가 예외 메시지를 소문자화해 **부분 문자열 매칭**으로
  분류한다. 버킷은 `access_denied` / `browser_runtime` / `network` / `no_data` /
  `selector_drift` / `timeout` / `unknown` **7종이고 `429`·`rate limit`·`quota`에 대응하는
  버킷이 없다.** 그리고 `LIVE_FAILURE_RETRYABLE_CATEGORIES`가 **`unknown`을 재시도 가능**에
  포함한다 — 즉 rate limit은 `unknown`으로 떨어져 **재시도된다.**
- **사용자 영향**: **`판정 불가`.** 코드 경로는 확정적으로 읽히지만 **실제로 이 경로가 429를
  얼마나 만났고 재시도가 회복을 지연시켰는지는 운영 로그 없이 판정할 수 없다.**
  §10 `OPEN-REG-01`로 등록했다.
- **V2 예방 제약** — **타입**: 실패 분류는 **예외 메시지가 아니라 응답 신호**(상태 코드 ·
  `resultCode` · 헤더)에서 나온다. 재시도 가능 여부는 **분류의 속성이지 목록 상수가
  아니며**, **분류 불가(`unknown`)는 재시도 가능이 될 수 없다** — 모르는 것을 다시 두드리는
  것이 안전하다는 근거가 없다.
- **검증 방법**: `unknown`이 재시도 경로로 가지 않음을 고정. rate-limit 신호가 **자기 버킷**을
  갖고 backoff가 그 버킷에 걸리는지 고정.
- **근거**: `app/services/koneps/live_failure.py:27`(`LIVE_FAILURE_RETRYABLE_CATEGORIES`가
  `unknown`을 포함), `:110-162`(분류 함수), `:165-167` · 소비자
  `app/services/koneps/browser_crawl.py:129`, `:203-219`, `:319-335` ·
  `app/services/koneps/collector.py:197`.
- **상태**: **`legacy에 잔존`**

### R-COL-03 · 문자열 부분일치 분류는 외부 메시지 변경에 조용히 깨진다

- **관찰**: 30여 개 리터럴 substring 매칭(`"captcha"` · `"403"` · `"net::"` ·
  `"selector"` …)으로 실패를 분류한다. **같은 패턴이 스모크 실패 분류에도 있다.**
- **사용자 영향**: 브라우저·라이브러리 **메시지 문구 하나가 바뀌면 조용히 버킷이 바뀐다.**
  운영자가 보는 실패 원인이 틀리고, 틀렸다는 신호도 없다.
- **V2 예방 제약** — **계약**: R-COL-02와 같은 제약이 이 항목도 닫는다 — 분류 입력이
  **구조화 신호**이고 자유 문자열이 아니다. 문자열 매칭이 불가피한 경계에서는 **매칭 실패가
  `분류 불가`로 산출되고 그 비율이 관측된다** — 조용히 다른 버킷으로 가지 않는다.
  ※ 재시도 정책 자체(`unknown → retryable` 여부)는 **활성 `OPEN-OPS-01`이 소유**하며
  이 문서가 결정하지 않는다.
- **검증 방법**: 알려지지 않은 메시지가 `분류 불가`로 떨어지고 그 카운트가 리포트에 실리는지
  고정.
- **근거**: `app/services/koneps/live_failure.py:115-159`(리터럴 매칭 블록) ·
  `app/services/smoke_failure_taxonomy.py`(같은 패턴).
- **상태**: **`legacy에 잔존`**

### R-COL-04 · 외부 API의 이중 오작동이 무한 루프 → SIGKILL → orphan을 냈다

- **관찰**: `totalCount` 누락 **+** API가 `pageNo`를 무시하고 같은 페이지를 무한 반환하는
  **이중 오작동** 시 무한 루프 → Celery time limit SIGKILL → orphan. 백스톱은
  `max_pages = ceil(max_total / per_page) + 2`이고, 주석이 **"정상 데이터는
  `max_total`/`total_pages`가 먼저 종료하므로 정상 truncation 위험이 없다"**고 적는다.
- **사용자 영향**: **수집 작업이 죽고 고아 행이 남는다.** 그 사이 수집은 0건이다.
- **V2 예방 제약** — **계약**: 외부 페이지네이션은 **진행 보장이 있어야 한다** — 같은 커서로
  같은 페이지가 돌아오면 **진행 없음을 감지해 실패로 산출**한다. 상한은 백스톱이지 1차
  방어선이 아니며, 상한에 걸린 것과 정상 종료가 **다른 결과 타입**이다.
- **검증 방법**: 같은 페이지를 반복 반환하는 stub에서 **진행 없음 오류**로 종료하는지 고정
  (상한 소진이 아니라). 상한 도달이 성공으로 계수되지 않음을 고정.
- **근거**: `app/services/koneps/collection.py:99-103`(runaway guard와 그 근거 주석).
- **상태**: **`legacy에서 수정됨`**

### R-COL-05 · `or` 폴백 사슬이 저장된 값의 의미를 소비자가 알 수 없게 만든다

- **관찰**: `closing_at = coerce(bidClseDt) or coerce(개찰일시) or coerce(bidNtceDt)` —
  마감일시가 없으면 **개찰일시**를, 그것도 없으면 **공고일시**를 마감으로 쓴다. 저장된 값이
  셋 중 무엇인지 소비자가 알 수 없다.
- **사용자 영향**: **마감 임박도 점수·알림 우선순위가 공고일시로 계산될 수 있다.** 마감이
  이미 지난 것처럼 보이거나 그 반대다.
- **V2 예방 제약** — **타입**: R-BASIS-06과 **같은 축**이다 — 침묵 폴백을 금지한다. 값과
  provenance가 한 값으로 묶여 이동하고 **출처 없는 시각을 만들 수 없다.** 마감 임박도처럼
  그 값에 의존하는 판정은 **출처를 입력으로 요구한다.**
- **검증 방법**: 폴백으로 채워진 마감 시각이 임박도 판정에 들어갈 때 **출처가 함께 전달되지
  않으면 컴파일 오류**임을 고정.
- **근거**: `app/services/koneps/openapi.py:410-419`(폴백 사슬).
- **상태**: **`legacy에 잔존`**

### R-COL-06 · 라이브 수집 실패가 합성 공고 2건을 저장하고 KPI에는 성공으로 계수된다

- **관찰**: 라이브 수집이 실패하면 `build_mock_items`가 **하드코딩 공고 2건**을 만들어 정상
  결과처럼 반환하고 `job_status = "fallback_mock"`으로 영속된다. mock item은 **실제
  영속 경로를 타서** Project/HistoricalData 행이 된다. 그리고 운영 리포트가 `fallback_mock`을
  **성공 계열로 계수**한다.
- **사용자 영향**: **수집 실패가 대시보드에서 성공으로 보이고, 동시에 가짜 공고 2건이 DB에
  들어간다.** 하류 소비자(추천·ML 데이터셋)는 메타데이터로만 구분할 수 있다.
  **관측 맹점 축과 데이터 오염 축이 같은 지점에서 만난다.**
- **V2 예방 제약** — **계약 + 테스트**: **합성 데이터가 운영 저장소에 들어갈 수 없다.**
  fixture와 운영 데이터는 출처 분류가 다르고(`data-extract.md`의 authoritative / observed /
  legacy-behavior 계층), 그 경계를 넘는 write는 타입상 존재하지 않는다. 실패는 **실패로
  계수**되며 폴백 성공이라는 상태가 없다.
- **검증 방법**: 합성 출처 레코드가 운영 write 경로에 들어가면 **컴파일 오류**임을 고정.
  수집 실패 시 KPI의 성공 계열이 증가하지 않음을 고정.
- **근거**: `app/services/koneps/collector.py:190-223`(실패 시 폴백 분기) ·
  `app/services/koneps/collection.py:255-315`(`build_mock_items`) ·
  `app/services/analytics_reporting/base.py:23-24` ·
  `app/services/analytics_reporting/task_runtime.py`(성공 계열 계수).
- **상태**: **`legacy에 잔존`**
  ※ 계열 7(관측 맹점)과 걸치지만 **이 계열이 소유**하고 R-ASYNC-08이 참조한다.

### R-COL-07 · 재개 커서가 `NULL`을 진행 상태로 겸용해 도달 불가 고아 집합을 만들었다

- **관찰**: 자격 원문 백필의 재개 키가 `eligibility_raw IS NULL`이다. **flags-only dict라도
  저장되면 그 키에서 빠지는데** `award_floor_rate`는 아직 `NULL`이라, **기본 실행으로는
  영원히 도달 불가한 고아 집합**이 생겼다. 소스가 그 집합을 이름으로 적고
  **`--floor-only` 모드의 존재 자체가 그 증거**다.
- **사용자 영향**: 일부 공고의 낙찰하한율이 **영원히 수집되지 않는다** → 그 공고의 하한
  판정이 영구히 미해결로 남는다.
- **V2 예방 제약** — **타입**: **재개 커서와 완료 상태를 같은 필드로 겸용하지 않는다.**
  진행 상태는 명시적 필드이고 "값이 비었다"가 "아직 처리 안 됨"을 뜻하지 않는다. 도달 불가
  집합의 크기가 **항상 관측 가능**해야 한다.
- **검증 방법**: 부분 저장 후 재개했을 때 **모든 미완 행에 도달**함을 property로 고정.
  도달 불가 집합 크기가 리포트에 실리는지 고정.
- **근거**: `scripts/backfill_award_floor_rate.py:21-27`(고아 집합과 `--floor-only`의 존재
  이유 — **0B가 직접 재확인했다**).
- **상태**: **`legacy에 잔존`**(우회 모드 추가로 완화)

### R-COL-08 · 유한한 쿼터를 이미 마감된 공고에 소진했다

- **관찰**: 자격 원문 수집 쿼터가 마감 임박·범위 밖 공고에 쓰였다 — **실측 2026-07-20:
  자격 원문 760건 중 610건이 이미 마감. 운영자 게이트 매칭 열린 공고 170건 중 보유는 5건.**
  그래서 **운영자 후보 우선 tier 정렬**을 도입했다(tier 1 = 운영자의 값싼 감시 규칙 게이트
  통과, tier 2 = 나머지).
- **사용자 영향**: **"운영자가 실제로 입찰할 수 있는 공고"의 자격을 못 얻었다.** 쿼터의
  대부분이 이미 지나간 공고에 쓰였다.
- **V2 예방 제약** — **계약**: 유한 쿼터를 쓰는 수집은 **우선순위 정책을 입력으로 요구**하고,
  그 정책은 정책 데이터다. 쿼터 소비가 **어느 tier에 얼마나 쓰였는지** 산출에 실린다 —
  "다 썼다"만으로는 부족하다.
- **검증 방법**: 마감 지난 공고가 tier 1에 들어가지 않음을 고정. tier별 쿼터 소비가 리포트에
  실리는지 고정.
- **근거**: `scripts/backfill_award_floor_rate.py:28-36`(tier 정렬과 실측 —
  **0B가 직접 재확인했다**).
- **상태**: **`legacy에서 수정됨`**(tier 정렬)

---

## 7. 비동기 중복 · 재시도 · queue 폭주

**계열 요약**: 2026-08-13 한 사건(큐 21,321건 적체)이 **서로 다른 결함 다섯을 동시에
드러냈다.** 그 사건의 귀속을 이 저장소가 한 번 틀렸고(투찰가 배치 산출로 적었다) 0A2가
정정했다 — **실제 원인은 유사공고 임베딩 백필 적체**이며 소스가 직접 그렇게 적는다.
관측 축의 뿌리는 **"생산된 행"만 보는 것**(R-ASYNC-08)이고, 실행 축의 뿌리는
**dialect 분기로 계약을 조용히 끄는 것**(R-ASYNC-04 · 05)이다.

### R-ASYNC-01 · 유사공고 임베딩 백필 적체로 큐에 21,321건이 쌓이는 동안 아무 신호도 없었다

- **관찰**: **2026-08-13**, 유사공고 투영 백필이 **단일 추론 워커를 4시간 점유**했고
  `bid_vector_ml_inference`에 **21,321건**이 쌓였다. 소스가 그 사건을 직접 적는다 —
  **"백로그는 며칠에 걸쳐 자라고 있었고 아무것도 보고하지 않았다. 기존 점검은 전부
  파이프라인이 생산한 행을 보는데, 소비자가 막힌 파이프라인은 볼 행 자체를 만들지 않는다."**
- **사용자 영향**: **유사 공고 패널이 영구히 빈 화면**으로 남는 경로가 존재했다 — 백필이
  "신선"이라 재계산하지 않는데 read model이 "낡음"이라 비우면 **어떤 파이프라인도 고치지
  못한다.** 검출까지 며칠.
- **V2 예방 제약** — **계약**: 관측은 **생산된 행이 아니라 큐 깊이·소비자 진행**에서 나온다
  (R-ASYNC-08과 같은 축). **산출의 부재 자체가 관측 대상**이어야 한다.
  ※ DB 기반 큐의 backlog 관측·가시성 timeout 계약은 **활성 `OPEN-OPS-10`이 소유**하며
  이 문서가 결정하지 않는다 — 설계가 아직 없다.
- **검증 방법**: 소비자를 멈춘 상태에서 **경보가 뜨는지** 고정. "생산된 행 0건"이 정상으로
  계수되지 않음을 고정.
- **근거**: commit `19f2c94`(#368, 2026-08-13) ·
  `app/services/task_queue_depth.py:5-15`(사건과 관측 맹점을 직접 적는다) ·
  `app/core/inference_config.py:95-113` · `tests/test_similarity_backfill_overlap_guard.py` ·
  `tests/test_pipeline_sweep_expiry.py`.
- **상태**: **`legacy에서 수정됨`** — 단 **검출 지연은 스모크 주기가 정해 최악 24시간**이다
  (`app/core/inference_config.py:108-112`가 "이 phase가 바꾸는 것은 '며칠 동안 아무 신호
  없음'에서 '하루 안에 드러남'이지 실시간 경보가 아니다"라고 직접 적는다).

### R-ASYNC-02 · 재발 방지선인 "투입 ≤ 소진" 부등식이 config 주석에만 있다

- **관찰**: 재발 방지선이 `필요 951/시 ≤ 투입 2,000/시 ≤ 소진 6,000/시`(3배 여유)이고,
  **60초 유지 시 투입이 6,000건/시로 소진과 정확히 같아져 여유가 0이 된다**고 주석이 적는다.
  **이 관계는 실행되지 않는다** — 주기·배치 크기·소비자 처리량이 각각 독립 상수다.
- **사용자 영향**: **관측된 피해 없음(잠재).** 상수 하나를 바꾸면 재발 방지선이 조용히
  사라진다.
- **V2 예방 제약** — **테스트**: **불변식이 주석이 아니라 검사로 존재한다.** 투입·소진·필요의
  관계를 **구성 로드 시점에 검증**하고 위반이면 기동을 거부한다. R-FLOOR-04와 같은 형태의
  실패("가정이 주석에만 있다")이며 같은 처방이다.
- **검증 방법**: 여유가 0 이하가 되는 상수 조합에서 구성 검증이 **실패**하는지 고정.
- **근거**: `app/core/inference_config.py:77-89`(부등식과 여유 0 조건, 그리고 **활성 대상이
  투입×6h = 12,000건을 넘으면 수명 보장이 깨진다**는 한계점까지 적는다 — 현재 5,822건).
- **상태**: **`legacy에 잔존`**

### R-ASYNC-03 · lease 경합에서 진 쪽이 즉시 포기해 한 카테고리가 구조적으로 굶었다

- **관찰**: 카테고리별 beat 엔트리가 같은 tick에 dispatch되면서 **하나의 advisory lease를
  다투고**, 진 쪽이 즉시 `duplicate_suppressed`로 끝나 **그 사이클 수집을 통째로 건너뛰었다.**
  운영 7일 실측: **service 23회 중 20회(87%) suppressed.**
- **사용자 영향**: **특정 카테고리의 공고가 87%의 사이클에서 수집되지 않았다.** 운영자에게는
  "그 카테고리에 공고가 없는 것"으로 보인다.
- **V2 예방 제약** — **타입**: 배타 실행의 단위가 **작업 종류가 아니라 작업 대상**이다 —
  카테고리별 작업이 같은 lease를 다투지 않도록 **lease 키가 대상을 포함**한다. 경합에서 진
  쪽은 **즉시 포기가 아니라 bounded retry**이고, 건너뛴 사이클은 **관측 가능한 결과**다.
- **검증 방법**: 두 카테고리를 같은 tick에 dispatch해 **양쪽 다 수집되는지** 고정.
  suppressed가 성공으로 계수되지 않음을 고정.
- **근거**: commit `f70df07`(#353, 2026-08-06) 1번째 커밋 본문 · `app/tasks/collection_jobs.py`.
- **상태**: **`legacy에서 수정됨`**

### R-ASYNC-04 · 단일비행 lease가 PostgreSQL이 아니면 무조건 성공을 반환한다

- **관찰**: `AdvisorySingletonLease.acquire()`가 `dialect != "postgresql"`이면
  **`self.acquired = True; return True`** — 중복 실행 억제라는 계약을 **정반대로 위반**한다.
  그리고 이 메커니즘을 검증하는 테스트는 `dialect.name = "postgresql"`을 반환하는 **가짜
  bind**를 쓴다. 저장소가 같은 함정의 전례를 기록한다 — 엔티티 DISTINCT가 **Postgres json
  컬럼에서만 죽었고 SQLite 스위트는 잡지 못했다**(`fe24c15`, #212, 2026-07-19).
- **사용자 영향**: **프로덕션이 아닌 엔진에서 도는 어떤 경로도 중복 실행 억제를 받지
  못한다.** 그리고 테스트는 초록이다.
- **V2 예방 제약** — **테스트**: **계약을 끄는 분기를 두지 않는다.** 지원하지 않는 엔진은
  **거부**이지 무조건 성공이 아니다. 그리고 **엔진 의존 동작은 실제 엔진에서 검증**한다 —
  가짜 bind로 통과하는 테스트는 그 계약을 고정하지 못한다.
- **검증 방법**: 미지원 dialect에서 lease 획득이 **실패**함을 고정. 배타성 테스트가 실제
  PostgreSQL에서 도는지 고정(testcontainer 등).
- **근거**: `app/services/task_singleton.py:28-32`(그 분기) · `tests/test_task_singleton.py`
  (가짜 bind) · commit `fe24c15`(#212, 2026-07-19, 같은 함정의 전례).
- **상태**: **`legacy에 잔존`**

### R-ASYNC-05 · outbox claim의 `FOR UPDATE SKIP LOCKED`도 같은 dialect 분기를 갖는다

- **관찰**: `_claim_one`이 PostgreSQL일 때만 `with_for_update(skip_locked=True)`를 붙인다.
  **비-PG에서는 두 워커가 같은 행을 claim할 수 있다.** 서비스 자신이 **"Deliver each claimed
  row once; ambiguous interrupted sends require review"**를 계약으로 적고 모호한 claim을
  격리하는 장치를 둔다 — **사후에 드러낼 뿐 막지는 않는다.**
- **사용자 영향**: **비-PG 환경에서 중복 발송이 가능하다.** 격리 장치는 이미 나간 것을
  되돌리지 못한다.
- **V2 예방 제약** — **테스트**: R-ASYNC-04와 **같은 제약**이다. 배타 claim은 엔진 기능에
  의존하므로 **그 엔진에서 검증**되어야 하고, 기능이 없는 엔진은 **거부**한다.
  ※ 알림 전달 의미는 이미 확정됐다 — 운영자 결정 `OPEN-NOTI-02`(**at-most-once**, 놓침을
  감수하고 회수는 앱 알림함이 소유).
- **검증 방법**: 동시 워커 두 개가 같은 행을 claim하지 못함을 실제 엔진에서 고정.
- **근거**: `app/services/notifications/delivery_outbox.py:14-15`(계약 문장), `:70-89`
  (dialect 분기), `:91-95`(모호 claim 격리).
- **상태**: **`legacy에 잔존`**

### R-ASYNC-06 · 주기 태스크 메시지에 수명이 없어 백로그가 정지 시간에 비례해 무한히 자랐다

- **관찰**: 21,321건은 **소비자가 막힌 동안 beat가 멈추지 않고 계속 밀어 넣은 결과**다.
  소비자가 멈춰도 생산자는 멈추지 않으므로 백로그에 **구조적 상한이 없으면 정지 시간에
  비례해 무한히 자란다.** 수정은 `expires = 주기 × 3`이고, 근거는 이 태스크들이 전부
  **idempotent sweep**이라 한 tick을 놓쳐도 다음 tick이 같은 일을 한다는 것이다.
- **사용자 영향**: **R-ASYNC-01과 같은 사건의 다른 축**이다.
- **V2 예방 제약** — **계약**: 주기 작업 메시지는 **수명을 갖는다.** 그 수명이 정당하려면
  작업이 idempotent여야 하므로, **idempotent임이 타입으로 선언된 작업만 주기 스케줄에
  등록할 수 있다.**
- **검증 방법**: 만료된 메시지가 소비되지 않음을 고정. 주기 등록 대상이 idempotent 선언을
  갖지 않으면 컴파일 오류임을 고정.
- **근거**: `app/core/inference_config.py:114-124` · `app/tasks/pipeline_schedules.py` ·
  `tests/test_pipeline_sweep_expiry.py`.
- **상태**: **`legacy에서 수정됨`**

### R-ASYNC-07 · 선택 순서가 `id ASC`라 노후도 상한 보장이 기아가 됐다

- **관찰**: 노후도 상한 보장은 **노후도 우선 정렬일 때만** 성립한다. 라이브 상태(활성
  5,822건)를 24시간 재생한 실측 — 노후도 우선은 **미서빙 0건 · 첫 전수 순회 3.8h · 최대
  5.95h**, `id` 순서는 **미서빙 2,502건 · 전수 순회 없음 · 최대 27.9h**.
- **사용자 영향**: **굶는 쪽이 "운영자가 아직 투찰할 수 있는, 시간이 가장 많이 남은
  공고"**다. 신규 공고가 최초 투영 1회를 받은 뒤 stale로 판정되는데 백필이 id 순서상 도달하지
  못해 **어떤 파이프라인도 고치지 못하는** 상태가 됐다.
- **V2 예방 제약** — **테스트**: **"상한 보장"은 선택 순서에 의존하므로 그 의존이 테스트로
  고정**된다. 보장을 주장하는 곳은 **그 보장이 깨지는 순서로 바꿨을 때 빨간불**이 되어야
  한다. R-FLOOR-04·R-ASYNC-02와 같은 부류의 처방이다.
- **검증 방법**: 선택 순서를 `id`로 바꾸면 노후도 상한 property가 **실패**하는지 고정.
- **근거**: `app/core/inference_config.py:66-71`(보장의 조건과 두 순서의 재생 실측) ·
  `tests/test_similarity_backfill_selection_order.py`.
- **상태**: **`legacy에서 수정됨`**

### R-ASYNC-08 · 관측이 전부 "생산된 행" 기반이라 소비자가 막히면 KPI가 전부 초록이었다

- **관찰**: 운영 대시보드 KPI(broker health · result backend health · worker separation ·
  stale queue · failure rate)를 **작업 실행 행에서 유도**한다. **소비자가 막히면 볼 행 자체가
  생기지 않으므로 이 KPI들은 전부 초록으로 남는다.** **0B가 두 라인 범위를 직접
  재확인했다** — 상태별 카운트 집계와 broker health 지표 산출이 그 형태다.
- **사용자 영향**: **며칠 동안 아무 신호도 없었다**(R-ASYNC-01). R-COL-06(폴백을 성공으로
  계수)이 **같은 축의 다른 사례**다.
- **V2 예방 제약** — **계약**: 관측 지표는 **산출물의 존재가 아니라 처리 진행**을 재야 한다.
  "행이 없다"가 "문제가 없다"로 읽힐 수 있는 지표는 **그 자체로 결함**이며, 부재를 볼 수
  있는 지표(큐 깊이 · 마지막 진행 시각)가 **함께 선언되지 않으면 지표로 등록될 수 없다.**
- **검증 방법**: 소비자 정지 시나리오에서 **최소 하나의 지표가 빨간불**이 되는지 고정.
- **근거**: `app/services/analytics_reporting/task_runtime.py:33-51`(상태별 카운트),
  `:145-167`(broker health 지표) · `app/services/task_queue_depth.py:5-10`.
- **상태**: **`legacy에 잔존`**(큐 깊이 1축만 추가됐다)

---

## 8. 모델 label/feature의 training-serving skew와 시간 누수

**계열 요약**: `#366`·`#367`·`#368` 세 PR이 같은 주에 이 축을 훑었다. 반복되는 형태는
**"측정할 수 없는 것을 없다고 하지 않는다"**이고, legacy가 그 규율을 실제로 지켰다 —
편향을 못 재서 **회피(embargo)를 택했고**(R-ML-04), 이득이 실재하는 피처를 커버리지가
**시간에 기울었다는 이유로 철회했다**(R-ML-02). V2가 계승할 것은 **그 판단 규율**이다.

### P-ML-01 · (회귀가 아니다) 학습과 추론이 같은 피처 모듈을 import한다 — 유지할 예방책

> **이 항목은 회귀가 아니라 legacy에 이미 있는 구조적 방지 장치**이며 **회귀 수에
> 계상하지 않는다.** 여기 적는 이유는 **V2가 이것을 잃지 않도록** 하기 위해서다.

- **관찰**: 추론과 학습이 **둘 다 `app.domain.award_rate_features`에서 같은 피처 이름·
  피처 공간·인코딩을 import한다.** 피처 정의가 한 곳에 있으므로 학습과 추론이 어긋날 수
  있는 표면 자체가 없다.
- **사용자 영향**: **해당 없음 — 회귀가 아니다.** 이 항목은 피해가 아니라 **피해를 막고
  있는 구조**이며, 5필드 중 이 자리에는 **왜 예방책인가**를 적는다: training-serving skew의
  고전적 원인은 **피처 계산이 두 벌 존재하는 것**이고, legacy는 그 표면을 구조적으로 없앴다.
- **V2가 유지할 것** — **타입**: 운영자 결정 `OPEN-ML-01`이 이 구조를 근거로 **ML 수학 커널
  8개를 전부 Python `ml-engine`에 두기로** 확정했다(`0a2/decisions.md`). **분리하면 학습·추론이
  같은 피처 모듈을 import하는 이 구조가 깨진다** — 그것이 결정의 명시적 근거다.
- **검증 방법**: 학습과 추론이 같은 피처 정의를 참조함을 architecture test로 고정.
  피처 정의가 두 곳에 생기면 빨간불이 되게 한다.
- **근거**: `app/ai/predictors/award_rate_gbm.py:60-66`(추론 측 import) ·
  `app/services/ml_training/award_rate_gbm.py:51-59`(학습 측 import), `:72-83`.
- **상태**: **`legacy에 이미 있는 예방책`**(회귀 아님)

### R-ML-01 · 학습 코퍼스의 69%가 서빙이 결코 마주치지 않는 모집단이었다

- **관찰**: 서빙이 마주하는 열린 공고는 **99.93%가 피드 출처**인데 **학습 코퍼스는 31%**
  뿐이었다. 발주기관이 없는 행은 "계약방법이 없는 공고"가 아니라 **개찰결과 피드로만 본
  공고**다. 수정은 후보 쿼리에 **서빙 분포 정합 필터**를 넣은 것이고, 소스가 그 필터를
  "메커니즘 필터가 아니라 **서빙 분포 정합**"이라고 구분해 적는다. 효과(측정 **2026-08-13**):
  용역 편향 **+5.91%p → +0.90%p**.
- **사용자 영향**: **홀드아웃 수치가 라이브에서 나올 수치가 아니었다.** 운영자가 보는 모델
  정확도가 **다른 모집단에서 잰 것**이었다.
- **V2 예방 제약** — **계약 + 테스트**: 학습 코퍼스와 서빙 모집단의 **분포 정합이 선언되고
  측정**된다. 모델 산출에 **어느 모집단에서 학습·평가됐는지**가 실리고, 정합이 선언되지
  않은 코퍼스로는 승격 게이트를 통과할 수 없다.
- **검증 방법**: 학습·서빙 모집단 기술 통계의 차이가 임계를 넘으면 승격이 **거부**되는지
  고정. 그 임계는 정책 데이터다.
- **근거**: commit `75254c1`(#366, 2026-08-13) 본문 ·
  `app/services/award_rate_dataset.py:19-36`(네 필터와 피드 출처 필터의 성격), `:108`.
- **상태**: **`legacy에서 수정됨`**

### R-ML-02 · 피처 커버리지가 도메인이 아니라 백필 진행 상태였다 — 이득이 있는데도 철회했다

- **관찰**: 공시 낙찰하한율을 피처로 넣었고 **홀드아웃 이득이 실재했다**(보유 층 평균
  0.8928 / 미보유 층 0.9310). 그런데 그 필드의 커버리지가 **백필 스크립트가 열린 공고만
  훑기 때문에 시간에 기울어** 있었다 — 즉 **커버리지가 도메인 사실이 아니라 백필 진행
  상태**였다. legacy는 이득이 있는데도 **철회하고 진단 전용으로 보존**했으며, **왜 안 쓰는지를
  소스에 남겼다** — "다음 사람이 '왜 이 좋은 축을 안 쓰지'에 스스로 답하지 못하고 다시 넣는
  것을 막기 위해서".
- **사용자 영향**: 이 축을 실었다면 모델이 미공시 공고의 낙찰률을 **체계적으로 낮게** 배우고,
  그 대상은 **하한이 공시되지 않아 guardrail이 공고별로 보호해 줄 수 없는 바로 그 집단**이다.
- **V2 예방 제약** — **계약**: 피처는 **커버리지의 출처가 선언되어야** 학습에 들어간다 —
  "도메인 사실"과 "수집 진행 상태"가 **다른 타입**이고 후자는 피처가 될 수 없다.
  진단 축은 **기본값으로 채우지 않는다**("안 넘겼다"와 "미공시"는 다른 상태이며, 조용히
  결측으로 채우면 **재도입 조건을 확인할 계측기가 먼저 거짓말을 한다**).
- **검증 방법**: 커버리지 출처가 선언되지 않은 필드가 피처 공간에 들어가면 **컴파일 오류**
  임을 고정. 진단 축에 기본값이 채워지지 않음을 고정.
- **근거**: commit `75254c1` 2번째 커밋 본문 ·
  `app/services/ml_training/award_rate_gbm.py:129-153`(철회 근거 전문), `:162-164`(기본값을
  주지 않는 이유).
- **상태**: **`legacy에서 수정됨`**(철회 + 진단 전용 보존 + 재도입 조건 명시)

### R-ML-03 · 비율 기반 rolling origin이 서로 다른 시점을 만들지 못해 홀드아웃이 100% 겹쳤다

- **관찰**: 행이 시간에 몰린 코퍼스에서 **다섯 fraction의 홀드아웃이 100% 겹쳤다**
  (0.60의 홀드아웃이 0.80의 1,046행을 전부 포함). **"모든 origin 통과"가 안정성이 아니라
  같은 행을 다섯 번 잰 것**이었다. 경계 동시각 행이 GBM 학습에만 새는 문제도 함께 있었다.
  수정은 fraction이 아니라 **겹치지 않는 주 단위 창**이다.
- **사용자 영향**: **모델 승격 판정의 근거가 실제보다 다섯 배 강해 보였다.**
- **V2 예방 제약** — **테스트**: 평가 창은 **서로 겹치지 않음이 검증**되고, 겹침 비율이
  산출에 실린다. **"여러 origin에서 통과"라는 주장은 origin들이 실제로 다른 표본일 때만**
  성립하므로 그 조건이 게이트의 일부다.
- **검증 방법**: 창 간 교집합이 0임을 property로 고정. 경계 동시각 행이 한쪽에만 들어가는지
  고정.
- **근거**: commit `5d38ac1`(#367, 2026-08-13) 본문 ·
  `app/services/ml_training/award_rate_holdout.py`.
- **상태**: **`legacy에서 수정됨`**

### R-ML-04 · 성숙도 편향의 방향을 측정할 수 없어 회피(embargo)를 택했다

- **관찰**: 게이트 홀드아웃이 코퍼스에서 **가장 미성숙한 구간(정산 41.5%)**에 앉아 있었다.
  그 선택의 편향 방향은 **"결과를 언제 알았는가" 타임스탬프가 없어 측정 불가**였다. 판단은
  **"측정할 수 없는 편향은 없다고 주장할 수 없으므로 회피한다"** — 성숙도 임계 미만 구간
  embargo. **사후 회고가 아니라 사건 시점의 판정 근거**다.
- **사용자 영향**: 모델 승격 게이트가 **편향된 구간에서 판정될 수 있었다.** 회피는 문제를
  막았지만 **측정 가능성 자체는 회복되지 않았다.**
- **V2 예방 제약** — **타입**: 운영자 결정 `OPEN-ML-04`가 확정했다 — **정산 관측 시각을
  canonical fact로** 넣는다. 그러면 편향 방향이 **측정 가능**해지고 회피가 아니라 보정이
  가능해진다. 같은 결정이 **"미정산"을 `0`으로 적재하지 않는 것**(R-PROV-08)도 함께 닫는다.
- **검증 방법**: 관측 시각이 없는 레코드가 성숙도 판정에 들어가면 **사유 있는 측정 불가**로
  산출됨을 고정(암묵 기본값 금지).
- **근거**: commit `5d38ac1` 본문 · `app/domain/settlement_maturity.py` ·
  `app/services/settlement_maturity.py:14-19`(모집단이 항상 피드 출처여야 하는 이유 —
  개찰결과 피드로만 본 공고는 **정산된 순간에만 관측되므로 성숙도가 정의상 100%가 되어
  embargo를 무력화한다**).
- **상태**: **`legacy에서 수정됨`**(회피). **근본 원인(관측 시각 부재)은 잔존이며
  `OPEN-ML-04` 결정으로 V2에서 해소된다.**

### R-ML-05 · 학습 cutoff가 "개찰 = 라벨 가용"을 가정하는 낙관을 legacy가 스스로 공시한다

- **관찰**: `opened_at < cutoff`는 **개찰이 곧 라벨 가용**이라고 가정한다. 실제로는 낙찰
  결과와 reserve detail이 **개찰 며칠 뒤에 수집되므로 그 시점에 아직 못 봤을 행이 학습에
  들어갈 수 있다.** 소스가 이 낙관을 **공시**하고 범위까지 적는다 — 비교 대상 베이스라인도
  같은 규칙 위에서 측정되므로 **비교의 공정성은 유지되지만 절대 오차의 낙관 편향은 남는다.**
- **사용자 영향**: **운영자가 보는 모델 절대 오차가 낙관적이다.** 모델 간 비교는 유효하다.
- **V2 예방 제약** — **타입**: 시간 누수 방지는 **"언제 일어났는가"가 아니라 "언제 알 수
  있었는가"** 기준이어야 한다. `OPEN-ML-04`가 넣기로 한 **관측 시각**이 이 항목의 전제이며,
  cutoff는 그 필드를 입력으로 요구한다 — **개찰 시각으로 cutoff를 계산하는 경로가 존재할 수
  없다.**
- **검증 방법**: 관측 시각이 cutoff 이후인 행이 학습 코퍼스에 포함되면 **거부**되는지 고정.
- **근거**: `app/services/award_rate_dataset.py:42-48`(낙관의 자기 공시).
- **상태**: **`legacy에 잔존`**(공시만)

### R-ML-06 · 게이트 리포트가 "못 이겼다"와 "못 쟀다"를 구별하지 못했다

- **관찰**: 리뷰 판정은 코드가 아니라 **결론과 공시**였다 — **"계측기는 맞는데 그 수치로 한
  말이 틀렸다"**. 실측: 145행 창은 seed 5종 재채점에서 **−3.77% ~ +7.12%로 부호가 뒤집혔고**
  (0/5 통과), MDE가 관측 효과를 넘어 유의성에 훨씬 큰 표본이 필요했다.
- **사용자 영향**: **통과하지 못할 판정이 통과로 보였다.**
- **V2 예방 제약** — **타입**: 게이트 산출은 **`통과` / `미통과` / `판정 불가`의 세 상태**다
  (`v2-지침서.md` §4.2, 운영자 결정 `OPEN-SET-02`의 제3 상태와 같은 규율). 표본이 MDE에
  못 미치면 **`판정 불가`이고 `미통과`가 아니며, 어느 쪽도 `통과`로 접히지 않는다.**
  R-FLOOR-05가 이 어휘의 소유자이고 이 항목은 **동형 적용**이다.
- **검증 방법**: 표본 부족 시나리오에서 산출이 `판정 불가`이고 부호 일관성 지표가 함께
  실리는지 고정.
- **근거**: commit `5d38ac1` 2번째 커밋 본문.
- **상태**: **`legacy에서 수정됨`**

### R-ML-07 · golden 기대값 일괄 재생성 명령이 존재하고 테스트가 스스로 성격을 자인한다

- **관찰**: 환경변수 하나로 golden을 **일괄 재생성**할 수 있다. 테스트 docstring이 성격을
  자인한다 — **"CHARACTERIZATION (not correctness): every expected value here is the
  *current* implementation's actual output"**.
- **사용자 영향**: **관측된 피해 없음.** 다만 이 golden들은 **회귀 검출 장치이지 정답 판정
  장치가 아니고**, 그 구분이 없으면 **"재생성 → 통과"가 승인 절차가 된다.**
- **V2 예방 제약** — **테스트**: **characterization 테스트와 정답 테스트를 같은 곳에 두지
  않는다.** 전자는 "바뀌었다"를 알리는 장치이고 후자는 "맞다"를 고정하는 장치다. 기대값을
  일괄 재생성하는 경로는 **정답 테스트에는 존재하지 않는다.** `data-extract.md`의 출처 계층
  (authoritative / observed / legacy-behavior)이 같은 구분을 fixture 축에서 한다 —
  **legacy 출력은 `legacy-behavior`이지 정답이 아니다.**
- **검증 방법**: 정답 fixture가 재생성 경로를 갖지 않음을 고정. 두 종류가 디렉터리·타입으로
  구분되는지 고정.
- **근거**: `tests/test_predictor_output_equivalence.py:3-6`(자인 docstring),
  `:43`(재생성 스위치).
- **상태**: **`legacy에 잔존`**

### R-ML-08 · 응답 계약의 필수 필드가 "현재 구현들의 교집합"에서 도출됐다

- **관찰**: 응답 모델의 required 17필드는 **"모든 golden에 존재하는 필드"**를 열거해 도출됐다.
  그런데 그 도출은 원래 **44개 golden**(standalone 시퀀스 모델 3건 포함)에서 나왔고,
  그 모델 은퇴로 golden이 **39개**가 되면서 **삭제된 5건 중 3건이 narrow-shape**였다 —
  즉 **"필수 17"의 도출 근거 집합 자체가 바뀌었다.** 소스가 그 사실과 "무엇이 이것을
  고정하는가"를 직접 적는다. **0B가 이 라인 범위를 직접 재확인했다.**
- **사용자 영향**: **관측된 피해 없음.** 그러나 계약의 근거가 "현재 구현들의 교집합"이면
  **구현 하나가 은퇴할 때 계약이 조용히 바뀔 수 있다.**
- **V2 예방 제약** — **계약**: 응답 계약은 **구현의 교집합이 아니라 소비자 요구에서** 나온다.
  계약은 명시적으로 선언되고, **구현이 계약을 만족하는지**가 검증 방향이다(그 반대가 아니다).
- **검증 방법**: 구현을 제거해도 계약이 바뀌지 않음을 고정. 계약 변경이 명시적 버전 변경을
  요구하는지 고정.
- **근거**: `app/ai/predictors/base.py:37-51`(도출 근거 집합의 변화와 무엇이 고정하는지).
- **상태**: **`legacy에 잔존`**

### R-ML-09 · 미학습 공종에 자신 있게 답하는 것을 막는 가드가 `#366`의 부수 효과로 생겼다

- **관찰**: **어휘에 없는 공종은 예외를 내지 않는다** — 미지 코드가 결측으로 다뤄져
  **모델이 다른 공종의 행으로 학습한 잎을 태워 그럴듯한 투찰율을 낸다.** 소스가 이를
  **"배운 적 없는 공종에 자신 있게 답하는 셈이라 정직 명세 위반이고 가격 사고"**라고 적는다.
  실측 동기는 R-ML-01이다 — 서빙 분포 정합 필터가 한 공종을 학습 코퍼스에서 **전량 제거**
  했고(0행) 다른 공종은 6행뿐이었다.
- **사용자 영향**: **배우지 않은 공종에 대해 그럴듯한 투찰율이 나왔다.** 운영자는 그것이
  학습되지 않은 답임을 알 수 없다.
- **V2 예방 제약** — **타입**: 학습 범위 밖 입력은 **사유 있는 측정 불가**로 산출되고
  숫자를 내지 않는다(R-FLOOR-05 어휘의 동형 적용). **결측과 미학습을 같은 표현으로 두지
  않는다** — 이 구분이 타입에 있어야 모델 백엔드가 바뀌어도 유지된다.
- **검증 방법**: 어휘 밖 공종 입력에서 산출이 **사유 있는 측정 불가**이고 폴백 경로가
  관측 가능함을 고정.
- **근거**: `app/ai/predictors/award_rate_gbm.py:25-31`(그 판단과 실측 동기를 직접 적는다).
- **상태**: **`legacy에서 수정됨`**(판정 단일 지점)

---

## 9. `OPEN` — 이 slice가 판정하지 못한 것

`capability-map.md` §12는 **out_of_scope**(0A2 Codex `approve`로 확정)이므로 0B가 발견한
미결은 **여기 등록**한다. id 접두 `OPEN-REG`는 §12의 id 공간과 충돌하지 않는다.
**중앙 registry와의 통합은 별도 slice**이며 이 문서는 통합하지 않는다.

| id | 결정 필요 사항 | 왜 이 slice가 판정할 수 없나 | 관련 |
| --- | --- | --- | --- |
| **OPEN-REG-01** | 라이브 크롤 rate-limit 오분류가 **실제로 회복을 지연시켰는가** | 코드 경로는 확정적으로 읽힌다(429가 `unknown`으로 떨어지고 `unknown`이 재시도 가능). **그러나 발생 빈도 근거가 0건**이다 — 운영 로그 없이는 판정할 수 없다. **운영자에게 로그 확인 가능 여부를 물어야 한다** | R-COL-02 |
| **OPEN-REG-02** | `#354` 이전에 영속된 **감사 문구의 소급 정정 범위** | commit에 기재가 없다. golden 6건의 문구 치환만 기록돼 있고 **과거 영속 행의 상태는 legacy DB 접근 없이 판정 불가**다 | R-BASIS-07 |
| **OPEN-REG-03** | rate scale 오해석의 **피해 규모** | `(1.5, 2.0]` 구간 오해석이 정산 백테스트에서 몇 건을 틀리게 했는지 **legacy 어디에도 없다.** 코퍼스 재계산이 필요하다 | R-RATE-01 |
| **OPEN-REG-04** | **"66% 오염"의 측정 방법·모수·측정일** | 출처 포인터는 commit `2f9bad7` subject까지 추적됐으나 **방법·모수·측정일은 여전히 없다.** 중앙 registry의 `OPEN-NUM-01`과 **같은 쟁점**이며 여기서는 포인터를 더한 상태로만 기록한다. **승격 금지** | R-PROV-01 |

**등록하지 않은 것**: 계열 2의 나머지 `판정 불가`(R-RATE-02 · 03 · 05)는 **결정을 기다리는
쟁점이 아니라 관측이 없는 사실**이다. V2 예방 제약이 이미 결정돼 있어(`OPEN-QUAL-08` ·
지침서 §5) 판정을 기다리지 않으므로 `OPEN`이 아니다.

## 10. 인계

### 10.1 fixture-curator (0C · M1)

- **경계값 corpus**: rate scale 경계(0.5 · 1.5 · 2.0 전후) · `base ÷ 추정가격` 1.15 전후 ·
  게시 하한율 밴드(0.30 · 0.995) 전후. 계열 2·3·4의 검증 방법이 전부 이 fixture를 요구한다.
- **`est == base` 코호트**(R-PROV-06)와 **legacy `clean` 유래 행**(R-PROV-07)은
  `OPEN-DEC-08`에 따라 **authoritative corpus에 들어갈 수 없다.** manifest의 출처 분류가
  이 배제를 강제해야 한다.
- **legacy 출력은 `legacy-behavior`이지 정답이 아니다**(R-ML-07) — golden 재생성 경로를
  가진 fixture는 정답 계층에 둘 수 없다.

### 10.2 0C 데이터 사전

- **정산 관측 시각**(`OPEN-ML-04` 결정)의 컬럼과 채움 경로 — R-ML-04 · R-ML-05 · R-PROV-08이
  전부 이 필드에 의존한다.
- **금액의 basis 태그**(`OPEN-STR-01` 결정) — 계열 1 전체의 전제.
- **provenance 어휘**(`EstimatedAmountSource` 상당) — R-PROV-04 · 05가 요구하는 sealed enum.

### 10.3 중앙 registry 통합 (별도 slice)

`OPEN-REG-01`~`04`는 **이 문서에만 있다.** `capability-map.md` §12와의 통합이 필요하며,
특히 **`OPEN-REG-04`는 §12의 `OPEN-NUM-01`과 같은 쟁점**이라 두 곳에 나뉘어 있다.
통합 slice가 이 중복을 정리해야 한다.

### 10.4 활성 OPEN에 대한 관찰 — 해소하지 않았다

0B는 `capability-map.md`를 편집하지 않는다(out_of_scope). 조사 중 **기존 활성 OPEN 45건
중 하나를 해소할 근거를 찾지 못했다.** 다만 **`OPEN-NUM-01`(66% 오염)에 대해 출처 포인터가
한 걸음 진전됐다**는 사실을 기록한다 — commit `2f9bad7`(#199) subject까지 추적됐다.
**이것은 해소 근거가 아니다**(방법·모수·측정일이 없다). 등록 갱신 여부는 별도 slice의
판단이다.
