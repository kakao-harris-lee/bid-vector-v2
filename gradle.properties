org.gradle.parallel=true
org.gradle.caching=true

# Gradle 10 에서 기본 활성이 되는 실행 모드. 9.x 에서 미리 켠다.
# detekt 2.0 alpha 의 비호환 이력(#9390) 때문에 문제를 에러가 아닌 경고로 받는다 —
# 실측 결과는 reports/evidence/m1/1a/commands.md 가 갖는다.
org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=warn

kotlin.code.style=official

# 환경 값은 설정 파일로 외부화한다(v2-지침서.md §5). 승인 문서에 JDK 지침이 없어
# 1A 가 정했다 — 근거는 reports/evidence/m1/1a/scope.md D-1.
bidvector.jvmToolchain=21
