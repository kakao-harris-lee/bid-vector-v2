"""ml_engine — bid-vector V2 독립 Python ML engine 패키지 (M5, v2-지침서.md §3.2).

하위 패키지 경계는 `pyproject.toml` `[tool.importlinter]`가 강제한다(M5/5A). 이 파일 자신은
로직을 담지 않는다 — 하위 패키지 목록은 `ml-engine/README.md` 참고.
"""

from __future__ import annotations
