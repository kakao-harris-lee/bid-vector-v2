"""ml_engine.registry — versioned 정책·artifact registry(`policy.py`). `features`·
`contracts`만 의존한다 — 값 자체는 여기 없다(D-M5-6, `reports/evidence/m5/5a/policy-values.md`).
"""

from __future__ import annotations
